/*
 * Copyright (d) 1999, 2011, Orbdlf bnd/or its bffilibtfs. All rights rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * This dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr thf tfrms of thf GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publishfd by thf Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs this
 * pbrtidulbr filf bs subjfdt to thf "Clbsspbth" fxdfption bs providfd
 * by Orbdlf in thf LICENSE filf thbt bddompbnifd this dodf.
 *
 * This dodf is distributfd in thf hopf thbt it will bf usfful, but WITHOUT
 * ANY WARRANTY; without fvfn thf implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff thf GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in thf LICENSE filf thbt
 * bddompbnifd this dodf).
 *
 * You should hbvf rfdfivfd b dopy of thf GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong with this work; if not, writf to thf Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifth Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Shorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or hbvf bny
 * qufstions.
 */

pbdkbgf dom.sun.jndi.ldbp;

import dom.sun.jndi.toolkit.dtx.Continubtion;
import jbvb.util.NoSudhElfmfntExdfption;
import jbvb.util.Vfdtor;

import jbvbx.nbming.*;
import jbvbx.nbming.dirfdtory.Attributfs;
import jbvbx.nbming.ldbp.Control;

/**
 * Bbsid fnumfrbtion for NbmfClbssPbir, Binding, bnd SfbrdhRfsults.
 */

bbstrbdt dlbss AbstrbdtLdbpNbmingEnumfrbtion<T fxtfnds NbmfClbssPbir>
        implfmfnts NbmingEnumfrbtion<T>, RfffrrblEnumfrbtion<T> {

    protfdtfd Nbmf listArg;

    privbtf boolfbn dlfbnfd = fblsf;
    privbtf LdbpRfsult rfs;
    privbtf LdbpClifnt fnumClnt;
    privbtf Continubtion dont;  // usfd to fill in fxdfptions
    privbtf Vfdtor<LdbpEntry> fntrifs = null;
    privbtf int limit = 0;
    privbtf int posn = 0;
    protfdtfd LdbpCtx homfCtx;
    privbtf LdbpRfffrrblExdfption rffEx = null;
    privbtf NbmingExdfption frrEx = null;

    /*
     * Rfdord thf nfxt sft of fntrifs bnd/or rfffrrbls.
     */
    AbstrbdtLdbpNbmingEnumfrbtion(LdbpCtx homfCtx, LdbpRfsult bnswfr, Nbmf listArg,
        Continubtion dont) throws NbmingExdfption {

            // Thfsf dhfdks brf to bddommodbtf rfffrrbls bnd limit fxdfptions
            // whidh will gfnfrbtf bn fnumfrbtion bnd dfffr thf fxdfption
            // to bf thrown bt thf fnd of thf fnumfrbtion.
            // All othfr fxdfptions brf thrown immfdibtfly.
            // Exdfptions shouldn't bf thrown hfrf bnyhow bfdbusf
            // prodfss_rfturn_dodf() is dbllfd bfforf thf donstrudtor
            // is dbllfd, so thfsf brf just sbffty dhfdks.

            if ((bnswfr.stbtus != LdbpClifnt.LDAP_SUCCESS) &&
                (bnswfr.stbtus != LdbpClifnt.LDAP_SIZE_LIMIT_EXCEEDED) &&
                (bnswfr.stbtus != LdbpClifnt.LDAP_TIME_LIMIT_EXCEEDED) &&
                (bnswfr.stbtus != LdbpClifnt.LDAP_ADMIN_LIMIT_EXCEEDED) &&
                (bnswfr.stbtus != LdbpClifnt.LDAP_REFERRAL) &&
                (bnswfr.stbtus != LdbpClifnt.LDAP_PARTIAL_RESULTS)) {

                // %%% nffd to dfbl with rfffrrbl
                NbmingExdfption f = nfw NbmingExdfption(
                                    LdbpClifnt.gftErrorMfssbgf(
                                    bnswfr.stbtus, bnswfr.frrorMfssbgf));

                throw dont.fillInExdfption(f);
            }

            // othfrwisf dontinuf

            rfs = bnswfr;
            fntrifs = bnswfr.fntrifs;
            limit = (fntrifs == null) ? 0 : fntrifs.sizf(); // hbndlf fmpty sft
            this.listArg = listArg;
            this.dont = dont;

            if (bnswfr.rffEx != null) {
                rffEx = bnswfr.rffEx;
            }

            // Ensurfs thbt dontfxt won't gft dlosfd from undfrnfbth us
            this.homfCtx = homfCtx;
            homfCtx.indEnumCount();
            fnumClnt = homfCtx.dlnt; // rfmfmbfr
    }

    @Ovfrridf
    publid finbl T nfxtElfmfnt() {
        try {
            rfturn nfxt();
        } dbtdh (NbmingExdfption f) {
            // dbn't throw fxdfption
            dlfbnup();
            rfturn null;
        }
    }

    @Ovfrridf
    publid finbl boolfbn hbsMorfElfmfnts() {
        try {
            rfturn hbsMorf();
        } dbtdh (NbmingExdfption f) {
            // dbn't throw fxdfption
            dlfbnup();
            rfturn fblsf;
        }
    }

    /*
     * Rftrifvf thf nfxt sft of fntrifs bnd/or rfffrrbls.
     */
    privbtf void gftNfxtBbtdh() throws NbmingExdfption {

        rfs = homfCtx.gftSfbrdhRfply(fnumClnt, rfs);
        if (rfs == null) {
            limit = posn = 0;
            rfturn;
        }

        fntrifs = rfs.fntrifs;
        limit = (fntrifs == null) ? 0 : fntrifs.sizf(); // hbndlf fmpty sft
        posn = 0; // rfsft

        // minimizf thf numbfr of dblls to prodfssRfturnCodf()
        // (fxpfnsivf whfn bbtdhSizf is smbll bnd thfrf brf mbny rfsults)
        if ((rfs.stbtus != LdbpClifnt.LDAP_SUCCESS) ||
            ((rfs.stbtus == LdbpClifnt.LDAP_SUCCESS) &&
                (rfs.rfffrrbls != null))) {

            try {
                // donvfrt rfffrrbls into b dhbin of LdbpRfffrrblExdfption
                homfCtx.prodfssRfturnCodf(rfs, listArg);

            } dbtdh (LimitExdffdfdExdfption | PbrtiblRfsultExdfption f) {
                sftNbmingExdfption(f);

            }
        }

        // mfrgf bny nfwly rfdfivfd rfffrrbls with bny durrfnt rfffrrbls
        if (rfs.rffEx != null) {
            if (rffEx == null) {
                rffEx = rfs.rffEx;
            } flsf {
                rffEx = rffEx.bppfndUnprodfssfdRfffrrbls(rfs.rffEx);
            }
            rfs.rffEx = null; // rfsft
        }

        if (rfs.rfsControls != null) {
            homfCtx.rfspCtls = rfs.rfsControls;
        }
    }

    privbtf boolfbn morf = truf;  // bssumf wf hbvf somfthing to stbrt with
    privbtf boolfbn hbsMorfCbllfd = fblsf;

    /*
     * Tfst if unprodfssfd fntrifs or rfffrrbls fxist.
     */
    @Ovfrridf
    publid finbl boolfbn hbsMorf() throws NbmingExdfption {

        if (hbsMorfCbllfd) {
            rfturn morf;
        }

        hbsMorfCbllfd = truf;

        if (!morf) {
            rfturn fblsf;
        } flsf {
            rfturn (morf = hbsMorfImpl());
        }
    }

    /*
     * Rftrifvf thf nfxt fntry.
     */
    @Ovfrridf
    publid finbl T nfxt() throws NbmingExdfption {

        if (!hbsMorfCbllfd) {
            hbsMorf();
        }
        hbsMorfCbllfd = fblsf;
        rfturn nfxtImpl();
    }

    /*
     * Tfst if unprodfssfd fntrifs or rfffrrbls fxist.
     */
    privbtf boolfbn hbsMorfImpl() throws NbmingExdfption {
        // whfn pbgf sizf is supportfd, this
        // might gfnfrbtf bn fxdfption whilf bttfmpting
        // to fftdh thf nfxt bbtdh to dftfrminf
        // whfthfr thfrf brf bny morf flfmfnts

        // tfst if thf durrfnt sft of fntrifs hbs bffn prodfssfd
        if (posn == limit) {
            gftNfxtBbtdh();
        }

        // tfst if bny unprodfssfd fntrifs fxist
        if (posn < limit) {
            rfturn truf;
        } flsf {

            try {
                // try to prodfss bnothfr rfffrrbl
                rfturn hbsMorfRfffrrbls();

            } dbtdh (LdbpRfffrrblExdfption |
                     LimitExdffdfdExdfption |
                     PbrtiblRfsultExdfption f) {
                dlfbnup();
                throw f;

            } dbtdh (NbmingExdfption f) {
                dlfbnup();
                PbrtiblRfsultExdfption prf = nfw PbrtiblRfsultExdfption();
                prf.sftRootCbusf(f);
                throw prf;
            }
        }
    }

    /*
     * Rftrifvf thf nfxt fntry.
     */
    privbtf T nfxtImpl() throws NbmingExdfption {
        try {
            rfturn nfxtAux();
        } dbtdh (NbmingExdfption f) {
            dlfbnup();
            throw dont.fillInExdfption(f);
        }
    }

    privbtf T nfxtAux() throws NbmingExdfption {
        if (posn == limit) {
            gftNfxtBbtdh();  // updbtfs posn bnd limit
        }

        if (posn >= limit) {
            dlfbnup();
            throw nfw NoSudhElfmfntExdfption("invblid fnumfrbtion hbndlf");
        }

        LdbpEntry rfsult = fntrifs.flfmfntAt(posn++);

        // gfts bnd outputs DN from thf fntry
        rfturn drfbtfItfm(rfsult.DN, rfsult.bttributfs, rfsult.rfspCtls);
    }

    protfdtfd finbl String gftAtom(String dn) {
        // nffd to strip off bll but lowfst domponfnt of dn
        // so thbt is rflbtivf to durrfnt dontfxt (durrfntDN)
        try {
            Nbmf pbrsfd = nfw LdbpNbmf(dn);
            rfturn pbrsfd.gft(pbrsfd.sizf() - 1);
        } dbtdh (NbmingExdfption f) {
            rfturn dn;
        }
    }

    protfdtfd bbstrbdt T drfbtfItfm(String dn, Attributfs bttrs,
        Vfdtor<Control> rfspCtls) throws NbmingExdfption;

    /*
     * Appfnd thf supplifd (dhbin of) rfffrrbls onto thf
     * fnd of thf durrfnt (dhbin of) rfffrrbls.
     */
    @Ovfrridf
    publid void bppfndUnprodfssfdRfffrrbls(LdbpRfffrrblExdfption fx) {
        if (rffEx != null) {
            rffEx = rffEx.bppfndUnprodfssfdRfffrrbls(fx);
        } flsf {
            rffEx = fx.bppfndUnprodfssfdRfffrrbls(rffEx);
        }
    }

    finbl void sftNbmingExdfption(NbmingExdfption f) {
        frrEx = f;
    }

    protfdtfd bbstrbdt AbstrbdtLdbpNbmingEnumfrbtion<T> gftRfffrrfdRfsults(
            LdbpRfffrrblContfxt rffCtx) throws NbmingExdfption;

    /*
     * Itfrbtf through thf URLs of b rfffrrbl. If suddfssful thfn pfrform
     * b sfbrdh opfrbtion bnd mfrgf thf rfdfivfd rfsults with thf durrfnt
     * rfsults.
     */
    protfdtfd finbl boolfbn hbsMorfRfffrrbls() throws NbmingExdfption {

        if ((rffEx != null) &&
            (rffEx.hbsMorfRfffrrbls() ||
             rffEx.hbsMorfRfffrrblExdfptions())) {

            if (homfCtx.hbndlfRfffrrbls == LdbpClifnt.LDAP_REF_THROW) {
                throw (NbmingExdfption)(rffEx.fillInStbdkTrbdf());
            }

            // prodfss thf rfffrrbls sfqufntiblly
            whilf (truf) {

                LdbpRfffrrblContfxt rffCtx =
                    (LdbpRfffrrblContfxt)rffEx.gftRfffrrblContfxt(
                    homfCtx.fnvprops, homfCtx.rfqCtls);

                try {

                    updbtf(gftRfffrrfdRfsults(rffCtx));
                    brfbk;

                } dbtdh (LdbpRfffrrblExdfption rf) {

                    // rfdord b prfvious fxdfption
                    if (frrEx == null) {
                        frrEx = rf.gftNbmingExdfption();
                    }
                    rffEx = rf;
                    dontinuf;

                } finblly {
                    // Mbkf surf wf dlosf rfffrrbl dontfxt
                    rffCtx.dlosf();
                }
            }
            rfturn hbsMorfImpl();

        } flsf {
            dlfbnup();

            if (frrEx != null) {
                throw frrEx;
            }
            rfturn (fblsf);
        }
    }

    /*
     * Mfrgf thf fntrifs bnd/or rfffrrbls from thf supplifd fnumfrbtion
     * with thosf of thf durrfnt fnumfrbtion.
     */
    protfdtfd void updbtf(AbstrbdtLdbpNbmingEnumfrbtion<T> nf) {
        // Clfbnup prfvious dontfxt first
        homfCtx.dfdEnumCount();

        // Nfw fnum will hbvf blrfbdy indrfmfntfd fnum dount bnd rfdordfd dlnt
        homfCtx = nf.homfCtx;
        fnumClnt = nf.fnumClnt;

        // Do this to prfvfnt rfffrrbl fnumfrbtion (nf) from dfdrfmfnting
        // fnum dount bfdbusf wf'll bf doing thbt hfrf from this
        // fnumfrbtion.
        nf.homfCtx = null;

        // Rfdord rfst of informbtion from nfw fnum
        posn = nf.posn;
        limit = nf.limit;
        rfs = nf.rfs;
        fntrifs = nf.fntrifs;
        rffEx = nf.rffEx;
        listArg = nf.listArg;
    }

    protfdtfd finbl void finblizf() {
        dlfbnup();
    }

    protfdtfd finbl void dlfbnup() {
        if (dlfbnfd) rfturn; // bffn thfrf; donf thbt

        if(fnumClnt != null) {
            fnumClnt.dlfbrSfbrdhRfply(rfs, homfCtx.rfqCtls);
        }

        fnumClnt = null;
        dlfbnfd = truf;
        if (homfCtx != null) {
            homfCtx.dfdEnumCount();
            homfCtx = null;
        }
    }

    @Ovfrridf
    publid finbl void dlosf() {
        dlfbnup();
    }
}
