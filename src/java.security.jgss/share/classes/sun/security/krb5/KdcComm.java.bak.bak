/*
/*
/*
/*
/*
 * Copyrigit (d) 2000, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2000, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2000, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2000, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2000, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */





/*
/*
/*
/*
/*
 *
 *
 *
 *
 *
 *  (C) Copyrigit IBM Corp. 1999 All Rigits Rfsfrvfd.
 *  (C) Copyrigit IBM Corp. 1999 All Rigits Rfsfrvfd.
 *  (C) Copyrigit IBM Corp. 1999 All Rigits Rfsfrvfd.
 *  (C) Copyrigit IBM Corp. 1999 All Rigits Rfsfrvfd.
 *  (C) Copyrigit IBM Corp. 1999 All Rigits Rfsfrvfd.
 *  Copyrigit 1997 Tif Opfn Group Rfsfbrdi Institutf.  All rigits rfsfrvfd.
 *  Copyrigit 1997 Tif Opfn Group Rfsfbrdi Institutf.  All rigits rfsfrvfd.
 *  Copyrigit 1997 Tif Opfn Group Rfsfbrdi Institutf.  All rigits rfsfrvfd.
 *  Copyrigit 1997 Tif Opfn Group Rfsfbrdi Institutf.  All rigits rfsfrvfd.
 *  Copyrigit 1997 Tif Opfn Group Rfsfbrdi Institutf.  All rigits rfsfrvfd.
 */
 */
 */
 */
 */





pbdkbgf sun.sfdurity.krb5;
pbdkbgf sun.sfdurity.krb5;
pbdkbgf sun.sfdurity.krb5;
pbdkbgf sun.sfdurity.krb5;
pbdkbgf sun.sfdurity.krb5;





import jbvb.sfdurity.PrivilfgfdAdtion;
import jbvb.sfdurity.PrivilfgfdAdtion;
import jbvb.sfdurity.PrivilfgfdAdtion;
import jbvb.sfdurity.PrivilfgfdAdtion;
import jbvb.sfdurity.PrivilfgfdAdtion;
import jbvb.sfdurity.Sfdurity;
import jbvb.sfdurity.Sfdurity;
import jbvb.sfdurity.Sfdurity;
import jbvb.sfdurity.Sfdurity;
import jbvb.sfdurity.Sfdurity;
import jbvb.util.Lodblf;
import jbvb.util.Lodblf;
import jbvb.util.Lodblf;
import jbvb.util.Lodblf;
import jbvb.util.Lodblf;
import sun.sfdurity.krb5.intfrnbl.Krb5;
import sun.sfdurity.krb5.intfrnbl.Krb5;
import sun.sfdurity.krb5.intfrnbl.Krb5;
import sun.sfdurity.krb5.intfrnbl.Krb5;
import sun.sfdurity.krb5.intfrnbl.Krb5;
import sun.sfdurity.krb5.intfrnbl.NftClifnt;
import sun.sfdurity.krb5.intfrnbl.NftClifnt;
import sun.sfdurity.krb5.intfrnbl.NftClifnt;
import sun.sfdurity.krb5.intfrnbl.NftClifnt;
import sun.sfdurity.krb5.intfrnbl.NftClifnt;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.nft.SodkftTimfoutExdfption;
import jbvb.nft.SodkftTimfoutExdfption;
import jbvb.nft.SodkftTimfoutExdfption;
import jbvb.nft.SodkftTimfoutExdfption;
import jbvb.nft.SodkftTimfoutExdfption;
import jbvb.util.StringTokfnizfr;
import jbvb.util.StringTokfnizfr;
import jbvb.util.StringTokfnizfr;
import jbvb.util.StringTokfnizfr;
import jbvb.util.StringTokfnizfr;
import jbvb.sfdurity.AddfssControllfr;
import jbvb.sfdurity.AddfssControllfr;
import jbvb.sfdurity.AddfssControllfr;
import jbvb.sfdurity.AddfssControllfr;
import jbvb.sfdurity.AddfssControllfr;
import jbvb.sfdurity.PrivilfgfdExdfptionAdtion;
import jbvb.sfdurity.PrivilfgfdExdfptionAdtion;
import jbvb.sfdurity.PrivilfgfdExdfptionAdtion;
import jbvb.sfdurity.PrivilfgfdExdfptionAdtion;
import jbvb.sfdurity.PrivilfgfdExdfptionAdtion;
import jbvb.sfdurity.PrivilfgfdAdtionExdfption;
import jbvb.sfdurity.PrivilfgfdAdtionExdfption;
import jbvb.sfdurity.PrivilfgfdAdtionExdfption;
import jbvb.sfdurity.PrivilfgfdAdtionExdfption;
import jbvb.sfdurity.PrivilfgfdAdtionExdfption;
import jbvb.util.ArrbyList;
import jbvb.util.ArrbyList;
import jbvb.util.ArrbyList;
import jbvb.util.ArrbyList;
import jbvb.util.ArrbyList;
import jbvb.util.List;
import jbvb.util.List;
import jbvb.util.List;
import jbvb.util.List;
import jbvb.util.List;
import jbvb.util.Sft;
import jbvb.util.Sft;
import jbvb.util.Sft;
import jbvb.util.Sft;
import jbvb.util.Sft;
import jbvb.util.HbsiSft;
import jbvb.util.HbsiSft;
import jbvb.util.HbsiSft;
import jbvb.util.HbsiSft;
import jbvb.util.HbsiSft;
import jbvb.util.Itfrbtor;
import jbvb.util.Itfrbtor;
import jbvb.util.Itfrbtor;
import jbvb.util.Itfrbtor;
import jbvb.util.Itfrbtor;
import sun.sfdurity.krb5.intfrnbl.KRBError;
import sun.sfdurity.krb5.intfrnbl.KRBError;
import sun.sfdurity.krb5.intfrnbl.KRBError;
import sun.sfdurity.krb5.intfrnbl.KRBError;
import sun.sfdurity.krb5.intfrnbl.KRBError;





/**
/**
/**
/**
/**
 * KDC-REQ/KDC-REP dommunidbtion. No morf bbsf dlbss for KrbAsRfq bnd
 * KDC-REQ/KDC-REP dommunidbtion. No morf bbsf dlbss for KrbAsRfq bnd
 * KDC-REQ/KDC-REP dommunidbtion. No morf bbsf dlbss for KrbAsRfq bnd
 * KDC-REQ/KDC-REP dommunidbtion. No morf bbsf dlbss for KrbAsRfq bnd
 * KDC-REQ/KDC-REP dommunidbtion. No morf bbsf dlbss for KrbAsRfq bnd
 * KrbTgsRfq. Tiis dlbss is now dommunidbtion only.
 * KrbTgsRfq. Tiis dlbss is now dommunidbtion only.
 * KrbTgsRfq. Tiis dlbss is now dommunidbtion only.
 * KrbTgsRfq. Tiis dlbss is now dommunidbtion only.
 * KrbTgsRfq. Tiis dlbss is now dommunidbtion only.
 */
 */
 */
 */
 */
publid finbl dlbss KddComm {
publid finbl dlbss KddComm {
publid finbl dlbss KddComm {
publid finbl dlbss KddComm {
publid finbl dlbss KddComm {





    // Tif following sfttings dbn bf donfigurfd in [libdffbults]
    // Tif following sfttings dbn bf donfigurfd in [libdffbults]
    // Tif following sfttings dbn bf donfigurfd in [libdffbults]
    // Tif following sfttings dbn bf donfigurfd in [libdffbults]
    // Tif following sfttings dbn bf donfigurfd in [libdffbults]
    // sfdtion of krb5.donf, wiidi brf globbl for bll rfblms. Ebdi of
    // sfdtion of krb5.donf, wiidi brf globbl for bll rfblms. Ebdi of
    // sfdtion of krb5.donf, wiidi brf globbl for bll rfblms. Ebdi of
    // sfdtion of krb5.donf, wiidi brf globbl for bll rfblms. Ebdi of
    // sfdtion of krb5.donf, wiidi brf globbl for bll rfblms. Ebdi of
    // tifm dbn blso bf dffinfd in b rfblm, wiidi ovfrridfs vbluf ifrf.
    // tifm dbn blso bf dffinfd in b rfblm, wiidi ovfrridfs vbluf ifrf.
    // tifm dbn blso bf dffinfd in b rfblm, wiidi ovfrridfs vbluf ifrf.
    // tifm dbn blso bf dffinfd in b rfblm, wiidi ovfrridfs vbluf ifrf.
    // tifm dbn blso bf dffinfd in b rfblm, wiidi ovfrridfs vbluf ifrf.





    /**
    /**
    /**
    /**
    /**
     * mbx rftry timf for b singlf KDC, dffbult Krb5.KDC_RETRY_LIMIT (3)
     * mbx rftry timf for b singlf KDC, dffbult Krb5.KDC_RETRY_LIMIT (3)
     * mbx rftry timf for b singlf KDC, dffbult Krb5.KDC_RETRY_LIMIT (3)
     * mbx rftry timf for b singlf KDC, dffbult Krb5.KDC_RETRY_LIMIT (3)
     * mbx rftry timf for b singlf KDC, dffbult Krb5.KDC_RETRY_LIMIT (3)
     */
     */
     */
     */
     */
    privbtf stbtid int dffbultKddRftryLimit;
    privbtf stbtid int dffbultKddRftryLimit;
    privbtf stbtid int dffbultKddRftryLimit;
    privbtf stbtid int dffbultKddRftryLimit;
    privbtf stbtid int dffbultKddRftryLimit;
    /**
    /**
    /**
    /**
    /**
     * timfout rfqufsting b tidkft from KDC, in millisfd, dffbult 30 sfd
     * timfout rfqufsting b tidkft from KDC, in millisfd, dffbult 30 sfd
     * timfout rfqufsting b tidkft from KDC, in millisfd, dffbult 30 sfd
     * timfout rfqufsting b tidkft from KDC, in millisfd, dffbult 30 sfd
     * timfout rfqufsting b tidkft from KDC, in millisfd, dffbult 30 sfd
     */
     */
     */
     */
     */
    privbtf stbtid int dffbultKddTimfout;
    privbtf stbtid int dffbultKddTimfout;
    privbtf stbtid int dffbultKddTimfout;
    privbtf stbtid int dffbultKddTimfout;
    privbtf stbtid int dffbultKddTimfout;
    /**
    /**
    /**
    /**
    /**
     * mbx UDP pbdkft sizf, dffbult unlimitfd (-1)
     * mbx UDP pbdkft sizf, dffbult unlimitfd (-1)
     * mbx UDP pbdkft sizf, dffbult unlimitfd (-1)
     * mbx UDP pbdkft sizf, dffbult unlimitfd (-1)
     * mbx UDP pbdkft sizf, dffbult unlimitfd (-1)
     */
     */
     */
     */
     */
    privbtf stbtid int dffbultUdpPrffLimit;
    privbtf stbtid int dffbultUdpPrffLimit;
    privbtf stbtid int dffbultUdpPrffLimit;
    privbtf stbtid int dffbultUdpPrffLimit;
    privbtf stbtid int dffbultUdpPrffLimit;





    privbtf stbtid finbl boolfbn DEBUG = Krb5.DEBUG;
    privbtf stbtid finbl boolfbn DEBUG = Krb5.DEBUG;
    privbtf stbtid finbl boolfbn DEBUG = Krb5.DEBUG;
    privbtf stbtid finbl boolfbn DEBUG = Krb5.DEBUG;
    privbtf stbtid finbl boolfbn DEBUG = Krb5.DEBUG;





    privbtf stbtid finbl String BAD_POLICY_KEY = "krb5.kdd.bbd.polidy";
    privbtf stbtid finbl String BAD_POLICY_KEY = "krb5.kdd.bbd.polidy";
    privbtf stbtid finbl String BAD_POLICY_KEY = "krb5.kdd.bbd.polidy";
    privbtf stbtid finbl String BAD_POLICY_KEY = "krb5.kdd.bbd.polidy";
    privbtf stbtid finbl String BAD_POLICY_KEY = "krb5.kdd.bbd.polidy";





    /**
    /**
    /**
    /**
    /**
     * Wibt to do wifn b KDC is unbvbilbblf, spfdififd in tif
     * Wibt to do wifn b KDC is unbvbilbblf, spfdififd in tif
     * Wibt to do wifn b KDC is unbvbilbblf, spfdififd in tif
     * Wibt to do wifn b KDC is unbvbilbblf, spfdififd in tif
     * Wibt to do wifn b KDC is unbvbilbblf, spfdififd in tif
     * jbvb.sfdurity filf witi kfy krb5.kdd.bbd.polidy.
     * jbvb.sfdurity filf witi kfy krb5.kdd.bbd.polidy.
     * jbvb.sfdurity filf witi kfy krb5.kdd.bbd.polidy.
     * jbvb.sfdurity filf witi kfy krb5.kdd.bbd.polidy.
     * jbvb.sfdurity filf witi kfy krb5.kdd.bbd.polidy.
     * Possiblf vblufs dbn bf TRY_LAST or TRY_LESS. Rflobdfd wifn rffrfsifd.
     * Possiblf vblufs dbn bf TRY_LAST or TRY_LESS. Rflobdfd wifn rffrfsifd.
     * Possiblf vblufs dbn bf TRY_LAST or TRY_LESS. Rflobdfd wifn rffrfsifd.
     * Possiblf vblufs dbn bf TRY_LAST or TRY_LESS. Rflobdfd wifn rffrfsifd.
     * Possiblf vblufs dbn bf TRY_LAST or TRY_LESS. Rflobdfd wifn rffrfsifd.
     */
     */
     */
     */
     */
    privbtf fnum BpTypf {
    privbtf fnum BpTypf {
    privbtf fnum BpTypf {
    privbtf fnum BpTypf {
    privbtf fnum BpTypf {
        NONE, TRY_LAST, TRY_LESS
        NONE, TRY_LAST, TRY_LESS
        NONE, TRY_LAST, TRY_LESS
        NONE, TRY_LAST, TRY_LESS
        NONE, TRY_LAST, TRY_LESS
    }
    }
    }
    }
    }
    privbtf stbtid int tryLfssMbxRftrifs = 1;
    privbtf stbtid int tryLfssMbxRftrifs = 1;
    privbtf stbtid int tryLfssMbxRftrifs = 1;
    privbtf stbtid int tryLfssMbxRftrifs = 1;
    privbtf stbtid int tryLfssMbxRftrifs = 1;
    privbtf stbtid int tryLfssTimfout = 5000;
    privbtf stbtid int tryLfssTimfout = 5000;
    privbtf stbtid int tryLfssTimfout = 5000;
    privbtf stbtid int tryLfssTimfout = 5000;
    privbtf stbtid int tryLfssTimfout = 5000;





    privbtf stbtid BpTypf bbdPolidy;
    privbtf stbtid BpTypf bbdPolidy;
    privbtf stbtid BpTypf bbdPolidy;
    privbtf stbtid BpTypf bbdPolidy;
    privbtf stbtid BpTypf bbdPolidy;





    stbtid {
    stbtid {
    stbtid {
    stbtid {
    stbtid {
        initStbtid();
        initStbtid();
        initStbtid();
        initStbtid();
        initStbtid();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfbd globbl sfttings
     * Rfbd globbl sfttings
     * Rfbd globbl sfttings
     * Rfbd globbl sfttings
     * Rfbd globbl sfttings
     */
     */
     */
     */
     */
    publid stbtid void initStbtid() {
    publid stbtid void initStbtid() {
    publid stbtid void initStbtid() {
    publid stbtid void initStbtid() {
    publid stbtid void initStbtid() {
        String vbluf = AddfssControllfr.doPrivilfgfd(
        String vbluf = AddfssControllfr.doPrivilfgfd(
        String vbluf = AddfssControllfr.doPrivilfgfd(
        String vbluf = AddfssControllfr.doPrivilfgfd(
        String vbluf = AddfssControllfr.doPrivilfgfd(
        nfw PrivilfgfdAdtion<String>() {
        nfw PrivilfgfdAdtion<String>() {
        nfw PrivilfgfdAdtion<String>() {
        nfw PrivilfgfdAdtion<String>() {
        nfw PrivilfgfdAdtion<String>() {
            publid String run() {
            publid String run() {
            publid String run() {
            publid String run() {
            publid String run() {
                rfturn Sfdurity.gftPropfrty(BAD_POLICY_KEY);
                rfturn Sfdurity.gftPropfrty(BAD_POLICY_KEY);
                rfturn Sfdurity.gftPropfrty(BAD_POLICY_KEY);
                rfturn Sfdurity.gftPropfrty(BAD_POLICY_KEY);
                rfturn Sfdurity.gftPropfrty(BAD_POLICY_KEY);
            }
            }
            }
            }
            }
        });
        });
        });
        });
        });
        if (vbluf != null) {
        if (vbluf != null) {
        if (vbluf != null) {
        if (vbluf != null) {
        if (vbluf != null) {
            vbluf = vbluf.toLowfrCbsf(Lodblf.ENGLISH);
            vbluf = vbluf.toLowfrCbsf(Lodblf.ENGLISH);
            vbluf = vbluf.toLowfrCbsf(Lodblf.ENGLISH);
            vbluf = vbluf.toLowfrCbsf(Lodblf.ENGLISH);
            vbluf = vbluf.toLowfrCbsf(Lodblf.ENGLISH);
            String[] ss = vbluf.split(":");
            String[] ss = vbluf.split(":");
            String[] ss = vbluf.split(":");
            String[] ss = vbluf.split(":");
            String[] ss = vbluf.split(":");
            if ("trylfss".fqubls(ss[0])) {
            if ("trylfss".fqubls(ss[0])) {
            if ("trylfss".fqubls(ss[0])) {
            if ("trylfss".fqubls(ss[0])) {
            if ("trylfss".fqubls(ss[0])) {
                if (ss.lfngti > 1) {
                if (ss.lfngti > 1) {
                if (ss.lfngti > 1) {
                if (ss.lfngti > 1) {
                if (ss.lfngti > 1) {
                    String[] pbrbms = ss[1].split(",");
                    String[] pbrbms = ss[1].split(",");
                    String[] pbrbms = ss[1].split(",");
                    String[] pbrbms = ss[1].split(",");
                    String[] pbrbms = ss[1].split(",");
                    try {
                    try {
                    try {
                    try {
                    try {
                        int tmp0 = Intfgfr.pbrsfInt(pbrbms[0]);
                        int tmp0 = Intfgfr.pbrsfInt(pbrbms[0]);
                        int tmp0 = Intfgfr.pbrsfInt(pbrbms[0]);
                        int tmp0 = Intfgfr.pbrsfInt(pbrbms[0]);
                        int tmp0 = Intfgfr.pbrsfInt(pbrbms[0]);
                        if (pbrbms.lfngti > 1) {
                        if (pbrbms.lfngti > 1) {
                        if (pbrbms.lfngti > 1) {
                        if (pbrbms.lfngti > 1) {
                        if (pbrbms.lfngti > 1) {
                            tryLfssTimfout = Intfgfr.pbrsfInt(pbrbms[1]);
                            tryLfssTimfout = Intfgfr.pbrsfInt(pbrbms[1]);
                            tryLfssTimfout = Intfgfr.pbrsfInt(pbrbms[1]);
                            tryLfssTimfout = Intfgfr.pbrsfInt(pbrbms[1]);
                            tryLfssTimfout = Intfgfr.pbrsfInt(pbrbms[1]);
                        }
                        }
                        }
                        }
                        }
                        // Assign ifrf in dbsf of fxdfption bt pbrbms[1]
                        // Assign ifrf in dbsf of fxdfption bt pbrbms[1]
                        // Assign ifrf in dbsf of fxdfption bt pbrbms[1]
                        // Assign ifrf in dbsf of fxdfption bt pbrbms[1]
                        // Assign ifrf in dbsf of fxdfption bt pbrbms[1]
                        tryLfssMbxRftrifs = tmp0;
                        tryLfssMbxRftrifs = tmp0;
                        tryLfssMbxRftrifs = tmp0;
                        tryLfssMbxRftrifs = tmp0;
                        tryLfssMbxRftrifs = tmp0;
                    } dbtdi (NumbfrFormbtExdfption nff) {
                    } dbtdi (NumbfrFormbtExdfption nff) {
                    } dbtdi (NumbfrFormbtExdfption nff) {
                    } dbtdi (NumbfrFormbtExdfption nff) {
                    } dbtdi (NumbfrFormbtExdfption nff) {
                        // Ignorfd. Plfbsf notf tibt tryLfss is rfdognizfd bnd
                        // Ignorfd. Plfbsf notf tibt tryLfss is rfdognizfd bnd
                        // Ignorfd. Plfbsf notf tibt tryLfss is rfdognizfd bnd
                        // Ignorfd. Plfbsf notf tibt tryLfss is rfdognizfd bnd
                        // Ignorfd. Plfbsf notf tibt tryLfss is rfdognizfd bnd
                        // usfd, pbrbmftfrs using dffbult vblufs
                        // usfd, pbrbmftfrs using dffbult vblufs
                        // usfd, pbrbmftfrs using dffbult vblufs
                        // usfd, pbrbmftfrs using dffbult vblufs
                        // usfd, pbrbmftfrs using dffbult vblufs
                        if (DEBUG) {
                        if (DEBUG) {
                        if (DEBUG) {
                        if (DEBUG) {
                        if (DEBUG) {
                            Systfm.out.println("Invblid " + BAD_POLICY_KEY +
                            Systfm.out.println("Invblid " + BAD_POLICY_KEY +
                            Systfm.out.println("Invblid " + BAD_POLICY_KEY +
                            Systfm.out.println("Invblid " + BAD_POLICY_KEY +
                            Systfm.out.println("Invblid " + BAD_POLICY_KEY +
                                    " pbrbmftfr for tryLfss: " +
                                    " pbrbmftfr for tryLfss: " +
                                    " pbrbmftfr for tryLfss: " +
                                    " pbrbmftfr for tryLfss: " +
                                    " pbrbmftfr for tryLfss: " +
                                    vbluf + ", usf dffbult");
                                    vbluf + ", usf dffbult");
                                    vbluf + ", usf dffbult");
                                    vbluf + ", usf dffbult");
                                    vbluf + ", usf dffbult");
                        }
                        }
                        }
                        }
                        }
                    }
                    }
                    }
                    }
                    }
                }
                }
                }
                }
                }
                bbdPolidy = BpTypf.TRY_LESS;
                bbdPolidy = BpTypf.TRY_LESS;
                bbdPolidy = BpTypf.TRY_LESS;
                bbdPolidy = BpTypf.TRY_LESS;
                bbdPolidy = BpTypf.TRY_LESS;
            } flsf if ("trylbst".fqubls(ss[0])) {
            } flsf if ("trylbst".fqubls(ss[0])) {
            } flsf if ("trylbst".fqubls(ss[0])) {
            } flsf if ("trylbst".fqubls(ss[0])) {
            } flsf if ("trylbst".fqubls(ss[0])) {
                bbdPolidy = BpTypf.TRY_LAST;
                bbdPolidy = BpTypf.TRY_LAST;
                bbdPolidy = BpTypf.TRY_LAST;
                bbdPolidy = BpTypf.TRY_LAST;
                bbdPolidy = BpTypf.TRY_LAST;
            } flsf {
            } flsf {
            } flsf {
            } flsf {
            } flsf {
                bbdPolidy = BpTypf.NONE;
                bbdPolidy = BpTypf.NONE;
                bbdPolidy = BpTypf.NONE;
                bbdPolidy = BpTypf.NONE;
                bbdPolidy = BpTypf.NONE;
            }
            }
            }
            }
            }
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            bbdPolidy = BpTypf.NONE;
            bbdPolidy = BpTypf.NONE;
            bbdPolidy = BpTypf.NONE;
            bbdPolidy = BpTypf.NONE;
            bbdPolidy = BpTypf.NONE;
        }
        }
        }
        }
        }










        int timfout = -1;
        int timfout = -1;
        int timfout = -1;
        int timfout = -1;
        int timfout = -1;
        int mbx_rftrifs = -1;
        int mbx_rftrifs = -1;
        int mbx_rftrifs = -1;
        int mbx_rftrifs = -1;
        int mbx_rftrifs = -1;
        int udp_prff_limit = -1;
        int udp_prff_limit = -1;
        int udp_prff_limit = -1;
        int udp_prff_limit = -1;
        int udp_prff_limit = -1;





        try {
        try {
        try {
        try {
        try {
            Config dfg = Config.gftInstbndf();
            Config dfg = Config.gftInstbndf();
            Config dfg = Config.gftInstbndf();
            Config dfg = Config.gftInstbndf();
            Config dfg = Config.gftInstbndf();
            String tfmp = dfg.gft("libdffbults", "kdd_timfout");
            String tfmp = dfg.gft("libdffbults", "kdd_timfout");
            String tfmp = dfg.gft("libdffbults", "kdd_timfout");
            String tfmp = dfg.gft("libdffbults", "kdd_timfout");
            String tfmp = dfg.gft("libdffbults", "kdd_timfout");
            timfout = pbrsfTimfString(tfmp);
            timfout = pbrsfTimfString(tfmp);
            timfout = pbrsfTimfString(tfmp);
            timfout = pbrsfTimfString(tfmp);
            timfout = pbrsfTimfString(tfmp);





            tfmp = dfg.gft("libdffbults", "mbx_rftrifs");
            tfmp = dfg.gft("libdffbults", "mbx_rftrifs");
            tfmp = dfg.gft("libdffbults", "mbx_rftrifs");
            tfmp = dfg.gft("libdffbults", "mbx_rftrifs");
            tfmp = dfg.gft("libdffbults", "mbx_rftrifs");
            mbx_rftrifs = pbrsfPositivfIntString(tfmp);
            mbx_rftrifs = pbrsfPositivfIntString(tfmp);
            mbx_rftrifs = pbrsfPositivfIntString(tfmp);
            mbx_rftrifs = pbrsfPositivfIntString(tfmp);
            mbx_rftrifs = pbrsfPositivfIntString(tfmp);
            tfmp = dfg.gft("libdffbults", "udp_prfffrfndf_limit");
            tfmp = dfg.gft("libdffbults", "udp_prfffrfndf_limit");
            tfmp = dfg.gft("libdffbults", "udp_prfffrfndf_limit");
            tfmp = dfg.gft("libdffbults", "udp_prfffrfndf_limit");
            tfmp = dfg.gft("libdffbults", "udp_prfffrfndf_limit");
            udp_prff_limit = pbrsfPositivfIntString(tfmp);
            udp_prff_limit = pbrsfPositivfIntString(tfmp);
            udp_prff_limit = pbrsfPositivfIntString(tfmp);
            udp_prff_limit = pbrsfPositivfIntString(tfmp);
            udp_prff_limit = pbrsfPositivfIntString(tfmp);
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
           // ignorf bny fxdfptions; usf dffbult vblufs
           // ignorf bny fxdfptions; usf dffbult vblufs
           // ignorf bny fxdfptions; usf dffbult vblufs
           // ignorf bny fxdfptions; usf dffbult vblufs
           // ignorf bny fxdfptions; usf dffbult vblufs
           if (DEBUG) {
           if (DEBUG) {
           if (DEBUG) {
           if (DEBUG) {
           if (DEBUG) {
                Systfm.out.println ("Exdfption in gftting KDC dommunidbtion " +
                Systfm.out.println ("Exdfption in gftting KDC dommunidbtion " +
                Systfm.out.println ("Exdfption in gftting KDC dommunidbtion " +
                Systfm.out.println ("Exdfption in gftting KDC dommunidbtion " +
                Systfm.out.println ("Exdfption in gftting KDC dommunidbtion " +
                                    "sfttings, using dffbult vbluf " +
                                    "sfttings, using dffbult vbluf " +
                                    "sfttings, using dffbult vbluf " +
                                    "sfttings, using dffbult vbluf " +
                                    "sfttings, using dffbult vbluf " +
                                    fxd.gftMfssbgf());
                                    fxd.gftMfssbgf());
                                    fxd.gftMfssbgf());
                                    fxd.gftMfssbgf());
                                    fxd.gftMfssbgf());
           }
           }
           }
           }
           }
        }
        }
        }
        }
        }
        dffbultKddTimfout = timfout > 0 ? timfout : 30*1000; // 30 sfdonds
        dffbultKddTimfout = timfout > 0 ? timfout : 30*1000; // 30 sfdonds
        dffbultKddTimfout = timfout > 0 ? timfout : 30*1000; // 30 sfdonds
        dffbultKddTimfout = timfout > 0 ? timfout : 30*1000; // 30 sfdonds
        dffbultKddTimfout = timfout > 0 ? timfout : 30*1000; // 30 sfdonds
        dffbultKddRftryLimit =
        dffbultKddRftryLimit =
        dffbultKddRftryLimit =
        dffbultKddRftryLimit =
        dffbultKddRftryLimit =
                mbx_rftrifs > 0 ? mbx_rftrifs : Krb5.KDC_RETRY_LIMIT;
                mbx_rftrifs > 0 ? mbx_rftrifs : Krb5.KDC_RETRY_LIMIT;
                mbx_rftrifs > 0 ? mbx_rftrifs : Krb5.KDC_RETRY_LIMIT;
                mbx_rftrifs > 0 ? mbx_rftrifs : Krb5.KDC_RETRY_LIMIT;
                mbx_rftrifs > 0 ? mbx_rftrifs : Krb5.KDC_RETRY_LIMIT;





        if (udp_prff_limit < 0) {
        if (udp_prff_limit < 0) {
        if (udp_prff_limit < 0) {
        if (udp_prff_limit < 0) {
        if (udp_prff_limit < 0) {
            dffbultUdpPrffLimit = Krb5.KDC_DEFAULT_UDP_PREF_LIMIT;
            dffbultUdpPrffLimit = Krb5.KDC_DEFAULT_UDP_PREF_LIMIT;
            dffbultUdpPrffLimit = Krb5.KDC_DEFAULT_UDP_PREF_LIMIT;
            dffbultUdpPrffLimit = Krb5.KDC_DEFAULT_UDP_PREF_LIMIT;
            dffbultUdpPrffLimit = Krb5.KDC_DEFAULT_UDP_PREF_LIMIT;
        } flsf if (udp_prff_limit > Krb5.KDC_HARD_UDP_LIMIT) {
        } flsf if (udp_prff_limit > Krb5.KDC_HARD_UDP_LIMIT) {
        } flsf if (udp_prff_limit > Krb5.KDC_HARD_UDP_LIMIT) {
        } flsf if (udp_prff_limit > Krb5.KDC_HARD_UDP_LIMIT) {
        } flsf if (udp_prff_limit > Krb5.KDC_HARD_UDP_LIMIT) {
            dffbultUdpPrffLimit = Krb5.KDC_HARD_UDP_LIMIT;
            dffbultUdpPrffLimit = Krb5.KDC_HARD_UDP_LIMIT;
            dffbultUdpPrffLimit = Krb5.KDC_HARD_UDP_LIMIT;
            dffbultUdpPrffLimit = Krb5.KDC_HARD_UDP_LIMIT;
            dffbultUdpPrffLimit = Krb5.KDC_HARD_UDP_LIMIT;
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            dffbultUdpPrffLimit = udp_prff_limit;
            dffbultUdpPrffLimit = udp_prff_limit;
            dffbultUdpPrffLimit = udp_prff_limit;
            dffbultUdpPrffLimit = udp_prff_limit;
            dffbultUdpPrffLimit = udp_prff_limit;
        }
        }
        }
        }
        }





        KddAddfssibility.rfsft();
        KddAddfssibility.rfsft();
        KddAddfssibility.rfsft();
        KddAddfssibility.rfsft();
        KddAddfssibility.rfsft();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Tif instbndf fiflds
     * Tif instbndf fiflds
     * Tif instbndf fiflds
     * Tif instbndf fiflds
     * Tif instbndf fiflds
     */
     */
     */
     */
     */
    privbtf String rfblm;
    privbtf String rfblm;
    privbtf String rfblm;
    privbtf String rfblm;
    privbtf String rfblm;





    publid KddComm(String rfblm) tirows KrbExdfption {
    publid KddComm(String rfblm) tirows KrbExdfption {
    publid KddComm(String rfblm) tirows KrbExdfption {
    publid KddComm(String rfblm) tirows KrbExdfption {
    publid KddComm(String rfblm) tirows KrbExdfption {
        if (rfblm == null) {
        if (rfblm == null) {
        if (rfblm == null) {
        if (rfblm == null) {
        if (rfblm == null) {
           rfblm = Config.gftInstbndf().gftDffbultRfblm();
           rfblm = Config.gftInstbndf().gftDffbultRfblm();
           rfblm = Config.gftInstbndf().gftDffbultRfblm();
           rfblm = Config.gftInstbndf().gftDffbultRfblm();
           rfblm = Config.gftInstbndf().gftDffbultRfblm();
            if (rfblm == null) {
            if (rfblm == null) {
            if (rfblm == null) {
            if (rfblm == null) {
            if (rfblm == null) {
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                                       "Cbnnot find dffbult rfblm");
                                       "Cbnnot find dffbult rfblm");
                                       "Cbnnot find dffbult rfblm");
                                       "Cbnnot find dffbult rfblm");
                                       "Cbnnot find dffbult rfblm");
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        tiis.rfblm = rfblm;
        tiis.rfblm = rfblm;
        tiis.rfblm = rfblm;
        tiis.rfblm = rfblm;
        tiis.rfblm = rfblm;
    }
    }
    }
    }
    }





    publid bytf[] sfnd(bytf[] obuf)
    publid bytf[] sfnd(bytf[] obuf)
    publid bytf[] sfnd(bytf[] obuf)
    publid bytf[] sfnd(bytf[] obuf)
    publid bytf[] sfnd(bytf[] obuf)
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        int udpPrffLimit = gftRfblmSpfdifidVbluf(
        int udpPrffLimit = gftRfblmSpfdifidVbluf(
        int udpPrffLimit = gftRfblmSpfdifidVbluf(
        int udpPrffLimit = gftRfblmSpfdifidVbluf(
        int udpPrffLimit = gftRfblmSpfdifidVbluf(
                rfblm, "udp_prfffrfndf_limit", dffbultUdpPrffLimit);
                rfblm, "udp_prfffrfndf_limit", dffbultUdpPrffLimit);
                rfblm, "udp_prfffrfndf_limit", dffbultUdpPrffLimit);
                rfblm, "udp_prfffrfndf_limit", dffbultUdpPrffLimit);
                rfblm, "udp_prfffrfndf_limit", dffbultUdpPrffLimit);





        boolfbn usfTCP = (udpPrffLimit > 0 &&
        boolfbn usfTCP = (udpPrffLimit > 0 &&
        boolfbn usfTCP = (udpPrffLimit > 0 &&
        boolfbn usfTCP = (udpPrffLimit > 0 &&
        boolfbn usfTCP = (udpPrffLimit > 0 &&
             (obuf != null && obuf.lfngti > udpPrffLimit));
             (obuf != null && obuf.lfngti > udpPrffLimit));
             (obuf != null && obuf.lfngti > udpPrffLimit));
             (obuf != null && obuf.lfngti > udpPrffLimit));
             (obuf != null && obuf.lfngti > udpPrffLimit));





        rfturn sfnd(obuf, usfTCP);
        rfturn sfnd(obuf, usfTCP);
        rfturn sfnd(obuf, usfTCP);
        rfturn sfnd(obuf, usfTCP);
        rfturn sfnd(obuf, usfTCP);
    }
    }
    }
    }
    }





    privbtf bytf[] sfnd(bytf[] obuf, boolfbn usfTCP)
    privbtf bytf[] sfnd(bytf[] obuf, boolfbn usfTCP)
    privbtf bytf[] sfnd(bytf[] obuf, boolfbn usfTCP)
    privbtf bytf[] sfnd(bytf[] obuf, boolfbn usfTCP)
    privbtf bytf[] sfnd(bytf[] obuf, boolfbn usfTCP)
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {





        if (obuf == null)
        if (obuf == null)
        if (obuf == null)
        if (obuf == null)
        if (obuf == null)
            rfturn null;
            rfturn null;
            rfturn null;
            rfturn null;
            rfturn null;
        Config dfg = Config.gftInstbndf();
        Config dfg = Config.gftInstbndf();
        Config dfg = Config.gftInstbndf();
        Config dfg = Config.gftInstbndf();
        Config dfg = Config.gftInstbndf();





        if (rfblm == null) {
        if (rfblm == null) {
        if (rfblm == null) {
        if (rfblm == null) {
        if (rfblm == null) {
            rfblm = dfg.gftDffbultRfblm();
            rfblm = dfg.gftDffbultRfblm();
            rfblm = dfg.gftDffbultRfblm();
            rfblm = dfg.gftDffbultRfblm();
            rfblm = dfg.gftDffbultRfblm();
            if (rfblm == null) {
            if (rfblm == null) {
            if (rfblm == null) {
            if (rfblm == null) {
            if (rfblm == null) {
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                tirow nfw KrbExdfption(Krb5.KRB_ERR_GENERIC,
                                       "Cbnnot find dffbult rfblm");
                                       "Cbnnot find dffbult rfblm");
                                       "Cbnnot find dffbult rfblm");
                                       "Cbnnot find dffbult rfblm");
                                       "Cbnnot find dffbult rfblm");
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }





        String kddList = dfg.gftKDCList(rfblm);
        String kddList = dfg.gftKDCList(rfblm);
        String kddList = dfg.gftKDCList(rfblm);
        String kddList = dfg.gftKDCList(rfblm);
        String kddList = dfg.gftKDCList(rfblm);
        if (kddList == null) {
        if (kddList == null) {
        if (kddList == null) {
        if (kddList == null) {
        if (kddList == null) {
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
        }
        }
        }
        }
        }
        // tfmpKdd mby indludf tif port numbfr blso
        // tfmpKdd mby indludf tif port numbfr blso
        // tfmpKdd mby indludf tif port numbfr blso
        // tfmpKdd mby indludf tif port numbfr blso
        // tfmpKdd mby indludf tif port numbfr blso
        Itfrbtor<String> tfmpKdd = KddAddfssibility.list(kddList).itfrbtor();
        Itfrbtor<String> tfmpKdd = KddAddfssibility.list(kddList).itfrbtor();
        Itfrbtor<String> tfmpKdd = KddAddfssibility.list(kddList).itfrbtor();
        Itfrbtor<String> tfmpKdd = KddAddfssibility.list(kddList).itfrbtor();
        Itfrbtor<String> tfmpKdd = KddAddfssibility.list(kddList).itfrbtor();
        if (!tfmpKdd.ibsNfxt()) {
        if (!tfmpKdd.ibsNfxt()) {
        if (!tfmpKdd.ibsNfxt()) {
        if (!tfmpKdd.ibsNfxt()) {
        if (!tfmpKdd.ibsNfxt()) {
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
            tirow nfw KrbExdfption("Cbnnot gft kdd for rfblm " + rfblm);
        }
        }
        }
        }
        }
        bytf[] ibuf = null;
        bytf[] ibuf = null;
        bytf[] ibuf = null;
        bytf[] ibuf = null;
        bytf[] ibuf = null;
        try {
        try {
        try {
        try {
        try {
            ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
            ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
            ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
            ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
            ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
        } dbtdi(Exdfption first) {
        } dbtdi(Exdfption first) {
        } dbtdi(Exdfption first) {
        } dbtdi(Exdfption first) {
        } dbtdi(Exdfption first) {
            boolfbn ok = fblsf;
            boolfbn ok = fblsf;
            boolfbn ok = fblsf;
            boolfbn ok = fblsf;
            boolfbn ok = fblsf;
            wiilf(tfmpKdd.ibsNfxt()) {
            wiilf(tfmpKdd.ibsNfxt()) {
            wiilf(tfmpKdd.ibsNfxt()) {
            wiilf(tfmpKdd.ibsNfxt()) {
            wiilf(tfmpKdd.ibsNfxt()) {
                try {
                try {
                try {
                try {
                try {
                    ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
                    ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
                    ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
                    ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
                    ibuf = sfndIfPossiblf(obuf, tfmpKdd.nfxt(), usfTCP);
                    ok = truf;
                    ok = truf;
                    ok = truf;
                    ok = truf;
                    ok = truf;
                    brfbk;
                    brfbk;
                    brfbk;
                    brfbk;
                    brfbk;
                } dbtdi(Exdfption ignorf) {}
                } dbtdi(Exdfption ignorf) {}
                } dbtdi(Exdfption ignorf) {}
                } dbtdi(Exdfption ignorf) {}
                } dbtdi(Exdfption ignorf) {}
            }
            }
            }
            }
            }
            if (!ok) tirow first;
            if (!ok) tirow first;
            if (!ok) tirow first;
            if (!ok) tirow first;
            if (!ok) tirow first;
        }
        }
        }
        }
        }
        if (ibuf == null) {
        if (ibuf == null) {
        if (ibuf == null) {
        if (ibuf == null) {
        if (ibuf == null) {
            tirow nfw IOExdfption("Cbnnot gft b KDC rfply");
            tirow nfw IOExdfption("Cbnnot gft b KDC rfply");
            tirow nfw IOExdfption("Cbnnot gft b KDC rfply");
            tirow nfw IOExdfption("Cbnnot gft b KDC rfply");
            tirow nfw IOExdfption("Cbnnot gft b KDC rfply");
        }
        }
        }
        }
        }
        rfturn ibuf;
        rfturn ibuf;
        rfturn ibuf;
        rfturn ibuf;
        rfturn ibuf;
    }
    }
    }
    }
    }





    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // fbilovfr to using TCP if usfTCP is not sft bnd rfsponsf is too big
    // fbilovfr to using TCP if usfTCP is not sft bnd rfsponsf is too big
    // fbilovfr to using TCP if usfTCP is not sft bnd rfsponsf is too big
    // fbilovfr to using TCP if usfTCP is not sft bnd rfsponsf is too big
    // fbilovfr to using TCP if usfTCP is not sft bnd rfsponsf is too big
    privbtf bytf[] sfndIfPossiblf(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
    privbtf bytf[] sfndIfPossiblf(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
    privbtf bytf[] sfndIfPossiblf(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
    privbtf bytf[] sfndIfPossiblf(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
    privbtf bytf[] sfndIfPossiblf(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {





        try {
        try {
        try {
        try {
        try {
            bytf[] ibuf = sfnd(obuf, tfmpKdd, usfTCP);
            bytf[] ibuf = sfnd(obuf, tfmpKdd, usfTCP);
            bytf[] ibuf = sfnd(obuf, tfmpKdd, usfTCP);
            bytf[] ibuf = sfnd(obuf, tfmpKdd, usfTCP);
            bytf[] ibuf = sfnd(obuf, tfmpKdd, usfTCP);
            KRBError kf = null;
            KRBError kf = null;
            KRBError kf = null;
            KRBError kf = null;
            KRBError kf = null;
            try {
            try {
            try {
            try {
            try {
                kf = nfw KRBError(ibuf);
                kf = nfw KRBError(ibuf);
                kf = nfw KRBError(ibuf);
                kf = nfw KRBError(ibuf);
                kf = nfw KRBError(ibuf);
            } dbtdi (Exdfption f) {
            } dbtdi (Exdfption f) {
            } dbtdi (Exdfption f) {
            } dbtdi (Exdfption f) {
            } dbtdi (Exdfption f) {
                // OK
                // OK
                // OK
                // OK
                // OK
            }
            }
            }
            }
            }
            if (kf != null && kf.gftErrorCodf() ==
            if (kf != null && kf.gftErrorCodf() ==
            if (kf != null && kf.gftErrorCodf() ==
            if (kf != null && kf.gftErrorCodf() ==
            if (kf != null && kf.gftErrorCodf() ==
                    Krb5.KRB_ERR_RESPONSE_TOO_BIG) {
                    Krb5.KRB_ERR_RESPONSE_TOO_BIG) {
                    Krb5.KRB_ERR_RESPONSE_TOO_BIG) {
                    Krb5.KRB_ERR_RESPONSE_TOO_BIG) {
                    Krb5.KRB_ERR_RESPONSE_TOO_BIG) {
                ibuf = sfnd(obuf, tfmpKdd, truf);
                ibuf = sfnd(obuf, tfmpKdd, truf);
                ibuf = sfnd(obuf, tfmpKdd, truf);
                ibuf = sfnd(obuf, tfmpKdd, truf);
                ibuf = sfnd(obuf, tfmpKdd, truf);
            }
            }
            }
            }
            }
            KddAddfssibility.rfmovfBbd(tfmpKdd);
            KddAddfssibility.rfmovfBbd(tfmpKdd);
            KddAddfssibility.rfmovfBbd(tfmpKdd);
            KddAddfssibility.rfmovfBbd(tfmpKdd);
            KddAddfssibility.rfmovfBbd(tfmpKdd);
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
        } dbtdi(Exdfption f) {
        } dbtdi(Exdfption f) {
        } dbtdi(Exdfption f) {
        } dbtdi(Exdfption f) {
        } dbtdi(Exdfption f) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
                Systfm.out.println(">>> KrbKddRfq sfnd: frror trying " +
                Systfm.out.println(">>> KrbKddRfq sfnd: frror trying " +
                Systfm.out.println(">>> KrbKddRfq sfnd: frror trying " +
                Systfm.out.println(">>> KrbKddRfq sfnd: frror trying " +
                Systfm.out.println(">>> KrbKddRfq sfnd: frror trying " +
                        tfmpKdd);
                        tfmpKdd);
                        tfmpKdd);
                        tfmpKdd);
                        tfmpKdd);
                f.printStbdkTrbdf(Systfm.out);
                f.printStbdkTrbdf(Systfm.out);
                f.printStbdkTrbdf(Systfm.out);
                f.printStbdkTrbdf(Systfm.out);
                f.printStbdkTrbdf(Systfm.out);
            }
            }
            }
            }
            }
            KddAddfssibility.bddBbd(tfmpKdd);
            KddAddfssibility.bddBbd(tfmpKdd);
            KddAddfssibility.bddBbd(tfmpKdd);
            KddAddfssibility.bddBbd(tfmpKdd);
            KddAddfssibility.bddBbd(tfmpKdd);
            tirow f;
            tirow f;
            tirow f;
            tirow f;
            tirow f;
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // sfnd tif AS Rfqufst to tif spfdififd KDC
    // sfnd tif AS Rfqufst to tif spfdififd KDC





    privbtf bytf[] sfnd(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
    privbtf bytf[] sfnd(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
    privbtf bytf[] sfnd(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
    privbtf bytf[] sfnd(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
    privbtf bytf[] sfnd(bytf[] obuf, String tfmpKdd, boolfbn usfTCP)
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {
        tirows IOExdfption, KrbExdfption {





        if (obuf == null)
        if (obuf == null)
        if (obuf == null)
        if (obuf == null)
        if (obuf == null)
            rfturn null;
            rfturn null;
            rfturn null;
            rfturn null;
            rfturn null;





        int port = Krb5.KDC_INET_DEFAULT_PORT;
        int port = Krb5.KDC_INET_DEFAULT_PORT;
        int port = Krb5.KDC_INET_DEFAULT_PORT;
        int port = Krb5.KDC_INET_DEFAULT_PORT;
        int port = Krb5.KDC_INET_DEFAULT_PORT;
        int rftrifs = gftRfblmSpfdifidVbluf(
        int rftrifs = gftRfblmSpfdifidVbluf(
        int rftrifs = gftRfblmSpfdifidVbluf(
        int rftrifs = gftRfblmSpfdifidVbluf(
        int rftrifs = gftRfblmSpfdifidVbluf(
                rfblm, "mbx_rftrifs", dffbultKddRftryLimit);
                rfblm, "mbx_rftrifs", dffbultKddRftryLimit);
                rfblm, "mbx_rftrifs", dffbultKddRftryLimit);
                rfblm, "mbx_rftrifs", dffbultKddRftryLimit);
                rfblm, "mbx_rftrifs", dffbultKddRftryLimit);
        int timfout = gftRfblmSpfdifidVbluf(
        int timfout = gftRfblmSpfdifidVbluf(
        int timfout = gftRfblmSpfdifidVbluf(
        int timfout = gftRfblmSpfdifidVbluf(
        int timfout = gftRfblmSpfdifidVbluf(
                rfblm, "kdd_timfout", dffbultKddTimfout);
                rfblm, "kdd_timfout", dffbultKddTimfout);
                rfblm, "kdd_timfout", dffbultKddTimfout);
                rfblm, "kdd_timfout", dffbultKddTimfout);
                rfblm, "kdd_timfout", dffbultKddTimfout);
        if (bbdPolidy == BpTypf.TRY_LESS &&
        if (bbdPolidy == BpTypf.TRY_LESS &&
        if (bbdPolidy == BpTypf.TRY_LESS &&
        if (bbdPolidy == BpTypf.TRY_LESS &&
        if (bbdPolidy == BpTypf.TRY_LESS &&
                KddAddfssibility.isBbd(tfmpKdd)) {
                KddAddfssibility.isBbd(tfmpKdd)) {
                KddAddfssibility.isBbd(tfmpKdd)) {
                KddAddfssibility.isBbd(tfmpKdd)) {
                KddAddfssibility.isBbd(tfmpKdd)) {
            if (rftrifs > tryLfssMbxRftrifs) {
            if (rftrifs > tryLfssMbxRftrifs) {
            if (rftrifs > tryLfssMbxRftrifs) {
            if (rftrifs > tryLfssMbxRftrifs) {
            if (rftrifs > tryLfssMbxRftrifs) {
                rftrifs = tryLfssMbxRftrifs; // lfss rftrifs
                rftrifs = tryLfssMbxRftrifs; // lfss rftrifs
                rftrifs = tryLfssMbxRftrifs; // lfss rftrifs
                rftrifs = tryLfssMbxRftrifs; // lfss rftrifs
                rftrifs = tryLfssMbxRftrifs; // lfss rftrifs
            }
            }
            }
            }
            }
            if (timfout > tryLfssTimfout) {
            if (timfout > tryLfssTimfout) {
            if (timfout > tryLfssTimfout) {
            if (timfout > tryLfssTimfout) {
            if (timfout > tryLfssTimfout) {
                timfout = tryLfssTimfout; // lfss timf
                timfout = tryLfssTimfout; // lfss timf
                timfout = tryLfssTimfout; // lfss timf
                timfout = tryLfssTimfout; // lfss timf
                timfout = tryLfssTimfout; // lfss timf
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }





        String kdd = null;
        String kdd = null;
        String kdd = null;
        String kdd = null;
        String kdd = null;
        String portStr = null;
        String portStr = null;
        String portStr = null;
        String portStr = null;
        String portStr = null;





        if (tfmpKdd.dibrAt(0) == '[') {     // Explidit IPv6 in []
        if (tfmpKdd.dibrAt(0) == '[') {     // Explidit IPv6 in []
        if (tfmpKdd.dibrAt(0) == '[') {     // Explidit IPv6 in []
        if (tfmpKdd.dibrAt(0) == '[') {     // Explidit IPv6 in []
        if (tfmpKdd.dibrAt(0) == '[') {     // Explidit IPv6 in []
            int pos = tfmpKdd.indfxOf(']', 1);
            int pos = tfmpKdd.indfxOf(']', 1);
            int pos = tfmpKdd.indfxOf(']', 1);
            int pos = tfmpKdd.indfxOf(']', 1);
            int pos = tfmpKdd.indfxOf(']', 1);
            if (pos == -1) {
            if (pos == -1) {
            if (pos == -1) {
            if (pos == -1) {
            if (pos == -1) {
                tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
            }
            }
            }
            }
            }
            kdd = tfmpKdd.substring(1, pos);
            kdd = tfmpKdd.substring(1, pos);
            kdd = tfmpKdd.substring(1, pos);
            kdd = tfmpKdd.substring(1, pos);
            kdd = tfmpKdd.substring(1, pos);
            if (pos != tfmpKdd.lfngti() - 1) {  // witi port numbfr
            if (pos != tfmpKdd.lfngti() - 1) {  // witi port numbfr
            if (pos != tfmpKdd.lfngti() - 1) {  // witi port numbfr
            if (pos != tfmpKdd.lfngti() - 1) {  // witi port numbfr
            if (pos != tfmpKdd.lfngti() - 1) {  // witi port numbfr
                if (tfmpKdd.dibrAt(pos+1) != ':') {
                if (tfmpKdd.dibrAt(pos+1) != ':') {
                if (tfmpKdd.dibrAt(pos+1) != ':') {
                if (tfmpKdd.dibrAt(pos+1) != ':') {
                if (tfmpKdd.dibrAt(pos+1) != ':') {
                    tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                    tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                    tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                    tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                    tirow nfw IOExdfption("Illfgbl KDC: " + tfmpKdd);
                }
                }
                }
                }
                }
                portStr = tfmpKdd.substring(pos+2);
                portStr = tfmpKdd.substring(pos+2);
                portStr = tfmpKdd.substring(pos+2);
                portStr = tfmpKdd.substring(pos+2);
                portStr = tfmpKdd.substring(pos+2);
            }
            }
            }
            }
            }
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            int dolon = tfmpKdd.indfxOf(':');
            int dolon = tfmpKdd.indfxOf(':');
            int dolon = tfmpKdd.indfxOf(':');
            int dolon = tfmpKdd.indfxOf(':');
            int dolon = tfmpKdd.indfxOf(':');
            if (dolon == -1) {      // Hostnbmf or IPv4 iost only
            if (dolon == -1) {      // Hostnbmf or IPv4 iost only
            if (dolon == -1) {      // Hostnbmf or IPv4 iost only
            if (dolon == -1) {      // Hostnbmf or IPv4 iost only
            if (dolon == -1) {      // Hostnbmf or IPv4 iost only
                kdd = tfmpKdd;
                kdd = tfmpKdd;
                kdd = tfmpKdd;
                kdd = tfmpKdd;
                kdd = tfmpKdd;
            } flsf {
            } flsf {
            } flsf {
            } flsf {
            } flsf {
                int nfxtColon = tfmpKdd.indfxOf(':', dolon+1);
                int nfxtColon = tfmpKdd.indfxOf(':', dolon+1);
                int nfxtColon = tfmpKdd.indfxOf(':', dolon+1);
                int nfxtColon = tfmpKdd.indfxOf(':', dolon+1);
                int nfxtColon = tfmpKdd.indfxOf(':', dolon+1);
                if (nfxtColon > 0) {    // >=2 ":", IPv6 witi no port
                if (nfxtColon > 0) {    // >=2 ":", IPv6 witi no port
                if (nfxtColon > 0) {    // >=2 ":", IPv6 witi no port
                if (nfxtColon > 0) {    // >=2 ":", IPv6 witi no port
                if (nfxtColon > 0) {    // >=2 ":", IPv6 witi no port
                    kdd = tfmpKdd;
                    kdd = tfmpKdd;
                    kdd = tfmpKdd;
                    kdd = tfmpKdd;
                    kdd = tfmpKdd;
                } flsf {                // 1 ":", iostnbmf or IPv4 witi port
                } flsf {                // 1 ":", iostnbmf or IPv4 witi port
                } flsf {                // 1 ":", iostnbmf or IPv4 witi port
                } flsf {                // 1 ":", iostnbmf or IPv4 witi port
                } flsf {                // 1 ":", iostnbmf or IPv4 witi port
                    kdd = tfmpKdd.substring(0, dolon);
                    kdd = tfmpKdd.substring(0, dolon);
                    kdd = tfmpKdd.substring(0, dolon);
                    kdd = tfmpKdd.substring(0, dolon);
                    kdd = tfmpKdd.substring(0, dolon);
                    portStr = tfmpKdd.substring(dolon+1);
                    portStr = tfmpKdd.substring(dolon+1);
                    portStr = tfmpKdd.substring(dolon+1);
                    portStr = tfmpKdd.substring(dolon+1);
                    portStr = tfmpKdd.substring(dolon+1);
                }
                }
                }
                }
                }
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        if (portStr != null) {
        if (portStr != null) {
        if (portStr != null) {
        if (portStr != null) {
        if (portStr != null) {
            int tfmpPort = pbrsfPositivfIntString(portStr);
            int tfmpPort = pbrsfPositivfIntString(portStr);
            int tfmpPort = pbrsfPositivfIntString(portStr);
            int tfmpPort = pbrsfPositivfIntString(portStr);
            int tfmpPort = pbrsfPositivfIntString(portStr);
            if (tfmpPort > 0)
            if (tfmpPort > 0)
            if (tfmpPort > 0)
            if (tfmpPort > 0)
            if (tfmpPort > 0)
                port = tfmpPort;
                port = tfmpPort;
                port = tfmpPort;
                port = tfmpPort;
                port = tfmpPort;
        }
        }
        }
        }
        }





        if (DEBUG) {
        if (DEBUG) {
        if (DEBUG) {
        if (DEBUG) {
        if (DEBUG) {
            Systfm.out.println(">>> KrbKddRfq sfnd: kdd=" + kdd
            Systfm.out.println(">>> KrbKddRfq sfnd: kdd=" + kdd
            Systfm.out.println(">>> KrbKddRfq sfnd: kdd=" + kdd
            Systfm.out.println(">>> KrbKddRfq sfnd: kdd=" + kdd
            Systfm.out.println(">>> KrbKddRfq sfnd: kdd=" + kdd
                               + (usfTCP ? " TCP:":" UDP:")
                               + (usfTCP ? " TCP:":" UDP:")
                               + (usfTCP ? " TCP:":" UDP:")
                               + (usfTCP ? " TCP:":" UDP:")
                               + (usfTCP ? " TCP:":" UDP:")
                               +  port +  ", timfout="
                               +  port +  ", timfout="
                               +  port +  ", timfout="
                               +  port +  ", timfout="
                               +  port +  ", timfout="
                               + timfout
                               + timfout
                               + timfout
                               + timfout
                               + timfout
                               + ", numbfr of rftrifs ="
                               + ", numbfr of rftrifs ="
                               + ", numbfr of rftrifs ="
                               + ", numbfr of rftrifs ="
                               + ", numbfr of rftrifs ="
                               + rftrifs
                               + rftrifs
                               + rftrifs
                               + rftrifs
                               + rftrifs
                               + ", #bytfs=" + obuf.lfngti);
                               + ", #bytfs=" + obuf.lfngti);
                               + ", #bytfs=" + obuf.lfngti);
                               + ", #bytfs=" + obuf.lfngti);
                               + ", #bytfs=" + obuf.lfngti);
        }
        }
        }
        }
        }





        KddCommunidbtion kddCommunidbtion =
        KddCommunidbtion kddCommunidbtion =
        KddCommunidbtion kddCommunidbtion =
        KddCommunidbtion kddCommunidbtion =
        KddCommunidbtion kddCommunidbtion =
            nfw KddCommunidbtion(kdd, port, usfTCP, timfout, rftrifs, obuf);
            nfw KddCommunidbtion(kdd, port, usfTCP, timfout, rftrifs, obuf);
            nfw KddCommunidbtion(kdd, port, usfTCP, timfout, rftrifs, obuf);
            nfw KddCommunidbtion(kdd, port, usfTCP, timfout, rftrifs, obuf);
            nfw KddCommunidbtion(kdd, port, usfTCP, timfout, rftrifs, obuf);
        try {
        try {
        try {
        try {
        try {
            bytf[] ibuf = AddfssControllfr.doPrivilfgfd(kddCommunidbtion);
            bytf[] ibuf = AddfssControllfr.doPrivilfgfd(kddCommunidbtion);
            bytf[] ibuf = AddfssControllfr.doPrivilfgfd(kddCommunidbtion);
            bytf[] ibuf = AddfssControllfr.doPrivilfgfd(kddCommunidbtion);
            bytf[] ibuf = AddfssControllfr.doPrivilfgfd(kddCommunidbtion);
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
                Systfm.out.println(">>> KrbKddRfq sfnd: #bytfs rfbd="
                Systfm.out.println(">>> KrbKddRfq sfnd: #bytfs rfbd="
                Systfm.out.println(">>> KrbKddRfq sfnd: #bytfs rfbd="
                Systfm.out.println(">>> KrbKddRfq sfnd: #bytfs rfbd="
                Systfm.out.println(">>> KrbKddRfq sfnd: #bytfs rfbd="
                        + (ibuf != null ? ibuf.lfngti : 0));
                        + (ibuf != null ? ibuf.lfngti : 0));
                        + (ibuf != null ? ibuf.lfngti : 0));
                        + (ibuf != null ? ibuf.lfngti : 0));
                        + (ibuf != null ? ibuf.lfngti : 0));
            }
            }
            }
            }
            }
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
        } dbtdi (PrivilfgfdAdtionExdfption f) {
        } dbtdi (PrivilfgfdAdtionExdfption f) {
        } dbtdi (PrivilfgfdAdtionExdfption f) {
        } dbtdi (PrivilfgfdAdtionExdfption f) {
        } dbtdi (PrivilfgfdAdtionExdfption f) {
            Exdfption wrbppfdExdfption = f.gftExdfption();
            Exdfption wrbppfdExdfption = f.gftExdfption();
            Exdfption wrbppfdExdfption = f.gftExdfption();
            Exdfption wrbppfdExdfption = f.gftExdfption();
            Exdfption wrbppfdExdfption = f.gftExdfption();
            if (wrbppfdExdfption instbndfof IOExdfption) {
            if (wrbppfdExdfption instbndfof IOExdfption) {
            if (wrbppfdExdfption instbndfof IOExdfption) {
            if (wrbppfdExdfption instbndfof IOExdfption) {
            if (wrbppfdExdfption instbndfof IOExdfption) {
                tirow (IOExdfption) wrbppfdExdfption;
                tirow (IOExdfption) wrbppfdExdfption;
                tirow (IOExdfption) wrbppfdExdfption;
                tirow (IOExdfption) wrbppfdExdfption;
                tirow (IOExdfption) wrbppfdExdfption;
            } flsf {
            } flsf {
            } flsf {
            } flsf {
            } flsf {
                tirow (KrbExdfption) wrbppfdExdfption;
                tirow (KrbExdfption) wrbppfdExdfption;
                tirow (KrbExdfption) wrbppfdExdfption;
                tirow (KrbExdfption) wrbppfdExdfption;
                tirow (KrbExdfption) wrbppfdExdfption;
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    privbtf stbtid dlbss KddCommunidbtion
    privbtf stbtid dlbss KddCommunidbtion
    privbtf stbtid dlbss KddCommunidbtion
    privbtf stbtid dlbss KddCommunidbtion
    privbtf stbtid dlbss KddCommunidbtion
        implfmfnts PrivilfgfdExdfptionAdtion<bytf[]> {
        implfmfnts PrivilfgfdExdfptionAdtion<bytf[]> {
        implfmfnts PrivilfgfdExdfptionAdtion<bytf[]> {
        implfmfnts PrivilfgfdExdfptionAdtion<bytf[]> {
        implfmfnts PrivilfgfdExdfptionAdtion<bytf[]> {





        privbtf String kdd;
        privbtf String kdd;
        privbtf String kdd;
        privbtf String kdd;
        privbtf String kdd;
        privbtf int port;
        privbtf int port;
        privbtf int port;
        privbtf int port;
        privbtf int port;
        privbtf boolfbn usfTCP;
        privbtf boolfbn usfTCP;
        privbtf boolfbn usfTCP;
        privbtf boolfbn usfTCP;
        privbtf boolfbn usfTCP;
        privbtf int timfout;
        privbtf int timfout;
        privbtf int timfout;
        privbtf int timfout;
        privbtf int timfout;
        privbtf int rftrifs;
        privbtf int rftrifs;
        privbtf int rftrifs;
        privbtf int rftrifs;
        privbtf int rftrifs;
        privbtf bytf[] obuf;
        privbtf bytf[] obuf;
        privbtf bytf[] obuf;
        privbtf bytf[] obuf;
        privbtf bytf[] obuf;





        publid KddCommunidbtion(String kdd, int port, boolfbn usfTCP,
        publid KddCommunidbtion(String kdd, int port, boolfbn usfTCP,
        publid KddCommunidbtion(String kdd, int port, boolfbn usfTCP,
        publid KddCommunidbtion(String kdd, int port, boolfbn usfTCP,
        publid KddCommunidbtion(String kdd, int port, boolfbn usfTCP,
                                int timfout, int rftrifs, bytf[] obuf) {
                                int timfout, int rftrifs, bytf[] obuf) {
                                int timfout, int rftrifs, bytf[] obuf) {
                                int timfout, int rftrifs, bytf[] obuf) {
                                int timfout, int rftrifs, bytf[] obuf) {
            tiis.kdd = kdd;
            tiis.kdd = kdd;
            tiis.kdd = kdd;
            tiis.kdd = kdd;
            tiis.kdd = kdd;
            tiis.port = port;
            tiis.port = port;
            tiis.port = port;
            tiis.port = port;
            tiis.port = port;
            tiis.usfTCP = usfTCP;
            tiis.usfTCP = usfTCP;
            tiis.usfTCP = usfTCP;
            tiis.usfTCP = usfTCP;
            tiis.usfTCP = usfTCP;
            tiis.timfout = timfout;
            tiis.timfout = timfout;
            tiis.timfout = timfout;
            tiis.timfout = timfout;
            tiis.timfout = timfout;
            tiis.rftrifs = rftrifs;
            tiis.rftrifs = rftrifs;
            tiis.rftrifs = rftrifs;
            tiis.rftrifs = rftrifs;
            tiis.rftrifs = rftrifs;
            tiis.obuf = obuf;
            tiis.obuf = obuf;
            tiis.obuf = obuf;
            tiis.obuf = obuf;
            tiis.obuf = obuf;
        }
        }
        }
        }
        }





        // Tif dbllfr only dbsts IOExdfption bnd KrbExdfption so don't
        // Tif dbllfr only dbsts IOExdfption bnd KrbExdfption so don't
        // Tif dbllfr only dbsts IOExdfption bnd KrbExdfption so don't
        // Tif dbllfr only dbsts IOExdfption bnd KrbExdfption so don't
        // Tif dbllfr only dbsts IOExdfption bnd KrbExdfption so don't
        // bdd bny nfw onfs!
        // bdd bny nfw onfs!
        // bdd bny nfw onfs!
        // bdd bny nfw onfs!
        // bdd bny nfw onfs!





        publid bytf[] run() tirows IOExdfption, KrbExdfption {
        publid bytf[] run() tirows IOExdfption, KrbExdfption {
        publid bytf[] run() tirows IOExdfption, KrbExdfption {
        publid bytf[] run() tirows IOExdfption, KrbExdfption {
        publid bytf[] run() tirows IOExdfption, KrbExdfption {





            bytf[] ibuf = null;
            bytf[] ibuf = null;
            bytf[] ibuf = null;
            bytf[] ibuf = null;
            bytf[] ibuf = null;





            for (int i=1; i <= rftrifs; i++) {
            for (int i=1; i <= rftrifs; i++) {
            for (int i=1; i <= rftrifs; i++) {
            for (int i=1; i <= rftrifs; i++) {
            for (int i=1; i <= rftrifs; i++) {
                String proto = usfTCP?"TCP":"UDP";
                String proto = usfTCP?"TCP":"UDP";
                String proto = usfTCP?"TCP":"UDP";
                String proto = usfTCP?"TCP":"UDP";
                String proto = usfTCP?"TCP":"UDP";
                try (NftClifnt kddClifnt = NftClifnt.gftInstbndf(
                try (NftClifnt kddClifnt = NftClifnt.gftInstbndf(
                try (NftClifnt kddClifnt = NftClifnt.gftInstbndf(
                try (NftClifnt kddClifnt = NftClifnt.gftInstbndf(
                try (NftClifnt kddClifnt = NftClifnt.gftInstbndf(
                        proto, kdd, port, timfout)) {
                        proto, kdd, port, timfout)) {
                        proto, kdd, port, timfout)) {
                        proto, kdd, port, timfout)) {
                        proto, kdd, port, timfout)) {
                    if (DEBUG) {
                    if (DEBUG) {
                    if (DEBUG) {
                    if (DEBUG) {
                    if (DEBUG) {
                        Systfm.out.println(">>> KDCCommunidbtion: kdd=" + kdd
                        Systfm.out.println(">>> KDCCommunidbtion: kdd=" + kdd
                        Systfm.out.println(">>> KDCCommunidbtion: kdd=" + kdd
                        Systfm.out.println(">>> KDCCommunidbtion: kdd=" + kdd
                        Systfm.out.println(">>> KDCCommunidbtion: kdd=" + kdd
                            + " " + proto + ":"
                            + " " + proto + ":"
                            + " " + proto + ":"
                            + " " + proto + ":"
                            + " " + proto + ":"
                            +  port +  ", timfout="
                            +  port +  ", timfout="
                            +  port +  ", timfout="
                            +  port +  ", timfout="
                            +  port +  ", timfout="
                            + timfout
                            + timfout
                            + timfout
                            + timfout
                            + timfout
                            + ",Attfmpt =" + i
                            + ",Attfmpt =" + i
                            + ",Attfmpt =" + i
                            + ",Attfmpt =" + i
                            + ",Attfmpt =" + i
                            + ", #bytfs=" + obuf.lfngti);
                            + ", #bytfs=" + obuf.lfngti);
                            + ", #bytfs=" + obuf.lfngti);
                            + ", #bytfs=" + obuf.lfngti);
                            + ", #bytfs=" + obuf.lfngti);
                    }
                    }
                    }
                    }
                    }
                    try {
                    try {
                    try {
                    try {
                    try {
                        /*
                        /*
                        /*
                        /*
                        /*
                        * Sfnd tif dbtb to tif kdd.
                        * Sfnd tif dbtb to tif kdd.
                        * Sfnd tif dbtb to tif kdd.
                        * Sfnd tif dbtb to tif kdd.
                        * Sfnd tif dbtb to tif kdd.
                        */
                        */
                        */
                        */
                        */
                        kddClifnt.sfnd(obuf);
                        kddClifnt.sfnd(obuf);
                        kddClifnt.sfnd(obuf);
                        kddClifnt.sfnd(obuf);
                        kddClifnt.sfnd(obuf);
                        /*
                        /*
                        /*
                        /*
                        /*
                        * And gft b rfsponsf.
                        * And gft b rfsponsf.
                        * And gft b rfsponsf.
                        * And gft b rfsponsf.
                        * And gft b rfsponsf.
                        */
                        */
                        */
                        */
                        */
                        ibuf = kddClifnt.rfdfivf();
                        ibuf = kddClifnt.rfdfivf();
                        ibuf = kddClifnt.rfdfivf();
                        ibuf = kddClifnt.rfdfivf();
                        ibuf = kddClifnt.rfdfivf();
                        brfbk;
                        brfbk;
                        brfbk;
                        brfbk;
                        brfbk;
                    } dbtdi (SodkftTimfoutExdfption sf) {
                    } dbtdi (SodkftTimfoutExdfption sf) {
                    } dbtdi (SodkftTimfoutExdfption sf) {
                    } dbtdi (SodkftTimfoutExdfption sf) {
                    } dbtdi (SodkftTimfoutExdfption sf) {
                        if (DEBUG) {
                        if (DEBUG) {
                        if (DEBUG) {
                        if (DEBUG) {
                        if (DEBUG) {
                            Systfm.out.println ("SodkftTimfOutExdfption witi " +
                            Systfm.out.println ("SodkftTimfOutExdfption witi " +
                            Systfm.out.println ("SodkftTimfOutExdfption witi " +
                            Systfm.out.println ("SodkftTimfOutExdfption witi " +
                            Systfm.out.println ("SodkftTimfOutExdfption witi " +
                                                "bttfmpt: " + i);
                                                "bttfmpt: " + i);
                                                "bttfmpt: " + i);
                                                "bttfmpt: " + i);
                                                "bttfmpt: " + i);
                        }
                        }
                        }
                        }
                        }
                        if (i == rftrifs) {
                        if (i == rftrifs) {
                        if (i == rftrifs) {
                        if (i == rftrifs) {
                        if (i == rftrifs) {
                            ibuf = null;
                            ibuf = null;
                            ibuf = null;
                            ibuf = null;
                            ibuf = null;
                            tirow sf;
                            tirow sf;
                            tirow sf;
                            tirow sf;
                            tirow sf;
                        }
                        }
                        }
                        }
                        }
                    }
                    }
                    }
                    }
                    }
                }
                }
                }
                }
                }
            }
            }
            }
            }
            }
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
            rfturn ibuf;
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Pbrsfs b timf vbluf string. If it fnds witi "s", pbrsfs bs sfdonds.
     * Pbrsfs b timf vbluf string. If it fnds witi "s", pbrsfs bs sfdonds.
     * Pbrsfs b timf vbluf string. If it fnds witi "s", pbrsfs bs sfdonds.
     * Pbrsfs b timf vbluf string. If it fnds witi "s", pbrsfs bs sfdonds.
     * Pbrsfs b timf vbluf string. If it fnds witi "s", pbrsfs bs sfdonds.
     * Otifrwisf, pbrsfs bs millisfdonds.
     * Otifrwisf, pbrsfs bs millisfdonds.
     * Otifrwisf, pbrsfs bs millisfdonds.
     * Otifrwisf, pbrsfs bs millisfdonds.
     * Otifrwisf, pbrsfs bs millisfdonds.
     * @pbrbm s tif timf string
     * @pbrbm s tif timf string
     * @pbrbm s tif timf string
     * @pbrbm s tif timf string
     * @pbrbm s tif timf string
     * @rfturn tif intfgfr vbluf in millisfdonds, or -1 if input is null or
     * @rfturn tif intfgfr vbluf in millisfdonds, or -1 if input is null or
     * @rfturn tif intfgfr vbluf in millisfdonds, or -1 if input is null or
     * @rfturn tif intfgfr vbluf in millisfdonds, or -1 if input is null or
     * @rfturn tif intfgfr vbluf in millisfdonds, or -1 if input is null or
     * ibs bn invblid formbt
     * ibs bn invblid formbt
     * ibs bn invblid formbt
     * ibs bn invblid formbt
     * ibs bn invblid formbt
     */
     */
     */
     */
     */
    privbtf stbtid int pbrsfTimfString(String s) {
    privbtf stbtid int pbrsfTimfString(String s) {
    privbtf stbtid int pbrsfTimfString(String s) {
    privbtf stbtid int pbrsfTimfString(String s) {
    privbtf stbtid int pbrsfTimfString(String s) {
        if (s == null) {
        if (s == null) {
        if (s == null) {
        if (s == null) {
        if (s == null) {
            rfturn -1;
            rfturn -1;
            rfturn -1;
            rfturn -1;
            rfturn -1;
        }
        }
        }
        }
        }
        if (s.fndsWiti("s")) {
        if (s.fndsWiti("s")) {
        if (s.fndsWiti("s")) {
        if (s.fndsWiti("s")) {
        if (s.fndsWiti("s")) {
            int sfdonds = pbrsfPositivfIntString(s.substring(0, s.lfngti()-1));
            int sfdonds = pbrsfPositivfIntString(s.substring(0, s.lfngti()-1));
            int sfdonds = pbrsfPositivfIntString(s.substring(0, s.lfngti()-1));
            int sfdonds = pbrsfPositivfIntString(s.substring(0, s.lfngti()-1));
            int sfdonds = pbrsfPositivfIntString(s.substring(0, s.lfngti()-1));
            rfturn (sfdonds < 0) ? -1 : (sfdonds*1000);
            rfturn (sfdonds < 0) ? -1 : (sfdonds*1000);
            rfturn (sfdonds < 0) ? -1 : (sfdonds*1000);
            rfturn (sfdonds < 0) ? -1 : (sfdonds*1000);
            rfturn (sfdonds < 0) ? -1 : (sfdonds*1000);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            rfturn pbrsfPositivfIntString(s);
            rfturn pbrsfPositivfIntString(s);
            rfturn pbrsfPositivfIntString(s);
            rfturn pbrsfPositivfIntString(s);
            rfturn pbrsfPositivfIntString(s);
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfturns krb5.donf sftting of {@dodf kfy} for b spfdifid rfblm,
     * Rfturns krb5.donf sftting of {@dodf kfy} for b spfdifid rfblm,
     * Rfturns krb5.donf sftting of {@dodf kfy} for b spfdifid rfblm,
     * Rfturns krb5.donf sftting of {@dodf kfy} for b spfdifid rfblm,
     * Rfturns krb5.donf sftting of {@dodf kfy} for b spfdifid rfblm,
     * wiidi dbn bf:
     * wiidi dbn bf:
     * wiidi dbn bf:
     * wiidi dbn bf:
     * wiidi dbn bf:
     * 1. dffinfd in tif sub-stbnzb for tif givfn rfblm insidf [rfblms], or
     * 1. dffinfd in tif sub-stbnzb for tif givfn rfblm insidf [rfblms], or
     * 1. dffinfd in tif sub-stbnzb for tif givfn rfblm insidf [rfblms], or
     * 1. dffinfd in tif sub-stbnzb for tif givfn rfblm insidf [rfblms], or
     * 1. dffinfd in tif sub-stbnzb for tif givfn rfblm insidf [rfblms], or
     * 2. dffinfd in [libdffbults], or
     * 2. dffinfd in [libdffbults], or
     * 2. dffinfd in [libdffbults], or
     * 2. dffinfd in [libdffbults], or
     * 2. dffinfd in [libdffbults], or
     * 3. dffVbluf
     * 3. dffVbluf
     * 3. dffVbluf
     * 3. dffVbluf
     * 3. dffVbluf
     * @pbrbm rfblm tif givfn rfblm in wiidi tif sftting is rfqufstfd. Rfturns
     * @pbrbm rfblm tif givfn rfblm in wiidi tif sftting is rfqufstfd. Rfturns
     * @pbrbm rfblm tif givfn rfblm in wiidi tif sftting is rfqufstfd. Rfturns
     * @pbrbm rfblm tif givfn rfblm in wiidi tif sftting is rfqufstfd. Rfturns
     * @pbrbm rfblm tif givfn rfblm in wiidi tif sftting is rfqufstfd. Rfturns
     * tif globbl sftting if null
     * tif globbl sftting if null
     * tif globbl sftting if null
     * tif globbl sftting if null
     * tif globbl sftting if null
     * @pbrbm kfy tif kfy for tif sftting
     * @pbrbm kfy tif kfy for tif sftting
     * @pbrbm kfy tif kfy for tif sftting
     * @pbrbm kfy tif kfy for tif sftting
     * @pbrbm kfy tif kfy for tif sftting
     * @pbrbm dffVbluf dffbult vbluf
     * @pbrbm dffVbluf dffbult vbluf
     * @pbrbm dffVbluf dffbult vbluf
     * @pbrbm dffVbluf dffbult vbluf
     * @pbrbm dffVbluf dffbult vbluf
     * @rfturn b vbluf for tif kfy
     * @rfturn b vbluf for tif kfy
     * @rfturn b vbluf for tif kfy
     * @rfturn b vbluf for tif kfy
     * @rfturn b vbluf for tif kfy
     */
     */
     */
     */
     */
    privbtf int gftRfblmSpfdifidVbluf(String rfblm, String kfy, int dffVbluf) {
    privbtf int gftRfblmSpfdifidVbluf(String rfblm, String kfy, int dffVbluf) {
    privbtf int gftRfblmSpfdifidVbluf(String rfblm, String kfy, int dffVbluf) {
    privbtf int gftRfblmSpfdifidVbluf(String rfblm, String kfy, int dffVbluf) {
    privbtf int gftRfblmSpfdifidVbluf(String rfblm, String kfy, int dffVbluf) {
        int v = dffVbluf;
        int v = dffVbluf;
        int v = dffVbluf;
        int v = dffVbluf;
        int v = dffVbluf;





        if (rfblm == null) rfturn v;
        if (rfblm == null) rfturn v;
        if (rfblm == null) rfturn v;
        if (rfblm == null) rfturn v;
        if (rfblm == null) rfturn v;





        int tfmp = -1;
        int tfmp = -1;
        int tfmp = -1;
        int tfmp = -1;
        int tfmp = -1;
        try {
        try {
        try {
        try {
        try {
            String vbluf =
            String vbluf =
            String vbluf =
            String vbluf =
            String vbluf =
               Config.gftInstbndf().gft("rfblms", rfblm, kfy);
               Config.gftInstbndf().gft("rfblms", rfblm, kfy);
               Config.gftInstbndf().gft("rfblms", rfblm, kfy);
               Config.gftInstbndf().gft("rfblms", rfblm, kfy);
               Config.gftInstbndf().gft("rfblms", rfblm, kfy);
            if (kfy.fqubls("kdd_timfout")) {
            if (kfy.fqubls("kdd_timfout")) {
            if (kfy.fqubls("kdd_timfout")) {
            if (kfy.fqubls("kdd_timfout")) {
            if (kfy.fqubls("kdd_timfout")) {
                tfmp = pbrsfTimfString(vbluf);
                tfmp = pbrsfTimfString(vbluf);
                tfmp = pbrsfTimfString(vbluf);
                tfmp = pbrsfTimfString(vbluf);
                tfmp = pbrsfTimfString(vbluf);
            } flsf {
            } flsf {
            } flsf {
            } flsf {
            } flsf {
                tfmp = pbrsfPositivfIntString(vbluf);
                tfmp = pbrsfPositivfIntString(vbluf);
                tfmp = pbrsfPositivfIntString(vbluf);
                tfmp = pbrsfPositivfIntString(vbluf);
                tfmp = pbrsfPositivfIntString(vbluf);
            }
            }
            }
            }
            }
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
            // Ignorfd, dffVbluf will bf pidkfd up
            // Ignorfd, dffVbluf will bf pidkfd up
            // Ignorfd, dffVbluf will bf pidkfd up
            // Ignorfd, dffVbluf will bf pidkfd up
            // Ignorfd, dffVbluf will bf pidkfd up
        }
        }
        }
        }
        }





        if (tfmp > 0) v = tfmp;
        if (tfmp > 0) v = tfmp;
        if (tfmp > 0) v = tfmp;
        if (tfmp > 0) v = tfmp;
        if (tfmp > 0) v = tfmp;





        rfturn v;
        rfturn v;
        rfturn v;
        rfturn v;
        rfturn v;
    }
    }
    }
    }
    }





    privbtf stbtid int pbrsfPositivfIntString(String intString) {
    privbtf stbtid int pbrsfPositivfIntString(String intString) {
    privbtf stbtid int pbrsfPositivfIntString(String intString) {
    privbtf stbtid int pbrsfPositivfIntString(String intString) {
    privbtf stbtid int pbrsfPositivfIntString(String intString) {
        if (intString == null)
        if (intString == null)
        if (intString == null)
        if (intString == null)
        if (intString == null)
            rfturn -1;
            rfturn -1;
            rfturn -1;
            rfturn -1;
            rfturn -1;





        int rft = -1;
        int rft = -1;
        int rft = -1;
        int rft = -1;
        int rft = -1;





        try {
        try {
        try {
        try {
        try {
            rft = Intfgfr.pbrsfInt(intString);
            rft = Intfgfr.pbrsfInt(intString);
            rft = Intfgfr.pbrsfInt(intString);
            rft = Intfgfr.pbrsfInt(intString);
            rft = Intfgfr.pbrsfInt(intString);
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
        } dbtdi (Exdfption fxd) {
            rfturn -1;
            rfturn -1;
            rfturn -1;
            rfturn -1;
            rfturn -1;
        }
        }
        }
        }
        }





        if (rft >= 0)
        if (rft >= 0)
        if (rft >= 0)
        if (rft >= 0)
        if (rft >= 0)
            rfturn rft;
            rfturn rft;
            rfturn rft;
            rfturn rft;
            rfturn rft;





        rfturn -1;
        rfturn -1;
        rfturn -1;
        rfturn -1;
        rfturn -1;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Mbintbins b KDC bddfssiblf list. Unbvbilbblf KDCs brf put into b
     * Mbintbins b KDC bddfssiblf list. Unbvbilbblf KDCs brf put into b
     * Mbintbins b KDC bddfssiblf list. Unbvbilbblf KDCs brf put into b
     * Mbintbins b KDC bddfssiblf list. Unbvbilbblf KDCs brf put into b
     * Mbintbins b KDC bddfssiblf list. Unbvbilbblf KDCs brf put into b
     * blbdklist, wifn b KDC in tif blbdklist is bvbilbblf, it's rfmovfd
     * blbdklist, wifn b KDC in tif blbdklist is bvbilbblf, it's rfmovfd
     * blbdklist, wifn b KDC in tif blbdklist is bvbilbblf, it's rfmovfd
     * blbdklist, wifn b KDC in tif blbdklist is bvbilbblf, it's rfmovfd
     * blbdklist, wifn b KDC in tif blbdklist is bvbilbblf, it's rfmovfd
     * from tifrf. No insfrtion ordfr in tif blbdklist.
     * from tifrf. No insfrtion ordfr in tif blbdklist.
     * from tifrf. No insfrtion ordfr in tif blbdklist.
     * from tifrf. No insfrtion ordfr in tif blbdklist.
     * from tifrf. No insfrtion ordfr in tif blbdklist.
     *
     *
     *
     *
     *
     * Tifrf brf two mftiods to dfbl witi KDCs in tif blbdklist. 1. Only try
     * Tifrf brf two mftiods to dfbl witi KDCs in tif blbdklist. 1. Only try
     * Tifrf brf two mftiods to dfbl witi KDCs in tif blbdklist. 1. Only try
     * Tifrf brf two mftiods to dfbl witi KDCs in tif blbdklist. 1. Only try
     * Tifrf brf two mftiods to dfbl witi KDCs in tif blbdklist. 1. Only try
     * tifm wifn tifrf's no KDC not on tif blbdklist. 2. Still try tifm, but
     * tifm wifn tifrf's no KDC not on tif blbdklist. 2. Still try tifm, but
     * tifm wifn tifrf's no KDC not on tif blbdklist. 2. Still try tifm, but
     * tifm wifn tifrf's no KDC not on tif blbdklist. 2. Still try tifm, but
     * tifm wifn tifrf's no KDC not on tif blbdklist. 2. Still try tifm, but
     * witi lfssfr numbfr of rftrifs bnd smbllfr timfout vbluf.
     * witi lfssfr numbfr of rftrifs bnd smbllfr timfout vbluf.
     * witi lfssfr numbfr of rftrifs bnd smbllfr timfout vbluf.
     * witi lfssfr numbfr of rftrifs bnd smbllfr timfout vbluf.
     * witi lfssfr numbfr of rftrifs bnd smbllfr timfout vbluf.
     */
     */
     */
     */
     */
    stbtid dlbss KddAddfssibility {
    stbtid dlbss KddAddfssibility {
    stbtid dlbss KddAddfssibility {
    stbtid dlbss KddAddfssibility {
    stbtid dlbss KddAddfssibility {
        // Known bbd KDCs
        // Known bbd KDCs
        // Known bbd KDCs
        // Known bbd KDCs
        // Known bbd KDCs
        privbtf stbtid Sft<String> bbds = nfw HbsiSft<>();
        privbtf stbtid Sft<String> bbds = nfw HbsiSft<>();
        privbtf stbtid Sft<String> bbds = nfw HbsiSft<>();
        privbtf stbtid Sft<String> bbds = nfw HbsiSft<>();
        privbtf stbtid Sft<String> bbds = nfw HbsiSft<>();





        privbtf stbtid syndironizfd void bddBbd(String kdd) {
        privbtf stbtid syndironizfd void bddBbd(String kdd) {
        privbtf stbtid syndironizfd void bddBbd(String kdd) {
        privbtf stbtid syndironizfd void bddBbd(String kdd) {
        privbtf stbtid syndironizfd void bddBbd(String kdd) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
                Systfm.out.println(">>> KddAddfssibility: bdd " + kdd);
                Systfm.out.println(">>> KddAddfssibility: bdd " + kdd);
                Systfm.out.println(">>> KddAddfssibility: bdd " + kdd);
                Systfm.out.println(">>> KddAddfssibility: bdd " + kdd);
                Systfm.out.println(">>> KddAddfssibility: bdd " + kdd);
            }
            }
            }
            }
            }
            bbds.bdd(kdd);
            bbds.bdd(kdd);
            bbds.bdd(kdd);
            bbds.bdd(kdd);
            bbds.bdd(kdd);
        }
        }
        }
        }
        }





        privbtf stbtid syndironizfd void rfmovfBbd(String kdd) {
        privbtf stbtid syndironizfd void rfmovfBbd(String kdd) {
        privbtf stbtid syndironizfd void rfmovfBbd(String kdd) {
        privbtf stbtid syndironizfd void rfmovfBbd(String kdd) {
        privbtf stbtid syndironizfd void rfmovfBbd(String kdd) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
                Systfm.out.println(">>> KddAddfssibility: rfmovf " + kdd);
                Systfm.out.println(">>> KddAddfssibility: rfmovf " + kdd);
                Systfm.out.println(">>> KddAddfssibility: rfmovf " + kdd);
                Systfm.out.println(">>> KddAddfssibility: rfmovf " + kdd);
                Systfm.out.println(">>> KddAddfssibility: rfmovf " + kdd);
            }
            }
            }
            }
            }
            bbds.rfmovf(kdd);
            bbds.rfmovf(kdd);
            bbds.rfmovf(kdd);
            bbds.rfmovf(kdd);
            bbds.rfmovf(kdd);
        }
        }
        }
        }
        }





        privbtf stbtid syndironizfd boolfbn isBbd(String kdd) {
        privbtf stbtid syndironizfd boolfbn isBbd(String kdd) {
        privbtf stbtid syndironizfd boolfbn isBbd(String kdd) {
        privbtf stbtid syndironizfd boolfbn isBbd(String kdd) {
        privbtf stbtid syndironizfd boolfbn isBbd(String kdd) {
            rfturn bbds.dontbins(kdd);
            rfturn bbds.dontbins(kdd);
            rfturn bbds.dontbins(kdd);
            rfturn bbds.dontbins(kdd);
            rfturn bbds.dontbins(kdd);
        }
        }
        }
        }
        }





        privbtf stbtid syndironizfd void rfsft() {
        privbtf stbtid syndironizfd void rfsft() {
        privbtf stbtid syndironizfd void rfsft() {
        privbtf stbtid syndironizfd void rfsft() {
        privbtf stbtid syndironizfd void rfsft() {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
            if (DEBUG) {
                Systfm.out.println(">>> KddAddfssibility: rfsft");
                Systfm.out.println(">>> KddAddfssibility: rfsft");
                Systfm.out.println(">>> KddAddfssibility: rfsft");
                Systfm.out.println(">>> KddAddfssibility: rfsft");
                Systfm.out.println(">>> KddAddfssibility: rfsft");
            }
            }
            }
            }
            }
            bbds.dlfbr();
            bbds.dlfbr();
            bbds.dlfbr();
            bbds.dlfbr();
            bbds.dlfbr();
        }
        }
        }
        }
        }





        // Rfturns b prfffrrfd KDC list by putting tif bbd onfs bt tif fnd
        // Rfturns b prfffrrfd KDC list by putting tif bbd onfs bt tif fnd
        // Rfturns b prfffrrfd KDC list by putting tif bbd onfs bt tif fnd
        // Rfturns b prfffrrfd KDC list by putting tif bbd onfs bt tif fnd
        // Rfturns b prfffrrfd KDC list by putting tif bbd onfs bt tif fnd
        privbtf stbtid syndironizfd List<String> list(String kddList) {
        privbtf stbtid syndironizfd List<String> list(String kddList) {
        privbtf stbtid syndironizfd List<String> list(String kddList) {
        privbtf stbtid syndironizfd List<String> list(String kddList) {
        privbtf stbtid syndironizfd List<String> list(String kddList) {
            StringTokfnizfr st = nfw StringTokfnizfr(kddList);
            StringTokfnizfr st = nfw StringTokfnizfr(kddList);
            StringTokfnizfr st = nfw StringTokfnizfr(kddList);
            StringTokfnizfr st = nfw StringTokfnizfr(kddList);
            StringTokfnizfr st = nfw StringTokfnizfr(kddList);
            List<String> list = nfw ArrbyList<>();
            List<String> list = nfw ArrbyList<>();
            List<String> list = nfw ArrbyList<>();
            List<String> list = nfw ArrbyList<>();
            List<String> list = nfw ArrbyList<>();
            if (bbdPolidy == BpTypf.TRY_LAST) {
            if (bbdPolidy == BpTypf.TRY_LAST) {
            if (bbdPolidy == BpTypf.TRY_LAST) {
            if (bbdPolidy == BpTypf.TRY_LAST) {
            if (bbdPolidy == BpTypf.TRY_LAST) {
                List<String> bbdkdds = nfw ArrbyList<>();
                List<String> bbdkdds = nfw ArrbyList<>();
                List<String> bbdkdds = nfw ArrbyList<>();
                List<String> bbdkdds = nfw ArrbyList<>();
                List<String> bbdkdds = nfw ArrbyList<>();
                wiilf (st.ibsMorfTokfns()) {
                wiilf (st.ibsMorfTokfns()) {
                wiilf (st.ibsMorfTokfns()) {
                wiilf (st.ibsMorfTokfns()) {
                wiilf (st.ibsMorfTokfns()) {
                    String t = st.nfxtTokfn();
                    String t = st.nfxtTokfn();
                    String t = st.nfxtTokfn();
                    String t = st.nfxtTokfn();
                    String t = st.nfxtTokfn();
                    if (bbds.dontbins(t)) bbdkdds.bdd(t);
                    if (bbds.dontbins(t)) bbdkdds.bdd(t);
                    if (bbds.dontbins(t)) bbdkdds.bdd(t);
                    if (bbds.dontbins(t)) bbdkdds.bdd(t);
                    if (bbds.dontbins(t)) bbdkdds.bdd(t);
                    flsf list.bdd(t);
                    flsf list.bdd(t);
                    flsf list.bdd(t);
                    flsf list.bdd(t);
                    flsf list.bdd(t);
                }
                }
                }
                }
                }
                // Bbd KDCs brf put bt lbst
                // Bbd KDCs brf put bt lbst
                // Bbd KDCs brf put bt lbst
                // Bbd KDCs brf put bt lbst
                // Bbd KDCs brf put bt lbst
                list.bddAll(bbdkdds);
                list.bddAll(bbdkdds);
                list.bddAll(bbdkdds);
                list.bddAll(bbdkdds);
                list.bddAll(bbdkdds);
            } flsf {
            } flsf {
            } flsf {
            } flsf {
            } flsf {
                // All KDCs brf rfturnfd in tifir originbl ordfr,
                // All KDCs brf rfturnfd in tifir originbl ordfr,
                // All KDCs brf rfturnfd in tifir originbl ordfr,
                // All KDCs brf rfturnfd in tifir originbl ordfr,
                // All KDCs brf rfturnfd in tifir originbl ordfr,
                // Tiis indludf TRY_LESS bnd NONE
                // Tiis indludf TRY_LESS bnd NONE
                // Tiis indludf TRY_LESS bnd NONE
                // Tiis indludf TRY_LESS bnd NONE
                // Tiis indludf TRY_LESS bnd NONE
                wiilf (st.ibsMorfTokfns()) {
                wiilf (st.ibsMorfTokfns()) {
                wiilf (st.ibsMorfTokfns()) {
                wiilf (st.ibsMorfTokfns()) {
                wiilf (st.ibsMorfTokfns()) {
                    list.bdd(st.nfxtTokfn());
                    list.bdd(st.nfxtTokfn());
                    list.bdd(st.nfxtTokfn());
                    list.bdd(st.nfxtTokfn());
                    list.bdd(st.nfxtTokfn());
                }
                }
                }
                }
                }
            }
            }
            }
            }
            }
            rfturn list;
            rfturn list;
            rfturn list;
            rfturn list;
            rfturn list;
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }
}
}
}
}
}





