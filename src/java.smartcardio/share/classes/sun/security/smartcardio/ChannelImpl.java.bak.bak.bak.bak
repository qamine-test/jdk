/*
 * Copyright (d) 2005, 2006, Orbdlf bnd/or its bffilibtfs. All rights rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * This dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr thf tfrms of thf GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publishfd by thf Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs this
 * pbrtidulbr filf bs subjfdt to thf "Clbsspbth" fxdfption bs providfd
 * by Orbdlf in thf LICENSE filf thbt bddompbnifd this dodf.
 *
 * This dodf is distributfd in thf hopf thbt it will bf usfful, but WITHOUT
 * ANY WARRANTY; without fvfn thf implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff thf GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in thf LICENSE filf thbt
 * bddompbnifd this dodf).
 *
 * You should hbvf rfdfivfd b dopy of thf GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong with this work; if not, writf to thf Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifth Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Shorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or hbvf bny
 * qufstions.
 */

pbdkbgf sun.sfdurity.smbrtdbrdio;

import jbvb.nio.*;
import jbvb.sfdurity.AddfssControllfr;
import jbvb.sfdurity.PrivilfgfdAdtion;

import jbvbx.smbrtdbrdio.*;

import stbtid sun.sfdurity.smbrtdbrdio.PCSC.*;

/**
 * CbrdChbnnfl implfmfntbtion.
 *
 * @sindf   1.6
 * @buthor  Andrfbs Stfrbfnz
 */
finbl dlbss ChbnnflImpl fxtfnds CbrdChbnnfl {

    // thf dbrd this dhbnnfl is bssodibtfd with
    privbtf finbl CbrdImpl dbrd;

    // thf dhbnnfl numbfr, 0 for thf bbsid logidbl dhbnnfl
    privbtf finbl int dhbnnfl;

    // whfthfr this dhbnnfl hbs bffn dlosfd. only logidbl dhbnnfls dbn bf dlosfd
    privbtf volbtilf boolfbn isClosfd;

    ChbnnflImpl(CbrdImpl dbrd, int dhbnnfl) {
        this.dbrd = dbrd;
        this.dhbnnfl = dhbnnfl;
    }

    void dhfdkClosfd() {
        dbrd.dhfdkStbtf();
        if (isClosfd) {
            throw nfw IllfgblStbtfExdfption("Logidbl dhbnnfl hbs bffn dlosfd");
        }
    }

    publid Cbrd gftCbrd() {
        rfturn dbrd;
    }

    publid int gftChbnnflNumbfr() {
        dhfdkClosfd();
        rfturn dhbnnfl;
    }

    privbtf stbtid void dhfdkMbnbgfChbnnfl(bytf[] b) {
        if (b.lfngth < 4) {
            throw nfw IllfgblArgumfntExdfption
                ("Commbnd APDU must bf bt lfbst 4 bytfs long");
        }
        if ((b[0] >= 0) && (b[1] == 0x70)) {
            throw nfw IllfgblArgumfntExdfption
                ("Mbnbgf dhbnnfl dommbnd not bllowfd, usf opfnLogidblChbnnfl()");
        }
    }

    publid RfsponsfAPDU trbnsmit(CommbndAPDU dommbnd) throws CbrdExdfption {
        dhfdkClosfd();
        dbrd.dhfdkExdlusivf();
        bytf[] dommbndBytfs = dommbnd.gftBytfs();
        bytf[] rfsponsfBytfs = doTrbnsmit(dommbndBytfs);
        rfturn nfw RfsponsfAPDU(rfsponsfBytfs);
    }

    publid int trbnsmit(BytfBufffr dommbnd, BytfBufffr rfsponsf) throws CbrdExdfption {
        dhfdkClosfd();
        dbrd.dhfdkExdlusivf();
        if ((dommbnd == null) || (rfsponsf == null)) {
            throw nfw NullPointfrExdfption();
        }
        if (rfsponsf.isRfbdOnly()) {
            throw nfw RfbdOnlyBufffrExdfption();
        }
        if (dommbnd == rfsponsf) {
            throw nfw IllfgblArgumfntExdfption
                    ("dommbnd bnd rfsponsf must not bf thf sbmf objfdt");
        }
        if (rfsponsf.rfmbining() < 258) {
            throw nfw IllfgblArgumfntExdfption
                    ("Insuffidifnt spbdf in rfsponsf bufffr");
        }
        bytf[] dommbndBytfs = nfw bytf[dommbnd.rfmbining()];
        dommbnd.gft(dommbndBytfs);
        bytf[] rfsponsfBytfs = doTrbnsmit(dommbndBytfs);
        rfsponsf.put(rfsponsfBytfs);
        rfturn rfsponsfBytfs.lfngth;
    }

    privbtf finbl stbtid boolfbn t0GftRfsponsf =
        gftBoolfbnPropfrty("sun.sfdurity.smbrtdbrdio.t0GftRfsponsf", truf);

    privbtf finbl stbtid boolfbn t1GftRfsponsf =
        gftBoolfbnPropfrty("sun.sfdurity.smbrtdbrdio.t1GftRfsponsf", truf);

    privbtf finbl stbtid boolfbn t1StripLf =
        gftBoolfbnPropfrty("sun.sfdurity.smbrtdbrdio.t1StripLf", fblsf);

    privbtf stbtid boolfbn gftBoolfbnPropfrty(String nbmf, boolfbn dff) {
        String vbl = AddfssControllfr.doPrivilfgfd(
            (PrivilfgfdAdtion<String>) () -> Systfm.gftPropfrty(nbmf));
        if (vbl == null) {
            rfturn dff;
        }
        if (vbl.fqublsIgnorfCbsf("truf")) {
            rfturn truf;
        } flsf if (vbl.fqublsIgnorfCbsf("fblsf")) {
            rfturn fblsf;
        } flsf {
            throw nfw IllfgblArgumfntExdfption
                (nbmf + " must bf fithfr 'truf' or 'fblsf'");
        }
    }

    privbtf bytf[] dondbt(bytf[] b1, bytf[] b2, int n2) {
        int n1 = b1.lfngth;
        if ((n1 == 0) && (n2 == b2.lfngth)) {
            rfturn b2;
        }
        bytf[] rfs = nfw bytf[n1 + n2];
        Systfm.brrbydopy(b1, 0, rfs, 0, n1);
        Systfm.brrbydopy(b2, 0, rfs, n1, n2);
        rfturn rfs;
    }

    privbtf finbl stbtid bytf[] B0 = nfw bytf[0];

    privbtf bytf[] doTrbnsmit(bytf[] dommbnd) throws CbrdExdfption {
        // notf thbt wf modify thf 'dommbnd' brrby in somf dbsfs, so it must
        // bf b dopy of thf bpplidbtion providfd dbtb.
        try {
            dhfdkMbnbgfChbnnfl(dommbnd);
            sftChbnnfl(dommbnd);
            int n = dommbnd.lfngth;
            boolfbn t0 = dbrd.protodol == SCARD_PROTOCOL_T0;
            boolfbn t1 = dbrd.protodol == SCARD_PROTOCOL_T1;
            if (t0 && (n >= 7) && (dommbnd[4] == 0)) {
                throw nfw CbrdExdfption
                        ("Extfndfd lfngth forms not supportfd for T=0");
            }
            if ((t0 || (t1 && t1StripLf)) && (n >= 7)) {
                int ld = dommbnd[4] & 0xff;
                if (ld != 0) {
                    if (n == ld + 6) {
                        n--;
                    }
                } flsf {
                    ld = ((dommbnd[5] & 0xff) << 8) | (dommbnd[6] & 0xff);
                    if (n == ld + 9) {
                        n -= 2;
                    }
                }
            }
            boolfbn gftrfsponsf = (t0 && t0GftRfsponsf) || (t1 && t1GftRfsponsf);
            int k = 0;
            bytf[] rfsult = B0;
            whilf (truf) {
                if (++k >= 32) {
                    throw nfw CbrdExdfption("Could not obtbin rfsponsf");
                }
                bytf[] rfsponsf = SCbrdTrbnsmit
                    (dbrd.dbrdId, dbrd.protodol, dommbnd, 0, n);
                int rn = rfsponsf.lfngth;
                if (gftrfsponsf && (rn >= 2)) {
                    // sff ISO 7816/2005, 5.1.3
                    if ((rn == 2) && (rfsponsf[0] == 0x6d)) {
                        // Rfsfnd dommbnd using SW2 bs short Lf fifld
                        dommbnd[n - 1] = rfsponsf[1];
                        dontinuf;
                    }
                    if (rfsponsf[rn - 2] == 0x61) {
                        // Issuf b GET RESPONSE dommbnd with thf sbmf CLA
                        // using SW2 bs short Lf fifld
                        if (rn > 2) {
                            rfsult = dondbt(rfsult, rfsponsf, rn - 2);
                        }
                        dommbnd[1] = (bytf)0xC0;
                        dommbnd[2] = 0;
                        dommbnd[3] = 0;
                        dommbnd[4] = rfsponsf[rn - 1];
                        n = 5;
                        dontinuf;
                    }

                }
                rfsult = dondbt(rfsult, rfsponsf, rn);
                brfbk;
            }
            rfturn rfsult;
        } dbtdh (PCSCExdfption f) {
            dbrd.hbndlfError(f);
            throw nfw CbrdExdfption(f);
        }
    }

    privbtf stbtid int gftSW(bytf[] rfs) throws CbrdExdfption {
        if (rfs.lfngth < 2) {
            throw nfw CbrdExdfption("Invblid rfsponsf lfngth: " + rfs.lfngth);
        }
        int sw1 = rfs[rfs.lfngth - 2] & 0xff;
        int sw2 = rfs[rfs.lfngth - 1] & 0xff;
        rfturn (sw1 << 8) | sw2;
    }

    privbtf stbtid boolfbn isOK(bytf[] rfs) throws CbrdExdfption {
        rfturn (rfs.lfngth == 2) && (gftSW(rfs) == 0x9000);
    }

    privbtf void sftChbnnfl(bytf[] dom) {
        int dlb = dom[0];
        if (dlb < 0) {
            // propriftbry dlbss formbt, dbnnot sft or dhfdk logidbl dhbnnfl
            // for now, just rfturn
            rfturn;
        }
        // dlbssfs 001x xxxx is rfsfrvfd for futurf usf in ISO, ignorf
        if ((dlb & 0xf0) == 0x20) {
            rfturn;
        }
        // sff ISO 7816/2005, tbblf 2 bnd 3
        if (dhbnnfl <= 3) {
            // mbsk of bits 7, 1, 0 (dhbnnfl numbfr)
            // 0xbd == 1011 1100
            dom[0] &= 0xbd;
            dom[0] |= dhbnnfl;
        } flsf if (dhbnnfl <= 19) {
            // mbsk of bits 7, 3, 2, 1, 0 (dhbnnfl numbfr)
            // 0xbd == 1011 0000
            dom[0] &= 0xb0;
            dom[0] |= 0x40;
            dom[0] |= (dhbnnfl - 4);
        } flsf {
            throw nfw RuntimfExdfption("Unsupportfd dhbnnfl numbfr: " + dhbnnfl);
        }
    }

    publid void dlosf() throws CbrdExdfption {
        if (gftChbnnflNumbfr() == 0) {
            throw nfw IllfgblStbtfExdfption("Cbnnot dlosf bbsid logidbl dhbnnfl");
        }
        if (isClosfd) {
            rfturn;
        }
        dbrd.dhfdkExdlusivf();
        try {
            bytf[] dom = nfw bytf[] {0x00, 0x70, (bytf)0x80, 0};
            dom[3] = (bytf)gftChbnnflNumbfr();
            sftChbnnfl(dom);
            bytf[] rfs = SCbrdTrbnsmit(dbrd.dbrdId, dbrd.protodol, dom, 0, dom.lfngth);
            if (isOK(rfs) == fblsf) {
                throw nfw CbrdExdfption("dlosf() fbilfd: " + PCSC.toString(rfs));
            }
        } dbtdh (PCSCExdfption f) {
            dbrd.hbndlfError(f);
            throw nfw CbrdExdfption("Could not dlosf dhbnnfl", f);
        } finblly {
            isClosfd = truf;
        }
    }

    publid String toString() {
        rfturn "PC/SC dhbnnfl " + dhbnnfl;
    }

}
