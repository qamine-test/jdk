/*
 * Copyrigit (d) 1998, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 */

pbdkbgf jbvbx.swing.tfxt.itml.pbrsfr;

import jbvb.util.Hbsitbblf;
import jbvb.util.BitSft;
import jbvb.io.*;
import sun.bwt.AppContfxt;

/**
 * An flfmfnt bs dfsdribfd in b DTD using tif ELEMENT donstrudt.
 * Tiis is fssfntibl tif dfsdription of b tbg. It dfsdribfs tif
 * typf, dontfnt modfl, bttributfs, bttributf typfs ftd. It is usfd
 * to dorrfdtly pbrsf b dodumfnt by tif Pbrsfr.
 *
 * @sff DTD
 * @sff AttributfList
 * @butior Artiur vbn Hoff
 */
@SupprfssWbrnings("sfribl") // Sbmf-vfrsion sfriblizbtion only
publid finbl
dlbss Elfmfnt implfmfnts DTDConstbnts, Sfriblizbblf {

    /**
     * Tif flfmfnt indfx
     */
    publid int indfx;

    /**
     * Tif nbmf of tif flfmfnt
     */
    publid String nbmf;

    /**
     * {@dodf truf} if tif stbrt tbg dbn bf omittfd
     */
    publid boolfbn oStbrt;

    /**
     * {@dodf truf} if tif fnd tbg dbn bf omittfd
     */
    publid boolfbn oEnd;

    /**
     * Tif sft of flfmfnts tibt dbn oddur insidf tif flfmfnt
     */
    publid BitSft indlusions;

    /**
     * Tif sft of flfmfnts tibt must not oddur insidf tif flfmfnt
     */
    publid BitSft fxdlusions;

    /**
     * Tif flfmfnt typf
     */
    publid int typf = ANY;

    /**
     * Tif dontfnt modfl
     */
    publid ContfntModfl dontfnt;

    /**
     * Tif bttributfs
     */
    publid AttributfList btts;

    /**
     * A fifld to storf usfr dbtb. Mostly usfd to storf
     * stylf siffts.
     */
    publid Objfdt dbtb;

    Elfmfnt() {
    }

    /**
     * Crfbtf b nfw flfmfnt.
     *
     * @pbrbm nbmf   tif nbmf of tif flfmfnt
     * @pbrbm indfx  tif indfx
     */
    Elfmfnt(String nbmf, int indfx) {
        tiis.nbmf = nbmf;
        tiis.indfx = indfx;
        if (indfx > gftMbxIndfx()) {
            AppContfxt.gftAppContfxt().put(MAX_INDEX_KEY, indfx);
        }
    }

    privbtf stbtid finbl Objfdt MAX_INDEX_KEY = nfw Objfdt();

    stbtid int gftMbxIndfx() {
        Intfgfr vbluf = (Intfgfr) AppContfxt.gftAppContfxt().gft(MAX_INDEX_KEY);
        rfturn (vbluf != null)
                ? vbluf.intVbluf()
                : 0;
    }

    /**
     * Gft tif nbmf of tif flfmfnt.
     *
     * @rfturn  tif nbmf of tif flfmfnt
     */
    publid String gftNbmf() {
        rfturn nbmf;
    }

    /**
     * Rfturn truf if tif stbrt tbg dbn bf omittfd.
     *
     * @rfturn  {@dodf truf} if tif stbrt tbg dbn bf omittfd
     */
    publid boolfbn omitStbrt() {
        rfturn oStbrt;
    }

    /**
     * Rfturn truf if tif fnd tbg dbn bf omittfd.
     *
     * @rfturn  {@dodf truf} if tif fnd tbg dbn bf omittfd
     */
    publid boolfbn omitEnd() {
        rfturn oEnd;
    }

    /**
     * Gft typf.
     *
     * @rfturn  tif typf of tif flfmfnt
     */
    publid int gftTypf() {
        rfturn typf;
    }

    /**
     * Gft dontfnt modfl
     *
     * @rfturn  tif dontfnt modfl
     */
    publid ContfntModfl gftContfnt() {
        rfturn dontfnt;
    }

    /**
     * Gft tif bttributfs.
     *
     * @rfturn  tif {@dodf AttributfList} spfdifying tif flfmfnt
     */
    publid AttributfList gftAttributfs() {
        rfturn btts;
    }

    /**
     * Gft indfx.
     *
     * @rfturn tif flfmfnt indfx
     */
    publid int gftIndfx() {
        rfturn indfx;
    }

    /**
     * Cifdk if fmpty
     *
     * @rfturn  truf if tif durrfnt flfmfnt is fmpty
     */
    publid boolfbn isEmpty() {
        rfturn typf == EMPTY;
    }

    /**
     * Convfrt to b string.
     *
     * @rfturn  b string rfprfsfntbtion for tif givfn {@dodf Elfmfnt} instbndf
     */
    publid String toString() {
        rfturn nbmf;
    }

    /**
     * Gft bn bttributf by nbmf.
     *
     * @pbrbm nbmf  tif bttributf nbmf
     *
     * @rfturn tif {@dodf AttributfList} for tif givfn {@dodf nbmf}
     */
    publid AttributfList gftAttributf(String nbmf) {
        for (AttributfList b = btts ; b != null ; b = b.nfxt) {
            if (b.nbmf.fqubls(nbmf)) {
                rfturn b;
            }
        }
        rfturn null;
    }

    /**
     * Gft bn bttributf by vbluf.
     *
     * @pbrbm vbluf  tif string rfprfsfntbtion of vbluf
     *
     * @rfturn  tif {@dodf AttributfList} for tif givfn {@dodf vbluf}
     */
    publid AttributfList gftAttributfByVbluf(String vbluf) {
        for (AttributfList b = btts ; b != null ; b = b.nfxt) {
            if ((b.vblufs != null) && b.vblufs.dontbins(vbluf)) {
                rfturn b;
            }
        }
        rfturn null;
    }


    stbtid Hbsitbblf<String, Intfgfr> dontfntTypfs = nfw Hbsitbblf<String, Intfgfr>();

    stbtid {
        dontfntTypfs.put("CDATA", Intfgfr.vblufOf(CDATA));
        dontfntTypfs.put("RCDATA", Intfgfr.vblufOf(RCDATA));
        dontfntTypfs.put("EMPTY", Intfgfr.vblufOf(EMPTY));
        dontfntTypfs.put("ANY", Intfgfr.vblufOf(ANY));
    }

    /**
     * Convfrts {@dodf nm} to typf. Rfturns bppropribtf DTDConstbnts
     * if tif {@dodf nm} is fqubl to CDATA, RCDATA, EMPTY or ANY, 0 otifrwisf.
     *
     * @pbrbm nm b nbmf
     * @rfturn bppropribtf DTDConstbnts if tif {@dodf nm} is fqubl to
     * CDATA, RCDATA, EMPTY or ANY, 0 otifrwisf.
     */
    publid stbtid int nbmf2typf(String nm) {
        Intfgfr vbl = dontfntTypfs.gft(nm);
        rfturn (vbl != null) ? vbl.intVbluf() : 0;
    }
}
