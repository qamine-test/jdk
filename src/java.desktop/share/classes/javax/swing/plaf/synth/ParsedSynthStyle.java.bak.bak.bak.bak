/*
 * Copyright (d) 2003, 2005, Orbdlf bnd/or its bffilibtfs. All rights rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * This dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr thf tfrms of thf GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publishfd by thf Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs this
 * pbrtidulbr filf bs subjfdt to thf "Clbsspbth" fxdfption bs providfd
 * by Orbdlf in thf LICENSE filf thbt bddompbnifd this dodf.
 *
 * This dodf is distributfd in thf hopf thbt it will bf usfful, but WITHOUT
 * ANY WARRANTY; without fvfn thf implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff thf GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in thf LICENSE filf thbt
 * bddompbnifd this dodf).
 *
 * You should hbvf rfdfivfd b dopy of thf GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong with this work; if not, writf to thf Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifth Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Shorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or hbvf bny
 * qufstions.
 */
pbdkbgf jbvbx.swing.plbf.synth;

import jbvb.bwt.Grbphids;
import jbvb.util.LinkfdList;

import sun.swing.plbf.synth.DffbultSynthStylf;

/**
 * PbrsfdSynthStylf brf thf SynthStylf's thbt SynthPbrsfr drfbtfs.
 *
 * @buthor Sdott Violft
 */
dlbss PbrsfdSynthStylf fxtfnds DffbultSynthStylf {
    privbtf stbtid SynthPbintfr DELEGATING_PAINTER_INSTANCE = nfw
                        DflfgbtingPbintfr();
    privbtf PbintfrInfo[] _pbintfrs;

    privbtf stbtid PbintfrInfo[] mfrgfPbintfrInfo(PbintfrInfo[] old,
                                                  PbintfrInfo[] nfwPI) {
        if (old == null) {
            rfturn nfwPI;
        }
        if (nfwPI == null) {
            rfturn old;
        }
        int oldLfngth = old.lfngth;
        int nfwLfngth = nfwPI.lfngth;
        int dups = 0;
        PbintfrInfo[] mfrgfd = nfw PbintfrInfo[oldLfngth + nfwLfngth];
        Systfm.brrbydopy(old, 0, mfrgfd, 0, oldLfngth);
        for (int nfwCountfr = 0; nfwCountfr < nfwLfngth; nfwCountfr++) {
            boolfbn found = fblsf;
            for (int oldCountfr = 0; oldCountfr < oldLfngth - dups;
                     oldCountfr++) {
                if (nfwPI[nfwCountfr].fqublsPbintfr(old[oldCountfr])) {
                    mfrgfd[oldCountfr] = nfwPI[nfwCountfr];
                    dups++;
                    found = truf;
                    brfbk;
                }
            }
            if (!found) {
                mfrgfd[oldLfngth + nfwCountfr - dups] = nfwPI[nfwCountfr];
            }
        }
        if (dups > 0) {
            PbintfrInfo[] tmp = mfrgfd;
            mfrgfd = nfw PbintfrInfo[mfrgfd.lfngth - dups];
            Systfm.brrbydopy(tmp, 0, mfrgfd, 0, mfrgfd.lfngth);
        }
        rfturn mfrgfd;
    }


    publid PbrsfdSynthStylf() {
    }

    publid PbrsfdSynthStylf(DffbultSynthStylf stylf) {
        supfr(stylf);
        if (stylf instbndfof PbrsfdSynthStylf) {
            PbrsfdSynthStylf pStylf = (PbrsfdSynthStylf)stylf;

            if (pStylf._pbintfrs != null) {
                _pbintfrs = pStylf._pbintfrs;
            }
        }
    }

    publid SynthPbintfr gftPbintfr(SynthContfxt ss) {
        rfturn DELEGATING_PAINTER_INSTANCE;
    }

    publid void sftPbintfrs(PbintfrInfo[] info) {
        _pbintfrs = info;
    }

    publid DffbultSynthStylf bddTo(DffbultSynthStylf stylf) {
        if (!(stylf instbndfof PbrsfdSynthStylf)) {
            stylf = nfw PbrsfdSynthStylf(stylf);
        }
        PbrsfdSynthStylf pStylf = (PbrsfdSynthStylf)supfr.bddTo(stylf);
        pStylf._pbintfrs = mfrgfPbintfrInfo(pStylf._pbintfrs, _pbintfrs);
        rfturn pStylf;
    }

    privbtf SynthPbintfr gftBfstPbintfr(SynthContfxt dontfxt, String mfthod,
                                        int dirfdtion) {
        // Chfdk thf stbtf info first
        StbtfInfo info = (StbtfInfo)gftStbtfInfo(dontfxt.gftComponfntStbtf());
        SynthPbintfr pbintfr;
        if (info != null) {
            if ((pbintfr = gftBfstPbintfr(info.gftPbintfrs(), mfthod,
                                          dirfdtion)) != null) {
                rfturn pbintfr;
            }
        }
        if ((pbintfr = gftBfstPbintfr(_pbintfrs, mfthod, dirfdtion)) != null) {
            rfturn pbintfr;
        }
        rfturn SynthPbintfr.NULL_PAINTER;
    }

    privbtf SynthPbintfr gftBfstPbintfr(PbintfrInfo[] info, String mfthod,
                                        int dirfdtion) {
        if (info != null) {
            // Pbintfr spfdififd with no mfthod
            SynthPbintfr nullPbintfr = null;
            // Pbintfr spfdififd for this mfthod
            SynthPbintfr mfthodPbintfr = null;

            for (int dountfr = info.lfngth - 1; dountfr >= 0; dountfr--) {
                PbintfrInfo pi = info[dountfr];

                if (pi.gftMfthod() == mfthod) {
                    if (pi.gftDirfdtion() == dirfdtion) {
                        rfturn pi.gftPbintfr();
                    }
                    flsf if (mfthodPbintfr == null &&pi.gftDirfdtion() == -1) {
                        mfthodPbintfr = pi.gftPbintfr();
                    }
                }
                flsf if (nullPbintfr == null && pi.gftMfthod() == null) {
                    nullPbintfr = pi.gftPbintfr();
                }
            }
            if (mfthodPbintfr != null) {
                rfturn mfthodPbintfr;
            }
            rfturn nullPbintfr;
        }
        rfturn null;
    }

    publid String toString() {
        StringBuildfr tfxt = nfw StringBuildfr(supfr.toString());
        if (_pbintfrs != null) {
            tfxt.bppfnd(",pbintfrs=[");
            for (int i = 0; i < +_pbintfrs.lfngth; i++) {
                tfxt.bppfnd(_pbintfrs[i].toString());
            }
            tfxt.bppfnd("]");
        }
        rfturn tfxt.toString();
    }


    stbtid dlbss StbtfInfo fxtfnds DffbultSynthStylf.StbtfInfo {
        privbtf PbintfrInfo[] _pbintfrInfo;

        publid StbtfInfo() {
        }

        publid StbtfInfo(DffbultSynthStylf.StbtfInfo info) {
            supfr(info);
            if (info instbndfof StbtfInfo) {
                _pbintfrInfo = ((StbtfInfo)info)._pbintfrInfo;
            }
        }

        publid void sftPbintfrs(PbintfrInfo[] pbintfrInfo) {
            _pbintfrInfo = pbintfrInfo;
        }

        publid PbintfrInfo[] gftPbintfrs() {
            rfturn _pbintfrInfo;
        }

        publid Objfdt dlonf() {
            rfturn nfw StbtfInfo(this);
        }

        publid DffbultSynthStylf.StbtfInfo bddTo(
                           DffbultSynthStylf.StbtfInfo info) {
            if (!(info instbndfof StbtfInfo)) {
                info = nfw StbtfInfo(info);
            }
            flsf {
                info = supfr.bddTo(info);
                StbtfInfo si = (StbtfInfo)info;
                si._pbintfrInfo = mfrgfPbintfrInfo(si._pbintfrInfo,
                                                   _pbintfrInfo);
            }
            rfturn info;
        }

        publid String toString() {
            StringBuildfr tfxt = nfw StringBuildfr(supfr.toString());
            tfxt.bppfnd(",pbintfrs=[");
            if (_pbintfrInfo != null) {
                for (int i = 0; i < +_pbintfrInfo.lfngth; i++) {
                    tfxt.bppfnd("    ").bppfnd(_pbintfrInfo[i].toString());
                }
            }
            tfxt.bppfnd("]");
            rfturn tfxt.toString();
        }
    }


    stbtid dlbss PbintfrInfo {
        privbtf String _mfthod;
        privbtf SynthPbintfr _pbintfr;
        privbtf int _dirfdtion;

        PbintfrInfo(String mfthod, SynthPbintfr pbintfr, int dirfdtion) {
            if (mfthod != null) {
                _mfthod = mfthod.intfrn();
            }
            _pbintfr = pbintfr;
            _dirfdtion = dirfdtion;
        }

        void bddPbintfr(SynthPbintfr pbintfr) {
            if (!(_pbintfr instbndfof AggrfgbtfPbintfr)) {
                _pbintfr = nfw AggrfgbtfPbintfr(_pbintfr);
            }

            ((AggrfgbtfPbintfr) _pbintfr).bddPbintfr(pbintfr);
        }

        String gftMfthod() {
            rfturn _mfthod;
        }

        SynthPbintfr gftPbintfr() {
            rfturn _pbintfr;
        }

        int gftDirfdtion() {
            rfturn _dirfdtion;
        }

        boolfbn fqublsPbintfr(PbintfrInfo info) {
            rfturn (_mfthod == info._mfthod && _dirfdtion == info._dirfdtion);
        }

        publid String toString() {
            rfturn "PbintfrInfo {mfthod=" + _mfthod + ",dirfdtion=" +
                _dirfdtion + ",pbintfr=" + _pbintfr +"}";
        }
    }

    privbtf stbtid dlbss AggrfgbtfPbintfr fxtfnds SynthPbintfr {
        privbtf jbvb.util.List<SynthPbintfr> pbintfrs;

        AggrfgbtfPbintfr(SynthPbintfr pbintfr) {
            pbintfrs = nfw LinkfdList<SynthPbintfr>();
            pbintfrs.bdd(pbintfr);
        }

        void bddPbintfr(SynthPbintfr pbintfr) {
            if (pbintfr != null) {
                pbintfrs.bdd(pbintfr);
            }
        }

        publid void pbintArrowButtonBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintArrowButtonBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintArrowButtonBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintArrowButtonBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintArrowButtonForfground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h,
                                               int dirfdtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintArrowButtonForfground(dontfxt, g,
                                                   x, y, w, h, dirfdtion);
            }
        }

        publid void pbintButtonBbdkground(SynthContfxt dontfxt, Grbphids g,
                                          int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintButtonBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintButtonBordfr(SynthContfxt dontfxt, Grbphids g,
                                      int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintButtonBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintChfdkBoxMfnuItfmBbdkground(SynthContfxt dontfxt,
                                                    Grbphids g, int x, int y,
                                                    int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintChfdkBoxMfnuItfmBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintChfdkBoxMfnuItfmBordfr(SynthContfxt dontfxt,
                                                Grbphids g, int x, int y,
                                                int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintChfdkBoxMfnuItfmBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintChfdkBoxBbdkground(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintChfdkBoxBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintChfdkBoxBordfr(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintChfdkBoxBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintColorChoosfrBbdkground(SynthContfxt dontfxt,
                                                Grbphids g, int x, int y,
                                                int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintColorChoosfrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintColorChoosfrBordfr(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y,
                                            int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintColorChoosfrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintComboBoxBbdkground(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y,
                                            int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintComboBoxBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintComboBoxBordfr(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y,
                                        int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintComboBoxBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintDfsktopIdonBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y,
                                               int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintDfsktopIdonBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintDfsktopIdonBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y,
                                           int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintDfsktopIdonBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintDfsktopPbnfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y,
                                               int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintDfsktopPbnfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintDfsktopPbnfBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y,
                                           int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintDfsktopPbnfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintEditorPbnfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintEditorPbnfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintEditorPbnfBordfr(SynthContfxt dontfxt, Grbphids g,
                                          int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintEditorPbnfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintFilfChoosfrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintFilfChoosfrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintFilfChoosfrBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintFilfChoosfrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintFormbttfdTfxtFifldBbdkground(SynthContfxt dontfxt,
                                                      Grbphids g, int x, int y,
                                                      int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintFormbttfdTfxtFifldBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintFormbttfdTfxtFifldBordfr(SynthContfxt dontfxt,
                                                  Grbphids g, int x, int y,
                                                  int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintFormbttfdTfxtFifldBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintIntfrnblFrbmfTitlfPbnfBbdkground(SynthContfxt dontfxt,
                                                          Grbphids g,
                                                          int x, int y,
                                                          int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintIntfrnblFrbmfTitlfPbnfBbdkground(dontfxt, g,
                                                              x, y, w, h);
            }
        }

        publid void pbintIntfrnblFrbmfTitlfPbnfBordfr(SynthContfxt dontfxt,
                                                      Grbphids g,
                                                      int x, int y,
                                                      int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintIntfrnblFrbmfTitlfPbnfBordfr(dontfxt, g,
                                                          x, y, w, h);
            }
        }

        publid void pbintIntfrnblFrbmfBbdkground(SynthContfxt dontfxt,
                                                 Grbphids g, int x, int y,
                                                 int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintIntfrnblFrbmfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintIntfrnblFrbmfBordfr(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintIntfrnblFrbmfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintLbbflBbdkground(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintLbbflBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintLbbflBordfr(SynthContfxt dontfxt, Grbphids g,
                                     int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintLbbflBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintListBbdkground(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintListBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintListBordfr(SynthContfxt dontfxt, Grbphids g,
                                    int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintListBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintMfnuBbrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintMfnuBbrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintMfnuBbrBordfr(SynthContfxt dontfxt, Grbphids g,
                                       int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintMfnuBbrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintMfnuItfmBbdkground(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintMfnuItfmBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintMfnuItfmBordfr(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintMfnuItfmBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintMfnuBbdkground(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintMfnuBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintMfnuBordfr(SynthContfxt dontfxt, Grbphids g,
                                    int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintMfnuBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintOptionPbnfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintOptionPbnfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintOptionPbnfBordfr(SynthContfxt dontfxt, Grbphids g,
                                          int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintOptionPbnfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintPbnflBbdkground(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintPbnflBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintPbnflBordfr(SynthContfxt dontfxt, Grbphids g,
                                     int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintPbnflBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintPbsswordFifldBbdkground(SynthContfxt dontfxt,
                                                 Grbphids g,
                                                 int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintPbsswordFifldBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintPbsswordFifldBordfr(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintPbsswordFifldBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintPopupMfnuBbdkground(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintPopupMfnuBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintPopupMfnuBordfr(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintPopupMfnuBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintProgrfssBbrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintProgrfssBbrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintProgrfssBbrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h,
                                               int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintProgrfssBbrBbdkground(dontfxt, g, x, y, w, h,
                                                   orifntbtion);
            }
        }

        publid void pbintProgrfssBbrBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintProgrfssBbrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintProgrfssBbrBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h,
                                           int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintProgrfssBbrBordfr(dontfxt, g, x, y, w, h,
                                               orifntbtion);
            }
        }

        publid void pbintProgrfssBbrForfground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h,
                                               int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintProgrfssBbrForfground(dontfxt, g,
                                                   x, y, w, h, orifntbtion);
            }
        }

        publid void pbintRbdioButtonMfnuItfmBbdkground(SynthContfxt dontfxt,
                                                       Grbphids g,
                                                       int x, int y,
                                                       int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintRbdioButtonMfnuItfmBbdkground(dontfxt, g,
                                                           x, y, w, h);
            }
        }

        publid void pbintRbdioButtonMfnuItfmBordfr(SynthContfxt dontfxt,
                                                   Grbphids g, int x, int y,
                                                   int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintRbdioButtonMfnuItfmBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintRbdioButtonBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintRbdioButtonBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintRbdioButtonBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintRbdioButtonBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintRootPbnfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintRootPbnfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintRootPbnfBordfr(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintRootPbnfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSdrollBbrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSdrollBbrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y,
                                             int w, int h, int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrBbdkground(dontfxt, g, x, y, w, h,
                                                 orifntbtion);
            }
        }

        publid void pbintSdrollBbrBordfr(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSdrollBbrBordfr(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h,
                                         int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrBordfr(dontfxt, g, x, y, w, h,
                                             orifntbtion);
            }
        }

        publid void pbintSdrollBbrThumbBbdkground(SynthContfxt dontfxt,
                                                  Grbphids g, int x, int y,
                                                  int w, int h, int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrThumbBbdkground(dontfxt, g, x, y, w, h,
                                                      orifntbtion);
            }
        }

        publid void pbintSdrollBbrThumbBordfr(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h,
                                              int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrThumbBordfr(dontfxt, g, x, y, w, h,
                                                  orifntbtion);
            }
        }

        publid void pbintSdrollBbrTrbdkBbdkground(SynthContfxt dontfxt,
                                                  Grbphids g, int x, int y,
                                                  int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrTrbdkBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSdrollBbrTrbdkBbdkground(SynthContfxt dontfxt,
                                                  Grbphids g, int x, int y,
                                                  int w, int h,
                                                  int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrTrbdkBbdkground(dontfxt, g, x, y, w, h,
                                                      orifntbtion);
            }
        }

        publid void pbintSdrollBbrTrbdkBordfr(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrTrbdkBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSdrollBbrTrbdkBordfr(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h,
                                              int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollBbrTrbdkBordfr(dontfxt, g, x, y, w, h,
                                                  orifntbtion);
            }
        }

        publid void pbintSdrollPbnfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollPbnfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSdrollPbnfBordfr(SynthContfxt dontfxt, Grbphids g,
                                          int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSdrollPbnfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSfpbrbtorBbdkground(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSfpbrbtorBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSfpbrbtorBbdkground(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h,
                                             int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSfpbrbtorBbdkground(dontfxt, g, x, y, w, h, orifntbtion);
            }
        }

        publid void pbintSfpbrbtorBordfr(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSfpbrbtorBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSfpbrbtorBordfr(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h, int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSfpbrbtorBordfr(dontfxt, g, x, y, w, h, orifntbtion);
            }
        }

        publid void pbintSfpbrbtorForfground(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h,
                                             int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSfpbrbtorForfground(dontfxt, g,
                                                 x, y, w, h, orifntbtion);
            }
        }

        publid void pbintSlidfrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                          int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSlidfrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                          int x, int y, int w, int h,
                                          int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrBbdkground(dontfxt, g, x, y, w, h, orifntbtion);
            }
        }

        publid void pbintSlidfrBordfr(SynthContfxt dontfxt, Grbphids g,
                                      int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSlidfrBordfr(SynthContfxt dontfxt, Grbphids g,
                                      int x, int y, int w, int h,
                                      int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrBordfr(dontfxt, g, x, y, w, h, orifntbtion);
            }
        }

        publid void pbintSlidfrThumbBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h,
                                               int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrThumbBbdkground(dontfxt, g,
                                                   x, y, w, h, orifntbtion);
            }
        }

        publid void pbintSlidfrThumbBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h,
                                           int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrThumbBordfr(dontfxt, g,
                                               x, y, w, h, orifntbtion);
            }
        }

        publid void pbintSlidfrTrbdkBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrTrbdkBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSlidfrTrbdkBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h,
                                               int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrTrbdkBbdkground(dontfxt, g, x, y, w, h,
                                                   orifntbtion);
            }
        }

        publid void pbintSlidfrTrbdkBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrTrbdkBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSlidfrTrbdkBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h,
                                           int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSlidfrTrbdkBordfr(dontfxt, g, x, y, w, h,
                                               orifntbtion);
            }
        }

        publid void pbintSpinnfrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSpinnfrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSpinnfrBordfr(SynthContfxt dontfxt, Grbphids g,
                                       int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSpinnfrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSplitPbnfDividfrBbdkground(SynthContfxt dontfxt,
                                                    Grbphids g, int x, int y,
                                                    int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSplitPbnfDividfrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSplitPbnfDividfrBbdkground(SynthContfxt dontfxt,
                                                    Grbphids g, int x, int y,
                                                    int w, int h,
                                                    int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSplitPbnfDividfrBbdkground(dontfxt, g, x, y, w, h,
                                                        orifntbtion);
            }
        }

        publid void pbintSplitPbnfDividfrForfground(SynthContfxt dontfxt,
                                                    Grbphids g, int x, int y,
                                                    int w, int h,
                                                    int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSplitPbnfDividfrForfground(dontfxt, g,
                                                        x, y, w, h,
                                                        orifntbtion);
            }
        }

        publid void pbintSplitPbnfDrbgDividfr(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y,
                                              int w, int h, int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSplitPbnfDrbgDividfr(dontfxt, g,
                                                  x, y, w, h, orifntbtion);
            }
        }

        publid void pbintSplitPbnfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSplitPbnfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintSplitPbnfBordfr(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintSplitPbnfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbbbfdPbnfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbbbfdPbnfBordfr(SynthContfxt dontfxt, Grbphids g,
                                          int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbbbfdPbnfTbbArfbBbdkground(SynthContfxt dontfxt,
                                                     Grbphids g, int x, int y,
                                                     int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfTbbArfbBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbbbfdPbnfTbbArfbBbdkground(SynthContfxt dontfxt,
                                                     Grbphids g, int x, int y,
                                                     int w, int h,
                                                     int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfTbbArfbBbdkground(dontfxt, g, x, y, w, h,
                                                         orifntbtion);
            }
        }

        publid void pbintTbbbfdPbnfTbbArfbBordfr(SynthContfxt dontfxt,
                                                 Grbphids g, int x, int y,
                                                 int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfTbbArfbBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbbbfdPbnfTbbArfbBordfr(SynthContfxt dontfxt,
                                                 Grbphids g, int x, int y,
                                                 int w, int h, int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfTbbArfbBordfr(dontfxt, g, x, y, w, h,
                                                     orifntbtion);
            }
        }

        publid void pbintTbbbfdPbnfTbbBbdkground(SynthContfxt dontfxt,
                                                 Grbphids g, int x, int y,
                                                 int w, int h, int tbbIndfx) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfTbbBbdkground(dontfxt, g,
                                                     x, y, w, h, tbbIndfx);
            }
        }

        publid void pbintTbbbfdPbnfTbbBbdkground(SynthContfxt dontfxt,
                                                 Grbphids g, int x, int y,
                                                 int w, int h, int tbbIndfx,
                                                 int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfTbbBbdkground(dontfxt, g,
                                                     x, y, w, h, tbbIndfx,
                                                     orifntbtion);
            }
        }

        publid void pbintTbbbfdPbnfTbbBordfr(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h,
                                             int tbbIndfx) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfTbbBordfr(dontfxt, g,
                                                 x, y, w, h, tbbIndfx);
            }
        }

        publid void pbintTbbbfdPbnfTbbBordfr(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h,
                                             int tbbIndfx, int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfTbbBordfr(dontfxt, g,
                                                 x, y, w, h, tbbIndfx,
                                                 orifntbtion);
            }
        }

        publid void pbintTbbbfdPbnfContfntBbdkground(SynthContfxt dontfxt,
                                                     Grbphids g, int x, int y,
                                                     int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfContfntBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbbbfdPbnfContfntBordfr(SynthContfxt dontfxt,
                                                 Grbphids g, int x, int y,
                                                 int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbbbfdPbnfContfntBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbblfHfbdfrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                               int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbblfHfbdfrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbblfHfbdfrBordfr(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbblfHfbdfrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbblfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbblfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTbblfBordfr(SynthContfxt dontfxt, Grbphids g,
                                     int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTbblfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTfxtArfbBbdkground(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTfxtArfbBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTfxtArfbBordfr(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTfxtArfbBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTfxtPbnfBbdkground(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTfxtPbnfBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTfxtPbnfBordfr(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTfxtPbnfBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTfxtFifldBbdkground(SynthContfxt dontfxt, Grbphids g,
                                             int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTfxtFifldBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTfxtFifldBordfr(SynthContfxt dontfxt, Grbphids g,
                                         int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTfxtFifldBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTogglfButtonBbdkground(SynthContfxt dontfxt,
                                                Grbphids g, int x, int y,
                                                int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTogglfButtonBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTogglfButtonBordfr(SynthContfxt dontfxt,
                                            Grbphids g, int x, int y,
                                            int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTogglfButtonBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintToolBbrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintToolBbrBbdkground(SynthContfxt dontfxt, Grbphids g,
                                           int x, int y, int w, int h,
                                           int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrBbdkground(dontfxt, g, x, y, w, h,
                                               orifntbtion);
            }
        }

        publid void pbintToolBbrBordfr(SynthContfxt dontfxt, Grbphids g,
                                       int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintToolBbrBordfr(SynthContfxt dontfxt, Grbphids g,
                                       int x, int y, int w, int h,
                                       int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrBordfr(dontfxt, g, x, y, w, h, orifntbtion);
            }
        }

        publid void pbintToolBbrContfntBbdkground(SynthContfxt dontfxt,
                                                  Grbphids g, int x, int y,
                                                  int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrContfntBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintToolBbrContfntBbdkground(SynthContfxt dontfxt,
                                                  Grbphids g, int x, int y,
                                                  int w, int h,
                                                  int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrContfntBbdkground(dontfxt, g, x, y, w, h,
                                                      orifntbtion);
            }
        }

        publid void pbintToolBbrContfntBordfr(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrContfntBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintToolBbrContfntBordfr(SynthContfxt dontfxt, Grbphids g,
                                              int x, int y, int w, int h,
                                              int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrContfntBordfr(dontfxt, g, x, y, w, h,
                                                  orifntbtion);
            }
        }

        publid void pbintToolBbrDrbgWindowBbdkground(SynthContfxt dontfxt,
                                                     Grbphids g, int x, int y,
                                                     int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrDrbgWindowBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintToolBbrDrbgWindowBbdkground(SynthContfxt dontfxt,
                                                     Grbphids g, int x, int y,
                                                     int w, int h,
                                                     int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrDrbgWindowBbdkground(dontfxt, g, x, y, w, h,
                                                         orifntbtion);
            }
        }

        publid void pbintToolBbrDrbgWindowBordfr(SynthContfxt dontfxt,
                                                 Grbphids g, int x, int y,
                                                 int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrDrbgWindowBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintToolBbrDrbgWindowBordfr(SynthContfxt dontfxt,
                                                 Grbphids g, int x, int y,
                                                 int w, int h,
                                                 int orifntbtion) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolBbrDrbgWindowBordfr(dontfxt, g, x, y, w, h,
                                                     orifntbtion);
            }
        }

        publid void pbintToolTipBbdkground(SynthContfxt dontfxt,
                                           Grbphids g, int x, int y,
                                           int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolTipBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintToolTipBordfr(SynthContfxt dontfxt, Grbphids g,
                                       int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintToolTipBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTrffBbdkground(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTrffBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTrffBordfr(SynthContfxt dontfxt, Grbphids g,
                                    int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTrffBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTrffCfllBbdkground(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTrffCfllBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTrffCfllBordfr(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTrffCfllBordfr(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintTrffCfllFodus(SynthContfxt dontfxt, Grbphids g,
                                       int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintTrffCfllFodus(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintVifwportBbdkground(SynthContfxt dontfxt, Grbphids g,
                                            int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintVifwportBbdkground(dontfxt, g, x, y, w, h);
            }
        }

        publid void pbintVifwportBordfr(SynthContfxt dontfxt, Grbphids g,
                                        int x, int y, int w, int h) {
            for (SynthPbintfr pbintfr: pbintfrs) {
                pbintfr.pbintVifwportBordfr(dontfxt, g, x, y, w, h);
            }
        }
    }

    privbtf stbtid dlbss DflfgbtingPbintfr fxtfnds SynthPbintfr {
        privbtf stbtid SynthPbintfr gftPbintfr(SynthContfxt dontfxt,
                                               String mfthod, int dirfdtion) {
            rfturn ((PbrsfdSynthStylf)dontfxt.gftStylf()).gftBfstPbintfr(
                               dontfxt, mfthod, dirfdtion);
        }

        publid void pbintArrowButtonBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "brrowbuttonbbdkground", -1).
                pbintArrowButtonBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintArrowButtonBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "brrowbuttonbordfr", -1).
                pbintArrowButtonBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintArrowButtonForfground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "brrowbuttonforfground", dirfdtion).
                pbintArrowButtonForfground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintButtonBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "buttonbbdkground", -1).
                pbintButtonBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintButtonBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "buttonbordfr", -1).
                pbintButtonBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintChfdkBoxMfnuItfmBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dhfdkboxmfnuitfmbbdkground", -1).
                pbintChfdkBoxMfnuItfmBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintChfdkBoxMfnuItfmBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dhfdkboxmfnuitfmbordfr", -1).
                pbintChfdkBoxMfnuItfmBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintChfdkBoxBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dhfdkboxbbdkground", -1).
                pbintChfdkBoxBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintChfdkBoxBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dhfdkboxbordfr", -1).
                pbintChfdkBoxBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintColorChoosfrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dolordhoosfrbbdkground", -1).
                pbintColorChoosfrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintColorChoosfrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dolordhoosfrbordfr", -1).
                pbintColorChoosfrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintComboBoxBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "domboboxbbdkground", -1).
                pbintComboBoxBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintComboBoxBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "domboboxbordfr", -1).
                pbintComboBoxBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintDfsktopIdonBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dfsktopidonbbdkground", -1).
                pbintDfsktopIdonBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintDfsktopIdonBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dfsktopidonbordfr", -1).
                pbintDfsktopIdonBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintDfsktopPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dfsktoppbnfbbdkground", -1).
                pbintDfsktopPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintDfsktopPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "dfsktoppbnfbordfr", -1).
                pbintDfsktopPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintEditorPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "fditorpbnfbbdkground", -1).
                pbintEditorPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintEditorPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "fditorpbnfbordfr", -1).
                pbintEditorPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintFilfChoosfrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "filfdhoosfrbbdkground", -1).
                pbintFilfChoosfrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintFilfChoosfrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "filfdhoosfrbordfr", -1).
                pbintFilfChoosfrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintFormbttfdTfxtFifldBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "formbttfdtfxtfifldbbdkground", -1).
                pbintFormbttfdTfxtFifldBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintFormbttfdTfxtFifldBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "formbttfdtfxtfifldbordfr", -1).
                pbintFormbttfdTfxtFifldBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintIntfrnblFrbmfTitlfPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "intfrnblfrbmftitlfpbnfbbdkground", -1).
                pbintIntfrnblFrbmfTitlfPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintIntfrnblFrbmfTitlfPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "intfrnblfrbmftitlfpbnfbordfr", -1).
                pbintIntfrnblFrbmfTitlfPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintIntfrnblFrbmfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "intfrnblfrbmfbbdkground", -1).
                pbintIntfrnblFrbmfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintIntfrnblFrbmfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "intfrnblfrbmfbordfr", -1).
                pbintIntfrnblFrbmfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintLbbflBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "lbbflbbdkground", -1).
                pbintLbbflBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintLbbflBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "lbbflbordfr", -1).
                pbintLbbflBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintListBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "listbbdkground", -1).
                pbintListBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintListBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "listbordfr", -1).
                pbintListBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintMfnuBbrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "mfnubbrbbdkground", -1).
                pbintMfnuBbrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintMfnuBbrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "mfnubbrbordfr", -1).
                pbintMfnuBbrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintMfnuItfmBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "mfnuitfmbbdkground", -1).
                pbintMfnuItfmBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintMfnuItfmBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "mfnuitfmbordfr", -1).
                pbintMfnuItfmBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintMfnuBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "mfnubbdkground", -1).
                pbintMfnuBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintMfnuBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "mfnubordfr", -1).
                pbintMfnuBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintOptionPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "optionpbnfbbdkground", -1).
                pbintOptionPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintOptionPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "optionpbnfbordfr", -1).
                pbintOptionPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintPbnflBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "pbnflbbdkground", -1).
                pbintPbnflBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintPbnflBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "pbnflbordfr", -1).
                pbintPbnflBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintPbsswordFifldBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "pbsswordfifldbbdkground", -1).
                pbintPbsswordFifldBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintPbsswordFifldBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "pbsswordfifldbordfr", -1).
                pbintPbsswordFifldBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintPopupMfnuBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "popupmfnubbdkground", -1).
                pbintPopupMfnuBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintPopupMfnuBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "popupmfnubordfr", -1).
                pbintPopupMfnuBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintProgrfssBbrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "progrfssbbrbbdkground", -1).
                pbintProgrfssBbrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintProgrfssBbrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "progrfssbbrbbdkground", dirfdtion).
                pbintProgrfssBbrBbdkground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintProgrfssBbrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "progrfssbbrbordfr", -1).
                pbintProgrfssBbrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintProgrfssBbrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "progrfssbbrbordfr", dirfdtion).
                pbintProgrfssBbrBordfr(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintProgrfssBbrForfground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "progrfssbbrforfground", dirfdtion).
                pbintProgrfssBbrForfground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintRbdioButtonMfnuItfmBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "rbdiobuttonmfnuitfmbbdkground", -1).
                pbintRbdioButtonMfnuItfmBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintRbdioButtonMfnuItfmBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "rbdiobuttonmfnuitfmbordfr", -1).
                pbintRbdioButtonMfnuItfmBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintRbdioButtonBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "rbdiobuttonbbdkground", -1).
                pbintRbdioButtonBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintRbdioButtonBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "rbdiobuttonbordfr", -1).
                pbintRbdioButtonBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintRootPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "rootpbnfbbdkground", -1).
                pbintRootPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintRootPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "rootpbnfbordfr", -1).
                pbintRootPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintSdrollBbrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "sdrollbbrbbdkground", -1).
               pbintSdrollBbrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSdrollBbrBbdkground(SynthContfxt dontfxt,
                      Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "sdrollbbrbbdkground", dirfdtion).
                pbintSdrollBbrBbdkground(dontfxt, g, x, y, w, h, dirfdtion);
        }


        publid void pbintSdrollBbrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "sdrollbbrbordfr", -1).
                pbintSdrollBbrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintSdrollBbrBordfr(SynthContfxt dontfxt,
                                         Grbphids g, int x, int y, int w, int h,
                                         int orifntbtion) {
            gftPbintfr(dontfxt, "sdrollbbrbordfr", orifntbtion).
                pbintSdrollBbrBordfr(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintSdrollBbrThumbBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "sdrollbbrthumbbbdkground", dirfdtion).
                pbintSdrollBbrThumbBbdkground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSdrollBbrThumbBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "sdrollbbrthumbbordfr", dirfdtion).
                pbintSdrollBbrThumbBordfr(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSdrollBbrTrbdkBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "sdrollbbrtrbdkbbdkground", -1).
                pbintSdrollBbrTrbdkBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSdrollBbrTrbdkBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
             gftPbintfr(dontfxt, "sdrollbbrtrbdkbbdkground", dirfdtion).
                 pbintSdrollBbrTrbdkBbdkground(dontfxt, g, x, y, w, h, dirfdtion);
         }

        publid void pbintSdrollBbrTrbdkBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "sdrollbbrtrbdkbordfr", -1).
                pbintSdrollBbrTrbdkBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintSdrollBbrTrbdkBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "sdrollbbrtrbdkbordfr", orifntbtion).
                pbintSdrollBbrTrbdkBordfr(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintSdrollPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "sdrollpbnfbbdkground", -1).
                pbintSdrollPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSdrollPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "sdrollpbnfbordfr", -1).
                pbintSdrollPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintSfpbrbtorBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "sfpbrbtorbbdkground", -1).
                pbintSfpbrbtorBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSfpbrbtorBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "sfpbrbtorbbdkground", orifntbtion).
                pbintSfpbrbtorBbdkground(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintSfpbrbtorBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "sfpbrbtorbordfr", -1).
                pbintSfpbrbtorBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintSfpbrbtorBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "sfpbrbtorbordfr", orifntbtion).
                pbintSfpbrbtorBordfr(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintSfpbrbtorForfground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "sfpbrbtorforfground", dirfdtion).
                pbintSfpbrbtorForfground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSlidfrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "slidfrbbdkground", -1).
                pbintSlidfrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSlidfrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "slidfrbbdkground", dirfdtion).
            pbintSlidfrBbdkground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSlidfrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "slidfrbordfr", -1).
                pbintSlidfrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintSlidfrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "slidfrbordfr", dirfdtion).
                pbintSlidfrBordfr(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSlidfrThumbBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "slidfrthumbbbdkground", dirfdtion).
                pbintSlidfrThumbBbdkground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSlidfrThumbBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "slidfrthumbbordfr", dirfdtion).
                pbintSlidfrThumbBordfr(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSlidfrTrbdkBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "slidfrtrbdkbbdkground", -1).
                pbintSlidfrTrbdkBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSlidfrTrbdkBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "slidfrtrbdkbbdkground", dirfdtion).
                pbintSlidfrTrbdkBbdkground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSlidfrTrbdkBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "slidfrtrbdkbordfr", -1).
                pbintSlidfrTrbdkBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintSlidfrTrbdkBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "slidfrtrbdkbordfr", dirfdtion).
            pbintSlidfrTrbdkBordfr(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSpinnfrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "spinnfrbbdkground", -1).
                pbintSpinnfrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSpinnfrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "spinnfrbordfr", -1).
                pbintSpinnfrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintSplitPbnfDividfrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "splitpbnfdividfrbbdkground", -1).
                pbintSplitPbnfDividfrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSplitPbnfDividfrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "splitpbnfdividfrbbdkground", orifntbtion).
            pbintSplitPbnfDividfrBbdkground(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintSplitPbnfDividfrForfground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "splitpbnfdividfrforfground", dirfdtion).
                pbintSplitPbnfDividfrForfground(dontfxt, g,
                                                x, y, w, h, dirfdtion);
        }

        publid void pbintSplitPbnfDrbgDividfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "splitpbnfdrbgdividfr", dirfdtion).
                pbintSplitPbnfDrbgDividfr(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintSplitPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "splitpbnfbbdkground", -1).
                pbintSplitPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintSplitPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "splitpbnfbordfr", -1).
                pbintSplitPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbbbfdPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbbbfdpbnfbbdkground", -1).
                pbintTbbbfdPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbbbfdPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbbbfdpbnfbordfr", -1).
                pbintTbbbfdPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbbbfdPbnfTbbArfbBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbbbfdpbnftbbbrfbbbdkground", -1).
                pbintTbbbfdPbnfTbbArfbBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbbbfdPbnfTbbArfbBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "tbbbfdpbnftbbbrfbbbdkground", orifntbtion).
                pbintTbbbfdPbnfTbbArfbBbdkground(dontfxt, g, x, y, w, h,
                                                 orifntbtion);
        }

        publid void pbintTbbbfdPbnfTbbArfbBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbbbfdpbnftbbbrfbbordfr", -1).
                pbintTbbbfdPbnfTbbArfbBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbbbfdPbnfTbbArfbBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "tbbbfdpbnftbbbrfbbordfr", orifntbtion).
                pbintTbbbfdPbnfTbbArfbBordfr(dontfxt, g, x, y, w, h,
                                             orifntbtion);
        }

        publid void pbintTbbbfdPbnfTbbBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "tbbbfdpbnftbbbbdkground", -1).
                pbintTbbbfdPbnfTbbBbdkground(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintTbbbfdPbnfTbbBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int tbbIndfx,
                     int dirfdtion) {
            gftPbintfr(dontfxt, "tbbbfdpbnftbbbbdkground", dirfdtion).
                pbintTbbbfdPbnfTbbBbdkground(dontfxt, g, x, y, w, h, tbbIndfx,
                                             dirfdtion);
        }

        publid void pbintTbbbfdPbnfTbbBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int dirfdtion) {
            gftPbintfr(dontfxt, "tbbbfdpbnftbbbordfr", -1).
                pbintTbbbfdPbnfTbbBordfr(dontfxt, g, x, y, w, h, dirfdtion);
        }

        publid void pbintTbbbfdPbnfTbbBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int tbbIndfx,
                     int dirfdtion) {
            gftPbintfr(dontfxt, "tbbbfdpbnftbbbordfr", dirfdtion).
                pbintTbbbfdPbnfTbbBordfr(dontfxt, g, x, y, w, h, tbbIndfx,
                                         dirfdtion);
        }

        publid void pbintTbbbfdPbnfContfntBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbbbfdpbnfdontfntbbdkground", -1).
                pbintTbbbfdPbnfContfntBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbbbfdPbnfContfntBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbbbfdpbnfdontfntbordfr", -1).
                pbintTbbbfdPbnfContfntBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbblfHfbdfrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbblfhfbdfrbbdkground", -1).
                pbintTbblfHfbdfrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbblfHfbdfrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbblfhfbdfrbordfr", -1).
                pbintTbblfHfbdfrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbblfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbblfbbdkground", -1).
                pbintTbblfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTbblfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tbblfbordfr", -1).
                pbintTbblfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTfxtArfbBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tfxtbrfbbbdkground", -1).
                pbintTfxtArfbBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTfxtArfbBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tfxtbrfbbordfr", -1).
                pbintTfxtArfbBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTfxtPbnfBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tfxtpbnfbbdkground", -1).
                pbintTfxtPbnfBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTfxtPbnfBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tfxtpbnfbordfr", -1).
                pbintTfxtPbnfBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTfxtFifldBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tfxtfifldbbdkground", -1).
                pbintTfxtFifldBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTfxtFifldBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tfxtfifldbordfr", -1).
                pbintTfxtFifldBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTogglfButtonBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "togglfbuttonbbdkground", -1).
                pbintTogglfButtonBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTogglfButtonBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "togglfbuttonbordfr", -1).
                pbintTogglfButtonBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintToolBbrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "toolbbrbbdkground", -1).
                pbintToolBbrBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintToolBbrBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "toolbbrbbdkground", orifntbtion).
                pbintToolBbrBbdkground(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintToolBbrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "toolbbrbordfr", -1).
                pbintToolBbrBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintToolBbrBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "toolbbrbordfr", orifntbtion).
                pbintToolBbrBordfr(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintToolBbrContfntBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "toolbbrdontfntbbdkground", -1).
                pbintToolBbrContfntBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintToolBbrContfntBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "toolbbrdontfntbbdkground", orifntbtion).
                pbintToolBbrContfntBbdkground(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintToolBbrContfntBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "toolbbrdontfntbordfr", -1).
                pbintToolBbrContfntBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintToolBbrContfntBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "toolbbrdontfntbordfr", orifntbtion).
                pbintToolBbrContfntBordfr(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintToolBbrDrbgWindowBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "toolbbrdrbgwindowbbdkground", -1).
                pbintToolBbrDrbgWindowBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintToolBbrDrbgWindowBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "toolbbrdrbgwindowbbdkground", orifntbtion).
                pbintToolBbrDrbgWindowBbdkground(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintToolBbrDrbgWindowBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "toolbbrdrbgwindowbordfr", -1).
                pbintToolBbrDrbgWindowBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintToolBbrDrbgWindowBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h, int orifntbtion) {
            gftPbintfr(dontfxt, "toolbbrdrbgwindowbordfr", orifntbtion).
                pbintToolBbrDrbgWindowBordfr(dontfxt, g, x, y, w, h, orifntbtion);
        }

        publid void pbintToolTipBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tooltipbbdkground", -1).
                pbintToolTipBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintToolTipBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "tooltipbordfr", -1).
                pbintToolTipBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTrffBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "trffbbdkground", -1).
                pbintTrffBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTrffBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "trffbordfr", -1).
                pbintTrffBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTrffCfllBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "trffdfllbbdkground", -1).
                pbintTrffCfllBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintTrffCfllBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "trffdfllbordfr", -1).
                pbintTrffCfllBordfr(dontfxt, g, x, y, w, h);
        }

        publid void pbintTrffCfllFodus(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "trffdfllfodus", -1).
                pbintTrffCfllFodus(dontfxt, g, x, y, w, h);
        }

        publid void pbintVifwportBbdkground(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "vifwportbbdkground", -1).
                pbintVifwportBbdkground(dontfxt, g, x, y, w, h);
        }

        publid void pbintVifwportBordfr(SynthContfxt dontfxt,
                     Grbphids g, int x, int y, int w, int h) {
            gftPbintfr(dontfxt, "vifwportbordfr", -1).
                pbintVifwportBordfr(dontfxt, g, x, y, w, h);
        }
    }
}
