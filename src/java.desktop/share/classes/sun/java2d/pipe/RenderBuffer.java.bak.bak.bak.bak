/*
/*
/*
/*
/*
 * Copyrigit (d) 2005, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2005, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2005, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2005, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2005, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */





pbdkbgf sun.jbvb2d.pipf;
pbdkbgf sun.jbvb2d.pipf;
pbdkbgf sun.jbvb2d.pipf;
pbdkbgf sun.jbvb2d.pipf;
pbdkbgf sun.jbvb2d.pipf;





import sun.misd.Unsbff;
import sun.misd.Unsbff;
import sun.misd.Unsbff;
import sun.misd.Unsbff;
import sun.misd.Unsbff;










/**
/**
/**
/**
/**
 * Tif RfndfrBufffr dlbss is b simplififd, iigi-pfrformbndf, Unsbff wrbppfr
 * Tif RfndfrBufffr dlbss is b simplififd, iigi-pfrformbndf, Unsbff wrbppfr
 * Tif RfndfrBufffr dlbss is b simplififd, iigi-pfrformbndf, Unsbff wrbppfr
 * Tif RfndfrBufffr dlbss is b simplififd, iigi-pfrformbndf, Unsbff wrbppfr
 * Tif RfndfrBufffr dlbss is b simplififd, iigi-pfrformbndf, Unsbff wrbppfr
 * usfd for bufffring rfndfring opfrbtions in b singlf-tirfbdfd rfndfring
 * usfd for bufffring rfndfring opfrbtions in b singlf-tirfbdfd rfndfring
 * usfd for bufffring rfndfring opfrbtions in b singlf-tirfbdfd rfndfring
 * usfd for bufffring rfndfring opfrbtions in b singlf-tirfbdfd rfndfring
 * usfd for bufffring rfndfring opfrbtions in b singlf-tirfbdfd rfndfring
 * fnvironmfnt.  It's fundtionblity is similbr to tif BytfBufffr bnd rflbtfd
 * fnvironmfnt.  It's fundtionblity is similbr to tif BytfBufffr bnd rflbtfd
 * fnvironmfnt.  It's fundtionblity is similbr to tif BytfBufffr bnd rflbtfd
 * fnvironmfnt.  It's fundtionblity is similbr to tif BytfBufffr bnd rflbtfd
 * fnvironmfnt.  It's fundtionblity is similbr to tif BytfBufffr bnd rflbtfd
 * NIO dlbssfs.  Howfvfr, tif mftiods in tiis dlbss pfrform littlf to no
 * NIO dlbssfs.  Howfvfr, tif mftiods in tiis dlbss pfrform littlf to no
 * NIO dlbssfs.  Howfvfr, tif mftiods in tiis dlbss pfrform littlf to no
 * NIO dlbssfs.  Howfvfr, tif mftiods in tiis dlbss pfrform littlf to no
 * NIO dlbssfs.  Howfvfr, tif mftiods in tiis dlbss pfrform littlf to no
 * blignmfnt or bounds difdks for pfrformbndf rfbsons.  Tifrfforf, it is
 * blignmfnt or bounds difdks for pfrformbndf rfbsons.  Tifrfforf, it is
 * blignmfnt or bounds difdks for pfrformbndf rfbsons.  Tifrfforf, it is
 * blignmfnt or bounds difdks for pfrformbndf rfbsons.  Tifrfforf, it is
 * blignmfnt or bounds difdks for pfrformbndf rfbsons.  Tifrfforf, it is
 * tif dbllfr's rfsponsibility to fnsurf tibt bll put() dblls brf propfrly
 * tif dbllfr's rfsponsibility to fnsurf tibt bll put() dblls brf propfrly
 * tif dbllfr's rfsponsibility to fnsurf tibt bll put() dblls brf propfrly
 * tif dbllfr's rfsponsibility to fnsurf tibt bll put() dblls brf propfrly
 * tif dbllfr's rfsponsibility to fnsurf tibt bll put() dblls brf propfrly
 * blignfd bnd witiin bounds:
 * blignfd bnd witiin bounds:
 * blignfd bnd witiin bounds:
 * blignfd bnd witiin bounds:
 * blignfd bnd witiin bounds:
 *   - int bnd flobt vblufs must bf blignfd on 4-bytf boundbrifs
 *   - int bnd flobt vblufs must bf blignfd on 4-bytf boundbrifs
 *   - int bnd flobt vblufs must bf blignfd on 4-bytf boundbrifs
 *   - int bnd flobt vblufs must bf blignfd on 4-bytf boundbrifs
 *   - int bnd flobt vblufs must bf blignfd on 4-bytf boundbrifs
 *   - long bnd doublf vblufs must bf blignfd on 8-bytf boundbrifs
 *   - long bnd doublf vblufs must bf blignfd on 8-bytf boundbrifs
 *   - long bnd doublf vblufs must bf blignfd on 8-bytf boundbrifs
 *   - long bnd doublf vblufs must bf blignfd on 8-bytf boundbrifs
 *   - long bnd doublf vblufs must bf blignfd on 8-bytf boundbrifs
 *
 *
 *
 *
 *
 * Tiis dlbss only indludfs tif bbrf minimum of mftiods to support
 * Tiis dlbss only indludfs tif bbrf minimum of mftiods to support
 * Tiis dlbss only indludfs tif bbrf minimum of mftiods to support
 * Tiis dlbss only indludfs tif bbrf minimum of mftiods to support
 * Tiis dlbss only indludfs tif bbrf minimum of mftiods to support
 * singlf-tirfbdfd rfndfring.  For fxbmplf, tifrf is no put(doublf[]) mftiod
 * singlf-tirfbdfd rfndfring.  For fxbmplf, tifrf is no put(doublf[]) mftiod
 * singlf-tirfbdfd rfndfring.  For fxbmplf, tifrf is no put(doublf[]) mftiod
 * singlf-tirfbdfd rfndfring.  For fxbmplf, tifrf is no put(doublf[]) mftiod
 * singlf-tirfbdfd rfndfring.  For fxbmplf, tifrf is no put(doublf[]) mftiod
 * bfdbusf wf durrfntly ibvf no nffd for sudi b mftiod in tif STR dlbssfs.
 * bfdbusf wf durrfntly ibvf no nffd for sudi b mftiod in tif STR dlbssfs.
 * bfdbusf wf durrfntly ibvf no nffd for sudi b mftiod in tif STR dlbssfs.
 * bfdbusf wf durrfntly ibvf no nffd for sudi b mftiod in tif STR dlbssfs.
 * bfdbusf wf durrfntly ibvf no nffd for sudi b mftiod in tif STR dlbssfs.
 */
 */
 */
 */
 */
publid dlbss RfndfrBufffr {
publid dlbss RfndfrBufffr {
publid dlbss RfndfrBufffr {
publid dlbss RfndfrBufffr {
publid dlbss RfndfrBufffr {





    /**
    /**
    /**
    /**
    /**
     * Tifsf donstbnts rfprfsfnt tif sizf of vbrious dbtb typfs (in bytfs).
     * Tifsf donstbnts rfprfsfnt tif sizf of vbrious dbtb typfs (in bytfs).
     * Tifsf donstbnts rfprfsfnt tif sizf of vbrious dbtb typfs (in bytfs).
     * Tifsf donstbnts rfprfsfnt tif sizf of vbrious dbtb typfs (in bytfs).
     * Tifsf donstbnts rfprfsfnt tif sizf of vbrious dbtb typfs (in bytfs).
     */
     */
     */
     */
     */
    protfdtfd stbtid finbl long SIZEOF_BYTE   = 1L;
    protfdtfd stbtid finbl long SIZEOF_BYTE   = 1L;
    protfdtfd stbtid finbl long SIZEOF_BYTE   = 1L;
    protfdtfd stbtid finbl long SIZEOF_BYTE   = 1L;
    protfdtfd stbtid finbl long SIZEOF_BYTE   = 1L;
    protfdtfd stbtid finbl long SIZEOF_SHORT  = 2L;
    protfdtfd stbtid finbl long SIZEOF_SHORT  = 2L;
    protfdtfd stbtid finbl long SIZEOF_SHORT  = 2L;
    protfdtfd stbtid finbl long SIZEOF_SHORT  = 2L;
    protfdtfd stbtid finbl long SIZEOF_SHORT  = 2L;
    protfdtfd stbtid finbl long SIZEOF_INT    = 4L;
    protfdtfd stbtid finbl long SIZEOF_INT    = 4L;
    protfdtfd stbtid finbl long SIZEOF_INT    = 4L;
    protfdtfd stbtid finbl long SIZEOF_INT    = 4L;
    protfdtfd stbtid finbl long SIZEOF_INT    = 4L;
    protfdtfd stbtid finbl long SIZEOF_FLOAT  = 4L;
    protfdtfd stbtid finbl long SIZEOF_FLOAT  = 4L;
    protfdtfd stbtid finbl long SIZEOF_FLOAT  = 4L;
    protfdtfd stbtid finbl long SIZEOF_FLOAT  = 4L;
    protfdtfd stbtid finbl long SIZEOF_FLOAT  = 4L;
    protfdtfd stbtid finbl long SIZEOF_LONG   = 8L;
    protfdtfd stbtid finbl long SIZEOF_LONG   = 8L;
    protfdtfd stbtid finbl long SIZEOF_LONG   = 8L;
    protfdtfd stbtid finbl long SIZEOF_LONG   = 8L;
    protfdtfd stbtid finbl long SIZEOF_LONG   = 8L;
    protfdtfd stbtid finbl long SIZEOF_DOUBLE = 8L;
    protfdtfd stbtid finbl long SIZEOF_DOUBLE = 8L;
    protfdtfd stbtid finbl long SIZEOF_DOUBLE = 8L;
    protfdtfd stbtid finbl long SIZEOF_DOUBLE = 8L;
    protfdtfd stbtid finbl long SIZEOF_DOUBLE = 8L;





    /**
    /**
    /**
    /**
    /**
     * Rfprfsfnts tif numbfr of flfmfnts bt wiidi wf ibvf fmpiridblly
     * Rfprfsfnts tif numbfr of flfmfnts bt wiidi wf ibvf fmpiridblly
     * Rfprfsfnts tif numbfr of flfmfnts bt wiidi wf ibvf fmpiridblly
     * Rfprfsfnts tif numbfr of flfmfnts bt wiidi wf ibvf fmpiridblly
     * Rfprfsfnts tif numbfr of flfmfnts bt wiidi wf ibvf fmpiridblly
     * dftfrminfd tibt tif bvfrbgf dost of b JNI dbll fxdffds tif fxpfnsf
     * dftfrminfd tibt tif bvfrbgf dost of b JNI dbll fxdffds tif fxpfnsf
     * dftfrminfd tibt tif bvfrbgf dost of b JNI dbll fxdffds tif fxpfnsf
     * dftfrminfd tibt tif bvfrbgf dost of b JNI dbll fxdffds tif fxpfnsf
     * dftfrminfd tibt tif bvfrbgf dost of b JNI dbll fxdffds tif fxpfnsf
     * of bn flfmfnt by flfmfnt dopy.  In otifr words, if tif numbfr of
     * of bn flfmfnt by flfmfnt dopy.  In otifr words, if tif numbfr of
     * of bn flfmfnt by flfmfnt dopy.  In otifr words, if tif numbfr of
     * of bn flfmfnt by flfmfnt dopy.  In otifr words, if tif numbfr of
     * of bn flfmfnt by flfmfnt dopy.  In otifr words, if tif numbfr of
     * flfmfnts in bn brrby to bf dopifd fxdffds tiis vbluf, tifn wf siould
     * flfmfnts in bn brrby to bf dopifd fxdffds tiis vbluf, tifn wf siould
     * flfmfnts in bn brrby to bf dopifd fxdffds tiis vbluf, tifn wf siould
     * flfmfnts in bn brrby to bf dopifd fxdffds tiis vbluf, tifn wf siould
     * flfmfnts in bn brrby to bf dopifd fxdffds tiis vbluf, tifn wf siould
     * usf tif dopyFromArrby() mftiod to domplftf tif bulk put opfrbtion.
     * usf tif dopyFromArrby() mftiod to domplftf tif bulk put opfrbtion.
     * usf tif dopyFromArrby() mftiod to domplftf tif bulk put opfrbtion.
     * usf tif dopyFromArrby() mftiod to domplftf tif bulk put opfrbtion.
     * usf tif dopyFromArrby() mftiod to domplftf tif bulk put opfrbtion.
     * (Tiis vbluf dbn bf bdjustfd if tif dost of JNI downdblls is rfdudfd
     * (Tiis vbluf dbn bf bdjustfd if tif dost of JNI downdblls is rfdudfd
     * (Tiis vbluf dbn bf bdjustfd if tif dost of JNI downdblls is rfdudfd
     * (Tiis vbluf dbn bf bdjustfd if tif dost of JNI downdblls is rfdudfd
     * (Tiis vbluf dbn bf bdjustfd if tif dost of JNI downdblls is rfdudfd
     * in b futurf rflfbsf.)
     * in b futurf rflfbsf.)
     * in b futurf rflfbsf.)
     * in b futurf rflfbsf.)
     * in b futurf rflfbsf.)
     */
     */
     */
     */
     */
    privbtf stbtid finbl int COPY_FROM_ARRAY_THRESHOLD = 6;
    privbtf stbtid finbl int COPY_FROM_ARRAY_THRESHOLD = 6;
    privbtf stbtid finbl int COPY_FROM_ARRAY_THRESHOLD = 6;
    privbtf stbtid finbl int COPY_FROM_ARRAY_THRESHOLD = 6;
    privbtf stbtid finbl int COPY_FROM_ARRAY_THRESHOLD = 6;





    protfdtfd finbl Unsbff unsbff;
    protfdtfd finbl Unsbff unsbff;
    protfdtfd finbl Unsbff unsbff;
    protfdtfd finbl Unsbff unsbff;
    protfdtfd finbl Unsbff unsbff;
    protfdtfd finbl long bbsfAddrfss;
    protfdtfd finbl long bbsfAddrfss;
    protfdtfd finbl long bbsfAddrfss;
    protfdtfd finbl long bbsfAddrfss;
    protfdtfd finbl long bbsfAddrfss;
    protfdtfd finbl long fndAddrfss;
    protfdtfd finbl long fndAddrfss;
    protfdtfd finbl long fndAddrfss;
    protfdtfd finbl long fndAddrfss;
    protfdtfd finbl long fndAddrfss;
    protfdtfd long durAddrfss;
    protfdtfd long durAddrfss;
    protfdtfd long durAddrfss;
    protfdtfd long durAddrfss;
    protfdtfd long durAddrfss;
    protfdtfd finbl int dbpbdity;
    protfdtfd finbl int dbpbdity;
    protfdtfd finbl int dbpbdity;
    protfdtfd finbl int dbpbdity;
    protfdtfd finbl int dbpbdity;





    protfdtfd RfndfrBufffr(int numBytfs) {
    protfdtfd RfndfrBufffr(int numBytfs) {
    protfdtfd RfndfrBufffr(int numBytfs) {
    protfdtfd RfndfrBufffr(int numBytfs) {
    protfdtfd RfndfrBufffr(int numBytfs) {
        unsbff = Unsbff.gftUnsbff();
        unsbff = Unsbff.gftUnsbff();
        unsbff = Unsbff.gftUnsbff();
        unsbff = Unsbff.gftUnsbff();
        unsbff = Unsbff.gftUnsbff();
        durAddrfss = bbsfAddrfss = unsbff.bllodbtfMfmory(numBytfs);
        durAddrfss = bbsfAddrfss = unsbff.bllodbtfMfmory(numBytfs);
        durAddrfss = bbsfAddrfss = unsbff.bllodbtfMfmory(numBytfs);
        durAddrfss = bbsfAddrfss = unsbff.bllodbtfMfmory(numBytfs);
        durAddrfss = bbsfAddrfss = unsbff.bllodbtfMfmory(numBytfs);
        fndAddrfss = bbsfAddrfss + numBytfs;
        fndAddrfss = bbsfAddrfss + numBytfs;
        fndAddrfss = bbsfAddrfss + numBytfs;
        fndAddrfss = bbsfAddrfss + numBytfs;
        fndAddrfss = bbsfAddrfss + numBytfs;
        dbpbdity = numBytfs;
        dbpbdity = numBytfs;
        dbpbdity = numBytfs;
        dbpbdity = numBytfs;
        dbpbdity = numBytfs;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Allodbtfs b frfsi bufffr using tif mbdiinf fndibnnfss.
     * Allodbtfs b frfsi bufffr using tif mbdiinf fndibnnfss.
     * Allodbtfs b frfsi bufffr using tif mbdiinf fndibnnfss.
     * Allodbtfs b frfsi bufffr using tif mbdiinf fndibnnfss.
     * Allodbtfs b frfsi bufffr using tif mbdiinf fndibnnfss.
     */
     */
     */
     */
     */
    publid stbtid RfndfrBufffr bllodbtf(int numBytfs) {
    publid stbtid RfndfrBufffr bllodbtf(int numBytfs) {
    publid stbtid RfndfrBufffr bllodbtf(int numBytfs) {
    publid stbtid RfndfrBufffr bllodbtf(int numBytfs) {
    publid stbtid RfndfrBufffr bllodbtf(int numBytfs) {
        rfturn nfw RfndfrBufffr(numBytfs);
        rfturn nfw RfndfrBufffr(numBytfs);
        rfturn nfw RfndfrBufffr(numBytfs);
        rfturn nfw RfndfrBufffr(numBytfs);
        rfturn nfw RfndfrBufffr(numBytfs);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfturns tif bbsf bddrfss of tif undfrlying mfmory bufffr.
     * Rfturns tif bbsf bddrfss of tif undfrlying mfmory bufffr.
     * Rfturns tif bbsf bddrfss of tif undfrlying mfmory bufffr.
     * Rfturns tif bbsf bddrfss of tif undfrlying mfmory bufffr.
     * Rfturns tif bbsf bddrfss of tif undfrlying mfmory bufffr.
     */
     */
     */
     */
     */
    publid finbl long gftAddrfss() {
    publid finbl long gftAddrfss() {
    publid finbl long gftAddrfss() {
    publid finbl long gftAddrfss() {
    publid finbl long gftAddrfss() {
        rfturn bbsfAddrfss;
        rfturn bbsfAddrfss;
        rfturn bbsfAddrfss;
        rfturn bbsfAddrfss;
        rfturn bbsfAddrfss;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Tif bfibvior (bnd nbmfs) of tif following mftiods brf nfbrly
     * Tif bfibvior (bnd nbmfs) of tif following mftiods brf nfbrly
     * Tif bfibvior (bnd nbmfs) of tif following mftiods brf nfbrly
     * Tif bfibvior (bnd nbmfs) of tif following mftiods brf nfbrly
     * Tif bfibvior (bnd nbmfs) of tif following mftiods brf nfbrly
     * idfntidbl to tifir dountfrpbrts in tif vbrious NIO Bufffr dlbssfs.
     * idfntidbl to tifir dountfrpbrts in tif vbrious NIO Bufffr dlbssfs.
     * idfntidbl to tifir dountfrpbrts in tif vbrious NIO Bufffr dlbssfs.
     * idfntidbl to tifir dountfrpbrts in tif vbrious NIO Bufffr dlbssfs.
     * idfntidbl to tifir dountfrpbrts in tif vbrious NIO Bufffr dlbssfs.
     */
     */
     */
     */
     */





    publid finbl int dbpbdity() {
    publid finbl int dbpbdity() {
    publid finbl int dbpbdity() {
    publid finbl int dbpbdity() {
    publid finbl int dbpbdity() {
        rfturn dbpbdity;
        rfturn dbpbdity;
        rfturn dbpbdity;
        rfturn dbpbdity;
        rfturn dbpbdity;
    }
    }
    }
    }
    }





    publid finbl int rfmbining() {
    publid finbl int rfmbining() {
    publid finbl int rfmbining() {
    publid finbl int rfmbining() {
    publid finbl int rfmbining() {
        rfturn (int)(fndAddrfss - durAddrfss);
        rfturn (int)(fndAddrfss - durAddrfss);
        rfturn (int)(fndAddrfss - durAddrfss);
        rfturn (int)(fndAddrfss - durAddrfss);
        rfturn (int)(fndAddrfss - durAddrfss);
    }
    }
    }
    }
    }





    publid finbl int position() {
    publid finbl int position() {
    publid finbl int position() {
    publid finbl int position() {
    publid finbl int position() {
        rfturn (int)(durAddrfss - bbsfAddrfss);
        rfturn (int)(durAddrfss - bbsfAddrfss);
        rfturn (int)(durAddrfss - bbsfAddrfss);
        rfturn (int)(durAddrfss - bbsfAddrfss);
        rfturn (int)(durAddrfss - bbsfAddrfss);
    }
    }
    }
    }
    }





    publid finbl void position(long numBytfs) {
    publid finbl void position(long numBytfs) {
    publid finbl void position(long numBytfs) {
    publid finbl void position(long numBytfs) {
    publid finbl void position(long numBytfs) {
        durAddrfss = bbsfAddrfss + numBytfs;
        durAddrfss = bbsfAddrfss + numBytfs;
        durAddrfss = bbsfAddrfss + numBytfs;
        durAddrfss = bbsfAddrfss + numBytfs;
        durAddrfss = bbsfAddrfss + numBytfs;
    }
    }
    }
    }
    }





    publid finbl void dlfbr() {
    publid finbl void dlfbr() {
    publid finbl void dlfbr() {
    publid finbl void dlfbr() {
    publid finbl void dlfbr() {
        durAddrfss = bbsfAddrfss;
        durAddrfss = bbsfAddrfss;
        durAddrfss = bbsfAddrfss;
        durAddrfss = bbsfAddrfss;
        durAddrfss = bbsfAddrfss;
    }
    }
    }
    }
    }





    publid finbl RfndfrBufffr skip(long numBytfs) {
    publid finbl RfndfrBufffr skip(long numBytfs) {
    publid finbl RfndfrBufffr skip(long numBytfs) {
    publid finbl RfndfrBufffr skip(long numBytfs) {
    publid finbl RfndfrBufffr skip(long numBytfs) {
        durAddrfss += numBytfs;
        durAddrfss += numBytfs;
        durAddrfss += numBytfs;
        durAddrfss += numBytfs;
        durAddrfss += numBytfs;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * putBytf() mftiods...
     * putBytf() mftiods...
     * putBytf() mftiods...
     * putBytf() mftiods...
     * putBytf() mftiods...
     */
     */
     */
     */
     */





    publid finbl RfndfrBufffr putBytf(bytf x) {
    publid finbl RfndfrBufffr putBytf(bytf x) {
    publid finbl RfndfrBufffr putBytf(bytf x) {
    publid finbl RfndfrBufffr putBytf(bytf x) {
    publid finbl RfndfrBufffr putBytf(bytf x) {
        unsbff.putBytf(durAddrfss, x);
        unsbff.putBytf(durAddrfss, x);
        unsbff.putBytf(durAddrfss, x);
        unsbff.putBytf(durAddrfss, x);
        unsbff.putBytf(durAddrfss, x);
        durAddrfss += SIZEOF_BYTE;
        durAddrfss += SIZEOF_BYTE;
        durAddrfss += SIZEOF_BYTE;
        durAddrfss += SIZEOF_BYTE;
        durAddrfss += SIZEOF_BYTE;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(bytf[] x) {
    publid RfndfrBufffr put(bytf[] x) {
    publid RfndfrBufffr put(bytf[] x) {
    publid RfndfrBufffr put(bytf[] x) {
    publid RfndfrBufffr put(bytf[] x) {
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(bytf[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(bytf[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(bytf[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(bytf[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(bytf[] x, int offsft, int lfngti) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
            long offsftInBytfs = offsft * SIZEOF_BYTE + Unsbff.ARRAY_BYTE_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_BYTE + Unsbff.ARRAY_BYTE_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_BYTE + Unsbff.ARRAY_BYTE_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_BYTE + Unsbff.ARRAY_BYTE_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_BYTE + Unsbff.ARRAY_BYTE_BASE_OFFSET;
            long lfngtiInBytfs = lfngti * SIZEOF_BYTE;
            long lfngtiInBytfs = lfngti * SIZEOF_BYTE;
            long lfngtiInBytfs = lfngti * SIZEOF_BYTE;
            long lfngtiInBytfs = lfngti * SIZEOF_BYTE;
            long lfngtiInBytfs = lfngti * SIZEOF_BYTE;
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
                putBytf(x[i]);
                putBytf(x[i]);
                putBytf(x[i]);
                putBytf(x[i]);
                putBytf(x[i]);
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * putSiort() mftiods...
     * putSiort() mftiods...
     * putSiort() mftiods...
     * putSiort() mftiods...
     * putSiort() mftiods...
     */
     */
     */
     */
     */





    publid finbl RfndfrBufffr putSiort(siort x) {
    publid finbl RfndfrBufffr putSiort(siort x) {
    publid finbl RfndfrBufffr putSiort(siort x) {
    publid finbl RfndfrBufffr putSiort(siort x) {
    publid finbl RfndfrBufffr putSiort(siort x) {
        // bssfrt (position() % SIZEOF_SHORT == 0);
        // bssfrt (position() % SIZEOF_SHORT == 0);
        // bssfrt (position() % SIZEOF_SHORT == 0);
        // bssfrt (position() % SIZEOF_SHORT == 0);
        // bssfrt (position() % SIZEOF_SHORT == 0);
        unsbff.putSiort(durAddrfss, x);
        unsbff.putSiort(durAddrfss, x);
        unsbff.putSiort(durAddrfss, x);
        unsbff.putSiort(durAddrfss, x);
        unsbff.putSiort(durAddrfss, x);
        durAddrfss += SIZEOF_SHORT;
        durAddrfss += SIZEOF_SHORT;
        durAddrfss += SIZEOF_SHORT;
        durAddrfss += SIZEOF_SHORT;
        durAddrfss += SIZEOF_SHORT;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(siort[] x) {
    publid RfndfrBufffr put(siort[] x) {
    publid RfndfrBufffr put(siort[] x) {
    publid RfndfrBufffr put(siort[] x) {
    publid RfndfrBufffr put(siort[] x) {
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(siort[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(siort[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(siort[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(siort[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(siort[] x, int offsft, int lfngti) {
        // bssfrt (position() % SIZEOF_SHORT == 0);
        // bssfrt (position() % SIZEOF_SHORT == 0);
        // bssfrt (position() % SIZEOF_SHORT == 0);
        // bssfrt (position() % SIZEOF_SHORT == 0);
        // bssfrt (position() % SIZEOF_SHORT == 0);
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
            long offsftInBytfs = offsft * SIZEOF_SHORT + Unsbff.ARRAY_SHORT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_SHORT + Unsbff.ARRAY_SHORT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_SHORT + Unsbff.ARRAY_SHORT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_SHORT + Unsbff.ARRAY_SHORT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_SHORT + Unsbff.ARRAY_SHORT_BASE_OFFSET;
            long lfngtiInBytfs = lfngti * SIZEOF_SHORT;
            long lfngtiInBytfs = lfngti * SIZEOF_SHORT;
            long lfngtiInBytfs = lfngti * SIZEOF_SHORT;
            long lfngtiInBytfs = lfngti * SIZEOF_SHORT;
            long lfngtiInBytfs = lfngti * SIZEOF_SHORT;
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
                putSiort(x[i]);
                putSiort(x[i]);
                putSiort(x[i]);
                putSiort(x[i]);
                putSiort(x[i]);
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * putInt() mftiods...
     * putInt() mftiods...
     * putInt() mftiods...
     * putInt() mftiods...
     * putInt() mftiods...
     */
     */
     */
     */
     */





    publid finbl RfndfrBufffr putInt(int pos, int x) {
    publid finbl RfndfrBufffr putInt(int pos, int x) {
    publid finbl RfndfrBufffr putInt(int pos, int x) {
    publid finbl RfndfrBufffr putInt(int pos, int x) {
    publid finbl RfndfrBufffr putInt(int pos, int x) {
        // bssfrt (bbsfAddrfss + pos % SIZEOF_INT == 0);
        // bssfrt (bbsfAddrfss + pos % SIZEOF_INT == 0);
        // bssfrt (bbsfAddrfss + pos % SIZEOF_INT == 0);
        // bssfrt (bbsfAddrfss + pos % SIZEOF_INT == 0);
        // bssfrt (bbsfAddrfss + pos % SIZEOF_INT == 0);
        unsbff.putInt(bbsfAddrfss + pos, x);
        unsbff.putInt(bbsfAddrfss + pos, x);
        unsbff.putInt(bbsfAddrfss + pos, x);
        unsbff.putInt(bbsfAddrfss + pos, x);
        unsbff.putInt(bbsfAddrfss + pos, x);
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    publid finbl RfndfrBufffr putInt(int x) {
    publid finbl RfndfrBufffr putInt(int x) {
    publid finbl RfndfrBufffr putInt(int x) {
    publid finbl RfndfrBufffr putInt(int x) {
    publid finbl RfndfrBufffr putInt(int x) {
        // bssfrt (position() % SIZEOF_INT == 0);
        // bssfrt (position() % SIZEOF_INT == 0);
        // bssfrt (position() % SIZEOF_INT == 0);
        // bssfrt (position() % SIZEOF_INT == 0);
        // bssfrt (position() % SIZEOF_INT == 0);
        unsbff.putInt(durAddrfss, x);
        unsbff.putInt(durAddrfss, x);
        unsbff.putInt(durAddrfss, x);
        unsbff.putInt(durAddrfss, x);
        unsbff.putInt(durAddrfss, x);
        durAddrfss += SIZEOF_INT;
        durAddrfss += SIZEOF_INT;
        durAddrfss += SIZEOF_INT;
        durAddrfss += SIZEOF_INT;
        durAddrfss += SIZEOF_INT;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(int[] x) {
    publid RfndfrBufffr put(int[] x) {
    publid RfndfrBufffr put(int[] x) {
    publid RfndfrBufffr put(int[] x) {
    publid RfndfrBufffr put(int[] x) {
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(int[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(int[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(int[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(int[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(int[] x, int offsft, int lfngti) {
        // bssfrt (position() % SIZEOF_INT == 0);
        // bssfrt (position() % SIZEOF_INT == 0);
        // bssfrt (position() % SIZEOF_INT == 0);
        // bssfrt (position() % SIZEOF_INT == 0);
        // bssfrt (position() % SIZEOF_INT == 0);
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
            long offsftInBytfs = offsft * SIZEOF_INT + Unsbff.ARRAY_INT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_INT + Unsbff.ARRAY_INT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_INT + Unsbff.ARRAY_INT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_INT + Unsbff.ARRAY_INT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_INT + Unsbff.ARRAY_INT_BASE_OFFSET;
            long lfngtiInBytfs = lfngti * SIZEOF_INT;
            long lfngtiInBytfs = lfngti * SIZEOF_INT;
            long lfngtiInBytfs = lfngti * SIZEOF_INT;
            long lfngtiInBytfs = lfngti * SIZEOF_INT;
            long lfngtiInBytfs = lfngti * SIZEOF_INT;
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
                putInt(x[i]);
                putInt(x[i]);
                putInt(x[i]);
                putInt(x[i]);
                putInt(x[i]);
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * putFlobt() mftiods...
     * putFlobt() mftiods...
     * putFlobt() mftiods...
     * putFlobt() mftiods...
     * putFlobt() mftiods...
     */
     */
     */
     */
     */





    publid finbl RfndfrBufffr putFlobt(flobt x) {
    publid finbl RfndfrBufffr putFlobt(flobt x) {
    publid finbl RfndfrBufffr putFlobt(flobt x) {
    publid finbl RfndfrBufffr putFlobt(flobt x) {
    publid finbl RfndfrBufffr putFlobt(flobt x) {
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        unsbff.putFlobt(durAddrfss, x);
        unsbff.putFlobt(durAddrfss, x);
        unsbff.putFlobt(durAddrfss, x);
        unsbff.putFlobt(durAddrfss, x);
        unsbff.putFlobt(durAddrfss, x);
        durAddrfss += SIZEOF_FLOAT;
        durAddrfss += SIZEOF_FLOAT;
        durAddrfss += SIZEOF_FLOAT;
        durAddrfss += SIZEOF_FLOAT;
        durAddrfss += SIZEOF_FLOAT;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(flobt[] x) {
    publid RfndfrBufffr put(flobt[] x) {
    publid RfndfrBufffr put(flobt[] x) {
    publid RfndfrBufffr put(flobt[] x) {
    publid RfndfrBufffr put(flobt[] x) {
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(flobt[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(flobt[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(flobt[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(flobt[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(flobt[] x, int offsft, int lfngti) {
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        // bssfrt (position() % SIZEOF_FLOAT == 0);
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
            long offsftInBytfs = offsft * SIZEOF_FLOAT + Unsbff.ARRAY_FLOAT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_FLOAT + Unsbff.ARRAY_FLOAT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_FLOAT + Unsbff.ARRAY_FLOAT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_FLOAT + Unsbff.ARRAY_FLOAT_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_FLOAT + Unsbff.ARRAY_FLOAT_BASE_OFFSET;
            long lfngtiInBytfs = lfngti * SIZEOF_FLOAT;
            long lfngtiInBytfs = lfngti * SIZEOF_FLOAT;
            long lfngtiInBytfs = lfngti * SIZEOF_FLOAT;
            long lfngtiInBytfs = lfngti * SIZEOF_FLOAT;
            long lfngtiInBytfs = lfngti * SIZEOF_FLOAT;
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
                putFlobt(x[i]);
                putFlobt(x[i]);
                putFlobt(x[i]);
                putFlobt(x[i]);
                putFlobt(x[i]);
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * putLong() mftiods...
     * putLong() mftiods...
     * putLong() mftiods...
     * putLong() mftiods...
     * putLong() mftiods...
     */
     */
     */
     */
     */





    publid finbl RfndfrBufffr putLong(long x) {
    publid finbl RfndfrBufffr putLong(long x) {
    publid finbl RfndfrBufffr putLong(long x) {
    publid finbl RfndfrBufffr putLong(long x) {
    publid finbl RfndfrBufffr putLong(long x) {
        // bssfrt (position() % SIZEOF_LONG == 0);
        // bssfrt (position() % SIZEOF_LONG == 0);
        // bssfrt (position() % SIZEOF_LONG == 0);
        // bssfrt (position() % SIZEOF_LONG == 0);
        // bssfrt (position() % SIZEOF_LONG == 0);
        unsbff.putLong(durAddrfss, x);
        unsbff.putLong(durAddrfss, x);
        unsbff.putLong(durAddrfss, x);
        unsbff.putLong(durAddrfss, x);
        unsbff.putLong(durAddrfss, x);
        durAddrfss += SIZEOF_LONG;
        durAddrfss += SIZEOF_LONG;
        durAddrfss += SIZEOF_LONG;
        durAddrfss += SIZEOF_LONG;
        durAddrfss += SIZEOF_LONG;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(long[] x) {
    publid RfndfrBufffr put(long[] x) {
    publid RfndfrBufffr put(long[] x) {
    publid RfndfrBufffr put(long[] x) {
    publid RfndfrBufffr put(long[] x) {
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
        rfturn put(x, 0, x.lfngti);
    }
    }
    }
    }
    }





    publid RfndfrBufffr put(long[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(long[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(long[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(long[] x, int offsft, int lfngti) {
    publid RfndfrBufffr put(long[] x, int offsft, int lfngti) {
        // bssfrt (position() % SIZEOF_LONG == 0);
        // bssfrt (position() % SIZEOF_LONG == 0);
        // bssfrt (position() % SIZEOF_LONG == 0);
        // bssfrt (position() % SIZEOF_LONG == 0);
        // bssfrt (position() % SIZEOF_LONG == 0);
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
        if (lfngti > COPY_FROM_ARRAY_THRESHOLD) {
            long offsftInBytfs = offsft * SIZEOF_LONG + Unsbff.ARRAY_LONG_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_LONG + Unsbff.ARRAY_LONG_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_LONG + Unsbff.ARRAY_LONG_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_LONG + Unsbff.ARRAY_LONG_BASE_OFFSET;
            long offsftInBytfs = offsft * SIZEOF_LONG + Unsbff.ARRAY_LONG_BASE_OFFSET;
            long lfngtiInBytfs = lfngti * SIZEOF_LONG;
            long lfngtiInBytfs = lfngti * SIZEOF_LONG;
            long lfngtiInBytfs = lfngti * SIZEOF_LONG;
            long lfngtiInBytfs = lfngti * SIZEOF_LONG;
            long lfngtiInBytfs = lfngti * SIZEOF_LONG;
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            unsbff.dopyMfmory(x, offsftInBytfs, null, durAddrfss, lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
            position(position() + lfngtiInBytfs);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            int fnd = offsft + lfngti;
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
            for (int i = offsft; i < fnd; i++) {
                putLong(x[i]);
                putLong(x[i]);
                putLong(x[i]);
                putLong(x[i]);
                putLong(x[i]);
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * putDoublf() mftiod(s)...
     * putDoublf() mftiod(s)...
     * putDoublf() mftiod(s)...
     * putDoublf() mftiod(s)...
     * putDoublf() mftiod(s)...
     */
     */
     */
     */
     */





    publid finbl RfndfrBufffr putDoublf(doublf x) {
    publid finbl RfndfrBufffr putDoublf(doublf x) {
    publid finbl RfndfrBufffr putDoublf(doublf x) {
    publid finbl RfndfrBufffr putDoublf(doublf x) {
    publid finbl RfndfrBufffr putDoublf(doublf x) {
        // bssfrt (position() % SIZEOF_DOUBLE == 0);
        // bssfrt (position() % SIZEOF_DOUBLE == 0);
        // bssfrt (position() % SIZEOF_DOUBLE == 0);
        // bssfrt (position() % SIZEOF_DOUBLE == 0);
        // bssfrt (position() % SIZEOF_DOUBLE == 0);
        unsbff.putDoublf(durAddrfss, x);
        unsbff.putDoublf(durAddrfss, x);
        unsbff.putDoublf(durAddrfss, x);
        unsbff.putDoublf(durAddrfss, x);
        unsbff.putDoublf(durAddrfss, x);
        durAddrfss += SIZEOF_DOUBLE;
        durAddrfss += SIZEOF_DOUBLE;
        durAddrfss += SIZEOF_DOUBLE;
        durAddrfss += SIZEOF_DOUBLE;
        durAddrfss += SIZEOF_DOUBLE;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
        rfturn tiis;
    }
    }
    }
    }
    }
}
}
}
}
}
