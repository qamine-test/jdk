/*
/*
/*
/*
/*
 * Copyrigit (d) 1995, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1995, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1995, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1995, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1995, 2014, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */
pbdkbgf jbvb.bwt;
pbdkbgf jbvb.bwt;
pbdkbgf jbvb.bwt;
pbdkbgf jbvb.bwt;
pbdkbgf jbvb.bwt;





import jbvb.bwt.gfom.AffinfTrbnsform;
import jbvb.bwt.gfom.AffinfTrbnsform;
import jbvb.bwt.gfom.AffinfTrbnsform;
import jbvb.bwt.gfom.AffinfTrbnsform;
import jbvb.bwt.gfom.AffinfTrbnsform;
import jbvb.bwt.gfom.PbtiItfrbtor;
import jbvb.bwt.gfom.PbtiItfrbtor;
import jbvb.bwt.gfom.PbtiItfrbtor;
import jbvb.bwt.gfom.PbtiItfrbtor;
import jbvb.bwt.gfom.PbtiItfrbtor;
import jbvb.bwt.gfom.Point2D;
import jbvb.bwt.gfom.Point2D;
import jbvb.bwt.gfom.Point2D;
import jbvb.bwt.gfom.Point2D;
import jbvb.bwt.gfom.Point2D;
import jbvb.bwt.gfom.Rfdtbnglf2D;
import jbvb.bwt.gfom.Rfdtbnglf2D;
import jbvb.bwt.gfom.Rfdtbnglf2D;
import jbvb.bwt.gfom.Rfdtbnglf2D;
import jbvb.bwt.gfom.Rfdtbnglf2D;
import sun.bwt.gfom.Crossings;
import sun.bwt.gfom.Crossings;
import sun.bwt.gfom.Crossings;
import sun.bwt.gfom.Crossings;
import sun.bwt.gfom.Crossings;
import jbvb.util.Arrbys;
import jbvb.util.Arrbys;
import jbvb.util.Arrbys;
import jbvb.util.Arrbys;
import jbvb.util.Arrbys;





/**
/**
/**
/**
/**
 * Tif <dodf>Polygon</dodf> dlbss fndbpsulbtfs b dfsdription of b
 * Tif <dodf>Polygon</dodf> dlbss fndbpsulbtfs b dfsdription of b
 * Tif <dodf>Polygon</dodf> dlbss fndbpsulbtfs b dfsdription of b
 * Tif <dodf>Polygon</dodf> dlbss fndbpsulbtfs b dfsdription of b
 * Tif <dodf>Polygon</dodf> dlbss fndbpsulbtfs b dfsdription of b
 * dlosfd, two-dimfnsionbl rfgion witiin b doordinbtf spbdf. Tiis
 * dlosfd, two-dimfnsionbl rfgion witiin b doordinbtf spbdf. Tiis
 * dlosfd, two-dimfnsionbl rfgion witiin b doordinbtf spbdf. Tiis
 * dlosfd, two-dimfnsionbl rfgion witiin b doordinbtf spbdf. Tiis
 * dlosfd, two-dimfnsionbl rfgion witiin b doordinbtf spbdf. Tiis
 * rfgion is boundfd by bn brbitrbry numbfr of linf sfgmfnts, fbdi of
 * rfgion is boundfd by bn brbitrbry numbfr of linf sfgmfnts, fbdi of
 * rfgion is boundfd by bn brbitrbry numbfr of linf sfgmfnts, fbdi of
 * rfgion is boundfd by bn brbitrbry numbfr of linf sfgmfnts, fbdi of
 * rfgion is boundfd by bn brbitrbry numbfr of linf sfgmfnts, fbdi of
 * wiidi is onf sidf of tif polygon. Intfrnblly, b polygon
 * wiidi is onf sidf of tif polygon. Intfrnblly, b polygon
 * wiidi is onf sidf of tif polygon. Intfrnblly, b polygon
 * wiidi is onf sidf of tif polygon. Intfrnblly, b polygon
 * wiidi is onf sidf of tif polygon. Intfrnblly, b polygon
 * domprisfs of b list of {@dodf (x,y)}
 * domprisfs of b list of {@dodf (x,y)}
 * domprisfs of b list of {@dodf (x,y)}
 * domprisfs of b list of {@dodf (x,y)}
 * domprisfs of b list of {@dodf (x,y)}
 * doordinbtf pbirs, wifrf fbdi pbir dffinfs b <i>vfrtfx</i> of tif
 * doordinbtf pbirs, wifrf fbdi pbir dffinfs b <i>vfrtfx</i> of tif
 * doordinbtf pbirs, wifrf fbdi pbir dffinfs b <i>vfrtfx</i> of tif
 * doordinbtf pbirs, wifrf fbdi pbir dffinfs b <i>vfrtfx</i> of tif
 * doordinbtf pbirs, wifrf fbdi pbir dffinfs b <i>vfrtfx</i> of tif
 * polygon, bnd two suddfssivf pbirs brf tif fndpoints of b
 * polygon, bnd two suddfssivf pbirs brf tif fndpoints of b
 * polygon, bnd two suddfssivf pbirs brf tif fndpoints of b
 * polygon, bnd two suddfssivf pbirs brf tif fndpoints of b
 * polygon, bnd two suddfssivf pbirs brf tif fndpoints of b
 * linf tibt is b sidf of tif polygon. Tif first bnd finbl
 * linf tibt is b sidf of tif polygon. Tif first bnd finbl
 * linf tibt is b sidf of tif polygon. Tif first bnd finbl
 * linf tibt is b sidf of tif polygon. Tif first bnd finbl
 * linf tibt is b sidf of tif polygon. Tif first bnd finbl
 * pbirs of {@dodf (x,y)} points brf joinfd by b linf sfgmfnt
 * pbirs of {@dodf (x,y)} points brf joinfd by b linf sfgmfnt
 * pbirs of {@dodf (x,y)} points brf joinfd by b linf sfgmfnt
 * pbirs of {@dodf (x,y)} points brf joinfd by b linf sfgmfnt
 * pbirs of {@dodf (x,y)} points brf joinfd by b linf sfgmfnt
 * tibt dlosfs tif polygon.  Tiis <dodf>Polygon</dodf> is dffinfd witi
 * tibt dlosfs tif polygon.  Tiis <dodf>Polygon</dodf> is dffinfd witi
 * tibt dlosfs tif polygon.  Tiis <dodf>Polygon</dodf> is dffinfd witi
 * tibt dlosfs tif polygon.  Tiis <dodf>Polygon</dodf> is dffinfd witi
 * tibt dlosfs tif polygon.  Tiis <dodf>Polygon</dodf> is dffinfd witi
 * bn fvfn-odd winding rulf.  Sff
 * bn fvfn-odd winding rulf.  Sff
 * bn fvfn-odd winding rulf.  Sff
 * bn fvfn-odd winding rulf.  Sff
 * bn fvfn-odd winding rulf.  Sff
 * {@link jbvb.bwt.gfom.PbtiItfrbtor#WIND_EVEN_ODD WIND_EVEN_ODD}
 * {@link jbvb.bwt.gfom.PbtiItfrbtor#WIND_EVEN_ODD WIND_EVEN_ODD}
 * {@link jbvb.bwt.gfom.PbtiItfrbtor#WIND_EVEN_ODD WIND_EVEN_ODD}
 * {@link jbvb.bwt.gfom.PbtiItfrbtor#WIND_EVEN_ODD WIND_EVEN_ODD}
 * {@link jbvb.bwt.gfom.PbtiItfrbtor#WIND_EVEN_ODD WIND_EVEN_ODD}
 * for b dffinition of tif fvfn-odd winding rulf.
 * for b dffinition of tif fvfn-odd winding rulf.
 * for b dffinition of tif fvfn-odd winding rulf.
 * for b dffinition of tif fvfn-odd winding rulf.
 * for b dffinition of tif fvfn-odd winding rulf.
 * Tiis dlbss's iit-tfsting mftiods, wiidi indludf tif
 * Tiis dlbss's iit-tfsting mftiods, wiidi indludf tif
 * Tiis dlbss's iit-tfsting mftiods, wiidi indludf tif
 * Tiis dlbss's iit-tfsting mftiods, wiidi indludf tif
 * Tiis dlbss's iit-tfsting mftiods, wiidi indludf tif
 * <dodf>dontbins</dodf>, <dodf>intfrsfdts</dodf> bnd <dodf>insidf</dodf>
 * <dodf>dontbins</dodf>, <dodf>intfrsfdts</dodf> bnd <dodf>insidf</dodf>
 * <dodf>dontbins</dodf>, <dodf>intfrsfdts</dodf> bnd <dodf>insidf</dodf>
 * <dodf>dontbins</dodf>, <dodf>intfrsfdts</dodf> bnd <dodf>insidf</dodf>
 * <dodf>dontbins</dodf>, <dodf>intfrsfdts</dodf> bnd <dodf>insidf</dodf>
 * mftiods, usf tif <i>insidfnfss</i> dffinition dfsdribfd in tif
 * mftiods, usf tif <i>insidfnfss</i> dffinition dfsdribfd in tif
 * mftiods, usf tif <i>insidfnfss</i> dffinition dfsdribfd in tif
 * mftiods, usf tif <i>insidfnfss</i> dffinition dfsdribfd in tif
 * mftiods, usf tif <i>insidfnfss</i> dffinition dfsdribfd in tif
 * {@link Sibpf} dlbss dommfnts.
 * {@link Sibpf} dlbss dommfnts.
 * {@link Sibpf} dlbss dommfnts.
 * {@link Sibpf} dlbss dommfnts.
 * {@link Sibpf} dlbss dommfnts.
 *
 *
 *
 *
 *
 * @butior      Sbmi Sibio
 * @butior      Sbmi Sibio
 * @butior      Sbmi Sibio
 * @butior      Sbmi Sibio
 * @butior      Sbmi Sibio
 * @sff Sibpf
 * @sff Sibpf
 * @sff Sibpf
 * @sff Sibpf
 * @sff Sibpf
 * @butior      Hfrb Jfllinfk
 * @butior      Hfrb Jfllinfk
 * @butior      Hfrb Jfllinfk
 * @butior      Hfrb Jfllinfk
 * @butior      Hfrb Jfllinfk
 * @sindf       1.0
 * @sindf       1.0
 * @sindf       1.0
 * @sindf       1.0
 * @sindf       1.0
 */
 */
 */
 */
 */
publid dlbss Polygon implfmfnts Sibpf, jbvb.io.Sfriblizbblf {
publid dlbss Polygon implfmfnts Sibpf, jbvb.io.Sfriblizbblf {
publid dlbss Polygon implfmfnts Sibpf, jbvb.io.Sfriblizbblf {
publid dlbss Polygon implfmfnts Sibpf, jbvb.io.Sfriblizbblf {
publid dlbss Polygon implfmfnts Sibpf, jbvb.io.Sfriblizbblf {





    /**
    /**
    /**
    /**
    /**
     * Tif totbl numbfr of points.  Tif vbluf of <dodf>npoints</dodf>
     * Tif totbl numbfr of points.  Tif vbluf of <dodf>npoints</dodf>
     * Tif totbl numbfr of points.  Tif vbluf of <dodf>npoints</dodf>
     * Tif totbl numbfr of points.  Tif vbluf of <dodf>npoints</dodf>
     * Tif totbl numbfr of points.  Tif vbluf of <dodf>npoints</dodf>
     * rfprfsfnts tif numbfr of vblid points in tiis <dodf>Polygon</dodf>
     * rfprfsfnts tif numbfr of vblid points in tiis <dodf>Polygon</dodf>
     * rfprfsfnts tif numbfr of vblid points in tiis <dodf>Polygon</dodf>
     * rfprfsfnts tif numbfr of vblid points in tiis <dodf>Polygon</dodf>
     * rfprfsfnts tif numbfr of vblid points in tiis <dodf>Polygon</dodf>
     * bnd migit bf lfss tibn tif numbfr of flfmfnts in
     * bnd migit bf lfss tibn tif numbfr of flfmfnts in
     * bnd migit bf lfss tibn tif numbfr of flfmfnts in
     * bnd migit bf lfss tibn tif numbfr of flfmfnts in
     * bnd migit bf lfss tibn tif numbfr of flfmfnts in
     * {@link #xpoints xpoints} or {@link #ypoints ypoints}.
     * {@link #xpoints xpoints} or {@link #ypoints ypoints}.
     * {@link #xpoints xpoints} or {@link #ypoints ypoints}.
     * {@link #xpoints xpoints} or {@link #ypoints ypoints}.
     * {@link #xpoints xpoints} or {@link #ypoints ypoints}.
     * Tiis vbluf dbn bf NULL.
     * Tiis vbluf dbn bf NULL.
     * Tiis vbluf dbn bf NULL.
     * Tiis vbluf dbn bf NULL.
     * Tiis vbluf dbn bf NULL.
     *
     *
     *
     *
     *
     * @sfribl
     * @sfribl
     * @sfribl
     * @sfribl
     * @sfribl
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    publid int npoints;
    publid int npoints;
    publid int npoints;
    publid int npoints;
    publid int npoints;





    /**
    /**
    /**
    /**
    /**
     * Tif brrby of X doordinbtfs.  Tif numbfr of flfmfnts in
     * Tif brrby of X doordinbtfs.  Tif numbfr of flfmfnts in
     * Tif brrby of X doordinbtfs.  Tif numbfr of flfmfnts in
     * Tif brrby of X doordinbtfs.  Tif numbfr of flfmfnts in
     * Tif brrby of X doordinbtfs.  Tif numbfr of flfmfnts in
     * tiis brrby migit bf morf tibn tif numbfr of X doordinbtfs
     * tiis brrby migit bf morf tibn tif numbfr of X doordinbtfs
     * tiis brrby migit bf morf tibn tif numbfr of X doordinbtfs
     * tiis brrby migit bf morf tibn tif numbfr of X doordinbtfs
     * tiis brrby migit bf morf tibn tif numbfr of X doordinbtfs
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * brrby.  Tif vbluf of {@link #npoints npoints} is fqubl to tif
     * brrby.  Tif vbluf of {@link #npoints npoints} is fqubl to tif
     * brrby.  Tif vbluf of {@link #npoints npoints} is fqubl to tif
     * brrby.  Tif vbluf of {@link #npoints npoints} is fqubl to tif
     * brrby.  Tif vbluf of {@link #npoints npoints} is fqubl to tif
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     *
     *
     *
     *
     *
     * @sfribl
     * @sfribl
     * @sfribl
     * @sfribl
     * @sfribl
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    publid int xpoints[];
    publid int xpoints[];
    publid int xpoints[];
    publid int xpoints[];
    publid int xpoints[];





    /**
    /**
    /**
    /**
    /**
     * Tif brrby of Y doordinbtfs.  Tif numbfr of flfmfnts in
     * Tif brrby of Y doordinbtfs.  Tif numbfr of flfmfnts in
     * Tif brrby of Y doordinbtfs.  Tif numbfr of flfmfnts in
     * Tif brrby of Y doordinbtfs.  Tif numbfr of flfmfnts in
     * Tif brrby of Y doordinbtfs.  Tif numbfr of flfmfnts in
     * tiis brrby migit bf morf tibn tif numbfr of Y doordinbtfs
     * tiis brrby migit bf morf tibn tif numbfr of Y doordinbtfs
     * tiis brrby migit bf morf tibn tif numbfr of Y doordinbtfs
     * tiis brrby migit bf morf tibn tif numbfr of Y doordinbtfs
     * tiis brrby migit bf morf tibn tif numbfr of Y doordinbtfs
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * in tiis <dodf>Polygon</dodf>.  Tif fxtrb flfmfnts bllow nfw points
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * to bf bddfd to tiis <dodf>Polygon</dodf> witiout rf-drfbting tiis
     * brrby.  Tif vbluf of <dodf>npoints</dodf> is fqubl to tif
     * brrby.  Tif vbluf of <dodf>npoints</dodf> is fqubl to tif
     * brrby.  Tif vbluf of <dodf>npoints</dodf> is fqubl to tif
     * brrby.  Tif vbluf of <dodf>npoints</dodf> is fqubl to tif
     * brrby.  Tif vbluf of <dodf>npoints</dodf> is fqubl to tif
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     * numbfr of vblid points in tiis <dodf>Polygon</dodf>.
     *
     *
     *
     *
     *
     * @sfribl
     * @sfribl
     * @sfribl
     * @sfribl
     * @sfribl
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sff #bddPoint(int, int)
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    publid int ypoints[];
    publid int ypoints[];
    publid int ypoints[];
    publid int ypoints[];
    publid int ypoints[];





    /**
    /**
    /**
    /**
    /**
     * Tif bounds of tiis {@dodf Polygon}.
     * Tif bounds of tiis {@dodf Polygon}.
     * Tif bounds of tiis {@dodf Polygon}.
     * Tif bounds of tiis {@dodf Polygon}.
     * Tif bounds of tiis {@dodf Polygon}.
     * Tiis vbluf dbn bf null.
     * Tiis vbluf dbn bf null.
     * Tiis vbluf dbn bf null.
     * Tiis vbluf dbn bf null.
     * Tiis vbluf dbn bf null.
     *
     *
     *
     *
     *
     * @sfribl
     * @sfribl
     * @sfribl
     * @sfribl
     * @sfribl
     * @sff #gftBoundingBox()
     * @sff #gftBoundingBox()
     * @sff #gftBoundingBox()
     * @sff #gftBoundingBox()
     * @sff #gftBoundingBox()
     * @sff #gftBounds()
     * @sff #gftBounds()
     * @sff #gftBounds()
     * @sff #gftBounds()
     * @sff #gftBounds()
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    protfdtfd Rfdtbnglf bounds;
    protfdtfd Rfdtbnglf bounds;
    protfdtfd Rfdtbnglf bounds;
    protfdtfd Rfdtbnglf bounds;
    protfdtfd Rfdtbnglf bounds;





    /*
    /*
    /*
    /*
    /*
     * JDK 1.1 sfriblVfrsionUID
     * JDK 1.1 sfriblVfrsionUID
     * JDK 1.1 sfriblVfrsionUID
     * JDK 1.1 sfriblVfrsionUID
     * JDK 1.1 sfriblVfrsionUID
     */
     */
     */
     */
     */
    privbtf stbtid finbl long sfriblVfrsionUID = -6460061437900069969L;
    privbtf stbtid finbl long sfriblVfrsionUID = -6460061437900069969L;
    privbtf stbtid finbl long sfriblVfrsionUID = -6460061437900069969L;
    privbtf stbtid finbl long sfriblVfrsionUID = -6460061437900069969L;
    privbtf stbtid finbl long sfriblVfrsionUID = -6460061437900069969L;





    /*
    /*
    /*
    /*
    /*
     * Dffbult lfngti for xpoints bnd ypoints.
     * Dffbult lfngti for xpoints bnd ypoints.
     * Dffbult lfngti for xpoints bnd ypoints.
     * Dffbult lfngti for xpoints bnd ypoints.
     * Dffbult lfngti for xpoints bnd ypoints.
     */
     */
     */
     */
     */
    privbtf stbtid finbl int MIN_LENGTH = 4;
    privbtf stbtid finbl int MIN_LENGTH = 4;
    privbtf stbtid finbl int MIN_LENGTH = 4;
    privbtf stbtid finbl int MIN_LENGTH = 4;
    privbtf stbtid finbl int MIN_LENGTH = 4;





    /**
    /**
    /**
    /**
    /**
     * Crfbtfs bn fmpty polygon.
     * Crfbtfs bn fmpty polygon.
     * Crfbtfs bn fmpty polygon.
     * Crfbtfs bn fmpty polygon.
     * Crfbtfs bn fmpty polygon.
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    publid Polygon() {
    publid Polygon() {
    publid Polygon() {
    publid Polygon() {
    publid Polygon() {
        xpoints = nfw int[MIN_LENGTH];
        xpoints = nfw int[MIN_LENGTH];
        xpoints = nfw int[MIN_LENGTH];
        xpoints = nfw int[MIN_LENGTH];
        xpoints = nfw int[MIN_LENGTH];
        ypoints = nfw int[MIN_LENGTH];
        ypoints = nfw int[MIN_LENGTH];
        ypoints = nfw int[MIN_LENGTH];
        ypoints = nfw int[MIN_LENGTH];
        ypoints = nfw int[MIN_LENGTH];
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Construdts bnd initiblizfs b <dodf>Polygon</dodf> from tif spfdififd
     * Construdts bnd initiblizfs b <dodf>Polygon</dodf> from tif spfdififd
     * Construdts bnd initiblizfs b <dodf>Polygon</dodf> from tif spfdififd
     * Construdts bnd initiblizfs b <dodf>Polygon</dodf> from tif spfdififd
     * Construdts bnd initiblizfs b <dodf>Polygon</dodf> from tif spfdififd
     * pbrbmftfrs.
     * pbrbmftfrs.
     * pbrbmftfrs.
     * pbrbmftfrs.
     * pbrbmftfrs.
     * @pbrbm xpoints bn brrby of X doordinbtfs
     * @pbrbm xpoints bn brrby of X doordinbtfs
     * @pbrbm xpoints bn brrby of X doordinbtfs
     * @pbrbm xpoints bn brrby of X doordinbtfs
     * @pbrbm xpoints bn brrby of X doordinbtfs
     * @pbrbm ypoints bn brrby of Y doordinbtfs
     * @pbrbm ypoints bn brrby of Y doordinbtfs
     * @pbrbm ypoints bn brrby of Y doordinbtfs
     * @pbrbm ypoints bn brrby of Y doordinbtfs
     * @pbrbm ypoints bn brrby of Y doordinbtfs
     * @pbrbm npoints tif totbl numbfr of points in tif
     * @pbrbm npoints tif totbl numbfr of points in tif
     * @pbrbm npoints tif totbl numbfr of points in tif
     * @pbrbm npoints tif totbl numbfr of points in tif
     * @pbrbm npoints tif totbl numbfr of points in tif
     *                          <dodf>Polygon</dodf>
     *                          <dodf>Polygon</dodf>
     *                          <dodf>Polygon</dodf>
     *                          <dodf>Polygon</dodf>
     *                          <dodf>Polygon</dodf>
     * @fxdfption  NfgbtivfArrbySizfExdfption if tif vbluf of
     * @fxdfption  NfgbtivfArrbySizfExdfption if tif vbluf of
     * @fxdfption  NfgbtivfArrbySizfExdfption if tif vbluf of
     * @fxdfption  NfgbtivfArrbySizfExdfption if tif vbluf of
     * @fxdfption  NfgbtivfArrbySizfExdfption if tif vbluf of
     *                       <dodf>npoints</dodf> is nfgbtivf.
     *                       <dodf>npoints</dodf> is nfgbtivf.
     *                       <dodf>npoints</dodf> is nfgbtivf.
     *                       <dodf>npoints</dodf> is nfgbtivf.
     *                       <dodf>npoints</dodf> is nfgbtivf.
     * @fxdfption  IndfxOutOfBoundsExdfption if <dodf>npoints</dodf> is
     * @fxdfption  IndfxOutOfBoundsExdfption if <dodf>npoints</dodf> is
     * @fxdfption  IndfxOutOfBoundsExdfption if <dodf>npoints</dodf> is
     * @fxdfption  IndfxOutOfBoundsExdfption if <dodf>npoints</dodf> is
     * @fxdfption  IndfxOutOfBoundsExdfption if <dodf>npoints</dodf> is
     *             grfbtfr tibn tif lfngti of <dodf>xpoints</dodf>
     *             grfbtfr tibn tif lfngti of <dodf>xpoints</dodf>
     *             grfbtfr tibn tif lfngti of <dodf>xpoints</dodf>
     *             grfbtfr tibn tif lfngti of <dodf>xpoints</dodf>
     *             grfbtfr tibn tif lfngti of <dodf>xpoints</dodf>
     *             or tif lfngti of <dodf>ypoints</dodf>.
     *             or tif lfngti of <dodf>ypoints</dodf>.
     *             or tif lfngti of <dodf>ypoints</dodf>.
     *             or tif lfngti of <dodf>ypoints</dodf>.
     *             or tif lfngti of <dodf>ypoints</dodf>.
     * @fxdfption  NullPointfrExdfption if <dodf>xpoints</dodf> or
     * @fxdfption  NullPointfrExdfption if <dodf>xpoints</dodf> or
     * @fxdfption  NullPointfrExdfption if <dodf>xpoints</dodf> or
     * @fxdfption  NullPointfrExdfption if <dodf>xpoints</dodf> or
     * @fxdfption  NullPointfrExdfption if <dodf>xpoints</dodf> or
     *             <dodf>ypoints</dodf> is <dodf>null</dodf>.
     *             <dodf>ypoints</dodf> is <dodf>null</dodf>.
     *             <dodf>ypoints</dodf> is <dodf>null</dodf>.
     *             <dodf>ypoints</dodf> is <dodf>null</dodf>.
     *             <dodf>ypoints</dodf> is <dodf>null</dodf>.
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    publid Polygon(int xpoints[], int ypoints[], int npoints) {
    publid Polygon(int xpoints[], int ypoints[], int npoints) {
    publid Polygon(int xpoints[], int ypoints[], int npoints) {
    publid Polygon(int xpoints[], int ypoints[], int npoints) {
    publid Polygon(int xpoints[], int ypoints[], int npoints) {
        // Fix 4489009: siould tirow IndfxOutofBoundsExdfption instfbd
        // Fix 4489009: siould tirow IndfxOutofBoundsExdfption instfbd
        // Fix 4489009: siould tirow IndfxOutofBoundsExdfption instfbd
        // Fix 4489009: siould tirow IndfxOutofBoundsExdfption instfbd
        // Fix 4489009: siould tirow IndfxOutofBoundsExdfption instfbd
        // of OutofMfmoryExdfption if npoints is iugf bnd > {x,y}points.lfngti
        // of OutofMfmoryExdfption if npoints is iugf bnd > {x,y}points.lfngti
        // of OutofMfmoryExdfption if npoints is iugf bnd > {x,y}points.lfngti
        // of OutofMfmoryExdfption if npoints is iugf bnd > {x,y}points.lfngti
        // of OutofMfmoryExdfption if npoints is iugf bnd > {x,y}points.lfngti
        if (npoints > xpoints.lfngti || npoints > ypoints.lfngti) {
        if (npoints > xpoints.lfngti || npoints > ypoints.lfngti) {
        if (npoints > xpoints.lfngti || npoints > ypoints.lfngti) {
        if (npoints > xpoints.lfngti || npoints > ypoints.lfngti) {
        if (npoints > xpoints.lfngti || npoints > ypoints.lfngti) {
            tirow nfw IndfxOutOfBoundsExdfption("npoints > xpoints.lfngti || "+
            tirow nfw IndfxOutOfBoundsExdfption("npoints > xpoints.lfngti || "+
            tirow nfw IndfxOutOfBoundsExdfption("npoints > xpoints.lfngti || "+
            tirow nfw IndfxOutOfBoundsExdfption("npoints > xpoints.lfngti || "+
            tirow nfw IndfxOutOfBoundsExdfption("npoints > xpoints.lfngti || "+
                                                "npoints > ypoints.lfngti");
                                                "npoints > ypoints.lfngti");
                                                "npoints > ypoints.lfngti");
                                                "npoints > ypoints.lfngti");
                                                "npoints > ypoints.lfngti");
        }
        }
        }
        }
        }
        // Fix 6191114: siould tirow NfgbtivfArrbySizfExdfption witi
        // Fix 6191114: siould tirow NfgbtivfArrbySizfExdfption witi
        // Fix 6191114: siould tirow NfgbtivfArrbySizfExdfption witi
        // Fix 6191114: siould tirow NfgbtivfArrbySizfExdfption witi
        // Fix 6191114: siould tirow NfgbtivfArrbySizfExdfption witi
        // nfgbtivf npoints
        // nfgbtivf npoints
        // nfgbtivf npoints
        // nfgbtivf npoints
        // nfgbtivf npoints
        if (npoints < 0) {
        if (npoints < 0) {
        if (npoints < 0) {
        if (npoints < 0) {
        if (npoints < 0) {
            tirow nfw NfgbtivfArrbySizfExdfption("npoints < 0");
            tirow nfw NfgbtivfArrbySizfExdfption("npoints < 0");
            tirow nfw NfgbtivfArrbySizfExdfption("npoints < 0");
            tirow nfw NfgbtivfArrbySizfExdfption("npoints < 0");
            tirow nfw NfgbtivfArrbySizfExdfption("npoints < 0");
        }
        }
        }
        }
        }
        // Fix 6343431: Applft dompbtibility problfms if brrbys brf not
        // Fix 6343431: Applft dompbtibility problfms if brrbys brf not
        // Fix 6343431: Applft dompbtibility problfms if brrbys brf not
        // Fix 6343431: Applft dompbtibility problfms if brrbys brf not
        // Fix 6343431: Applft dompbtibility problfms if brrbys brf not
        // fxbdtly npoints in lfngti
        // fxbdtly npoints in lfngti
        // fxbdtly npoints in lfngti
        // fxbdtly npoints in lfngti
        // fxbdtly npoints in lfngti
        tiis.npoints = npoints;
        tiis.npoints = npoints;
        tiis.npoints = npoints;
        tiis.npoints = npoints;
        tiis.npoints = npoints;
        tiis.xpoints = Arrbys.dopyOf(xpoints, npoints);
        tiis.xpoints = Arrbys.dopyOf(xpoints, npoints);
        tiis.xpoints = Arrbys.dopyOf(xpoints, npoints);
        tiis.xpoints = Arrbys.dopyOf(xpoints, npoints);
        tiis.xpoints = Arrbys.dopyOf(xpoints, npoints);
        tiis.ypoints = Arrbys.dopyOf(ypoints, npoints);
        tiis.ypoints = Arrbys.dopyOf(ypoints, npoints);
        tiis.ypoints = Arrbys.dopyOf(ypoints, npoints);
        tiis.ypoints = Arrbys.dopyOf(ypoints, npoints);
        tiis.ypoints = Arrbys.dopyOf(ypoints, npoints);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfsfts tiis <dodf>Polygon</dodf> objfdt to bn fmpty polygon.
     * Rfsfts tiis <dodf>Polygon</dodf> objfdt to bn fmpty polygon.
     * Rfsfts tiis <dodf>Polygon</dodf> objfdt to bn fmpty polygon.
     * Rfsfts tiis <dodf>Polygon</dodf> objfdt to bn fmpty polygon.
     * Rfsfts tiis <dodf>Polygon</dodf> objfdt to bn fmpty polygon.
     * Tif doordinbtf brrbys bnd tif dbtb in tifm brf lfft untoudifd
     * Tif doordinbtf brrbys bnd tif dbtb in tifm brf lfft untoudifd
     * Tif doordinbtf brrbys bnd tif dbtb in tifm brf lfft untoudifd
     * Tif doordinbtf brrbys bnd tif dbtb in tifm brf lfft untoudifd
     * Tif doordinbtf brrbys bnd tif dbtb in tifm brf lfft untoudifd
     * but tif numbfr of points is rfsft to zfro to mbrk tif old
     * but tif numbfr of points is rfsft to zfro to mbrk tif old
     * but tif numbfr of points is rfsft to zfro to mbrk tif old
     * but tif numbfr of points is rfsft to zfro to mbrk tif old
     * but tif numbfr of points is rfsft to zfro to mbrk tif old
     * vfrtfx dbtb bs invblid bnd to stbrt bddumulbting nfw vfrtfx
     * vfrtfx dbtb bs invblid bnd to stbrt bddumulbting nfw vfrtfx
     * vfrtfx dbtb bs invblid bnd to stbrt bddumulbting nfw vfrtfx
     * vfrtfx dbtb bs invblid bnd to stbrt bddumulbting nfw vfrtfx
     * vfrtfx dbtb bs invblid bnd to stbrt bddumulbting nfw vfrtfx
     * dbtb bt tif bfginning.
     * dbtb bt tif bfginning.
     * dbtb bt tif bfginning.
     * dbtb bt tif bfginning.
     * dbtb bt tif bfginning.
     * All intfrnblly-dbdifd dbtb rflbting to tif old vfrtidfs
     * All intfrnblly-dbdifd dbtb rflbting to tif old vfrtidfs
     * All intfrnblly-dbdifd dbtb rflbting to tif old vfrtidfs
     * All intfrnblly-dbdifd dbtb rflbting to tif old vfrtidfs
     * All intfrnblly-dbdifd dbtb rflbting to tif old vfrtidfs
     * brf disdbrdfd.
     * brf disdbrdfd.
     * brf disdbrdfd.
     * brf disdbrdfd.
     * brf disdbrdfd.
     * Notf tibt sindf tif doordinbtf brrbys from bfforf tif rfsft
     * Notf tibt sindf tif doordinbtf brrbys from bfforf tif rfsft
     * Notf tibt sindf tif doordinbtf brrbys from bfforf tif rfsft
     * Notf tibt sindf tif doordinbtf brrbys from bfforf tif rfsft
     * Notf tibt sindf tif doordinbtf brrbys from bfforf tif rfsft
     * brf rfusfd, drfbting b nfw fmpty <dodf>Polygon</dodf> migit
     * brf rfusfd, drfbting b nfw fmpty <dodf>Polygon</dodf> migit
     * brf rfusfd, drfbting b nfw fmpty <dodf>Polygon</dodf> migit
     * brf rfusfd, drfbting b nfw fmpty <dodf>Polygon</dodf> migit
     * brf rfusfd, drfbting b nfw fmpty <dodf>Polygon</dodf> migit
     * bf morf mfmory fffidifnt tibn rfsftting tif durrfnt onf if
     * bf morf mfmory fffidifnt tibn rfsftting tif durrfnt onf if
     * bf morf mfmory fffidifnt tibn rfsftting tif durrfnt onf if
     * bf morf mfmory fffidifnt tibn rfsftting tif durrfnt onf if
     * bf morf mfmory fffidifnt tibn rfsftting tif durrfnt onf if
     * tif numbfr of vfrtidfs in tif nfw polygon dbtb is signifidbntly
     * tif numbfr of vfrtidfs in tif nfw polygon dbtb is signifidbntly
     * tif numbfr of vfrtidfs in tif nfw polygon dbtb is signifidbntly
     * tif numbfr of vfrtidfs in tif nfw polygon dbtb is signifidbntly
     * tif numbfr of vfrtidfs in tif nfw polygon dbtb is signifidbntly
     * smbllfr tibn tif numbfr of vfrtidfs in tif dbtb from bfforf tif
     * smbllfr tibn tif numbfr of vfrtidfs in tif dbtb from bfforf tif
     * smbllfr tibn tif numbfr of vfrtidfs in tif dbtb from bfforf tif
     * smbllfr tibn tif numbfr of vfrtidfs in tif dbtb from bfforf tif
     * smbllfr tibn tif numbfr of vfrtidfs in tif dbtb from bfforf tif
     * rfsft.
     * rfsft.
     * rfsft.
     * rfsft.
     * rfsft.
     * @sff         jbvb.bwt.Polygon#invblidbtf
     * @sff         jbvb.bwt.Polygon#invblidbtf
     * @sff         jbvb.bwt.Polygon#invblidbtf
     * @sff         jbvb.bwt.Polygon#invblidbtf
     * @sff         jbvb.bwt.Polygon#invblidbtf
     * @sindf 1.4
     * @sindf 1.4
     * @sindf 1.4
     * @sindf 1.4
     * @sindf 1.4
     */
     */
     */
     */
     */
    publid void rfsft() {
    publid void rfsft() {
    publid void rfsft() {
    publid void rfsft() {
    publid void rfsft() {
        npoints = 0;
        npoints = 0;
        npoints = 0;
        npoints = 0;
        npoints = 0;
        bounds = null;
        bounds = null;
        bounds = null;
        bounds = null;
        bounds = null;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Invblidbtfs or flusifs bny intfrnblly-dbdifd dbtb tibt dfpfnds
     * Invblidbtfs or flusifs bny intfrnblly-dbdifd dbtb tibt dfpfnds
     * Invblidbtfs or flusifs bny intfrnblly-dbdifd dbtb tibt dfpfnds
     * Invblidbtfs or flusifs bny intfrnblly-dbdifd dbtb tibt dfpfnds
     * Invblidbtfs or flusifs bny intfrnblly-dbdifd dbtb tibt dfpfnds
     * on tif vfrtfx doordinbtfs of tiis <dodf>Polygon</dodf>.
     * on tif vfrtfx doordinbtfs of tiis <dodf>Polygon</dodf>.
     * on tif vfrtfx doordinbtfs of tiis <dodf>Polygon</dodf>.
     * on tif vfrtfx doordinbtfs of tiis <dodf>Polygon</dodf>.
     * on tif vfrtfx doordinbtfs of tiis <dodf>Polygon</dodf>.
     * Tiis mftiod siould bf dbllfd bftfr bny dirfdt mbnipulbtion
     * Tiis mftiod siould bf dbllfd bftfr bny dirfdt mbnipulbtion
     * Tiis mftiod siould bf dbllfd bftfr bny dirfdt mbnipulbtion
     * Tiis mftiod siould bf dbllfd bftfr bny dirfdt mbnipulbtion
     * Tiis mftiod siould bf dbllfd bftfr bny dirfdt mbnipulbtion
     * of tif doordinbtfs in tif <dodf>xpoints</dodf> or
     * of tif doordinbtfs in tif <dodf>xpoints</dodf> or
     * of tif doordinbtfs in tif <dodf>xpoints</dodf> or
     * of tif doordinbtfs in tif <dodf>xpoints</dodf> or
     * of tif doordinbtfs in tif <dodf>xpoints</dodf> or
     * <dodf>ypoints</dodf> brrbys to bvoid indonsistfnt rfsults
     * <dodf>ypoints</dodf> brrbys to bvoid indonsistfnt rfsults
     * <dodf>ypoints</dodf> brrbys to bvoid indonsistfnt rfsults
     * <dodf>ypoints</dodf> brrbys to bvoid indonsistfnt rfsults
     * <dodf>ypoints</dodf> brrbys to bvoid indonsistfnt rfsults
     * from mftiods sudi bs <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>
     * from mftiods sudi bs <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>
     * from mftiods sudi bs <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>
     * from mftiods sudi bs <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>
     * from mftiods sudi bs <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>
     * tibt migit dbdif dbtb from fbrlifr domputbtions rflbting to
     * tibt migit dbdif dbtb from fbrlifr domputbtions rflbting to
     * tibt migit dbdif dbtb from fbrlifr domputbtions rflbting to
     * tibt migit dbdif dbtb from fbrlifr domputbtions rflbting to
     * tibt migit dbdif dbtb from fbrlifr domputbtions rflbting to
     * tif vfrtfx doordinbtfs.
     * tif vfrtfx doordinbtfs.
     * tif vfrtfx doordinbtfs.
     * tif vfrtfx doordinbtfs.
     * tif vfrtfx doordinbtfs.
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sindf 1.4
     * @sindf 1.4
     * @sindf 1.4
     * @sindf 1.4
     * @sindf 1.4
     */
     */
     */
     */
     */
    publid void invblidbtf() {
    publid void invblidbtf() {
    publid void invblidbtf() {
    publid void invblidbtf() {
    publid void invblidbtf() {
        bounds = null;
        bounds = null;
        bounds = null;
        bounds = null;
        bounds = null;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Trbnslbtfs tif vfrtidfs of tif <dodf>Polygon</dodf> by
     * Trbnslbtfs tif vfrtidfs of tif <dodf>Polygon</dodf> by
     * Trbnslbtfs tif vfrtidfs of tif <dodf>Polygon</dodf> by
     * Trbnslbtfs tif vfrtidfs of tif <dodf>Polygon</dodf> by
     * Trbnslbtfs tif vfrtidfs of tif <dodf>Polygon</dodf> by
     * <dodf>dfltbX</dodf> blong tif x bxis bnd by
     * <dodf>dfltbX</dodf> blong tif x bxis bnd by
     * <dodf>dfltbX</dodf> blong tif x bxis bnd by
     * <dodf>dfltbX</dodf> blong tif x bxis bnd by
     * <dodf>dfltbX</dodf> blong tif x bxis bnd by
     * <dodf>dfltbY</dodf> blong tif y bxis.
     * <dodf>dfltbY</dodf> blong tif y bxis.
     * <dodf>dfltbY</dodf> blong tif y bxis.
     * <dodf>dfltbY</dodf> blong tif y bxis.
     * <dodf>dfltbY</dodf> blong tif y bxis.
     * @pbrbm dfltbX tif bmount to trbnslbtf blong tif X bxis
     * @pbrbm dfltbX tif bmount to trbnslbtf blong tif X bxis
     * @pbrbm dfltbX tif bmount to trbnslbtf blong tif X bxis
     * @pbrbm dfltbX tif bmount to trbnslbtf blong tif X bxis
     * @pbrbm dfltbX tif bmount to trbnslbtf blong tif X bxis
     * @pbrbm dfltbY tif bmount to trbnslbtf blong tif Y bxis
     * @pbrbm dfltbY tif bmount to trbnslbtf blong tif Y bxis
     * @pbrbm dfltbY tif bmount to trbnslbtf blong tif Y bxis
     * @pbrbm dfltbY tif bmount to trbnslbtf blong tif Y bxis
     * @pbrbm dfltbY tif bmount to trbnslbtf blong tif Y bxis
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     */
     */
     */
     */
     */
    publid void trbnslbtf(int dfltbX, int dfltbY) {
    publid void trbnslbtf(int dfltbX, int dfltbY) {
    publid void trbnslbtf(int dfltbX, int dfltbY) {
    publid void trbnslbtf(int dfltbX, int dfltbY) {
    publid void trbnslbtf(int dfltbX, int dfltbY) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
            xpoints[i] += dfltbX;
            xpoints[i] += dfltbX;
            xpoints[i] += dfltbX;
            xpoints[i] += dfltbX;
            xpoints[i] += dfltbX;
            ypoints[i] += dfltbY;
            ypoints[i] += dfltbY;
            ypoints[i] += dfltbY;
            ypoints[i] += dfltbY;
            ypoints[i] += dfltbY;
        }
        }
        }
        }
        }
        if (bounds != null) {
        if (bounds != null) {
        if (bounds != null) {
        if (bounds != null) {
        if (bounds != null) {
            bounds.trbnslbtf(dfltbX, dfltbY);
            bounds.trbnslbtf(dfltbX, dfltbY);
            bounds.trbnslbtf(dfltbX, dfltbY);
            bounds.trbnslbtf(dfltbX, dfltbY);
            bounds.trbnslbtf(dfltbX, dfltbY);
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /*
    /*
    /*
    /*
    /*
     * Cbldulbtfs tif bounding box of tif points pbssfd to tif donstrudtor.
     * Cbldulbtfs tif bounding box of tif points pbssfd to tif donstrudtor.
     * Cbldulbtfs tif bounding box of tif points pbssfd to tif donstrudtor.
     * Cbldulbtfs tif bounding box of tif points pbssfd to tif donstrudtor.
     * Cbldulbtfs tif bounding box of tif points pbssfd to tif donstrudtor.
     * Sfts <dodf>bounds</dodf> to tif rfsult.
     * Sfts <dodf>bounds</dodf> to tif rfsult.
     * Sfts <dodf>bounds</dodf> to tif rfsult.
     * Sfts <dodf>bounds</dodf> to tif rfsult.
     * Sfts <dodf>bounds</dodf> to tif rfsult.
     * @pbrbm xpoints[] brrby of <i>x</i> doordinbtfs
     * @pbrbm xpoints[] brrby of <i>x</i> doordinbtfs
     * @pbrbm xpoints[] brrby of <i>x</i> doordinbtfs
     * @pbrbm xpoints[] brrby of <i>x</i> doordinbtfs
     * @pbrbm xpoints[] brrby of <i>x</i> doordinbtfs
     * @pbrbm ypoints[] brrby of <i>y</i> doordinbtfs
     * @pbrbm ypoints[] brrby of <i>y</i> doordinbtfs
     * @pbrbm ypoints[] brrby of <i>y</i> doordinbtfs
     * @pbrbm ypoints[] brrby of <i>y</i> doordinbtfs
     * @pbrbm ypoints[] brrby of <i>y</i> doordinbtfs
     * @pbrbm npoints tif totbl numbfr of points
     * @pbrbm npoints tif totbl numbfr of points
     * @pbrbm npoints tif totbl numbfr of points
     * @pbrbm npoints tif totbl numbfr of points
     * @pbrbm npoints tif totbl numbfr of points
     */
     */
     */
     */
     */
    void dbldulbtfBounds(int xpoints[], int ypoints[], int npoints) {
    void dbldulbtfBounds(int xpoints[], int ypoints[], int npoints) {
    void dbldulbtfBounds(int xpoints[], int ypoints[], int npoints) {
    void dbldulbtfBounds(int xpoints[], int ypoints[], int npoints) {
    void dbldulbtfBounds(int xpoints[], int ypoints[], int npoints) {
        int boundsMinX = Intfgfr.MAX_VALUE;
        int boundsMinX = Intfgfr.MAX_VALUE;
        int boundsMinX = Intfgfr.MAX_VALUE;
        int boundsMinX = Intfgfr.MAX_VALUE;
        int boundsMinX = Intfgfr.MAX_VALUE;
        int boundsMinY = Intfgfr.MAX_VALUE;
        int boundsMinY = Intfgfr.MAX_VALUE;
        int boundsMinY = Intfgfr.MAX_VALUE;
        int boundsMinY = Intfgfr.MAX_VALUE;
        int boundsMinY = Intfgfr.MAX_VALUE;
        int boundsMbxX = Intfgfr.MIN_VALUE;
        int boundsMbxX = Intfgfr.MIN_VALUE;
        int boundsMbxX = Intfgfr.MIN_VALUE;
        int boundsMbxX = Intfgfr.MIN_VALUE;
        int boundsMbxX = Intfgfr.MIN_VALUE;
        int boundsMbxY = Intfgfr.MIN_VALUE;
        int boundsMbxY = Intfgfr.MIN_VALUE;
        int boundsMbxY = Intfgfr.MIN_VALUE;
        int boundsMbxY = Intfgfr.MIN_VALUE;
        int boundsMbxY = Intfgfr.MIN_VALUE;





        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
            int x = xpoints[i];
            int x = xpoints[i];
            int x = xpoints[i];
            int x = xpoints[i];
            int x = xpoints[i];
            boundsMinX = Mbti.min(boundsMinX, x);
            boundsMinX = Mbti.min(boundsMinX, x);
            boundsMinX = Mbti.min(boundsMinX, x);
            boundsMinX = Mbti.min(boundsMinX, x);
            boundsMinX = Mbti.min(boundsMinX, x);
            boundsMbxX = Mbti.mbx(boundsMbxX, x);
            boundsMbxX = Mbti.mbx(boundsMbxX, x);
            boundsMbxX = Mbti.mbx(boundsMbxX, x);
            boundsMbxX = Mbti.mbx(boundsMbxX, x);
            boundsMbxX = Mbti.mbx(boundsMbxX, x);
            int y = ypoints[i];
            int y = ypoints[i];
            int y = ypoints[i];
            int y = ypoints[i];
            int y = ypoints[i];
            boundsMinY = Mbti.min(boundsMinY, y);
            boundsMinY = Mbti.min(boundsMinY, y);
            boundsMinY = Mbti.min(boundsMinY, y);
            boundsMinY = Mbti.min(boundsMinY, y);
            boundsMinY = Mbti.min(boundsMinY, y);
            boundsMbxY = Mbti.mbx(boundsMbxY, y);
            boundsMbxY = Mbti.mbx(boundsMbxY, y);
            boundsMbxY = Mbti.mbx(boundsMbxY, y);
            boundsMbxY = Mbti.mbx(boundsMbxY, y);
            boundsMbxY = Mbti.mbx(boundsMbxY, y);
        }
        }
        }
        }
        }
        bounds = nfw Rfdtbnglf(boundsMinX, boundsMinY,
        bounds = nfw Rfdtbnglf(boundsMinX, boundsMinY,
        bounds = nfw Rfdtbnglf(boundsMinX, boundsMinY,
        bounds = nfw Rfdtbnglf(boundsMinX, boundsMinY,
        bounds = nfw Rfdtbnglf(boundsMinX, boundsMinY,
                               boundsMbxX - boundsMinX,
                               boundsMbxX - boundsMinX,
                               boundsMbxX - boundsMinX,
                               boundsMbxX - boundsMinX,
                               boundsMbxX - boundsMinX,
                               boundsMbxY - boundsMinY);
                               boundsMbxY - boundsMinY);
                               boundsMbxY - boundsMinY);
                               boundsMbxY - boundsMinY);
                               boundsMbxY - boundsMinY);
    }
    }
    }
    }
    }





    /*
    /*
    /*
    /*
    /*
     * Rfsizfs tif bounding box to bddommodbtf tif spfdififd doordinbtfs.
     * Rfsizfs tif bounding box to bddommodbtf tif spfdififd doordinbtfs.
     * Rfsizfs tif bounding box to bddommodbtf tif spfdififd doordinbtfs.
     * Rfsizfs tif bounding box to bddommodbtf tif spfdififd doordinbtfs.
     * Rfsizfs tif bounding box to bddommodbtf tif spfdififd doordinbtfs.
     * @pbrbm x,&nbsp;y tif spfdififd doordinbtfs
     * @pbrbm x,&nbsp;y tif spfdififd doordinbtfs
     * @pbrbm x,&nbsp;y tif spfdififd doordinbtfs
     * @pbrbm x,&nbsp;y tif spfdififd doordinbtfs
     * @pbrbm x,&nbsp;y tif spfdififd doordinbtfs
     */
     */
     */
     */
     */
    void updbtfBounds(int x, int y) {
    void updbtfBounds(int x, int y) {
    void updbtfBounds(int x, int y) {
    void updbtfBounds(int x, int y) {
    void updbtfBounds(int x, int y) {
        if (x < bounds.x) {
        if (x < bounds.x) {
        if (x < bounds.x) {
        if (x < bounds.x) {
        if (x < bounds.x) {
            bounds.widti = bounds.widti + (bounds.x - x);
            bounds.widti = bounds.widti + (bounds.x - x);
            bounds.widti = bounds.widti + (bounds.x - x);
            bounds.widti = bounds.widti + (bounds.x - x);
            bounds.widti = bounds.widti + (bounds.x - x);
            bounds.x = x;
            bounds.x = x;
            bounds.x = x;
            bounds.x = x;
            bounds.x = x;
        }
        }
        }
        }
        }
        flsf {
        flsf {
        flsf {
        flsf {
        flsf {
            bounds.widti = Mbti.mbx(bounds.widti, x - bounds.x);
            bounds.widti = Mbti.mbx(bounds.widti, x - bounds.x);
            bounds.widti = Mbti.mbx(bounds.widti, x - bounds.x);
            bounds.widti = Mbti.mbx(bounds.widti, x - bounds.x);
            bounds.widti = Mbti.mbx(bounds.widti, x - bounds.x);
            // bounds.x = bounds.x;
            // bounds.x = bounds.x;
            // bounds.x = bounds.x;
            // bounds.x = bounds.x;
            // bounds.x = bounds.x;
        }
        }
        }
        }
        }





        if (y < bounds.y) {
        if (y < bounds.y) {
        if (y < bounds.y) {
        if (y < bounds.y) {
        if (y < bounds.y) {
            bounds.ifigit = bounds.ifigit + (bounds.y - y);
            bounds.ifigit = bounds.ifigit + (bounds.y - y);
            bounds.ifigit = bounds.ifigit + (bounds.y - y);
            bounds.ifigit = bounds.ifigit + (bounds.y - y);
            bounds.ifigit = bounds.ifigit + (bounds.y - y);
            bounds.y = y;
            bounds.y = y;
            bounds.y = y;
            bounds.y = y;
            bounds.y = y;
        }
        }
        }
        }
        }
        flsf {
        flsf {
        flsf {
        flsf {
        flsf {
            bounds.ifigit = Mbti.mbx(bounds.ifigit, y - bounds.y);
            bounds.ifigit = Mbti.mbx(bounds.ifigit, y - bounds.y);
            bounds.ifigit = Mbti.mbx(bounds.ifigit, y - bounds.y);
            bounds.ifigit = Mbti.mbx(bounds.ifigit, y - bounds.y);
            bounds.ifigit = Mbti.mbx(bounds.ifigit, y - bounds.y);
            // bounds.y = bounds.y;
            // bounds.y = bounds.y;
            // bounds.y = bounds.y;
            // bounds.y = bounds.y;
            // bounds.y = bounds.y;
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Appfnds tif spfdififd doordinbtfs to tiis <dodf>Polygon</dodf>.
     * Appfnds tif spfdififd doordinbtfs to tiis <dodf>Polygon</dodf>.
     * Appfnds tif spfdififd doordinbtfs to tiis <dodf>Polygon</dodf>.
     * Appfnds tif spfdififd doordinbtfs to tiis <dodf>Polygon</dodf>.
     * Appfnds tif spfdififd doordinbtfs to tiis <dodf>Polygon</dodf>.
     * <p>
     * <p>
     * <p>
     * <p>
     * <p>
     * If bn opfrbtion tibt dbldulbtfs tif bounding box of tiis
     * If bn opfrbtion tibt dbldulbtfs tif bounding box of tiis
     * If bn opfrbtion tibt dbldulbtfs tif bounding box of tiis
     * If bn opfrbtion tibt dbldulbtfs tif bounding box of tiis
     * If bn opfrbtion tibt dbldulbtfs tif bounding box of tiis
     * <dodf>Polygon</dodf> ibs blrfbdy bffn pfrformfd, sudi bs
     * <dodf>Polygon</dodf> ibs blrfbdy bffn pfrformfd, sudi bs
     * <dodf>Polygon</dodf> ibs blrfbdy bffn pfrformfd, sudi bs
     * <dodf>Polygon</dodf> ibs blrfbdy bffn pfrformfd, sudi bs
     * <dodf>Polygon</dodf> ibs blrfbdy bffn pfrformfd, sudi bs
     * <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>, tifn tiis
     * <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>, tifn tiis
     * <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>, tifn tiis
     * <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>, tifn tiis
     * <dodf>gftBounds</dodf> or <dodf>dontbins</dodf>, tifn tiis
     * mftiod updbtfs tif bounding box.
     * mftiod updbtfs tif bounding box.
     * mftiod updbtfs tif bounding box.
     * mftiod updbtfs tif bounding box.
     * mftiod updbtfs tif bounding box.
     * @pbrbm       x tif spfdififd X doordinbtf
     * @pbrbm       x tif spfdififd X doordinbtf
     * @pbrbm       x tif spfdififd X doordinbtf
     * @pbrbm       x tif spfdififd X doordinbtf
     * @pbrbm       x tif spfdififd X doordinbtf
     * @pbrbm       y tif spfdififd Y doordinbtf
     * @pbrbm       y tif spfdififd Y doordinbtf
     * @pbrbm       y tif spfdififd Y doordinbtf
     * @pbrbm       y tif spfdififd Y doordinbtf
     * @pbrbm       y tif spfdififd Y doordinbtf
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#gftBounds
     * @sff         jbvb.bwt.Polygon#dontbins
     * @sff         jbvb.bwt.Polygon#dontbins
     * @sff         jbvb.bwt.Polygon#dontbins
     * @sff         jbvb.bwt.Polygon#dontbins
     * @sff         jbvb.bwt.Polygon#dontbins
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    publid void bddPoint(int x, int y) {
    publid void bddPoint(int x, int y) {
    publid void bddPoint(int x, int y) {
    publid void bddPoint(int x, int y) {
    publid void bddPoint(int x, int y) {
        if (npoints >= xpoints.lfngti || npoints >= ypoints.lfngti) {
        if (npoints >= xpoints.lfngti || npoints >= ypoints.lfngti) {
        if (npoints >= xpoints.lfngti || npoints >= ypoints.lfngti) {
        if (npoints >= xpoints.lfngti || npoints >= ypoints.lfngti) {
        if (npoints >= xpoints.lfngti || npoints >= ypoints.lfngti) {
            int nfwLfngti = npoints * 2;
            int nfwLfngti = npoints * 2;
            int nfwLfngti = npoints * 2;
            int nfwLfngti = npoints * 2;
            int nfwLfngti = npoints * 2;
            // Mbkf surf tibt nfwLfngti will bf grfbtfr tibn MIN_LENGTH bnd
            // Mbkf surf tibt nfwLfngti will bf grfbtfr tibn MIN_LENGTH bnd
            // Mbkf surf tibt nfwLfngti will bf grfbtfr tibn MIN_LENGTH bnd
            // Mbkf surf tibt nfwLfngti will bf grfbtfr tibn MIN_LENGTH bnd
            // Mbkf surf tibt nfwLfngti will bf grfbtfr tibn MIN_LENGTH bnd
            // blignfd to tif powfr of 2
            // blignfd to tif powfr of 2
            // blignfd to tif powfr of 2
            // blignfd to tif powfr of 2
            // blignfd to tif powfr of 2
            if (nfwLfngti < MIN_LENGTH) {
            if (nfwLfngti < MIN_LENGTH) {
            if (nfwLfngti < MIN_LENGTH) {
            if (nfwLfngti < MIN_LENGTH) {
            if (nfwLfngti < MIN_LENGTH) {
                nfwLfngti = MIN_LENGTH;
                nfwLfngti = MIN_LENGTH;
                nfwLfngti = MIN_LENGTH;
                nfwLfngti = MIN_LENGTH;
                nfwLfngti = MIN_LENGTH;
            } flsf if ((nfwLfngti & (nfwLfngti - 1)) != 0) {
            } flsf if ((nfwLfngti & (nfwLfngti - 1)) != 0) {
            } flsf if ((nfwLfngti & (nfwLfngti - 1)) != 0) {
            } flsf if ((nfwLfngti & (nfwLfngti - 1)) != 0) {
            } flsf if ((nfwLfngti & (nfwLfngti - 1)) != 0) {
                nfwLfngti = Intfgfr.iigifstOnfBit(nfwLfngti);
                nfwLfngti = Intfgfr.iigifstOnfBit(nfwLfngti);
                nfwLfngti = Intfgfr.iigifstOnfBit(nfwLfngti);
                nfwLfngti = Intfgfr.iigifstOnfBit(nfwLfngti);
                nfwLfngti = Intfgfr.iigifstOnfBit(nfwLfngti);
            }
            }
            }
            }
            }





            xpoints = Arrbys.dopyOf(xpoints, nfwLfngti);
            xpoints = Arrbys.dopyOf(xpoints, nfwLfngti);
            xpoints = Arrbys.dopyOf(xpoints, nfwLfngti);
            xpoints = Arrbys.dopyOf(xpoints, nfwLfngti);
            xpoints = Arrbys.dopyOf(xpoints, nfwLfngti);
            ypoints = Arrbys.dopyOf(ypoints, nfwLfngti);
            ypoints = Arrbys.dopyOf(ypoints, nfwLfngti);
            ypoints = Arrbys.dopyOf(ypoints, nfwLfngti);
            ypoints = Arrbys.dopyOf(ypoints, nfwLfngti);
            ypoints = Arrbys.dopyOf(ypoints, nfwLfngti);
        }
        }
        }
        }
        }
        xpoints[npoints] = x;
        xpoints[npoints] = x;
        xpoints[npoints] = x;
        xpoints[npoints] = x;
        xpoints[npoints] = x;
        ypoints[npoints] = y;
        ypoints[npoints] = y;
        ypoints[npoints] = y;
        ypoints[npoints] = y;
        ypoints[npoints] = y;
        npoints++;
        npoints++;
        npoints++;
        npoints++;
        npoints++;
        if (bounds != null) {
        if (bounds != null) {
        if (bounds != null) {
        if (bounds != null) {
        if (bounds != null) {
            updbtfBounds(x, y);
            updbtfBounds(x, y);
            updbtfBounds(x, y);
            updbtfBounds(x, y);
            updbtfBounds(x, y);
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Gfts tif bounding box of tiis <dodf>Polygon</dodf>.
     * Gfts tif bounding box of tiis <dodf>Polygon</dodf>.
     * Gfts tif bounding box of tiis <dodf>Polygon</dodf>.
     * Gfts tif bounding box of tiis <dodf>Polygon</dodf>.
     * Gfts tif bounding box of tiis <dodf>Polygon</dodf>.
     * Tif bounding box is tif smbllfst {@link Rfdtbnglf} wiosf
     * Tif bounding box is tif smbllfst {@link Rfdtbnglf} wiosf
     * Tif bounding box is tif smbllfst {@link Rfdtbnglf} wiosf
     * Tif bounding box is tif smbllfst {@link Rfdtbnglf} wiosf
     * Tif bounding box is tif smbllfst {@link Rfdtbnglf} wiosf
     * sidfs brf pbrbllfl to tif x bnd y bxfs of tif
     * sidfs brf pbrbllfl to tif x bnd y bxfs of tif
     * sidfs brf pbrbllfl to tif x bnd y bxfs of tif
     * sidfs brf pbrbllfl to tif x bnd y bxfs of tif
     * sidfs brf pbrbllfl to tif x bnd y bxfs of tif
     * doordinbtf spbdf, bnd dbn domplftfly dontbin tif <dodf>Polygon</dodf>.
     * doordinbtf spbdf, bnd dbn domplftfly dontbin tif <dodf>Polygon</dodf>.
     * doordinbtf spbdf, bnd dbn domplftfly dontbin tif <dodf>Polygon</dodf>.
     * doordinbtf spbdf, bnd dbn domplftfly dontbin tif <dodf>Polygon</dodf>.
     * doordinbtf spbdf, bnd dbn domplftfly dontbin tif <dodf>Polygon</dodf>.
     * @rfturn b <dodf>Rfdtbnglf</dodf> tibt dffinfs tif bounds of tiis
     * @rfturn b <dodf>Rfdtbnglf</dodf> tibt dffinfs tif bounds of tiis
     * @rfturn b <dodf>Rfdtbnglf</dodf> tibt dffinfs tif bounds of tiis
     * @rfturn b <dodf>Rfdtbnglf</dodf> tibt dffinfs tif bounds of tiis
     * @rfturn b <dodf>Rfdtbnglf</dodf> tibt dffinfs tif bounds of tiis
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     */
     */
     */
     */
     */
    publid Rfdtbnglf gftBounds() {
    publid Rfdtbnglf gftBounds() {
    publid Rfdtbnglf gftBounds() {
    publid Rfdtbnglf gftBounds() {
    publid Rfdtbnglf gftBounds() {
        rfturn gftBoundingBox();
        rfturn gftBoundingBox();
        rfturn gftBoundingBox();
        rfturn gftBoundingBox();
        rfturn gftBoundingBox();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfturns tif bounds of tiis <dodf>Polygon</dodf>.
     * Rfturns tif bounds of tiis <dodf>Polygon</dodf>.
     * Rfturns tif bounds of tiis <dodf>Polygon</dodf>.
     * Rfturns tif bounds of tiis <dodf>Polygon</dodf>.
     * Rfturns tif bounds of tiis <dodf>Polygon</dodf>.
     * @rfturn tif bounds of tiis <dodf>Polygon</dodf>.
     * @rfturn tif bounds of tiis <dodf>Polygon</dodf>.
     * @rfturn tif bounds of tiis <dodf>Polygon</dodf>.
     * @rfturn tif bounds of tiis <dodf>Polygon</dodf>.
     * @rfturn tif bounds of tiis <dodf>Polygon</dodf>.
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * rfplbdfd by <dodf>gftBounds()</dodf>.
     * rfplbdfd by <dodf>gftBounds()</dodf>.
     * rfplbdfd by <dodf>gftBounds()</dodf>.
     * rfplbdfd by <dodf>gftBounds()</dodf>.
     * rfplbdfd by <dodf>gftBounds()</dodf>.
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    @Dfprfdbtfd
    @Dfprfdbtfd
    @Dfprfdbtfd
    @Dfprfdbtfd
    @Dfprfdbtfd
    publid Rfdtbnglf gftBoundingBox() {
    publid Rfdtbnglf gftBoundingBox() {
    publid Rfdtbnglf gftBoundingBox() {
    publid Rfdtbnglf gftBoundingBox() {
    publid Rfdtbnglf gftBoundingBox() {
        if (npoints == 0) {
        if (npoints == 0) {
        if (npoints == 0) {
        if (npoints == 0) {
        if (npoints == 0) {
            rfturn nfw Rfdtbnglf();
            rfturn nfw Rfdtbnglf();
            rfturn nfw Rfdtbnglf();
            rfturn nfw Rfdtbnglf();
            rfturn nfw Rfdtbnglf();
        }
        }
        }
        }
        }
        if (bounds == null) {
        if (bounds == null) {
        if (bounds == null) {
        if (bounds == null) {
        if (bounds == null) {
            dbldulbtfBounds(xpoints, ypoints, npoints);
            dbldulbtfBounds(xpoints, ypoints, npoints);
            dbldulbtfBounds(xpoints, ypoints, npoints);
            dbldulbtfBounds(xpoints, ypoints, npoints);
            dbldulbtfBounds(xpoints, ypoints, npoints);
        }
        }
        }
        }
        }
        rfturn bounds.gftBounds();
        rfturn bounds.gftBounds();
        rfturn bounds.gftBounds();
        rfturn bounds.gftBounds();
        rfturn bounds.gftBounds();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Dftfrminfs wiftifr tif spfdififd {@link Point} is insidf tiis
     * Dftfrminfs wiftifr tif spfdififd {@link Point} is insidf tiis
     * Dftfrminfs wiftifr tif spfdififd {@link Point} is insidf tiis
     * Dftfrminfs wiftifr tif spfdififd {@link Point} is insidf tiis
     * Dftfrminfs wiftifr tif spfdififd {@link Point} is insidf tiis
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * @pbrbm p tif spfdififd <dodf>Point</dodf> to bf tfstfd
     * @pbrbm p tif spfdififd <dodf>Point</dodf> to bf tfstfd
     * @pbrbm p tif spfdififd <dodf>Point</dodf> to bf tfstfd
     * @pbrbm p tif spfdififd <dodf>Point</dodf> to bf tfstfd
     * @pbrbm p tif spfdififd <dodf>Point</dodf> to bf tfstfd
     * @rfturn <dodf>truf</dodf> if tif <dodf>Polygon</dodf> dontbins tif
     * @rfturn <dodf>truf</dodf> if tif <dodf>Polygon</dodf> dontbins tif
     * @rfturn <dodf>truf</dodf> if tif <dodf>Polygon</dodf> dontbins tif
     * @rfturn <dodf>truf</dodf> if tif <dodf>Polygon</dodf> dontbins tif
     * @rfturn <dodf>truf</dodf> if tif <dodf>Polygon</dodf> dontbins tif
     *                  <dodf>Point</dodf>; <dodf>fblsf</dodf> otifrwisf.
     *                  <dodf>Point</dodf>; <dodf>fblsf</dodf> otifrwisf.
     *                  <dodf>Point</dodf>; <dodf>fblsf</dodf> otifrwisf.
     *                  <dodf>Point</dodf>; <dodf>fblsf</dodf> otifrwisf.
     *                  <dodf>Point</dodf>; <dodf>fblsf</dodf> otifrwisf.
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    publid boolfbn dontbins(Point p) {
    publid boolfbn dontbins(Point p) {
    publid boolfbn dontbins(Point p) {
    publid boolfbn dontbins(Point p) {
    publid boolfbn dontbins(Point p) {
        rfturn dontbins(p.x, p.y);
        rfturn dontbins(p.x, p.y);
        rfturn dontbins(p.x, p.y);
        rfturn dontbins(p.x, p.y);
        rfturn dontbins(p.x, p.y);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf insidf tiis
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf insidf tiis
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf insidf tiis
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf insidf tiis
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf insidf tiis
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     *
     *
     *
     *
     *
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         {@dodf fblsf} otifrwisf.
     *         {@dodf fblsf} otifrwisf.
     *         {@dodf fblsf} otifrwisf.
     *         {@dodf fblsf} otifrwisf.
     *         {@dodf fblsf} otifrwisf.
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     * @sindf 1.1
     */
     */
     */
     */
     */
    publid boolfbn dontbins(int x, int y) {
    publid boolfbn dontbins(int x, int y) {
    publid boolfbn dontbins(int x, int y) {
    publid boolfbn dontbins(int x, int y) {
    publid boolfbn dontbins(int x, int y) {
        rfturn dontbins((doublf) x, (doublf) y);
        rfturn dontbins((doublf) x, (doublf) y);
        rfturn dontbins((doublf) x, (doublf) y);
        rfturn dontbins((doublf) x, (doublf) y);
        rfturn dontbins((doublf) x, (doublf) y);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf dontbinfd in tiis
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf dontbinfd in tiis
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf dontbinfd in tiis
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf dontbinfd in tiis
     * Dftfrminfs wiftifr tif spfdififd doordinbtfs brf dontbinfd in tiis
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * <dodf>Polygon</dodf>.
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm x tif spfdififd X doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @pbrbm y tif spfdififd Y doordinbtf to bf tfstfd
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     * @rfturn {@dodf truf} if tiis {@dodf Polygon} dontbins
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         tif spfdififd doordinbtfs {@dodf (x,y)};
     *         {@dodf fblsf} otifrwisf.
     *         {@dodf fblsf} otifrwisf.
     *         {@dodf fblsf} otifrwisf.
     *         {@dodf fblsf} otifrwisf.
     *         {@dodf fblsf} otifrwisf.
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @sff #dontbins(doublf, doublf)
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * @dfprfdbtfd As of JDK vfrsion 1.1,
     * rfplbdfd by <dodf>dontbins(int, int)</dodf>.
     * rfplbdfd by <dodf>dontbins(int, int)</dodf>.
     * rfplbdfd by <dodf>dontbins(int, int)</dodf>.
     * rfplbdfd by <dodf>dontbins(int, int)</dodf>.
     * rfplbdfd by <dodf>dontbins(int, int)</dodf>.
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     * @sindf 1.0
     */
     */
     */
     */
     */
    @Dfprfdbtfd
    @Dfprfdbtfd
    @Dfprfdbtfd
    @Dfprfdbtfd
    @Dfprfdbtfd
    publid boolfbn insidf(int x, int y) {
    publid boolfbn insidf(int x, int y) {
    publid boolfbn insidf(int x, int y) {
    publid boolfbn insidf(int x, int y) {
    publid boolfbn insidf(int x, int y) {
        rfturn dontbins((doublf) x, (doublf) y);
        rfturn dontbins((doublf) x, (doublf) y);
        rfturn dontbins((doublf) x, (doublf) y);
        rfturn dontbins((doublf) x, (doublf) y);
        rfturn dontbins((doublf) x, (doublf) y);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid Rfdtbnglf2D gftBounds2D() {
    publid Rfdtbnglf2D gftBounds2D() {
    publid Rfdtbnglf2D gftBounds2D() {
    publid Rfdtbnglf2D gftBounds2D() {
    publid Rfdtbnglf2D gftBounds2D() {
        rfturn gftBounds();
        rfturn gftBounds();
        rfturn gftBounds();
        rfturn gftBounds();
        rfturn gftBounds();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid boolfbn dontbins(doublf x, doublf y) {
    publid boolfbn dontbins(doublf x, doublf y) {
    publid boolfbn dontbins(doublf x, doublf y) {
    publid boolfbn dontbins(doublf x, doublf y) {
    publid boolfbn dontbins(doublf x, doublf y) {
        if (npoints <= 2 || !gftBoundingBox().dontbins(x, y)) {
        if (npoints <= 2 || !gftBoundingBox().dontbins(x, y)) {
        if (npoints <= 2 || !gftBoundingBox().dontbins(x, y)) {
        if (npoints <= 2 || !gftBoundingBox().dontbins(x, y)) {
        if (npoints <= 2 || !gftBoundingBox().dontbins(x, y)) {
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
        }
        }
        }
        }
        }
        int iits = 0;
        int iits = 0;
        int iits = 0;
        int iits = 0;
        int iits = 0;





        int lbstx = xpoints[npoints - 1];
        int lbstx = xpoints[npoints - 1];
        int lbstx = xpoints[npoints - 1];
        int lbstx = xpoints[npoints - 1];
        int lbstx = xpoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int durx, dury;
        int durx, dury;
        int durx, dury;
        int durx, dury;
        int durx, dury;





        // Wblk tif fdgfs of tif polygon
        // Wblk tif fdgfs of tif polygon
        // Wblk tif fdgfs of tif polygon
        // Wblk tif fdgfs of tif polygon
        // Wblk tif fdgfs of tif polygon
        for (int i = 0; i < npoints; lbstx = durx, lbsty = dury, i++) {
        for (int i = 0; i < npoints; lbstx = durx, lbsty = dury, i++) {
        for (int i = 0; i < npoints; lbstx = durx, lbsty = dury, i++) {
        for (int i = 0; i < npoints; lbstx = durx, lbsty = dury, i++) {
        for (int i = 0; i < npoints; lbstx = durx, lbsty = dury, i++) {
            durx = xpoints[i];
            durx = xpoints[i];
            durx = xpoints[i];
            durx = xpoints[i];
            durx = xpoints[i];
            dury = ypoints[i];
            dury = ypoints[i];
            dury = ypoints[i];
            dury = ypoints[i];
            dury = ypoints[i];





            if (dury == lbsty) {
            if (dury == lbsty) {
            if (dury == lbsty) {
            if (dury == lbsty) {
            if (dury == lbsty) {
                dontinuf;
                dontinuf;
                dontinuf;
                dontinuf;
                dontinuf;
            }
            }
            }
            }
            }





            int lfftx;
            int lfftx;
            int lfftx;
            int lfftx;
            int lfftx;
            if (durx < lbstx) {
            if (durx < lbstx) {
            if (durx < lbstx) {
            if (durx < lbstx) {
            if (durx < lbstx) {
                if (x >= lbstx) {
                if (x >= lbstx) {
                if (x >= lbstx) {
                if (x >= lbstx) {
                if (x >= lbstx) {
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                }
                }
                }
                }
                }
                lfftx = durx;
                lfftx = durx;
                lfftx = durx;
                lfftx = durx;
                lfftx = durx;
            } flsf {
            } flsf {
            } flsf {
            } flsf {
            } flsf {
                if (x >= durx) {
                if (x >= durx) {
                if (x >= durx) {
                if (x >= durx) {
                if (x >= durx) {
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                }
                }
                }
                }
                }
                lfftx = lbstx;
                lfftx = lbstx;
                lfftx = lbstx;
                lfftx = lbstx;
                lfftx = lbstx;
            }
            }
            }
            }
            }





            doublf tfst1, tfst2;
            doublf tfst1, tfst2;
            doublf tfst1, tfst2;
            doublf tfst1, tfst2;
            doublf tfst1, tfst2;
            if (dury < lbsty) {
            if (dury < lbsty) {
            if (dury < lbsty) {
            if (dury < lbsty) {
            if (dury < lbsty) {
                if (y < dury || y >= lbsty) {
                if (y < dury || y >= lbsty) {
                if (y < dury || y >= lbsty) {
                if (y < dury || y >= lbsty) {
                if (y < dury || y >= lbsty) {
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                }
                }
                }
                }
                }
                if (x < lfftx) {
                if (x < lfftx) {
                if (x < lfftx) {
                if (x < lfftx) {
                if (x < lfftx) {
                    iits++;
                    iits++;
                    iits++;
                    iits++;
                    iits++;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                }
                }
                }
                }
                }
                tfst1 = x - durx;
                tfst1 = x - durx;
                tfst1 = x - durx;
                tfst1 = x - durx;
                tfst1 = x - durx;
                tfst2 = y - dury;
                tfst2 = y - dury;
                tfst2 = y - dury;
                tfst2 = y - dury;
                tfst2 = y - dury;
            } flsf {
            } flsf {
            } flsf {
            } flsf {
            } flsf {
                if (y < lbsty || y >= dury) {
                if (y < lbsty || y >= dury) {
                if (y < lbsty || y >= dury) {
                if (y < lbsty || y >= dury) {
                if (y < lbsty || y >= dury) {
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                }
                }
                }
                }
                }
                if (x < lfftx) {
                if (x < lfftx) {
                if (x < lfftx) {
                if (x < lfftx) {
                if (x < lfftx) {
                    iits++;
                    iits++;
                    iits++;
                    iits++;
                    iits++;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                    dontinuf;
                }
                }
                }
                }
                }
                tfst1 = x - lbstx;
                tfst1 = x - lbstx;
                tfst1 = x - lbstx;
                tfst1 = x - lbstx;
                tfst1 = x - lbstx;
                tfst2 = y - lbsty;
                tfst2 = y - lbsty;
                tfst2 = y - lbsty;
                tfst2 = y - lbsty;
                tfst2 = y - lbsty;
            }
            }
            }
            }
            }





            if (tfst1 < (tfst2 / (lbsty - dury) * (lbstx - durx))) {
            if (tfst1 < (tfst2 / (lbsty - dury) * (lbstx - durx))) {
            if (tfst1 < (tfst2 / (lbsty - dury) * (lbstx - durx))) {
            if (tfst1 < (tfst2 / (lbsty - dury) * (lbstx - durx))) {
            if (tfst1 < (tfst2 / (lbsty - dury) * (lbstx - durx))) {
                iits++;
                iits++;
                iits++;
                iits++;
                iits++;
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }





        rfturn ((iits & 1) != 0);
        rfturn ((iits & 1) != 0);
        rfturn ((iits & 1) != 0);
        rfturn ((iits & 1) != 0);
        rfturn ((iits & 1) != 0);
    }
    }
    }
    }
    }





    privbtf Crossings gftCrossings(doublf xlo, doublf ylo,
    privbtf Crossings gftCrossings(doublf xlo, doublf ylo,
    privbtf Crossings gftCrossings(doublf xlo, doublf ylo,
    privbtf Crossings gftCrossings(doublf xlo, doublf ylo,
    privbtf Crossings gftCrossings(doublf xlo, doublf ylo,
                                   doublf xii, doublf yii)
                                   doublf xii, doublf yii)
                                   doublf xii, doublf yii)
                                   doublf xii, doublf yii)
                                   doublf xii, doublf yii)
    {
    {
    {
    {
    {
        Crossings dross = nfw Crossings.EvfnOdd(xlo, ylo, xii, yii);
        Crossings dross = nfw Crossings.EvfnOdd(xlo, ylo, xii, yii);
        Crossings dross = nfw Crossings.EvfnOdd(xlo, ylo, xii, yii);
        Crossings dross = nfw Crossings.EvfnOdd(xlo, ylo, xii, yii);
        Crossings dross = nfw Crossings.EvfnOdd(xlo, ylo, xii, yii);
        int lbstx = xpoints[npoints - 1];
        int lbstx = xpoints[npoints - 1];
        int lbstx = xpoints[npoints - 1];
        int lbstx = xpoints[npoints - 1];
        int lbstx = xpoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int lbsty = ypoints[npoints - 1];
        int durx, dury;
        int durx, dury;
        int durx, dury;
        int durx, dury;
        int durx, dury;





        // Wblk tif fdgfs of tif polygon
        // Wblk tif fdgfs of tif polygon
        // Wblk tif fdgfs of tif polygon
        // Wblk tif fdgfs of tif polygon
        // Wblk tif fdgfs of tif polygon
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
        for (int i = 0; i < npoints; i++) {
            durx = xpoints[i];
            durx = xpoints[i];
            durx = xpoints[i];
            durx = xpoints[i];
            durx = xpoints[i];
            dury = ypoints[i];
            dury = ypoints[i];
            dury = ypoints[i];
            dury = ypoints[i];
            dury = ypoints[i];
            if (dross.bddumulbtfLinf(lbstx, lbsty, durx, dury)) {
            if (dross.bddumulbtfLinf(lbstx, lbsty, durx, dury)) {
            if (dross.bddumulbtfLinf(lbstx, lbsty, durx, dury)) {
            if (dross.bddumulbtfLinf(lbstx, lbsty, durx, dury)) {
            if (dross.bddumulbtfLinf(lbstx, lbsty, durx, dury)) {
                rfturn null;
                rfturn null;
                rfturn null;
                rfturn null;
                rfturn null;
            }
            }
            }
            }
            }
            lbstx = durx;
            lbstx = durx;
            lbstx = durx;
            lbstx = durx;
            lbstx = durx;
            lbsty = dury;
            lbsty = dury;
            lbsty = dury;
            lbsty = dury;
            lbsty = dury;
        }
        }
        }
        }
        }





        rfturn dross;
        rfturn dross;
        rfturn dross;
        rfturn dross;
        rfturn dross;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid boolfbn dontbins(Point2D p) {
    publid boolfbn dontbins(Point2D p) {
    publid boolfbn dontbins(Point2D p) {
    publid boolfbn dontbins(Point2D p) {
    publid boolfbn dontbins(Point2D p) {
        rfturn dontbins(p.gftX(), p.gftY());
        rfturn dontbins(p.gftX(), p.gftY());
        rfturn dontbins(p.gftX(), p.gftY());
        rfturn dontbins(p.gftX(), p.gftY());
        rfturn dontbins(p.gftX(), p.gftY());
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid boolfbn intfrsfdts(doublf x, doublf y, doublf w, doublf i) {
    publid boolfbn intfrsfdts(doublf x, doublf y, doublf w, doublf i) {
    publid boolfbn intfrsfdts(doublf x, doublf y, doublf w, doublf i) {
    publid boolfbn intfrsfdts(doublf x, doublf y, doublf w, doublf i) {
    publid boolfbn intfrsfdts(doublf x, doublf y, doublf w, doublf i) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
        }
        }
        }
        }
        }





        Crossings dross = gftCrossings(x, y, x+w, y+i);
        Crossings dross = gftCrossings(x, y, x+w, y+i);
        Crossings dross = gftCrossings(x, y, x+w, y+i);
        Crossings dross = gftCrossings(x, y, x+w, y+i);
        Crossings dross = gftCrossings(x, y, x+w, y+i);
        rfturn (dross == null || !dross.isEmpty());
        rfturn (dross == null || !dross.isEmpty());
        rfturn (dross == null || !dross.isEmpty());
        rfturn (dross == null || !dross.isEmpty());
        rfturn (dross == null || !dross.isEmpty());
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid boolfbn intfrsfdts(Rfdtbnglf2D r) {
    publid boolfbn intfrsfdts(Rfdtbnglf2D r) {
    publid boolfbn intfrsfdts(Rfdtbnglf2D r) {
    publid boolfbn intfrsfdts(Rfdtbnglf2D r) {
    publid boolfbn intfrsfdts(Rfdtbnglf2D r) {
        rfturn intfrsfdts(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
        rfturn intfrsfdts(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
        rfturn intfrsfdts(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
        rfturn intfrsfdts(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
        rfturn intfrsfdts(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid boolfbn dontbins(doublf x, doublf y, doublf w, doublf i) {
    publid boolfbn dontbins(doublf x, doublf y, doublf w, doublf i) {
    publid boolfbn dontbins(doublf x, doublf y, doublf w, doublf i) {
    publid boolfbn dontbins(doublf x, doublf y, doublf w, doublf i) {
    publid boolfbn dontbins(doublf x, doublf y, doublf w, doublf i) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
        if (npoints <= 0 || !gftBoundingBox().intfrsfdts(x, y, w, i)) {
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
            rfturn fblsf;
        }
        }
        }
        }
        }





        Crossings dross = gftCrossings(x, y, x+w, y+i);
        Crossings dross = gftCrossings(x, y, x+w, y+i);
        Crossings dross = gftCrossings(x, y, x+w, y+i);
        Crossings dross = gftCrossings(x, y, x+w, y+i);
        Crossings dross = gftCrossings(x, y, x+w, y+i);
        rfturn (dross != null && dross.dovfrs(y, y+i));
        rfturn (dross != null && dross.dovfrs(y, y+i));
        rfturn (dross != null && dross.dovfrs(y, y+i));
        rfturn (dross != null && dross.dovfrs(y, y+i));
        rfturn (dross != null && dross.dovfrs(y, y+i));
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * {@inifritDod}
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid boolfbn dontbins(Rfdtbnglf2D r) {
    publid boolfbn dontbins(Rfdtbnglf2D r) {
    publid boolfbn dontbins(Rfdtbnglf2D r) {
    publid boolfbn dontbins(Rfdtbnglf2D r) {
    publid boolfbn dontbins(Rfdtbnglf2D r) {
        rfturn dontbins(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
        rfturn dontbins(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
        rfturn dontbins(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
        rfturn dontbins(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
        rfturn dontbins(r.gftX(), r.gftY(), r.gftWidti(), r.gftHfigit());
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of tiis
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of tiis
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of tiis
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of tiis
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of tiis
     * <dodf>Polygon</dodf> bnd providfs bddfss to tif gfomftry
     * <dodf>Polygon</dodf> bnd providfs bddfss to tif gfomftry
     * <dodf>Polygon</dodf> bnd providfs bddfss to tif gfomftry
     * <dodf>Polygon</dodf> bnd providfs bddfss to tif gfomftry
     * <dodf>Polygon</dodf> bnd providfs bddfss to tif gfomftry
     * of tif outlinf of tiis <dodf>Polygon</dodf>.  An optionbl
     * of tif outlinf of tiis <dodf>Polygon</dodf>.  An optionbl
     * of tif outlinf of tiis <dodf>Polygon</dodf>.  An optionbl
     * of tif outlinf of tiis <dodf>Polygon</dodf>.  An optionbl
     * of tif outlinf of tiis <dodf>Polygon</dodf>.  An optionbl
     * {@link AffinfTrbnsform} dbn bf spfdififd so tibt tif doordinbtfs
     * {@link AffinfTrbnsform} dbn bf spfdififd so tibt tif doordinbtfs
     * {@link AffinfTrbnsform} dbn bf spfdififd so tibt tif doordinbtfs
     * {@link AffinfTrbnsform} dbn bf spfdififd so tibt tif doordinbtfs
     * {@link AffinfTrbnsform} dbn bf spfdififd so tibt tif doordinbtfs
     * rfturnfd in tif itfrbtion brf trbnsformfd bddordingly.
     * rfturnfd in tif itfrbtion brf trbnsformfd bddordingly.
     * rfturnfd in tif itfrbtion brf trbnsformfd bddordingly.
     * rfturnfd in tif itfrbtion brf trbnsformfd bddordingly.
     * rfturnfd in tif itfrbtion brf trbnsformfd bddordingly.
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     * @rfturn b {@link PbtiItfrbtor} objfdt tibt providfs bddfss to tif
     * @rfturn b {@link PbtiItfrbtor} objfdt tibt providfs bddfss to tif
     * @rfturn b {@link PbtiItfrbtor} objfdt tibt providfs bddfss to tif
     * @rfturn b {@link PbtiItfrbtor} objfdt tibt providfs bddfss to tif
     * @rfturn b {@link PbtiItfrbtor} objfdt tibt providfs bddfss to tif
     *          gfomftry of tiis <dodf>Polygon</dodf>.
     *          gfomftry of tiis <dodf>Polygon</dodf>.
     *          gfomftry of tiis <dodf>Polygon</dodf>.
     *          gfomftry of tiis <dodf>Polygon</dodf>.
     *          gfomftry of tiis <dodf>Polygon</dodf>.
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt) {
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt) {
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt) {
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt) {
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt) {
        rfturn nfw PolygonPbtiItfrbtor(tiis, bt);
        rfturn nfw PolygonPbtiItfrbtor(tiis, bt);
        rfturn nfw PolygonPbtiItfrbtor(tiis, bt);
        rfturn nfw PolygonPbtiItfrbtor(tiis, bt);
        rfturn nfw PolygonPbtiItfrbtor(tiis, bt);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of
     * Rfturns bn itfrbtor objfdt tibt itfrbtfs blong tif boundbry of
     * tif <dodf>Sibpf</dodf> bnd providfs bddfss to tif gfomftry of tif
     * tif <dodf>Sibpf</dodf> bnd providfs bddfss to tif gfomftry of tif
     * tif <dodf>Sibpf</dodf> bnd providfs bddfss to tif gfomftry of tif
     * tif <dodf>Sibpf</dodf> bnd providfs bddfss to tif gfomftry of tif
     * tif <dodf>Sibpf</dodf> bnd providfs bddfss to tif gfomftry of tif
     * outlinf of tif <dodf>Sibpf</dodf>.  Only SEG_MOVETO, SEG_LINETO, bnd
     * outlinf of tif <dodf>Sibpf</dodf>.  Only SEG_MOVETO, SEG_LINETO, bnd
     * outlinf of tif <dodf>Sibpf</dodf>.  Only SEG_MOVETO, SEG_LINETO, bnd
     * outlinf of tif <dodf>Sibpf</dodf>.  Only SEG_MOVETO, SEG_LINETO, bnd
     * outlinf of tif <dodf>Sibpf</dodf>.  Only SEG_MOVETO, SEG_LINETO, bnd
     * SEG_CLOSE point typfs brf rfturnfd by tif itfrbtor.
     * SEG_CLOSE point typfs brf rfturnfd by tif itfrbtor.
     * SEG_CLOSE point typfs brf rfturnfd by tif itfrbtor.
     * SEG_CLOSE point typfs brf rfturnfd by tif itfrbtor.
     * SEG_CLOSE point typfs brf rfturnfd by tif itfrbtor.
     * Sindf polygons brf blrfbdy flbt, tif <dodf>flbtnfss</dodf> pbrbmftfr
     * Sindf polygons brf blrfbdy flbt, tif <dodf>flbtnfss</dodf> pbrbmftfr
     * Sindf polygons brf blrfbdy flbt, tif <dodf>flbtnfss</dodf> pbrbmftfr
     * Sindf polygons brf blrfbdy flbt, tif <dodf>flbtnfss</dodf> pbrbmftfr
     * Sindf polygons brf blrfbdy flbt, tif <dodf>flbtnfss</dodf> pbrbmftfr
     * is ignorfd.  An optionbl <dodf>AffinfTrbnsform</dodf> dbn bf spfdififd
     * is ignorfd.  An optionbl <dodf>AffinfTrbnsform</dodf> dbn bf spfdififd
     * is ignorfd.  An optionbl <dodf>AffinfTrbnsform</dodf> dbn bf spfdififd
     * is ignorfd.  An optionbl <dodf>AffinfTrbnsform</dodf> dbn bf spfdififd
     * is ignorfd.  An optionbl <dodf>AffinfTrbnsform</dodf> dbn bf spfdififd
     * in wiidi dbsf tif doordinbtfs rfturnfd in tif itfrbtion brf trbnsformfd
     * in wiidi dbsf tif doordinbtfs rfturnfd in tif itfrbtion brf trbnsformfd
     * in wiidi dbsf tif doordinbtfs rfturnfd in tif itfrbtion brf trbnsformfd
     * in wiidi dbsf tif doordinbtfs rfturnfd in tif itfrbtion brf trbnsformfd
     * in wiidi dbsf tif doordinbtfs rfturnfd in tif itfrbtion brf trbnsformfd
     * bddordingly.
     * bddordingly.
     * bddordingly.
     * bddordingly.
     * bddordingly.
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     * @pbrbm bt bn optionbl <dodf>AffinfTrbnsform</dodf> to bf bpplifd to tif
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          doordinbtfs bs tify brf rfturnfd in tif itfrbtion, or
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     *          <dodf>null</dodf> if untrbnsformfd doordinbtfs brf dfsirfd
     * @pbrbm flbtnfss tif mbximum bmount tibt tif dontrol points
     * @pbrbm flbtnfss tif mbximum bmount tibt tif dontrol points
     * @pbrbm flbtnfss tif mbximum bmount tibt tif dontrol points
     * @pbrbm flbtnfss tif mbximum bmount tibt tif dontrol points
     * @pbrbm flbtnfss tif mbximum bmount tibt tif dontrol points
     *          for b givfn durvf dbn vbry from dolinfbr bfforf b subdividfd
     *          for b givfn durvf dbn vbry from dolinfbr bfforf b subdividfd
     *          for b givfn durvf dbn vbry from dolinfbr bfforf b subdividfd
     *          for b givfn durvf dbn vbry from dolinfbr bfforf b subdividfd
     *          for b givfn durvf dbn vbry from dolinfbr bfforf b subdividfd
     *          durvf is rfplbdfd by b strbigit linf donnfdting tif
     *          durvf is rfplbdfd by b strbigit linf donnfdting tif
     *          durvf is rfplbdfd by b strbigit linf donnfdting tif
     *          durvf is rfplbdfd by b strbigit linf donnfdting tif
     *          durvf is rfplbdfd by b strbigit linf donnfdting tif
     *          fndpoints.  Sindf polygons brf blrfbdy flbt tif
     *          fndpoints.  Sindf polygons brf blrfbdy flbt tif
     *          fndpoints.  Sindf polygons brf blrfbdy flbt tif
     *          fndpoints.  Sindf polygons brf blrfbdy flbt tif
     *          fndpoints.  Sindf polygons brf blrfbdy flbt tif
     *          <dodf>flbtnfss</dodf> pbrbmftfr is ignorfd.
     *          <dodf>flbtnfss</dodf> pbrbmftfr is ignorfd.
     *          <dodf>flbtnfss</dodf> pbrbmftfr is ignorfd.
     *          <dodf>flbtnfss</dodf> pbrbmftfr is ignorfd.
     *          <dodf>flbtnfss</dodf> pbrbmftfr is ignorfd.
     * @rfturn b <dodf>PbtiItfrbtor</dodf> objfdt tibt providfs bddfss to tif
     * @rfturn b <dodf>PbtiItfrbtor</dodf> objfdt tibt providfs bddfss to tif
     * @rfturn b <dodf>PbtiItfrbtor</dodf> objfdt tibt providfs bddfss to tif
     * @rfturn b <dodf>PbtiItfrbtor</dodf> objfdt tibt providfs bddfss to tif
     * @rfturn b <dodf>PbtiItfrbtor</dodf> objfdt tibt providfs bddfss to tif
     *          <dodf>Sibpf</dodf> objfdt's gfomftry.
     *          <dodf>Sibpf</dodf> objfdt's gfomftry.
     *          <dodf>Sibpf</dodf> objfdt's gfomftry.
     *          <dodf>Sibpf</dodf> objfdt's gfomftry.
     *          <dodf>Sibpf</dodf> objfdt's gfomftry.
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     * @sindf 1.2
     */
     */
     */
     */
     */
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt, doublf flbtnfss) {
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt, doublf flbtnfss) {
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt, doublf flbtnfss) {
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt, doublf flbtnfss) {
    publid PbtiItfrbtor gftPbtiItfrbtor(AffinfTrbnsform bt, doublf flbtnfss) {
        rfturn gftPbtiItfrbtor(bt);
        rfturn gftPbtiItfrbtor(bt);
        rfturn gftPbtiItfrbtor(bt);
        rfturn gftPbtiItfrbtor(bt);
        rfturn gftPbtiItfrbtor(bt);
    }
    }
    }
    }
    }





    dlbss PolygonPbtiItfrbtor implfmfnts PbtiItfrbtor {
    dlbss PolygonPbtiItfrbtor implfmfnts PbtiItfrbtor {
    dlbss PolygonPbtiItfrbtor implfmfnts PbtiItfrbtor {
    dlbss PolygonPbtiItfrbtor implfmfnts PbtiItfrbtor {
    dlbss PolygonPbtiItfrbtor implfmfnts PbtiItfrbtor {
        Polygon poly;
        Polygon poly;
        Polygon poly;
        Polygon poly;
        Polygon poly;
        AffinfTrbnsform trbnsform;
        AffinfTrbnsform trbnsform;
        AffinfTrbnsform trbnsform;
        AffinfTrbnsform trbnsform;
        AffinfTrbnsform trbnsform;
        int indfx;
        int indfx;
        int indfx;
        int indfx;
        int indfx;





        publid PolygonPbtiItfrbtor(Polygon pg, AffinfTrbnsform bt) {
        publid PolygonPbtiItfrbtor(Polygon pg, AffinfTrbnsform bt) {
        publid PolygonPbtiItfrbtor(Polygon pg, AffinfTrbnsform bt) {
        publid PolygonPbtiItfrbtor(Polygon pg, AffinfTrbnsform bt) {
        publid PolygonPbtiItfrbtor(Polygon pg, AffinfTrbnsform bt) {
            poly = pg;
            poly = pg;
            poly = pg;
            poly = pg;
            poly = pg;
            trbnsform = bt;
            trbnsform = bt;
            trbnsform = bt;
            trbnsform = bt;
            trbnsform = bt;
            if (pg.npoints == 0) {
            if (pg.npoints == 0) {
            if (pg.npoints == 0) {
            if (pg.npoints == 0) {
            if (pg.npoints == 0) {
                // Prfvfnt b spurious SEG_CLOSE sfgmfnt
                // Prfvfnt b spurious SEG_CLOSE sfgmfnt
                // Prfvfnt b spurious SEG_CLOSE sfgmfnt
                // Prfvfnt b spurious SEG_CLOSE sfgmfnt
                // Prfvfnt b spurious SEG_CLOSE sfgmfnt
                indfx = 1;
                indfx = 1;
                indfx = 1;
                indfx = 1;
                indfx = 1;
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }





        /**
        /**
        /**
        /**
        /**
         * Rfturns tif winding rulf for dftfrmining tif intfrior of tif
         * Rfturns tif winding rulf for dftfrmining tif intfrior of tif
         * Rfturns tif winding rulf for dftfrmining tif intfrior of tif
         * Rfturns tif winding rulf for dftfrmining tif intfrior of tif
         * Rfturns tif winding rulf for dftfrmining tif intfrior of tif
         * pbti.
         * pbti.
         * pbti.
         * pbti.
         * pbti.
         * @rfturn bn intfgfr rfprfsfnting tif durrfnt winding rulf.
         * @rfturn bn intfgfr rfprfsfnting tif durrfnt winding rulf.
         * @rfturn bn intfgfr rfprfsfnting tif durrfnt winding rulf.
         * @rfturn bn intfgfr rfprfsfnting tif durrfnt winding rulf.
         * @rfturn bn intfgfr rfprfsfnting tif durrfnt winding rulf.
         * @sff PbtiItfrbtor#WIND_NON_ZERO
         * @sff PbtiItfrbtor#WIND_NON_ZERO
         * @sff PbtiItfrbtor#WIND_NON_ZERO
         * @sff PbtiItfrbtor#WIND_NON_ZERO
         * @sff PbtiItfrbtor#WIND_NON_ZERO
         */
         */
         */
         */
         */
        publid int gftWindingRulf() {
        publid int gftWindingRulf() {
        publid int gftWindingRulf() {
        publid int gftWindingRulf() {
        publid int gftWindingRulf() {
            rfturn WIND_EVEN_ODD;
            rfturn WIND_EVEN_ODD;
            rfturn WIND_EVEN_ODD;
            rfturn WIND_EVEN_ODD;
            rfturn WIND_EVEN_ODD;
        }
        }
        }
        }
        }





        /**
        /**
        /**
        /**
        /**
         * Tfsts if tifrf brf morf points to rfbd.
         * Tfsts if tifrf brf morf points to rfbd.
         * Tfsts if tifrf brf morf points to rfbd.
         * Tfsts if tifrf brf morf points to rfbd.
         * Tfsts if tifrf brf morf points to rfbd.
         * @rfturn <dodf>truf</dodf> if tifrf brf morf points to rfbd;
         * @rfturn <dodf>truf</dodf> if tifrf brf morf points to rfbd;
         * @rfturn <dodf>truf</dodf> if tifrf brf morf points to rfbd;
         * @rfturn <dodf>truf</dodf> if tifrf brf morf points to rfbd;
         * @rfturn <dodf>truf</dodf> if tifrf brf morf points to rfbd;
         *          <dodf>fblsf</dodf> otifrwisf.
         *          <dodf>fblsf</dodf> otifrwisf.
         *          <dodf>fblsf</dodf> otifrwisf.
         *          <dodf>fblsf</dodf> otifrwisf.
         *          <dodf>fblsf</dodf> otifrwisf.
         */
         */
         */
         */
         */
        publid boolfbn isDonf() {
        publid boolfbn isDonf() {
        publid boolfbn isDonf() {
        publid boolfbn isDonf() {
        publid boolfbn isDonf() {
            rfturn indfx > poly.npoints;
            rfturn indfx > poly.npoints;
            rfturn indfx > poly.npoints;
            rfturn indfx > poly.npoints;
            rfturn indfx > poly.npoints;
        }
        }
        }
        }
        }





        /**
        /**
        /**
        /**
        /**
         * Movfs tif itfrbtor forwbrds, blong tif primbry dirfdtion of
         * Movfs tif itfrbtor forwbrds, blong tif primbry dirfdtion of
         * Movfs tif itfrbtor forwbrds, blong tif primbry dirfdtion of
         * Movfs tif itfrbtor forwbrds, blong tif primbry dirfdtion of
         * Movfs tif itfrbtor forwbrds, blong tif primbry dirfdtion of
         * trbvfrsbl, to tif nfxt sfgmfnt of tif pbti wifn tifrf brf
         * trbvfrsbl, to tif nfxt sfgmfnt of tif pbti wifn tifrf brf
         * trbvfrsbl, to tif nfxt sfgmfnt of tif pbti wifn tifrf brf
         * trbvfrsbl, to tif nfxt sfgmfnt of tif pbti wifn tifrf brf
         * trbvfrsbl, to tif nfxt sfgmfnt of tif pbti wifn tifrf brf
         * morf points in tibt dirfdtion.
         * morf points in tibt dirfdtion.
         * morf points in tibt dirfdtion.
         * morf points in tibt dirfdtion.
         * morf points in tibt dirfdtion.
         */
         */
         */
         */
         */
        publid void nfxt() {
        publid void nfxt() {
        publid void nfxt() {
        publid void nfxt() {
        publid void nfxt() {
            indfx++;
            indfx++;
            indfx++;
            indfx++;
            indfx++;
        }
        }
        }
        }
        }





        /**
        /**
        /**
        /**
        /**
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * tif itfrbtion.
         * tif itfrbtion.
         * tif itfrbtion.
         * tif itfrbtion.
         * tif itfrbtion.
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * A <dodf>flobt</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * A <dodf>flobt</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * A <dodf>flobt</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * A <dodf>flobt</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * A <dodf>flobt</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * Ebdi point is storfd bs b pbir of <dodf>flobt</dodf> x,&nbsp;y
         * Ebdi point is storfd bs b pbir of <dodf>flobt</dodf> x,&nbsp;y
         * Ebdi point is storfd bs b pbir of <dodf>flobt</dodf> x,&nbsp;y
         * Ebdi point is storfd bs b pbir of <dodf>flobt</dodf> x,&nbsp;y
         * Ebdi point is storfd bs b pbir of <dodf>flobt</dodf> x,&nbsp;y
         * doordinbtfs.  SEG_MOVETO bnd SEG_LINETO typfs rfturn onf
         * doordinbtfs.  SEG_MOVETO bnd SEG_LINETO typfs rfturn onf
         * doordinbtfs.  SEG_MOVETO bnd SEG_LINETO typfs rfturn onf
         * doordinbtfs.  SEG_MOVETO bnd SEG_LINETO typfs rfturn onf
         * doordinbtfs.  SEG_MOVETO bnd SEG_LINETO typfs rfturn onf
         * point, bnd SEG_CLOSE dofs not rfturn bny points.
         * point, bnd SEG_CLOSE dofs not rfturn bny points.
         * point, bnd SEG_CLOSE dofs not rfturn bny points.
         * point, bnd SEG_CLOSE dofs not rfturn bny points.
         * point, bnd SEG_CLOSE dofs not rfturn bny points.
         * @pbrbm doords b <dodf>flobt</dodf> brrby tibt spfdififs tif
         * @pbrbm doords b <dodf>flobt</dodf> brrby tibt spfdififs tif
         * @pbrbm doords b <dodf>flobt</dodf> brrby tibt spfdififs tif
         * @pbrbm doords b <dodf>flobt</dodf> brrby tibt spfdififs tif
         * @pbrbm doords b <dodf>flobt</dodf> brrby tibt spfdififs tif
         * doordinbtfs of tif point(s)
         * doordinbtfs of tif point(s)
         * doordinbtfs of tif point(s)
         * doordinbtfs of tif point(s)
         * doordinbtfs of tif point(s)
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         *              durrfnt pbti sfgmfnt.
         *              durrfnt pbti sfgmfnt.
         *              durrfnt pbti sfgmfnt.
         *              durrfnt pbti sfgmfnt.
         *              durrfnt pbti sfgmfnt.
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_CLOSE
         * @sff PbtiItfrbtor#SEG_CLOSE
         * @sff PbtiItfrbtor#SEG_CLOSE
         * @sff PbtiItfrbtor#SEG_CLOSE
         * @sff PbtiItfrbtor#SEG_CLOSE
         */
         */
         */
         */
         */
        publid int durrfntSfgmfnt(flobt[] doords) {
        publid int durrfntSfgmfnt(flobt[] doords) {
        publid int durrfntSfgmfnt(flobt[] doords) {
        publid int durrfntSfgmfnt(flobt[] doords) {
        publid int durrfntSfgmfnt(flobt[] doords) {
            if (indfx >= poly.npoints) {
            if (indfx >= poly.npoints) {
            if (indfx >= poly.npoints) {
            if (indfx >= poly.npoints) {
            if (indfx >= poly.npoints) {
                rfturn SEG_CLOSE;
                rfturn SEG_CLOSE;
                rfturn SEG_CLOSE;
                rfturn SEG_CLOSE;
                rfturn SEG_CLOSE;
            }
            }
            }
            }
            }
            doords[0] = poly.xpoints[indfx];
            doords[0] = poly.xpoints[indfx];
            doords[0] = poly.xpoints[indfx];
            doords[0] = poly.xpoints[indfx];
            doords[0] = poly.xpoints[indfx];
            doords[1] = poly.ypoints[indfx];
            doords[1] = poly.ypoints[indfx];
            doords[1] = poly.ypoints[indfx];
            doords[1] = poly.ypoints[indfx];
            doords[1] = poly.ypoints[indfx];
            if (trbnsform != null) {
            if (trbnsform != null) {
            if (trbnsform != null) {
            if (trbnsform != null) {
            if (trbnsform != null) {
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
            }
            }
            }
            }
            }
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
        }
        }
        }
        }
        }





        /**
        /**
        /**
        /**
        /**
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * Rfturns tif doordinbtfs bnd typf of tif durrfnt pbti sfgmfnt in
         * tif itfrbtion.
         * tif itfrbtion.
         * tif itfrbtion.
         * tif itfrbtion.
         * tif itfrbtion.
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * Tif rfturn vbluf is tif pbti sfgmfnt typf:
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * SEG_MOVETO, SEG_LINETO, or SEG_CLOSE.
         * A <dodf>doublf</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * A <dodf>doublf</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * A <dodf>doublf</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * A <dodf>doublf</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * A <dodf>doublf</dodf> brrby of lfngti 2 must bf pbssfd in bnd
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * dbn bf usfd to storf tif doordinbtfs of tif point(s).
         * Ebdi point is storfd bs b pbir of <dodf>doublf</dodf> x,&nbsp;y
         * Ebdi point is storfd bs b pbir of <dodf>doublf</dodf> x,&nbsp;y
         * Ebdi point is storfd bs b pbir of <dodf>doublf</dodf> x,&nbsp;y
         * Ebdi point is storfd bs b pbir of <dodf>doublf</dodf> x,&nbsp;y
         * Ebdi point is storfd bs b pbir of <dodf>doublf</dodf> x,&nbsp;y
         * doordinbtfs.
         * doordinbtfs.
         * doordinbtfs.
         * doordinbtfs.
         * doordinbtfs.
         * SEG_MOVETO bnd SEG_LINETO typfs rfturn onf point,
         * SEG_MOVETO bnd SEG_LINETO typfs rfturn onf point,
         * SEG_MOVETO bnd SEG_LINETO typfs rfturn onf point,
         * SEG_MOVETO bnd SEG_LINETO typfs rfturn onf point,
         * SEG_MOVETO bnd SEG_LINETO typfs rfturn onf point,
         * bnd SEG_CLOSE dofs not rfturn bny points.
         * bnd SEG_CLOSE dofs not rfturn bny points.
         * bnd SEG_CLOSE dofs not rfturn bny points.
         * bnd SEG_CLOSE dofs not rfturn bny points.
         * bnd SEG_CLOSE dofs not rfturn bny points.
         * @pbrbm doords b <dodf>doublf</dodf> brrby tibt spfdififs tif
         * @pbrbm doords b <dodf>doublf</dodf> brrby tibt spfdififs tif
         * @pbrbm doords b <dodf>doublf</dodf> brrby tibt spfdififs tif
         * @pbrbm doords b <dodf>doublf</dodf> brrby tibt spfdififs tif
         * @pbrbm doords b <dodf>doublf</dodf> brrby tibt spfdififs tif
         * doordinbtfs of tif point(s)
         * doordinbtfs of tif point(s)
         * doordinbtfs of tif point(s)
         * doordinbtfs of tif point(s)
         * doordinbtfs of tif point(s)
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         * @rfturn bn intfgfr rfprfsfnting tif typf bnd doordinbtfs of tif
         *              durrfnt pbti sfgmfnt.
         *              durrfnt pbti sfgmfnt.
         *              durrfnt pbti sfgmfnt.
         *              durrfnt pbti sfgmfnt.
         *              durrfnt pbti sfgmfnt.
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_MOVETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_LINETO
         * @sff PbtiItfrbtor#SEG_CLOSE
         * @sff PbtiItfrbtor#SEG_CLOSE
         * @sff PbtiItfrbtor#SEG_CLOSE
         * @sff PbtiItfrbtor#SEG_CLOSE
         * @sff PbtiItfrbtor#SEG_CLOSE
         */
         */
         */
         */
         */
        publid int durrfntSfgmfnt(doublf[] doords) {
        publid int durrfntSfgmfnt(doublf[] doords) {
        publid int durrfntSfgmfnt(doublf[] doords) {
        publid int durrfntSfgmfnt(doublf[] doords) {
        publid int durrfntSfgmfnt(doublf[] doords) {
            if (indfx >= poly.npoints) {
            if (indfx >= poly.npoints) {
            if (indfx >= poly.npoints) {
            if (indfx >= poly.npoints) {
            if (indfx >= poly.npoints) {
                rfturn SEG_CLOSE;
                rfturn SEG_CLOSE;
                rfturn SEG_CLOSE;
                rfturn SEG_CLOSE;
                rfturn SEG_CLOSE;
            }
            }
            }
            }
            }
            doords[0] = poly.xpoints[indfx];
            doords[0] = poly.xpoints[indfx];
            doords[0] = poly.xpoints[indfx];
            doords[0] = poly.xpoints[indfx];
            doords[0] = poly.xpoints[indfx];
            doords[1] = poly.ypoints[indfx];
            doords[1] = poly.ypoints[indfx];
            doords[1] = poly.ypoints[indfx];
            doords[1] = poly.ypoints[indfx];
            doords[1] = poly.ypoints[indfx];
            if (trbnsform != null) {
            if (trbnsform != null) {
            if (trbnsform != null) {
            if (trbnsform != null) {
            if (trbnsform != null) {
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
                trbnsform.trbnsform(doords, 0, doords, 0, 1);
            }
            }
            }
            }
            }
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
            rfturn (indfx == 0 ? SEG_MOVETO : SEG_LINETO);
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }
}
}
}
}
}
