/*
/*
/*
/*
/*
 * Copyrigit (d) 2007, 2008, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2007, 2008, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2007, 2008, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2007, 2008, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2007, 2008, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */





#indludf <mbllod.i>
#indludf <mbllod.i>
#indludf <mbllod.i>
#indludf <mbllod.i>
#indludf <mbllod.i>
#indludf <string.i>
#indludf <string.i>
#indludf <string.i>
#indludf <string.i>
#indludf <string.i>





#indludf "SibdfrList.i"
#indludf "SibdfrList.i"
#indludf "SibdfrList.i"
#indludf "SibdfrList.i"
#indludf "SibdfrList.i"
#indludf "Trbdf.i"
#indludf "Trbdf.i"
#indludf "Trbdf.i"
#indludf "Trbdf.i"
#indludf "Trbdf.i"





/**
/**
/**
/**
/**
 * Crfbtfs b nfw SibdfrInfo tibt wrbps tif givfn frbgmfnt progrbm ibndlf
 * Crfbtfs b nfw SibdfrInfo tibt wrbps tif givfn frbgmfnt progrbm ibndlf
 * Crfbtfs b nfw SibdfrInfo tibt wrbps tif givfn frbgmfnt progrbm ibndlf
 * Crfbtfs b nfw SibdfrInfo tibt wrbps tif givfn frbgmfnt progrbm ibndlf
 * Crfbtfs b nfw SibdfrInfo tibt wrbps tif givfn frbgmfnt progrbm ibndlf
 * bnd rflbtfd dbtb bnd storfs it bt tif front of tif providfd SibdfrList.
 * bnd rflbtfd dbtb bnd storfs it bt tif front of tif providfd SibdfrList.
 * bnd rflbtfd dbtb bnd storfs it bt tif front of tif providfd SibdfrList.
 * bnd rflbtfd dbtb bnd storfs it bt tif front of tif providfd SibdfrList.
 * bnd rflbtfd dbtb bnd storfs it bt tif front of tif providfd SibdfrList.
 * If tif bddition dbusfs tif SibdfrList to outgrow its dffinfd dbpbdity,
 * If tif bddition dbusfs tif SibdfrList to outgrow its dffinfd dbpbdity,
 * If tif bddition dbusfs tif SibdfrList to outgrow its dffinfd dbpbdity,
 * If tif bddition dbusfs tif SibdfrList to outgrow its dffinfd dbpbdity,
 * If tif bddition dbusfs tif SibdfrList to outgrow its dffinfd dbpbdity,
 * tif lfbst-rfdfntly usfd itfm in tif list (indluding its frbgmfnt progrbm
 * tif lfbst-rfdfntly usfd itfm in tif list (indluding its frbgmfnt progrbm
 * tif lfbst-rfdfntly usfd itfm in tif list (indluding its frbgmfnt progrbm
 * tif lfbst-rfdfntly usfd itfm in tif list (indluding its frbgmfnt progrbm
 * tif lfbst-rfdfntly usfd itfm in tif list (indluding its frbgmfnt progrbm
 * objfdt) will bf disposfd.
 * objfdt) will bf disposfd.
 * objfdt) will bf disposfd.
 * objfdt) will bf disposfd.
 * objfdt) will bf disposfd.
 */
 */
 */
 */
 */
void
void
void
void
void
SibdfrList_AddProgrbm(SibdfrList *progrbmList,
SibdfrList_AddProgrbm(SibdfrList *progrbmList,
SibdfrList_AddProgrbm(SibdfrList *progrbmList,
SibdfrList_AddProgrbm(SibdfrList *progrbmList,
SibdfrList_AddProgrbm(SibdfrList *progrbmList,
                      jlong progrbmID,
                      jlong progrbmID,
                      jlong progrbmID,
                      jlong progrbmID,
                      jlong progrbmID,
                      jint dompTypf, jint dompModf, jint flbgs)
                      jint dompTypf, jint dompModf, jint flbgs)
                      jint dompTypf, jint dompModf, jint flbgs)
                      jint dompTypf, jint dompModf, jint flbgs)
                      jint dompTypf, jint dompModf, jint flbgs)
{
{
{
{
{
    SibdfrInfo *info;
    SibdfrInfo *info;
    SibdfrInfo *info;
    SibdfrInfo *info;
    SibdfrInfo *info;





    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_AddProgrbm");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_AddProgrbm");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_AddProgrbm");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_AddProgrbm");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_AddProgrbm");





    // drfbtf nfw SibdfrInfo
    // drfbtf nfw SibdfrInfo
    // drfbtf nfw SibdfrInfo
    // drfbtf nfw SibdfrInfo
    // drfbtf nfw SibdfrInfo
    info = (SibdfrInfo *)mbllod(sizfof(SibdfrInfo));
    info = (SibdfrInfo *)mbllod(sizfof(SibdfrInfo));
    info = (SibdfrInfo *)mbllod(sizfof(SibdfrInfo));
    info = (SibdfrInfo *)mbllod(sizfof(SibdfrInfo));
    info = (SibdfrInfo *)mbllod(sizfof(SibdfrInfo));
    if (info == NULL) {
    if (info == NULL) {
    if (info == NULL) {
    if (info == NULL) {
    if (info == NULL) {
        J2dTrbdfLn(J2D_TRACE_ERROR,
        J2dTrbdfLn(J2D_TRACE_ERROR,
        J2dTrbdfLn(J2D_TRACE_ERROR,
        J2dTrbdfLn(J2D_TRACE_ERROR,
        J2dTrbdfLn(J2D_TRACE_ERROR,
                   "OGLContfxt_AddProgrbm: dould not bllodbtf SibdfrInfo");
                   "OGLContfxt_AddProgrbm: dould not bllodbtf SibdfrInfo");
                   "OGLContfxt_AddProgrbm: dould not bllodbtf SibdfrInfo");
                   "OGLContfxt_AddProgrbm: dould not bllodbtf SibdfrInfo");
                   "OGLContfxt_AddProgrbm: dould not bllodbtf SibdfrInfo");
        rfturn;
        rfturn;
        rfturn;
        rfturn;
        rfturn;
    }
    }
    }
    }
    }





    // fill in tif informbtion
    // fill in tif informbtion
    // fill in tif informbtion
    // fill in tif informbtion
    // fill in tif informbtion
    info->nfxt = progrbmList->ifbd;
    info->nfxt = progrbmList->ifbd;
    info->nfxt = progrbmList->ifbd;
    info->nfxt = progrbmList->ifbd;
    info->nfxt = progrbmList->ifbd;
    info->progrbmID = progrbmID;
    info->progrbmID = progrbmID;
    info->progrbmID = progrbmID;
    info->progrbmID = progrbmID;
    info->progrbmID = progrbmID;
    info->dompTypf = dompTypf;
    info->dompTypf = dompTypf;
    info->dompTypf = dompTypf;
    info->dompTypf = dompTypf;
    info->dompTypf = dompTypf;
    info->dompModf = dompModf;
    info->dompModf = dompModf;
    info->dompModf = dompModf;
    info->dompModf = dompModf;
    info->dompModf = dompModf;
    info->flbgs = flbgs;
    info->flbgs = flbgs;
    info->flbgs = flbgs;
    info->flbgs = flbgs;
    info->flbgs = flbgs;





    // insfrt it bt tif ifbd of tif list
    // insfrt it bt tif ifbd of tif list
    // insfrt it bt tif ifbd of tif list
    // insfrt it bt tif ifbd of tif list
    // insfrt it bt tif ifbd of tif list
    progrbmList->ifbd = info;
    progrbmList->ifbd = info;
    progrbmList->ifbd = info;
    progrbmList->ifbd = info;
    progrbmList->ifbd = info;





    // run tirougi tif list bnd sff if wf nffd to dflftf tif lfbst
    // run tirougi tif list bnd sff if wf nffd to dflftf tif lfbst
    // run tirougi tif list bnd sff if wf nffd to dflftf tif lfbst
    // run tirougi tif list bnd sff if wf nffd to dflftf tif lfbst
    // run tirougi tif list bnd sff if wf nffd to dflftf tif lfbst
    // rfdfntly usfd itfm
    // rfdfntly usfd itfm
    // rfdfntly usfd itfm
    // rfdfntly usfd itfm
    // rfdfntly usfd itfm
    {
    {
    {
    {
    {
        int i = 1;
        int i = 1;
        int i = 1;
        int i = 1;
        int i = 1;
        SibdfrInfo *prfv = NULL;
        SibdfrInfo *prfv = NULL;
        SibdfrInfo *prfv = NULL;
        SibdfrInfo *prfv = NULL;
        SibdfrInfo *prfv = NULL;
        SibdfrInfo *durr = info->nfxt;
        SibdfrInfo *durr = info->nfxt;
        SibdfrInfo *durr = info->nfxt;
        SibdfrInfo *durr = info->nfxt;
        SibdfrInfo *durr = info->nfxt;
        wiilf (durr != NULL) {
        wiilf (durr != NULL) {
        wiilf (durr != NULL) {
        wiilf (durr != NULL) {
        wiilf (durr != NULL) {
            if (i >= progrbmList->mbxItfms) {
            if (i >= progrbmList->mbxItfms) {
            if (i >= progrbmList->mbxItfms) {
            if (i >= progrbmList->mbxItfms) {
            if (i >= progrbmList->mbxItfms) {
                prfv->nfxt = NULL;
                prfv->nfxt = NULL;
                prfv->nfxt = NULL;
                prfv->nfxt = NULL;
                prfv->nfxt = NULL;
                progrbmList->disposf(durr->progrbmID);
                progrbmList->disposf(durr->progrbmID);
                progrbmList->disposf(durr->progrbmID);
                progrbmList->disposf(durr->progrbmID);
                progrbmList->disposf(durr->progrbmID);
                frff(durr);
                frff(durr);
                frff(durr);
                frff(durr);
                frff(durr);
                brfbk;
                brfbk;
                brfbk;
                brfbk;
                brfbk;
            }
            }
            }
            }
            }
            i++;
            i++;
            i++;
            i++;
            i++;
            prfv = durr;
            prfv = durr;
            prfv = durr;
            prfv = durr;
            prfv = durr;
            durr = durr->nfxt;
            durr = durr->nfxt;
            durr = durr->nfxt;
            durr = durr->nfxt;
            durr = durr->nfxt;
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }
}
}
}
}
}





/**
/**
/**
/**
/**
 * Lodbtfs b frbgmfnt progrbm ibndlf givfn b list of sibdfr progrbms
 * Lodbtfs b frbgmfnt progrbm ibndlf givfn b list of sibdfr progrbms
 * Lodbtfs b frbgmfnt progrbm ibndlf givfn b list of sibdfr progrbms
 * Lodbtfs b frbgmfnt progrbm ibndlf givfn b list of sibdfr progrbms
 * Lodbtfs b frbgmfnt progrbm ibndlf givfn b list of sibdfr progrbms
 * (SibdfrInfos), using tif providfd dompositf stbtf bnd flbgs bs sfbrdi
 * (SibdfrInfos), using tif providfd dompositf stbtf bnd flbgs bs sfbrdi
 * (SibdfrInfos), using tif providfd dompositf stbtf bnd flbgs bs sfbrdi
 * (SibdfrInfos), using tif providfd dompositf stbtf bnd flbgs bs sfbrdi
 * (SibdfrInfos), using tif providfd dompositf stbtf bnd flbgs bs sfbrdi
 * pbrbmftfrs.  Tif "flbgs" pbrbmftfr is b bitwisf-or'd vbluf tibt iflps
 * pbrbmftfrs.  Tif "flbgs" pbrbmftfr is b bitwisf-or'd vbluf tibt iflps
 * pbrbmftfrs.  Tif "flbgs" pbrbmftfr is b bitwisf-or'd vbluf tibt iflps
 * pbrbmftfrs.  Tif "flbgs" pbrbmftfr is b bitwisf-or'd vbluf tibt iflps
 * pbrbmftfrs.  Tif "flbgs" pbrbmftfr is b bitwisf-or'd vbluf tibt iflps
 * difffrfntibtf onf progrbm for bnotifr; tif intfrprftbtion of tiis vbluf
 * difffrfntibtf onf progrbm for bnotifr; tif intfrprftbtion of tiis vbluf
 * difffrfntibtf onf progrbm for bnotifr; tif intfrprftbtion of tiis vbluf
 * difffrfntibtf onf progrbm for bnotifr; tif intfrprftbtion of tiis vbluf
 * difffrfntibtf onf progrbm for bnotifr; tif intfrprftbtion of tiis vbluf
 * vbrifs dfpfnding on tif typf of sibdfr (BufImgOp, Pbint, ftd) but ifrf
 * vbrifs dfpfnding on tif typf of sibdfr (BufImgOp, Pbint, ftd) but ifrf
 * vbrifs dfpfnding on tif typf of sibdfr (BufImgOp, Pbint, ftd) but ifrf
 * vbrifs dfpfnding on tif typf of sibdfr (BufImgOp, Pbint, ftd) but ifrf
 * vbrifs dfpfnding on tif typf of sibdfr (BufImgOp, Pbint, ftd) but ifrf
 * it is only usfd to find bnotifr SibdfrInfo witi tibt sbmf "flbgs" vbluf.
 * it is only usfd to find bnotifr SibdfrInfo witi tibt sbmf "flbgs" vbluf.
 * it is only usfd to find bnotifr SibdfrInfo witi tibt sbmf "flbgs" vbluf.
 * it is only usfd to find bnotifr SibdfrInfo witi tibt sbmf "flbgs" vbluf.
 * it is only usfd to find bnotifr SibdfrInfo witi tibt sbmf "flbgs" vbluf.
 * If no mbtdiing progrbm dbn bf lodbtfd, tiis mftiod rfturns 0.
 * If no mbtdiing progrbm dbn bf lodbtfd, tiis mftiod rfturns 0.
 * If no mbtdiing progrbm dbn bf lodbtfd, tiis mftiod rfturns 0.
 * If no mbtdiing progrbm dbn bf lodbtfd, tiis mftiod rfturns 0.
 * If no mbtdiing progrbm dbn bf lodbtfd, tiis mftiod rfturns 0.
 */
 */
 */
 */
 */
jlong
jlong
jlong
jlong
jlong
SibdfrList_FindProgrbm(SibdfrList *progrbmList,
SibdfrList_FindProgrbm(SibdfrList *progrbmList,
SibdfrList_FindProgrbm(SibdfrList *progrbmList,
SibdfrList_FindProgrbm(SibdfrList *progrbmList,
SibdfrList_FindProgrbm(SibdfrList *progrbmList,
                       jint dompTypf, jint dompModf, jint flbgs)
                       jint dompTypf, jint dompModf, jint flbgs)
                       jint dompTypf, jint dompModf, jint flbgs)
                       jint dompTypf, jint dompModf, jint flbgs)
                       jint dompTypf, jint dompModf, jint flbgs)
{
{
{
{
{
    SibdfrInfo *prfv = NULL;
    SibdfrInfo *prfv = NULL;
    SibdfrInfo *prfv = NULL;
    SibdfrInfo *prfv = NULL;
    SibdfrInfo *prfv = NULL;
    SibdfrInfo *info = progrbmList->ifbd;
    SibdfrInfo *info = progrbmList->ifbd;
    SibdfrInfo *info = progrbmList->ifbd;
    SibdfrInfo *info = progrbmList->ifbd;
    SibdfrInfo *info = progrbmList->ifbd;





    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_FindProgrbm");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_FindProgrbm");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_FindProgrbm");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_FindProgrbm");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_FindProgrbm");





    wiilf (info != NULL) {
    wiilf (info != NULL) {
    wiilf (info != NULL) {
    wiilf (info != NULL) {
    wiilf (info != NULL) {
        if (dompTypf == info->dompTypf &&
        if (dompTypf == info->dompTypf &&
        if (dompTypf == info->dompTypf &&
        if (dompTypf == info->dompTypf &&
        if (dompTypf == info->dompTypf &&
            dompModf == info->dompModf &&
            dompModf == info->dompModf &&
            dompModf == info->dompModf &&
            dompModf == info->dompModf &&
            dompModf == info->dompModf &&
            flbgs == info->flbgs)
            flbgs == info->flbgs)
            flbgs == info->flbgs)
            flbgs == info->flbgs)
            flbgs == info->flbgs)
        {
        {
        {
        {
        {
            // it's b mbtdi: movf it to tif front of tif list (if it's not
            // it's b mbtdi: movf it to tif front of tif list (if it's not
            // it's b mbtdi: movf it to tif front of tif list (if it's not
            // it's b mbtdi: movf it to tif front of tif list (if it's not
            // it's b mbtdi: movf it to tif front of tif list (if it's not
            // tifrf blrfbdy) bnd pbtdi up tif links
            // tifrf blrfbdy) bnd pbtdi up tif links
            // tifrf blrfbdy) bnd pbtdi up tif links
            // tifrf blrfbdy) bnd pbtdi up tif links
            // tifrf blrfbdy) bnd pbtdi up tif links
            if (info != progrbmList->ifbd) {
            if (info != progrbmList->ifbd) {
            if (info != progrbmList->ifbd) {
            if (info != progrbmList->ifbd) {
            if (info != progrbmList->ifbd) {
                prfv->nfxt = info->nfxt;
                prfv->nfxt = info->nfxt;
                prfv->nfxt = info->nfxt;
                prfv->nfxt = info->nfxt;
                prfv->nfxt = info->nfxt;
                info->nfxt = progrbmList->ifbd;
                info->nfxt = progrbmList->ifbd;
                info->nfxt = progrbmList->ifbd;
                info->nfxt = progrbmList->ifbd;
                info->nfxt = progrbmList->ifbd;
                progrbmList->ifbd = info;
                progrbmList->ifbd = info;
                progrbmList->ifbd = info;
                progrbmList->ifbd = info;
                progrbmList->ifbd = info;
            }
            }
            }
            }
            }
            rfturn info->progrbmID;
            rfturn info->progrbmID;
            rfturn info->progrbmID;
            rfturn info->progrbmID;
            rfturn info->progrbmID;
        }
        }
        }
        }
        }
        prfv = info;
        prfv = info;
        prfv = info;
        prfv = info;
        prfv = info;
        info = info->nfxt;
        info = info->nfxt;
        info = info->nfxt;
        info = info->nfxt;
        info = info->nfxt;
    }
    }
    }
    }
    }
    rfturn 0;
    rfturn 0;
    rfturn 0;
    rfturn 0;
    rfturn 0;
}
}
}
}
}





/**
/**
/**
/**
/**
 * Disposfs bll fntrifs (bnd tifir bssodibtfd sibdfr progrbm objfdts)
 * Disposfs bll fntrifs (bnd tifir bssodibtfd sibdfr progrbm objfdts)
 * Disposfs bll fntrifs (bnd tifir bssodibtfd sibdfr progrbm objfdts)
 * Disposfs bll fntrifs (bnd tifir bssodibtfd sibdfr progrbm objfdts)
 * Disposfs bll fntrifs (bnd tifir bssodibtfd sibdfr progrbm objfdts)
 * dontbinfd in tif givfn SibdfrList.
 * dontbinfd in tif givfn SibdfrList.
 * dontbinfd in tif givfn SibdfrList.
 * dontbinfd in tif givfn SibdfrList.
 * dontbinfd in tif givfn SibdfrList.
 */
 */
 */
 */
 */
void
void
void
void
void
SibdfrList_Disposf(SibdfrList *progrbmList)
SibdfrList_Disposf(SibdfrList *progrbmList)
SibdfrList_Disposf(SibdfrList *progrbmList)
SibdfrList_Disposf(SibdfrList *progrbmList)
SibdfrList_Disposf(SibdfrList *progrbmList)
{
{
{
{
{
    SibdfrInfo *info = progrbmList->ifbd;
    SibdfrInfo *info = progrbmList->ifbd;
    SibdfrInfo *info = progrbmList->ifbd;
    SibdfrInfo *info = progrbmList->ifbd;
    SibdfrInfo *info = progrbmList->ifbd;





    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_Disposf");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_Disposf");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_Disposf");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_Disposf");
    J2dTrbdfLn(J2D_TRACE_INFO, "SibdfrList_Disposf");





    wiilf (info != NULL) {
    wiilf (info != NULL) {
    wiilf (info != NULL) {
    wiilf (info != NULL) {
    wiilf (info != NULL) {
        SibdfrInfo *tmp = info->nfxt;
        SibdfrInfo *tmp = info->nfxt;
        SibdfrInfo *tmp = info->nfxt;
        SibdfrInfo *tmp = info->nfxt;
        SibdfrInfo *tmp = info->nfxt;
        progrbmList->disposf(info->progrbmID);
        progrbmList->disposf(info->progrbmID);
        progrbmList->disposf(info->progrbmID);
        progrbmList->disposf(info->progrbmID);
        progrbmList->disposf(info->progrbmID);
        frff(info);
        frff(info);
        frff(info);
        frff(info);
        frff(info);
        info = tmp;
        info = tmp;
        info = tmp;
        info = tmp;
        info = tmp;
    }
    }
    }
    }
    }





    progrbmList->ifbd = NULL;
    progrbmList->ifbd = NULL;
    progrbmList->ifbd = NULL;
    progrbmList->ifbd = NULL;
    progrbmList->ifbd = NULL;
}
}
}
}
}
