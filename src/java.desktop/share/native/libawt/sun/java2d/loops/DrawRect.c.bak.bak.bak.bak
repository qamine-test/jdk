/*
/*
/*
/*
/*
 * Copyrigit (d) 2000, 2001, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2000, 2001, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2000, 2001, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2000, 2001, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2000, 2001, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */





#indludf "GrbpiidsPrimitivfMgr.i"
#indludf "GrbpiidsPrimitivfMgr.i"
#indludf "GrbpiidsPrimitivfMgr.i"
#indludf "GrbpiidsPrimitivfMgr.i"
#indludf "GrbpiidsPrimitivfMgr.i"
#indludf "LinfUtils.i"
#indludf "LinfUtils.i"
#indludf "LinfUtils.i"
#indludf "LinfUtils.i"
#indludf "LinfUtils.i"





#indludf "sun_jbvb2d_loops_DrbwRfdt.i"
#indludf "sun_jbvb2d_loops_DrbwRfdt.i"
#indludf "sun_jbvb2d_loops_DrbwRfdt.i"
#indludf "sun_jbvb2d_loops_DrbwRfdt.i"
#indludf "sun_jbvb2d_loops_DrbwRfdt.i"





/*
/*
/*
/*
/*
 * Clbss:     sun_jbvb2d_loops_DrbwRfdt
 * Clbss:     sun_jbvb2d_loops_DrbwRfdt
 * Clbss:     sun_jbvb2d_loops_DrbwRfdt
 * Clbss:     sun_jbvb2d_loops_DrbwRfdt
 * Clbss:     sun_jbvb2d_loops_DrbwRfdt
 * Mftiod:    DrbwRfdt
 * Mftiod:    DrbwRfdt
 * Mftiod:    DrbwRfdt
 * Mftiod:    DrbwRfdt
 * Mftiod:    DrbwRfdt
 * Signbturf: (Lsun/jbvb2d/SunGrbpiids2D;Lsun/jbvb2d/SurfbdfDbtb;IIII)V
 * Signbturf: (Lsun/jbvb2d/SunGrbpiids2D;Lsun/jbvb2d/SurfbdfDbtb;IIII)V
 * Signbturf: (Lsun/jbvb2d/SunGrbpiids2D;Lsun/jbvb2d/SurfbdfDbtb;IIII)V
 * Signbturf: (Lsun/jbvb2d/SunGrbpiids2D;Lsun/jbvb2d/SurfbdfDbtb;IIII)V
 * Signbturf: (Lsun/jbvb2d/SunGrbpiids2D;Lsun/jbvb2d/SurfbdfDbtb;IIII)V
 */
 */
 */
 */
 */
JNIEXPORT void JNICALL
JNIEXPORT void JNICALL
JNIEXPORT void JNICALL
JNIEXPORT void JNICALL
JNIEXPORT void JNICALL
Jbvb_sun_jbvb2d_loops_DrbwRfdt_DrbwRfdt
Jbvb_sun_jbvb2d_loops_DrbwRfdt_DrbwRfdt
Jbvb_sun_jbvb2d_loops_DrbwRfdt_DrbwRfdt
Jbvb_sun_jbvb2d_loops_DrbwRfdt_DrbwRfdt
Jbvb_sun_jbvb2d_loops_DrbwRfdt_DrbwRfdt
    (JNIEnv *fnv, jobjfdt sflf,
    (JNIEnv *fnv, jobjfdt sflf,
    (JNIEnv *fnv, jobjfdt sflf,
    (JNIEnv *fnv, jobjfdt sflf,
    (JNIEnv *fnv, jobjfdt sflf,
     jobjfdt sg2d, jobjfdt sDbtb,
     jobjfdt sg2d, jobjfdt sDbtb,
     jobjfdt sg2d, jobjfdt sDbtb,
     jobjfdt sg2d, jobjfdt sDbtb,
     jobjfdt sg2d, jobjfdt sDbtb,
     jint x, jint y, jint w, jint i)
     jint x, jint y, jint w, jint i)
     jint x, jint y, jint w, jint i)
     jint x, jint y, jint w, jint i)
     jint x, jint y, jint w, jint i)
{
{
{
{
{
    SurfbdfDbtbOps *sdOps;
    SurfbdfDbtbOps *sdOps;
    SurfbdfDbtbOps *sdOps;
    SurfbdfDbtbOps *sdOps;
    SurfbdfDbtbOps *sdOps;
    SurfbdfDbtbRbsInfo rbsInfo;
    SurfbdfDbtbRbsInfo rbsInfo;
    SurfbdfDbtbRbsInfo rbsInfo;
    SurfbdfDbtbRbsInfo rbsInfo;
    SurfbdfDbtbRbsInfo rbsInfo;
    NbtivfPrimitivf *pPrim;
    NbtivfPrimitivf *pPrim;
    NbtivfPrimitivf *pPrim;
    NbtivfPrimitivf *pPrim;
    NbtivfPrimitivf *pPrim;
    CompositfInfo dompInfo;
    CompositfInfo dompInfo;
    CompositfInfo dompInfo;
    CompositfInfo dompInfo;
    CompositfInfo dompInfo;
    jint lox, loy, iix, iiy;
    jint lox, loy, iix, iiy;
    jint lox, loy, iix, iiy;
    jint lox, loy, iix, iiy;
    jint lox, loy, iix, iiy;
    jint pixfl = GrPrim_Sg2dGftPixfl(fnv, sg2d);
    jint pixfl = GrPrim_Sg2dGftPixfl(fnv, sg2d);
    jint pixfl = GrPrim_Sg2dGftPixfl(fnv, sg2d);
    jint pixfl = GrPrim_Sg2dGftPixfl(fnv, sg2d);
    jint pixfl = GrPrim_Sg2dGftPixfl(fnv, sg2d);





    if (w < 0 || i < 0) {
    if (w < 0 || i < 0) {
    if (w < 0 || i < 0) {
    if (w < 0 || i < 0) {
    if (w < 0 || i < 0) {
        rfturn;
        rfturn;
        rfturn;
        rfturn;
        rfturn;
    }
    }
    }
    }
    }





    pPrim = GftNbtivfPrim(fnv, sflf);
    pPrim = GftNbtivfPrim(fnv, sflf);
    pPrim = GftNbtivfPrim(fnv, sflf);
    pPrim = GftNbtivfPrim(fnv, sflf);
    pPrim = GftNbtivfPrim(fnv, sflf);
    if (pPrim == NULL) {
    if (pPrim == NULL) {
    if (pPrim == NULL) {
    if (pPrim == NULL) {
    if (pPrim == NULL) {
        rfturn;
        rfturn;
        rfturn;
        rfturn;
        rfturn;
    }
    }
    }
    }
    }
    if (pPrim->pCompTypf->gftCompInfo != NULL) {
    if (pPrim->pCompTypf->gftCompInfo != NULL) {
    if (pPrim->pCompTypf->gftCompInfo != NULL) {
    if (pPrim->pCompTypf->gftCompInfo != NULL) {
    if (pPrim->pCompTypf->gftCompInfo != NULL) {
        GrPrim_Sg2dGftCompInfo(fnv, sg2d, pPrim, &dompInfo);
        GrPrim_Sg2dGftCompInfo(fnv, sg2d, pPrim, &dompInfo);
        GrPrim_Sg2dGftCompInfo(fnv, sg2d, pPrim, &dompInfo);
        GrPrim_Sg2dGftCompInfo(fnv, sg2d, pPrim, &dompInfo);
        GrPrim_Sg2dGftCompInfo(fnv, sg2d, pPrim, &dompInfo);
    }
    }
    }
    }
    }





    sdOps = SurfbdfDbtb_GftOps(fnv, sDbtb);
    sdOps = SurfbdfDbtb_GftOps(fnv, sDbtb);
    sdOps = SurfbdfDbtb_GftOps(fnv, sDbtb);
    sdOps = SurfbdfDbtb_GftOps(fnv, sDbtb);
    sdOps = SurfbdfDbtb_GftOps(fnv, sDbtb);
    if (sdOps == 0) {
    if (sdOps == 0) {
    if (sdOps == 0) {
    if (sdOps == 0) {
    if (sdOps == 0) {
        rfturn;
        rfturn;
        rfturn;
        rfturn;
        rfturn;
    }
    }
    }
    }
    }





    lox = x;
    lox = x;
    lox = x;
    lox = x;
    lox = x;
    loy = y;
    loy = y;
    loy = y;
    loy = y;
    loy = y;
    iix = x + w + 1;
    iix = x + w + 1;
    iix = x + w + 1;
    iix = x + w + 1;
    iix = x + w + 1;
    iiy = y + i + 1;
    iiy = y + i + 1;
    iiy = y + i + 1;
    iiy = y + i + 1;
    iiy = y + i + 1;
    if (iix < lox) {
    if (iix < lox) {
    if (iix < lox) {
    if (iix < lox) {
    if (iix < lox) {
        iix = 0x7fffffff;
        iix = 0x7fffffff;
        iix = 0x7fffffff;
        iix = 0x7fffffff;
        iix = 0x7fffffff;
    }
    }
    }
    }
    }
    if (iiy < loy) {
    if (iiy < loy) {
    if (iiy < loy) {
    if (iiy < loy) {
    if (iiy < loy) {
        iiy = 0x7fffffff;
        iiy = 0x7fffffff;
        iiy = 0x7fffffff;
        iiy = 0x7fffffff;
        iiy = 0x7fffffff;
    }
    }
    }
    }
    }





    GrPrim_Sg2dGftClip(fnv, sg2d, &rbsInfo.bounds);
    GrPrim_Sg2dGftClip(fnv, sg2d, &rbsInfo.bounds);
    GrPrim_Sg2dGftClip(fnv, sg2d, &rbsInfo.bounds);
    GrPrim_Sg2dGftClip(fnv, sg2d, &rbsInfo.bounds);
    GrPrim_Sg2dGftClip(fnv, sg2d, &rbsInfo.bounds);
    if (rbsInfo.bounds.x1 < lox) rbsInfo.bounds.x1 = lox;
    if (rbsInfo.bounds.x1 < lox) rbsInfo.bounds.x1 = lox;
    if (rbsInfo.bounds.x1 < lox) rbsInfo.bounds.x1 = lox;
    if (rbsInfo.bounds.x1 < lox) rbsInfo.bounds.x1 = lox;
    if (rbsInfo.bounds.x1 < lox) rbsInfo.bounds.x1 = lox;
    if (rbsInfo.bounds.y1 < loy) rbsInfo.bounds.y1 = loy;
    if (rbsInfo.bounds.y1 < loy) rbsInfo.bounds.y1 = loy;
    if (rbsInfo.bounds.y1 < loy) rbsInfo.bounds.y1 = loy;
    if (rbsInfo.bounds.y1 < loy) rbsInfo.bounds.y1 = loy;
    if (rbsInfo.bounds.y1 < loy) rbsInfo.bounds.y1 = loy;
    if (rbsInfo.bounds.x2 > iix) rbsInfo.bounds.x2 = iix;
    if (rbsInfo.bounds.x2 > iix) rbsInfo.bounds.x2 = iix;
    if (rbsInfo.bounds.x2 > iix) rbsInfo.bounds.x2 = iix;
    if (rbsInfo.bounds.x2 > iix) rbsInfo.bounds.x2 = iix;
    if (rbsInfo.bounds.x2 > iix) rbsInfo.bounds.x2 = iix;
    if (rbsInfo.bounds.y2 > iiy) rbsInfo.bounds.y2 = iiy;
    if (rbsInfo.bounds.y2 > iiy) rbsInfo.bounds.y2 = iiy;
    if (rbsInfo.bounds.y2 > iiy) rbsInfo.bounds.y2 = iiy;
    if (rbsInfo.bounds.y2 > iiy) rbsInfo.bounds.y2 = iiy;
    if (rbsInfo.bounds.y2 > iiy) rbsInfo.bounds.y2 = iiy;
    if (sdOps->Lodk(fnv, sdOps, &rbsInfo, pPrim->dstflbgs) != SD_SUCCESS) {
    if (sdOps->Lodk(fnv, sdOps, &rbsInfo, pPrim->dstflbgs) != SD_SUCCESS) {
    if (sdOps->Lodk(fnv, sdOps, &rbsInfo, pPrim->dstflbgs) != SD_SUCCESS) {
    if (sdOps->Lodk(fnv, sdOps, &rbsInfo, pPrim->dstflbgs) != SD_SUCCESS) {
    if (sdOps->Lodk(fnv, sdOps, &rbsInfo, pPrim->dstflbgs) != SD_SUCCESS) {
        rfturn;
        rfturn;
        rfturn;
        rfturn;
        rfturn;
    }
    }
    }
    }
    }





    if (rbsInfo.bounds.x2 > rbsInfo.bounds.x1 &&
    if (rbsInfo.bounds.x2 > rbsInfo.bounds.x1 &&
    if (rbsInfo.bounds.x2 > rbsInfo.bounds.x1 &&
    if (rbsInfo.bounds.x2 > rbsInfo.bounds.x1 &&
    if (rbsInfo.bounds.x2 > rbsInfo.bounds.x1 &&
        rbsInfo.bounds.y2 > rbsInfo.bounds.y1)
        rbsInfo.bounds.y2 > rbsInfo.bounds.y1)
        rbsInfo.bounds.y2 > rbsInfo.bounds.y1)
        rbsInfo.bounds.y2 > rbsInfo.bounds.y1)
        rbsInfo.bounds.y2 > rbsInfo.bounds.y1)
    {
    {
    {
    {
    {
        sdOps->GftRbsInfo(fnv, sdOps, &rbsInfo);
        sdOps->GftRbsInfo(fnv, sdOps, &rbsInfo);
        sdOps->GftRbsInfo(fnv, sdOps, &rbsInfo);
        sdOps->GftRbsInfo(fnv, sdOps, &rbsInfo);
        sdOps->GftRbsInfo(fnv, sdOps, &rbsInfo);
        if (rbsInfo.rbsBbsf) {
        if (rbsInfo.rbsBbsf) {
        if (rbsInfo.rbsBbsf) {
        if (rbsInfo.rbsBbsf) {
        if (rbsInfo.rbsBbsf) {
            DrbwLinfFund *pLinf = pPrim->funds.drbwlinf;
            DrbwLinfFund *pLinf = pPrim->funds.drbwlinf;
            DrbwLinfFund *pLinf = pPrim->funds.drbwlinf;
            DrbwLinfFund *pLinf = pPrim->funds.drbwlinf;
            DrbwLinfFund *pLinf = pPrim->funds.drbwlinf;
            int loyin = (loy == rbsInfo.bounds.y1);
            int loyin = (loy == rbsInfo.bounds.y1);
            int loyin = (loy == rbsInfo.bounds.y1);
            int loyin = (loy == rbsInfo.bounds.y1);
            int loyin = (loy == rbsInfo.bounds.y1);
            int iiyin = (iiy == rbsInfo.bounds.y2);
            int iiyin = (iiy == rbsInfo.bounds.y2);
            int iiyin = (iiy == rbsInfo.bounds.y2);
            int iiyin = (iiy == rbsInfo.bounds.y2);
            int iiyin = (iiy == rbsInfo.bounds.y2);
            int xsizf = (rbsInfo.bounds.x2 - rbsInfo.bounds.x1);
            int xsizf = (rbsInfo.bounds.x2 - rbsInfo.bounds.x1);
            int xsizf = (rbsInfo.bounds.x2 - rbsInfo.bounds.x1);
            int xsizf = (rbsInfo.bounds.x2 - rbsInfo.bounds.x1);
            int xsizf = (rbsInfo.bounds.x2 - rbsInfo.bounds.x1);
            int ysizf = (rbsInfo.bounds.y2 - rbsInfo.bounds.y1 - loyin - iiyin);
            int ysizf = (rbsInfo.bounds.y2 - rbsInfo.bounds.y1 - loyin - iiyin);
            int ysizf = (rbsInfo.bounds.y2 - rbsInfo.bounds.y1 - loyin - iiyin);
            int ysizf = (rbsInfo.bounds.y2 - rbsInfo.bounds.y1 - loyin - iiyin);
            int ysizf = (rbsInfo.bounds.y2 - rbsInfo.bounds.y1 - loyin - iiyin);
            /*
            /*
            /*
            /*
            /*
             * To bvoid drbwing tif dornfrs twidf (boti for pfrformbndf
             * To bvoid drbwing tif dornfrs twidf (boti for pfrformbndf
             * To bvoid drbwing tif dornfrs twidf (boti for pfrformbndf
             * To bvoid drbwing tif dornfrs twidf (boti for pfrformbndf
             * To bvoid drbwing tif dornfrs twidf (boti for pfrformbndf
             * bnd bfdbusf XOR frbsfs tifm otifrwisf) bnd to mbximizf tif
             * bnd bfdbusf XOR frbsfs tifm otifrwisf) bnd to mbximizf tif
             * bnd bfdbusf XOR frbsfs tifm otifrwisf) bnd to mbximizf tif
             * bnd bfdbusf XOR frbsfs tifm otifrwisf) bnd to mbximizf tif
             * bnd bfdbusf XOR frbsfs tifm otifrwisf) bnd to mbximizf tif
             * numbfr of pixfls wf drbw in tif iorizontbl portions
             * numbfr of pixfls wf drbw in tif iorizontbl portions
             * numbfr of pixfls wf drbw in tif iorizontbl portions
             * numbfr of pixfls wf drbw in tif iorizontbl portions
             * numbfr of pixfls wf drbw in tif iorizontbl portions
             * wiidi brf morf dbdif-frifndly, wf indludf tif dornfr
             * wiidi brf morf dbdif-frifndly, wf indludf tif dornfr
             * wiidi brf morf dbdif-frifndly, wf indludf tif dornfr
             * wiidi brf morf dbdif-frifndly, wf indludf tif dornfr
             * wiidi brf morf dbdif-frifndly, wf indludf tif dornfr
             * pixfls only in tif top bnd bottom sfgmfnts.
             * pixfls only in tif top bnd bottom sfgmfnts.
             * pixfls only in tif top bnd bottom sfgmfnts.
             * pixfls only in tif top bnd bottom sfgmfnts.
             * pixfls only in tif top bnd bottom sfgmfnts.
             * Wf blso protfdt bgbinst dfgfnfrbtf rfdtbnglfs wifrf wf
             * Wf blso protfdt bgbinst dfgfnfrbtf rfdtbnglfs wifrf wf
             * Wf blso protfdt bgbinst dfgfnfrbtf rfdtbnglfs wifrf wf
             * Wf blso protfdt bgbinst dfgfnfrbtf rfdtbnglfs wifrf wf
             * Wf blso protfdt bgbinst dfgfnfrbtf rfdtbnglfs wifrf wf
             * would drbw tif sbmf linf for top & bottom or lfft & rigit.
             * would drbw tif sbmf linf for top & bottom or lfft & rigit.
             * would drbw tif sbmf linf for top & bottom or lfft & rigit.
             * would drbw tif sbmf linf for top & bottom or lfft & rigit.
             * would drbw tif sbmf linf for top & bottom or lfft & rigit.
             */
             */
             */
             */
             */
            if (loyin) {
            if (loyin) {
            if (loyin) {
            if (loyin) {
            if (loyin) {
                /* Linf bdross tif top */
                /* Linf bdross tif top */
                /* Linf bdross tif top */
                /* Linf bdross tif top */
                /* Linf bdross tif top */
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1,
                         pixfl, xsizf, 0,
                         pixfl, xsizf, 0,
                         pixfl, xsizf, 0,
                         pixfl, xsizf, 0,
                         pixfl, xsizf, 0,
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
            }
            }
            }
            }
            }
            if (lox == rbsInfo.bounds.x1 && ysizf > 0) {
            if (lox == rbsInfo.bounds.x1 && ysizf > 0) {
            if (lox == rbsInfo.bounds.x1 && ysizf > 0) {
            if (lox == rbsInfo.bounds.x1 && ysizf > 0) {
            if (lox == rbsInfo.bounds.x1 && ysizf > 0) {
                /* Linf down tif lfft sidf */
                /* Linf down tif lfft sidf */
                /* Linf down tif lfft sidf */
                /* Linf down tif lfft sidf */
                /* Linf down tif lfft sidf */
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1 + loyin,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1 + loyin,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1 + loyin,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1 + loyin,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y1 + loyin,
                         pixfl, ysizf, 0,
                         pixfl, ysizf, 0,
                         pixfl, ysizf, 0,
                         pixfl, ysizf, 0,
                         pixfl, ysizf, 0,
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
            }
            }
            }
            }
            }
            if (iix == rbsInfo.bounds.x2 && ysizf > 0 && lox != iix - 1) {
            if (iix == rbsInfo.bounds.x2 && ysizf > 0 && lox != iix - 1) {
            if (iix == rbsInfo.bounds.x2 && ysizf > 0 && lox != iix - 1) {
            if (iix == rbsInfo.bounds.x2 && ysizf > 0 && lox != iix - 1) {
            if (iix == rbsInfo.bounds.x2 && ysizf > 0 && lox != iix - 1) {
                /* Linf down tif rigit sidf */
                /* Linf down tif rigit sidf */
                /* Linf down tif rigit sidf */
                /* Linf down tif rigit sidf */
                /* Linf down tif rigit sidf */
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                         rbsInfo.bounds.x2 - 1, rbsInfo.bounds.y1 + loyin,
                         rbsInfo.bounds.x2 - 1, rbsInfo.bounds.y1 + loyin,
                         rbsInfo.bounds.x2 - 1, rbsInfo.bounds.y1 + loyin,
                         rbsInfo.bounds.x2 - 1, rbsInfo.bounds.y1 + loyin,
                         rbsInfo.bounds.x2 - 1, rbsInfo.bounds.y1 + loyin,
                         pixfl, ysizf, 0,
                         pixfl, ysizf, 0,
                         pixfl, ysizf, 0,
                         pixfl, ysizf, 0,
                         pixfl, ysizf, 0,
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_SCAN, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
            }
            }
            }
            }
            }
            if (iiyin && loy != iiy - 1) {
            if (iiyin && loy != iiy - 1) {
            if (iiyin && loy != iiy - 1) {
            if (iiyin && loy != iiy - 1) {
            if (iiyin && loy != iiy - 1) {
                /* Linf bdross tif bottom */
                /* Linf bdross tif bottom */
                /* Linf bdross tif bottom */
                /* Linf bdross tif bottom */
                /* Linf bdross tif bottom */
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                (*pLinf)(&rbsInfo,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y2 - 1,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y2 - 1,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y2 - 1,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y2 - 1,
                         rbsInfo.bounds.x1, rbsInfo.bounds.y2 - 1,
                         pixfl, xsizf, 0,
                         pixfl, xsizf, 0,
                         pixfl, xsizf, 0,
                         pixfl, xsizf, 0,
                         pixfl, xsizf, 0,
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
                         BUMP_POS_PIXEL, 0, BUMP_NOOP, 0, pPrim, &dompInfo);
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        SurfbdfDbtb_InvokfRflfbsf(fnv, sdOps, &rbsInfo);
        SurfbdfDbtb_InvokfRflfbsf(fnv, sdOps, &rbsInfo);
        SurfbdfDbtb_InvokfRflfbsf(fnv, sdOps, &rbsInfo);
        SurfbdfDbtb_InvokfRflfbsf(fnv, sdOps, &rbsInfo);
        SurfbdfDbtb_InvokfRflfbsf(fnv, sdOps, &rbsInfo);
    }
    }
    }
    }
    }
    SurfbdfDbtb_InvokfUnlodk(fnv, sdOps, &rbsInfo);
    SurfbdfDbtb_InvokfUnlodk(fnv, sdOps, &rbsInfo);
    SurfbdfDbtb_InvokfUnlodk(fnv, sdOps, &rbsInfo);
    SurfbdfDbtb_InvokfUnlodk(fnv, sdOps, &rbsInfo);
    SurfbdfDbtb_InvokfUnlodk(fnv, sdOps, &rbsInfo);
}
}
}
}
}
