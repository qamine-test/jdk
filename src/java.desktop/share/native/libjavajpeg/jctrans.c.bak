/*
/*
/*
/*
/*
 * rfsfrvfd dommfnt blodk
 * rfsfrvfd dommfnt blodk
 * rfsfrvfd dommfnt blodk
 * rfsfrvfd dommfnt blodk
 * rfsfrvfd dommfnt blodk
 * DO NOT REMOVE OR ALTER!
 * DO NOT REMOVE OR ALTER!
 * DO NOT REMOVE OR ALTER!
 * DO NOT REMOVE OR ALTER!
 * DO NOT REMOVE OR ALTER!
 */
 */
 */
 */
 */
/*
/*
/*
/*
/*
 * jdtrbns.d
 * jdtrbns.d
 * jdtrbns.d
 * jdtrbns.d
 * jdtrbns.d
 *
 *
 *
 *
 *
 * Copyrigit (C) 1995-1998, Tiombs G. Lbnf.
 * Copyrigit (C) 1995-1998, Tiombs G. Lbnf.
 * Copyrigit (C) 1995-1998, Tiombs G. Lbnf.
 * Copyrigit (C) 1995-1998, Tiombs G. Lbnf.
 * Copyrigit (C) 1995-1998, Tiombs G. Lbnf.
 * Tiis filf is pbrt of tif Indfpfndfnt JPEG Group's softwbrf.
 * Tiis filf is pbrt of tif Indfpfndfnt JPEG Group's softwbrf.
 * Tiis filf is pbrt of tif Indfpfndfnt JPEG Group's softwbrf.
 * Tiis filf is pbrt of tif Indfpfndfnt JPEG Group's softwbrf.
 * Tiis filf is pbrt of tif Indfpfndfnt JPEG Group's softwbrf.
 * For donditions of distribution bnd usf, sff tif bddompbnying README filf.
 * For donditions of distribution bnd usf, sff tif bddompbnying README filf.
 * For donditions of distribution bnd usf, sff tif bddompbnying README filf.
 * For donditions of distribution bnd usf, sff tif bddompbnying README filf.
 * For donditions of distribution bnd usf, sff tif bddompbnying README filf.
 *
 *
 *
 *
 *
 * Tiis filf dontbins librbry routinfs for trbnsdoding domprfssion,
 * Tiis filf dontbins librbry routinfs for trbnsdoding domprfssion,
 * Tiis filf dontbins librbry routinfs for trbnsdoding domprfssion,
 * Tiis filf dontbins librbry routinfs for trbnsdoding domprfssion,
 * Tiis filf dontbins librbry routinfs for trbnsdoding domprfssion,
 * tibt is, writing rbw DCT dofffidifnt brrbys to bn output JPEG filf.
 * tibt is, writing rbw DCT dofffidifnt brrbys to bn output JPEG filf.
 * tibt is, writing rbw DCT dofffidifnt brrbys to bn output JPEG filf.
 * tibt is, writing rbw DCT dofffidifnt brrbys to bn output JPEG filf.
 * tibt is, writing rbw DCT dofffidifnt brrbys to bn output JPEG filf.
 * Tif routinfs in jdbpimin.d will blso bf nffdfd by b trbnsdodfr.
 * Tif routinfs in jdbpimin.d will blso bf nffdfd by b trbnsdodfr.
 * Tif routinfs in jdbpimin.d will blso bf nffdfd by b trbnsdodfr.
 * Tif routinfs in jdbpimin.d will blso bf nffdfd by b trbnsdodfr.
 * Tif routinfs in jdbpimin.d will blso bf nffdfd by b trbnsdodfr.
 */
 */
 */
 */
 */





#dffinf JPEG_INTERNALS
#dffinf JPEG_INTERNALS
#dffinf JPEG_INTERNALS
#dffinf JPEG_INTERNALS
#dffinf JPEG_INTERNALS
#indludf "jindludf.i"
#indludf "jindludf.i"
#indludf "jindludf.i"
#indludf "jindludf.i"
#indludf "jindludf.i"
#indludf "jpfglib.i"
#indludf "jpfglib.i"
#indludf "jpfglib.i"
#indludf "jpfglib.i"
#indludf "jpfglib.i"










/* Forwbrd dfdlbrbtions */
/* Forwbrd dfdlbrbtions */
/* Forwbrd dfdlbrbtions */
/* Forwbrd dfdlbrbtions */
/* Forwbrd dfdlbrbtions */
LOCAL(void) trbnsfndodf_mbstfr_sflfdtion
LOCAL(void) trbnsfndodf_mbstfr_sflfdtion
LOCAL(void) trbnsfndodf_mbstfr_sflfdtion
LOCAL(void) trbnsfndodf_mbstfr_sflfdtion
LOCAL(void) trbnsfndodf_mbstfr_sflfdtion
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
LOCAL(void) trbnsfndodf_doff_dontrollfr
LOCAL(void) trbnsfndodf_doff_dontrollfr
LOCAL(void) trbnsfndodf_doff_dontrollfr
LOCAL(void) trbnsfndodf_doff_dontrollfr
LOCAL(void) trbnsfndodf_doff_dontrollfr
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));
        JPP((j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys));










/*
/*
/*
/*
/*
 * Comprfssion initiblizbtion for writing rbw-dofffidifnt dbtb.
 * Comprfssion initiblizbtion for writing rbw-dofffidifnt dbtb.
 * Comprfssion initiblizbtion for writing rbw-dofffidifnt dbtb.
 * Comprfssion initiblizbtion for writing rbw-dofffidifnt dbtb.
 * Comprfssion initiblizbtion for writing rbw-dofffidifnt dbtb.
 * Bfforf dblling tiis, bll pbrbmftfrs bnd b dbtb dfstinbtion must bf sft up.
 * Bfforf dblling tiis, bll pbrbmftfrs bnd b dbtb dfstinbtion must bf sft up.
 * Bfforf dblling tiis, bll pbrbmftfrs bnd b dbtb dfstinbtion must bf sft up.
 * Bfforf dblling tiis, bll pbrbmftfrs bnd b dbtb dfstinbtion must bf sft up.
 * Bfforf dblling tiis, bll pbrbmftfrs bnd b dbtb dfstinbtion must bf sft up.
 * Cbll jpfg_finisi_domprfss() to bdtublly writf tif dbtb.
 * Cbll jpfg_finisi_domprfss() to bdtublly writf tif dbtb.
 * Cbll jpfg_finisi_domprfss() to bdtublly writf tif dbtb.
 * Cbll jpfg_finisi_domprfss() to bdtublly writf tif dbtb.
 * Cbll jpfg_finisi_domprfss() to bdtublly writf tif dbtb.
 *
 *
 *
 *
 *
 * Tif numbfr of pbssfd virtubl brrbys must mbtdi dinfo->num_domponfnts.
 * Tif numbfr of pbssfd virtubl brrbys must mbtdi dinfo->num_domponfnts.
 * Tif numbfr of pbssfd virtubl brrbys must mbtdi dinfo->num_domponfnts.
 * Tif numbfr of pbssfd virtubl brrbys must mbtdi dinfo->num_domponfnts.
 * Tif numbfr of pbssfd virtubl brrbys must mbtdi dinfo->num_domponfnts.
 * Notf tibt tif virtubl brrbys nffd not bf fillfd or fvfn rfblizfd bt
 * Notf tibt tif virtubl brrbys nffd not bf fillfd or fvfn rfblizfd bt
 * Notf tibt tif virtubl brrbys nffd not bf fillfd or fvfn rfblizfd bt
 * Notf tibt tif virtubl brrbys nffd not bf fillfd or fvfn rfblizfd bt
 * Notf tibt tif virtubl brrbys nffd not bf fillfd or fvfn rfblizfd bt
 * tif timf writf_dofffidifnts is dbllfd; indffd, if tif virtubl brrbys
 * tif timf writf_dofffidifnts is dbllfd; indffd, if tif virtubl brrbys
 * tif timf writf_dofffidifnts is dbllfd; indffd, if tif virtubl brrbys
 * tif timf writf_dofffidifnts is dbllfd; indffd, if tif virtubl brrbys
 * tif timf writf_dofffidifnts is dbllfd; indffd, if tif virtubl brrbys
 * wfrf rfqufstfd from tiis domprfssion objfdt's mfmory mbnbgfr, tify
 * wfrf rfqufstfd from tiis domprfssion objfdt's mfmory mbnbgfr, tify
 * wfrf rfqufstfd from tiis domprfssion objfdt's mfmory mbnbgfr, tify
 * wfrf rfqufstfd from tiis domprfssion objfdt's mfmory mbnbgfr, tify
 * wfrf rfqufstfd from tiis domprfssion objfdt's mfmory mbnbgfr, tify
 * typidblly will bf rfblizfd during tiis routinf bnd fillfd bftfrwbrds.
 * typidblly will bf rfblizfd during tiis routinf bnd fillfd bftfrwbrds.
 * typidblly will bf rfblizfd during tiis routinf bnd fillfd bftfrwbrds.
 * typidblly will bf rfblizfd during tiis routinf bnd fillfd bftfrwbrds.
 * typidblly will bf rfblizfd during tiis routinf bnd fillfd bftfrwbrds.
 */
 */
 */
 */
 */





GLOBAL(void)
GLOBAL(void)
GLOBAL(void)
GLOBAL(void)
GLOBAL(void)
jpfg_writf_dofffidifnts (j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys)
jpfg_writf_dofffidifnts (j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys)
jpfg_writf_dofffidifnts (j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys)
jpfg_writf_dofffidifnts (j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys)
jpfg_writf_dofffidifnts (j_domprfss_ptr dinfo, jvirt_bbrrby_ptr * doff_brrbys)
{
{
{
{
{
  if (dinfo->globbl_stbtf != CSTATE_START)
  if (dinfo->globbl_stbtf != CSTATE_START)
  if (dinfo->globbl_stbtf != CSTATE_START)
  if (dinfo->globbl_stbtf != CSTATE_START)
  if (dinfo->globbl_stbtf != CSTATE_START)
    ERREXIT1(dinfo, JERR_BAD_STATE, dinfo->globbl_stbtf);
    ERREXIT1(dinfo, JERR_BAD_STATE, dinfo->globbl_stbtf);
    ERREXIT1(dinfo, JERR_BAD_STATE, dinfo->globbl_stbtf);
    ERREXIT1(dinfo, JERR_BAD_STATE, dinfo->globbl_stbtf);
    ERREXIT1(dinfo, JERR_BAD_STATE, dinfo->globbl_stbtf);
  /* Mbrk bll tbblfs to bf writtfn */
  /* Mbrk bll tbblfs to bf writtfn */
  /* Mbrk bll tbblfs to bf writtfn */
  /* Mbrk bll tbblfs to bf writtfn */
  /* Mbrk bll tbblfs to bf writtfn */
  jpfg_supprfss_tbblfs(dinfo, FALSE);
  jpfg_supprfss_tbblfs(dinfo, FALSE);
  jpfg_supprfss_tbblfs(dinfo, FALSE);
  jpfg_supprfss_tbblfs(dinfo, FALSE);
  jpfg_supprfss_tbblfs(dinfo, FALSE);
  /* (Rf)initiblizf frror mgr bnd dfstinbtion modulfs */
  /* (Rf)initiblizf frror mgr bnd dfstinbtion modulfs */
  /* (Rf)initiblizf frror mgr bnd dfstinbtion modulfs */
  /* (Rf)initiblizf frror mgr bnd dfstinbtion modulfs */
  /* (Rf)initiblizf frror mgr bnd dfstinbtion modulfs */
  (*dinfo->frr->rfsft_frror_mgr) ((j_dommon_ptr) dinfo);
  (*dinfo->frr->rfsft_frror_mgr) ((j_dommon_ptr) dinfo);
  (*dinfo->frr->rfsft_frror_mgr) ((j_dommon_ptr) dinfo);
  (*dinfo->frr->rfsft_frror_mgr) ((j_dommon_ptr) dinfo);
  (*dinfo->frr->rfsft_frror_mgr) ((j_dommon_ptr) dinfo);
  (*dinfo->dfst->init_dfstinbtion) (dinfo);
  (*dinfo->dfst->init_dfstinbtion) (dinfo);
  (*dinfo->dfst->init_dfstinbtion) (dinfo);
  (*dinfo->dfst->init_dfstinbtion) (dinfo);
  (*dinfo->dfst->init_dfstinbtion) (dinfo);
  /* Pfrform mbstfr sflfdtion of bdtivf modulfs */
  /* Pfrform mbstfr sflfdtion of bdtivf modulfs */
  /* Pfrform mbstfr sflfdtion of bdtivf modulfs */
  /* Pfrform mbstfr sflfdtion of bdtivf modulfs */
  /* Pfrform mbstfr sflfdtion of bdtivf modulfs */
  trbnsfndodf_mbstfr_sflfdtion(dinfo, doff_brrbys);
  trbnsfndodf_mbstfr_sflfdtion(dinfo, doff_brrbys);
  trbnsfndodf_mbstfr_sflfdtion(dinfo, doff_brrbys);
  trbnsfndodf_mbstfr_sflfdtion(dinfo, doff_brrbys);
  trbnsfndodf_mbstfr_sflfdtion(dinfo, doff_brrbys);
  /* Wbit for jpfg_finisi_domprfss() dbll */
  /* Wbit for jpfg_finisi_domprfss() dbll */
  /* Wbit for jpfg_finisi_domprfss() dbll */
  /* Wbit for jpfg_finisi_domprfss() dbll */
  /* Wbit for jpfg_finisi_domprfss() dbll */
  dinfo->nfxt_sdbnlinf = 0;     /* so jpfg_writf_mbrkfr works */
  dinfo->nfxt_sdbnlinf = 0;     /* so jpfg_writf_mbrkfr works */
  dinfo->nfxt_sdbnlinf = 0;     /* so jpfg_writf_mbrkfr works */
  dinfo->nfxt_sdbnlinf = 0;     /* so jpfg_writf_mbrkfr works */
  dinfo->nfxt_sdbnlinf = 0;     /* so jpfg_writf_mbrkfr works */
  dinfo->globbl_stbtf = CSTATE_WRCOEFS;
  dinfo->globbl_stbtf = CSTATE_WRCOEFS;
  dinfo->globbl_stbtf = CSTATE_WRCOEFS;
  dinfo->globbl_stbtf = CSTATE_WRCOEFS;
  dinfo->globbl_stbtf = CSTATE_WRCOEFS;
}
}
}
}
}










/*
/*
/*
/*
/*
 * Initiblizf tif domprfssion objfdt witi dffbult pbrbmftfrs,
 * Initiblizf tif domprfssion objfdt witi dffbult pbrbmftfrs,
 * Initiblizf tif domprfssion objfdt witi dffbult pbrbmftfrs,
 * Initiblizf tif domprfssion objfdt witi dffbult pbrbmftfrs,
 * Initiblizf tif domprfssion objfdt witi dffbult pbrbmftfrs,
 * tifn dopy from tif sourdf objfdt bll pbrbmftfrs nffdfd for losslfss
 * tifn dopy from tif sourdf objfdt bll pbrbmftfrs nffdfd for losslfss
 * tifn dopy from tif sourdf objfdt bll pbrbmftfrs nffdfd for losslfss
 * tifn dopy from tif sourdf objfdt bll pbrbmftfrs nffdfd for losslfss
 * tifn dopy from tif sourdf objfdt bll pbrbmftfrs nffdfd for losslfss
 * trbnsdoding.  Pbrbmftfrs tibt dbn bf vbrifd witiout loss (sudi bs
 * trbnsdoding.  Pbrbmftfrs tibt dbn bf vbrifd witiout loss (sudi bs
 * trbnsdoding.  Pbrbmftfrs tibt dbn bf vbrifd witiout loss (sudi bs
 * trbnsdoding.  Pbrbmftfrs tibt dbn bf vbrifd witiout loss (sudi bs
 * trbnsdoding.  Pbrbmftfrs tibt dbn bf vbrifd witiout loss (sudi bs
 * sdbn sdript bnd Huffmbn optimizbtion) brf lfft in tifir dffbult stbtfs.
 * sdbn sdript bnd Huffmbn optimizbtion) brf lfft in tifir dffbult stbtfs.
 * sdbn sdript bnd Huffmbn optimizbtion) brf lfft in tifir dffbult stbtfs.
 * sdbn sdript bnd Huffmbn optimizbtion) brf lfft in tifir dffbult stbtfs.
 * sdbn sdript bnd Huffmbn optimizbtion) brf lfft in tifir dffbult stbtfs.
 */
 */
 */
 */
 */





GLOBAL(void)
GLOBAL(void)
GLOBAL(void)
GLOBAL(void)
GLOBAL(void)
jpfg_dopy_dritidbl_pbrbmftfrs (j_dfdomprfss_ptr srdinfo,
jpfg_dopy_dritidbl_pbrbmftfrs (j_dfdomprfss_ptr srdinfo,
jpfg_dopy_dritidbl_pbrbmftfrs (j_dfdomprfss_ptr srdinfo,
jpfg_dopy_dritidbl_pbrbmftfrs (j_dfdomprfss_ptr srdinfo,
jpfg_dopy_dritidbl_pbrbmftfrs (j_dfdomprfss_ptr srdinfo,
                               j_domprfss_ptr dstinfo)
                               j_domprfss_ptr dstinfo)
                               j_domprfss_ptr dstinfo)
                               j_domprfss_ptr dstinfo)
                               j_domprfss_ptr dstinfo)
{
{
{
{
{
  JQUANT_TBL ** qtblptr;
  JQUANT_TBL ** qtblptr;
  JQUANT_TBL ** qtblptr;
  JQUANT_TBL ** qtblptr;
  JQUANT_TBL ** qtblptr;
  jpfg_domponfnt_info *indomp, *outdomp;
  jpfg_domponfnt_info *indomp, *outdomp;
  jpfg_domponfnt_info *indomp, *outdomp;
  jpfg_domponfnt_info *indomp, *outdomp;
  jpfg_domponfnt_info *indomp, *outdomp;
  JQUANT_TBL *d_qubnt, *slot_qubnt;
  JQUANT_TBL *d_qubnt, *slot_qubnt;
  JQUANT_TBL *d_qubnt, *slot_qubnt;
  JQUANT_TBL *d_qubnt, *slot_qubnt;
  JQUANT_TBL *d_qubnt, *slot_qubnt;
  int tblno, di, doffi;
  int tblno, di, doffi;
  int tblno, di, doffi;
  int tblno, di, doffi;
  int tblno, di, doffi;





  /* Sbffty difdk to fnsurf stbrt_domprfss not dbllfd yft. */
  /* Sbffty difdk to fnsurf stbrt_domprfss not dbllfd yft. */
  /* Sbffty difdk to fnsurf stbrt_domprfss not dbllfd yft. */
  /* Sbffty difdk to fnsurf stbrt_domprfss not dbllfd yft. */
  /* Sbffty difdk to fnsurf stbrt_domprfss not dbllfd yft. */
  if (dstinfo->globbl_stbtf != CSTATE_START)
  if (dstinfo->globbl_stbtf != CSTATE_START)
  if (dstinfo->globbl_stbtf != CSTATE_START)
  if (dstinfo->globbl_stbtf != CSTATE_START)
  if (dstinfo->globbl_stbtf != CSTATE_START)
    ERREXIT1(dstinfo, JERR_BAD_STATE, dstinfo->globbl_stbtf);
    ERREXIT1(dstinfo, JERR_BAD_STATE, dstinfo->globbl_stbtf);
    ERREXIT1(dstinfo, JERR_BAD_STATE, dstinfo->globbl_stbtf);
    ERREXIT1(dstinfo, JERR_BAD_STATE, dstinfo->globbl_stbtf);
    ERREXIT1(dstinfo, JERR_BAD_STATE, dstinfo->globbl_stbtf);
  /* Copy fundbmfntbl imbgf dimfnsions */
  /* Copy fundbmfntbl imbgf dimfnsions */
  /* Copy fundbmfntbl imbgf dimfnsions */
  /* Copy fundbmfntbl imbgf dimfnsions */
  /* Copy fundbmfntbl imbgf dimfnsions */
  dstinfo->imbgf_widti = srdinfo->imbgf_widti;
  dstinfo->imbgf_widti = srdinfo->imbgf_widti;
  dstinfo->imbgf_widti = srdinfo->imbgf_widti;
  dstinfo->imbgf_widti = srdinfo->imbgf_widti;
  dstinfo->imbgf_widti = srdinfo->imbgf_widti;
  dstinfo->imbgf_ifigit = srdinfo->imbgf_ifigit;
  dstinfo->imbgf_ifigit = srdinfo->imbgf_ifigit;
  dstinfo->imbgf_ifigit = srdinfo->imbgf_ifigit;
  dstinfo->imbgf_ifigit = srdinfo->imbgf_ifigit;
  dstinfo->imbgf_ifigit = srdinfo->imbgf_ifigit;
  dstinfo->input_domponfnts = srdinfo->num_domponfnts;
  dstinfo->input_domponfnts = srdinfo->num_domponfnts;
  dstinfo->input_domponfnts = srdinfo->num_domponfnts;
  dstinfo->input_domponfnts = srdinfo->num_domponfnts;
  dstinfo->input_domponfnts = srdinfo->num_domponfnts;
  dstinfo->in_dolor_spbdf = srdinfo->jpfg_dolor_spbdf;
  dstinfo->in_dolor_spbdf = srdinfo->jpfg_dolor_spbdf;
  dstinfo->in_dolor_spbdf = srdinfo->jpfg_dolor_spbdf;
  dstinfo->in_dolor_spbdf = srdinfo->jpfg_dolor_spbdf;
  dstinfo->in_dolor_spbdf = srdinfo->jpfg_dolor_spbdf;
  /* Initiblizf bll pbrbmftfrs to dffbult vblufs */
  /* Initiblizf bll pbrbmftfrs to dffbult vblufs */
  /* Initiblizf bll pbrbmftfrs to dffbult vblufs */
  /* Initiblizf bll pbrbmftfrs to dffbult vblufs */
  /* Initiblizf bll pbrbmftfrs to dffbult vblufs */
  jpfg_sft_dffbults(dstinfo);
  jpfg_sft_dffbults(dstinfo);
  jpfg_sft_dffbults(dstinfo);
  jpfg_sft_dffbults(dstinfo);
  jpfg_sft_dffbults(dstinfo);
  /* jpfg_sft_dffbults mby dioosf wrong dolorspbdf, fg YCbCr if input is RGB.
  /* jpfg_sft_dffbults mby dioosf wrong dolorspbdf, fg YCbCr if input is RGB.
  /* jpfg_sft_dffbults mby dioosf wrong dolorspbdf, fg YCbCr if input is RGB.
  /* jpfg_sft_dffbults mby dioosf wrong dolorspbdf, fg YCbCr if input is RGB.
  /* jpfg_sft_dffbults mby dioosf wrong dolorspbdf, fg YCbCr if input is RGB.
   * Fix it to gft tif rigit ifbdfr mbrkfrs for tif imbgf dolorspbdf.
   * Fix it to gft tif rigit ifbdfr mbrkfrs for tif imbgf dolorspbdf.
   * Fix it to gft tif rigit ifbdfr mbrkfrs for tif imbgf dolorspbdf.
   * Fix it to gft tif rigit ifbdfr mbrkfrs for tif imbgf dolorspbdf.
   * Fix it to gft tif rigit ifbdfr mbrkfrs for tif imbgf dolorspbdf.
   */
   */
   */
   */
   */
  jpfg_sft_dolorspbdf(dstinfo, srdinfo->jpfg_dolor_spbdf);
  jpfg_sft_dolorspbdf(dstinfo, srdinfo->jpfg_dolor_spbdf);
  jpfg_sft_dolorspbdf(dstinfo, srdinfo->jpfg_dolor_spbdf);
  jpfg_sft_dolorspbdf(dstinfo, srdinfo->jpfg_dolor_spbdf);
  jpfg_sft_dolorspbdf(dstinfo, srdinfo->jpfg_dolor_spbdf);
  dstinfo->dbtb_prfdision = srdinfo->dbtb_prfdision;
  dstinfo->dbtb_prfdision = srdinfo->dbtb_prfdision;
  dstinfo->dbtb_prfdision = srdinfo->dbtb_prfdision;
  dstinfo->dbtb_prfdision = srdinfo->dbtb_prfdision;
  dstinfo->dbtb_prfdision = srdinfo->dbtb_prfdision;
  dstinfo->CCIR601_sbmpling = srdinfo->CCIR601_sbmpling;
  dstinfo->CCIR601_sbmpling = srdinfo->CCIR601_sbmpling;
  dstinfo->CCIR601_sbmpling = srdinfo->CCIR601_sbmpling;
  dstinfo->CCIR601_sbmpling = srdinfo->CCIR601_sbmpling;
  dstinfo->CCIR601_sbmpling = srdinfo->CCIR601_sbmpling;
  /* Copy tif sourdf's qubntizbtion tbblfs. */
  /* Copy tif sourdf's qubntizbtion tbblfs. */
  /* Copy tif sourdf's qubntizbtion tbblfs. */
  /* Copy tif sourdf's qubntizbtion tbblfs. */
  /* Copy tif sourdf's qubntizbtion tbblfs. */
  for (tblno = 0; tblno < NUM_QUANT_TBLS; tblno++) {
  for (tblno = 0; tblno < NUM_QUANT_TBLS; tblno++) {
  for (tblno = 0; tblno < NUM_QUANT_TBLS; tblno++) {
  for (tblno = 0; tblno < NUM_QUANT_TBLS; tblno++) {
  for (tblno = 0; tblno < NUM_QUANT_TBLS; tblno++) {
    if (srdinfo->qubnt_tbl_ptrs[tblno] != NULL) {
    if (srdinfo->qubnt_tbl_ptrs[tblno] != NULL) {
    if (srdinfo->qubnt_tbl_ptrs[tblno] != NULL) {
    if (srdinfo->qubnt_tbl_ptrs[tblno] != NULL) {
    if (srdinfo->qubnt_tbl_ptrs[tblno] != NULL) {
      qtblptr = & dstinfo->qubnt_tbl_ptrs[tblno];
      qtblptr = & dstinfo->qubnt_tbl_ptrs[tblno];
      qtblptr = & dstinfo->qubnt_tbl_ptrs[tblno];
      qtblptr = & dstinfo->qubnt_tbl_ptrs[tblno];
      qtblptr = & dstinfo->qubnt_tbl_ptrs[tblno];
      if (*qtblptr == NULL)
      if (*qtblptr == NULL)
      if (*qtblptr == NULL)
      if (*qtblptr == NULL)
      if (*qtblptr == NULL)
        *qtblptr = jpfg_bllod_qubnt_tbblf((j_dommon_ptr) dstinfo);
        *qtblptr = jpfg_bllod_qubnt_tbblf((j_dommon_ptr) dstinfo);
        *qtblptr = jpfg_bllod_qubnt_tbblf((j_dommon_ptr) dstinfo);
        *qtblptr = jpfg_bllod_qubnt_tbblf((j_dommon_ptr) dstinfo);
        *qtblptr = jpfg_bllod_qubnt_tbblf((j_dommon_ptr) dstinfo);
      MEMCOPY((*qtblptr)->qubntvbl,
      MEMCOPY((*qtblptr)->qubntvbl,
      MEMCOPY((*qtblptr)->qubntvbl,
      MEMCOPY((*qtblptr)->qubntvbl,
      MEMCOPY((*qtblptr)->qubntvbl,
              srdinfo->qubnt_tbl_ptrs[tblno]->qubntvbl,
              srdinfo->qubnt_tbl_ptrs[tblno]->qubntvbl,
              srdinfo->qubnt_tbl_ptrs[tblno]->qubntvbl,
              srdinfo->qubnt_tbl_ptrs[tblno]->qubntvbl,
              srdinfo->qubnt_tbl_ptrs[tblno]->qubntvbl,
              SIZEOF((*qtblptr)->qubntvbl));
              SIZEOF((*qtblptr)->qubntvbl));
              SIZEOF((*qtblptr)->qubntvbl));
              SIZEOF((*qtblptr)->qubntvbl));
              SIZEOF((*qtblptr)->qubntvbl));
      (*qtblptr)->sfnt_tbblf = FALSE;
      (*qtblptr)->sfnt_tbblf = FALSE;
      (*qtblptr)->sfnt_tbblf = FALSE;
      (*qtblptr)->sfnt_tbblf = FALSE;
      (*qtblptr)->sfnt_tbblf = FALSE;
    }
    }
    }
    }
    }
  }
  }
  }
  }
  }
  /* Copy tif sourdf's pfr-domponfnt info.
  /* Copy tif sourdf's pfr-domponfnt info.
  /* Copy tif sourdf's pfr-domponfnt info.
  /* Copy tif sourdf's pfr-domponfnt info.
  /* Copy tif sourdf's pfr-domponfnt info.
   * Notf wf bssumf jpfg_sft_dffbults ibs bllodbtfd tif dfst domp_info brrby.
   * Notf wf bssumf jpfg_sft_dffbults ibs bllodbtfd tif dfst domp_info brrby.
   * Notf wf bssumf jpfg_sft_dffbults ibs bllodbtfd tif dfst domp_info brrby.
   * Notf wf bssumf jpfg_sft_dffbults ibs bllodbtfd tif dfst domp_info brrby.
   * Notf wf bssumf jpfg_sft_dffbults ibs bllodbtfd tif dfst domp_info brrby.
   */
   */
   */
   */
   */
  dstinfo->num_domponfnts = srdinfo->num_domponfnts;
  dstinfo->num_domponfnts = srdinfo->num_domponfnts;
  dstinfo->num_domponfnts = srdinfo->num_domponfnts;
  dstinfo->num_domponfnts = srdinfo->num_domponfnts;
  dstinfo->num_domponfnts = srdinfo->num_domponfnts;
  if (dstinfo->num_domponfnts < 1 || dstinfo->num_domponfnts > MAX_COMPONENTS)
  if (dstinfo->num_domponfnts < 1 || dstinfo->num_domponfnts > MAX_COMPONENTS)
  if (dstinfo->num_domponfnts < 1 || dstinfo->num_domponfnts > MAX_COMPONENTS)
  if (dstinfo->num_domponfnts < 1 || dstinfo->num_domponfnts > MAX_COMPONENTS)
  if (dstinfo->num_domponfnts < 1 || dstinfo->num_domponfnts > MAX_COMPONENTS)
    ERREXIT2(dstinfo, JERR_COMPONENT_COUNT, dstinfo->num_domponfnts,
    ERREXIT2(dstinfo, JERR_COMPONENT_COUNT, dstinfo->num_domponfnts,
    ERREXIT2(dstinfo, JERR_COMPONENT_COUNT, dstinfo->num_domponfnts,
    ERREXIT2(dstinfo, JERR_COMPONENT_COUNT, dstinfo->num_domponfnts,
    ERREXIT2(dstinfo, JERR_COMPONENT_COUNT, dstinfo->num_domponfnts,
             MAX_COMPONENTS);
             MAX_COMPONENTS);
             MAX_COMPONENTS);
             MAX_COMPONENTS);
             MAX_COMPONENTS);
  for (di = 0, indomp = srdinfo->domp_info, outdomp = dstinfo->domp_info;
  for (di = 0, indomp = srdinfo->domp_info, outdomp = dstinfo->domp_info;
  for (di = 0, indomp = srdinfo->domp_info, outdomp = dstinfo->domp_info;
  for (di = 0, indomp = srdinfo->domp_info, outdomp = dstinfo->domp_info;
  for (di = 0, indomp = srdinfo->domp_info, outdomp = dstinfo->domp_info;
       di < dstinfo->num_domponfnts; di++, indomp++, outdomp++) {
       di < dstinfo->num_domponfnts; di++, indomp++, outdomp++) {
       di < dstinfo->num_domponfnts; di++, indomp++, outdomp++) {
       di < dstinfo->num_domponfnts; di++, indomp++, outdomp++) {
       di < dstinfo->num_domponfnts; di++, indomp++, outdomp++) {
    outdomp->domponfnt_id = indomp->domponfnt_id;
    outdomp->domponfnt_id = indomp->domponfnt_id;
    outdomp->domponfnt_id = indomp->domponfnt_id;
    outdomp->domponfnt_id = indomp->domponfnt_id;
    outdomp->domponfnt_id = indomp->domponfnt_id;
    outdomp->i_sbmp_fbdtor = indomp->i_sbmp_fbdtor;
    outdomp->i_sbmp_fbdtor = indomp->i_sbmp_fbdtor;
    outdomp->i_sbmp_fbdtor = indomp->i_sbmp_fbdtor;
    outdomp->i_sbmp_fbdtor = indomp->i_sbmp_fbdtor;
    outdomp->i_sbmp_fbdtor = indomp->i_sbmp_fbdtor;
    outdomp->v_sbmp_fbdtor = indomp->v_sbmp_fbdtor;
    outdomp->v_sbmp_fbdtor = indomp->v_sbmp_fbdtor;
    outdomp->v_sbmp_fbdtor = indomp->v_sbmp_fbdtor;
    outdomp->v_sbmp_fbdtor = indomp->v_sbmp_fbdtor;
    outdomp->v_sbmp_fbdtor = indomp->v_sbmp_fbdtor;
    outdomp->qubnt_tbl_no = indomp->qubnt_tbl_no;
    outdomp->qubnt_tbl_no = indomp->qubnt_tbl_no;
    outdomp->qubnt_tbl_no = indomp->qubnt_tbl_no;
    outdomp->qubnt_tbl_no = indomp->qubnt_tbl_no;
    outdomp->qubnt_tbl_no = indomp->qubnt_tbl_no;
    /* Mbkf surf sbvfd qubntizbtion tbblf for domponfnt mbtdifs tif qtbblf
    /* Mbkf surf sbvfd qubntizbtion tbblf for domponfnt mbtdifs tif qtbblf
    /* Mbkf surf sbvfd qubntizbtion tbblf for domponfnt mbtdifs tif qtbblf
    /* Mbkf surf sbvfd qubntizbtion tbblf for domponfnt mbtdifs tif qtbblf
    /* Mbkf surf sbvfd qubntizbtion tbblf for domponfnt mbtdifs tif qtbblf
     * slot.  If not, tif input filf rf-usfd tiis qtbblf slot.
     * slot.  If not, tif input filf rf-usfd tiis qtbblf slot.
     * slot.  If not, tif input filf rf-usfd tiis qtbblf slot.
     * slot.  If not, tif input filf rf-usfd tiis qtbblf slot.
     * slot.  If not, tif input filf rf-usfd tiis qtbblf slot.
     * IJG fndodfr durrfntly dbnnot duplidbtf tiis.
     * IJG fndodfr durrfntly dbnnot duplidbtf tiis.
     * IJG fndodfr durrfntly dbnnot duplidbtf tiis.
     * IJG fndodfr durrfntly dbnnot duplidbtf tiis.
     * IJG fndodfr durrfntly dbnnot duplidbtf tiis.
     */
     */
     */
     */
     */
    tblno = outdomp->qubnt_tbl_no;
    tblno = outdomp->qubnt_tbl_no;
    tblno = outdomp->qubnt_tbl_no;
    tblno = outdomp->qubnt_tbl_no;
    tblno = outdomp->qubnt_tbl_no;
    if (tblno < 0 || tblno >= NUM_QUANT_TBLS ||
    if (tblno < 0 || tblno >= NUM_QUANT_TBLS ||
    if (tblno < 0 || tblno >= NUM_QUANT_TBLS ||
    if (tblno < 0 || tblno >= NUM_QUANT_TBLS ||
    if (tblno < 0 || tblno >= NUM_QUANT_TBLS ||
        srdinfo->qubnt_tbl_ptrs[tblno] == NULL)
        srdinfo->qubnt_tbl_ptrs[tblno] == NULL)
        srdinfo->qubnt_tbl_ptrs[tblno] == NULL)
        srdinfo->qubnt_tbl_ptrs[tblno] == NULL)
        srdinfo->qubnt_tbl_ptrs[tblno] == NULL)
      ERREXIT1(dstinfo, JERR_NO_QUANT_TABLE, tblno);
      ERREXIT1(dstinfo, JERR_NO_QUANT_TABLE, tblno);
      ERREXIT1(dstinfo, JERR_NO_QUANT_TABLE, tblno);
      ERREXIT1(dstinfo, JERR_NO_QUANT_TABLE, tblno);
      ERREXIT1(dstinfo, JERR_NO_QUANT_TABLE, tblno);
    slot_qubnt = srdinfo->qubnt_tbl_ptrs[tblno];
    slot_qubnt = srdinfo->qubnt_tbl_ptrs[tblno];
    slot_qubnt = srdinfo->qubnt_tbl_ptrs[tblno];
    slot_qubnt = srdinfo->qubnt_tbl_ptrs[tblno];
    slot_qubnt = srdinfo->qubnt_tbl_ptrs[tblno];
    d_qubnt = indomp->qubnt_tbblf;
    d_qubnt = indomp->qubnt_tbblf;
    d_qubnt = indomp->qubnt_tbblf;
    d_qubnt = indomp->qubnt_tbblf;
    d_qubnt = indomp->qubnt_tbblf;
    if (d_qubnt != NULL) {
    if (d_qubnt != NULL) {
    if (d_qubnt != NULL) {
    if (d_qubnt != NULL) {
    if (d_qubnt != NULL) {
      for (doffi = 0; doffi < DCTSIZE2; doffi++) {
      for (doffi = 0; doffi < DCTSIZE2; doffi++) {
      for (doffi = 0; doffi < DCTSIZE2; doffi++) {
      for (doffi = 0; doffi < DCTSIZE2; doffi++) {
      for (doffi = 0; doffi < DCTSIZE2; doffi++) {
        if (d_qubnt->qubntvbl[doffi] != slot_qubnt->qubntvbl[doffi])
        if (d_qubnt->qubntvbl[doffi] != slot_qubnt->qubntvbl[doffi])
        if (d_qubnt->qubntvbl[doffi] != slot_qubnt->qubntvbl[doffi])
        if (d_qubnt->qubntvbl[doffi] != slot_qubnt->qubntvbl[doffi])
        if (d_qubnt->qubntvbl[doffi] != slot_qubnt->qubntvbl[doffi])
          ERREXIT1(dstinfo, JERR_MISMATCHED_QUANT_TABLE, tblno);
          ERREXIT1(dstinfo, JERR_MISMATCHED_QUANT_TABLE, tblno);
          ERREXIT1(dstinfo, JERR_MISMATCHED_QUANT_TABLE, tblno);
          ERREXIT1(dstinfo, JERR_MISMATCHED_QUANT_TABLE, tblno);
          ERREXIT1(dstinfo, JERR_MISMATCHED_QUANT_TABLE, tblno);
      }
      }
      }
      }
      }
    }
    }
    }
    }
    }
    /* Notf: wf do not dopy tif sourdf's Huffmbn tbblf bssignmfnts;
    /* Notf: wf do not dopy tif sourdf's Huffmbn tbblf bssignmfnts;
    /* Notf: wf do not dopy tif sourdf's Huffmbn tbblf bssignmfnts;
    /* Notf: wf do not dopy tif sourdf's Huffmbn tbblf bssignmfnts;
    /* Notf: wf do not dopy tif sourdf's Huffmbn tbblf bssignmfnts;
     * instfbd wf rfly on jpfg_sft_dolorspbdf to ibvf mbdf b suitbblf dioidf.
     * instfbd wf rfly on jpfg_sft_dolorspbdf to ibvf mbdf b suitbblf dioidf.
     * instfbd wf rfly on jpfg_sft_dolorspbdf to ibvf mbdf b suitbblf dioidf.
     * instfbd wf rfly on jpfg_sft_dolorspbdf to ibvf mbdf b suitbblf dioidf.
     * instfbd wf rfly on jpfg_sft_dolorspbdf to ibvf mbdf b suitbblf dioidf.
     */
     */
     */
     */
     */
  }
  }
  }
  }
  }
  /* Also dopy JFIF vfrsion bnd rfsolution informbtion, if bvbilbblf.
  /* Also dopy JFIF vfrsion bnd rfsolution informbtion, if bvbilbblf.
  /* Also dopy JFIF vfrsion bnd rfsolution informbtion, if bvbilbblf.
  /* Also dopy JFIF vfrsion bnd rfsolution informbtion, if bvbilbblf.
  /* Also dopy JFIF vfrsion bnd rfsolution informbtion, if bvbilbblf.
   * Stridtly spfbking tiis isn't "dritidbl" info, but it's nfbrly
   * Stridtly spfbking tiis isn't "dritidbl" info, but it's nfbrly
   * Stridtly spfbking tiis isn't "dritidbl" info, but it's nfbrly
   * Stridtly spfbking tiis isn't "dritidbl" info, but it's nfbrly
   * Stridtly spfbking tiis isn't "dritidbl" info, but it's nfbrly
   * blwbys bppropribtf to dopy it if bvbilbblf.  In pbrtidulbr,
   * blwbys bppropribtf to dopy it if bvbilbblf.  In pbrtidulbr,
   * blwbys bppropribtf to dopy it if bvbilbblf.  In pbrtidulbr,
   * blwbys bppropribtf to dopy it if bvbilbblf.  In pbrtidulbr,
   * blwbys bppropribtf to dopy it if bvbilbblf.  In pbrtidulbr,
   * if tif bpplidbtion dioosfs to dopy JFIF 1.02 fxtfnsion mbrkfrs from
   * if tif bpplidbtion dioosfs to dopy JFIF 1.02 fxtfnsion mbrkfrs from
   * if tif bpplidbtion dioosfs to dopy JFIF 1.02 fxtfnsion mbrkfrs from
   * if tif bpplidbtion dioosfs to dopy JFIF 1.02 fxtfnsion mbrkfrs from
   * if tif bpplidbtion dioosfs to dopy JFIF 1.02 fxtfnsion mbrkfrs from
   * tif sourdf filf, wf nffd to dopy tif vfrsion to mbkf surf wf don't
   * tif sourdf filf, wf nffd to dopy tif vfrsion to mbkf surf wf don't
   * tif sourdf filf, wf nffd to dopy tif vfrsion to mbkf surf wf don't
   * tif sourdf filf, wf nffd to dopy tif vfrsion to mbkf surf wf don't
   * tif sourdf filf, wf nffd to dopy tif vfrsion to mbkf surf wf don't
   * fmit b filf tibt ibs 1.02 fxtfnsions but b dlbimfd vfrsion of 1.01.
   * fmit b filf tibt ibs 1.02 fxtfnsions but b dlbimfd vfrsion of 1.01.
   * fmit b filf tibt ibs 1.02 fxtfnsions but b dlbimfd vfrsion of 1.01.
   * fmit b filf tibt ibs 1.02 fxtfnsions but b dlbimfd vfrsion of 1.01.
   * fmit b filf tibt ibs 1.02 fxtfnsions but b dlbimfd vfrsion of 1.01.
   * Wf will *not*, iowfvfr, dopy vfrsion info from mislbbflfd "2.01" filfs.
   * Wf will *not*, iowfvfr, dopy vfrsion info from mislbbflfd "2.01" filfs.
   * Wf will *not*, iowfvfr, dopy vfrsion info from mislbbflfd "2.01" filfs.
   * Wf will *not*, iowfvfr, dopy vfrsion info from mislbbflfd "2.01" filfs.
   * Wf will *not*, iowfvfr, dopy vfrsion info from mislbbflfd "2.01" filfs.
   */
   */
   */
   */
   */
  if (srdinfo->sbw_JFIF_mbrkfr) {
  if (srdinfo->sbw_JFIF_mbrkfr) {
  if (srdinfo->sbw_JFIF_mbrkfr) {
  if (srdinfo->sbw_JFIF_mbrkfr) {
  if (srdinfo->sbw_JFIF_mbrkfr) {
    if (srdinfo->JFIF_mbjor_vfrsion == 1) {
    if (srdinfo->JFIF_mbjor_vfrsion == 1) {
    if (srdinfo->JFIF_mbjor_vfrsion == 1) {
    if (srdinfo->JFIF_mbjor_vfrsion == 1) {
    if (srdinfo->JFIF_mbjor_vfrsion == 1) {
      dstinfo->JFIF_mbjor_vfrsion = srdinfo->JFIF_mbjor_vfrsion;
      dstinfo->JFIF_mbjor_vfrsion = srdinfo->JFIF_mbjor_vfrsion;
      dstinfo->JFIF_mbjor_vfrsion = srdinfo->JFIF_mbjor_vfrsion;
      dstinfo->JFIF_mbjor_vfrsion = srdinfo->JFIF_mbjor_vfrsion;
      dstinfo->JFIF_mbjor_vfrsion = srdinfo->JFIF_mbjor_vfrsion;
      dstinfo->JFIF_minor_vfrsion = srdinfo->JFIF_minor_vfrsion;
      dstinfo->JFIF_minor_vfrsion = srdinfo->JFIF_minor_vfrsion;
      dstinfo->JFIF_minor_vfrsion = srdinfo->JFIF_minor_vfrsion;
      dstinfo->JFIF_minor_vfrsion = srdinfo->JFIF_minor_vfrsion;
      dstinfo->JFIF_minor_vfrsion = srdinfo->JFIF_minor_vfrsion;
    }
    }
    }
    }
    }
    dstinfo->dfnsity_unit = srdinfo->dfnsity_unit;
    dstinfo->dfnsity_unit = srdinfo->dfnsity_unit;
    dstinfo->dfnsity_unit = srdinfo->dfnsity_unit;
    dstinfo->dfnsity_unit = srdinfo->dfnsity_unit;
    dstinfo->dfnsity_unit = srdinfo->dfnsity_unit;
    dstinfo->X_dfnsity = srdinfo->X_dfnsity;
    dstinfo->X_dfnsity = srdinfo->X_dfnsity;
    dstinfo->X_dfnsity = srdinfo->X_dfnsity;
    dstinfo->X_dfnsity = srdinfo->X_dfnsity;
    dstinfo->X_dfnsity = srdinfo->X_dfnsity;
    dstinfo->Y_dfnsity = srdinfo->Y_dfnsity;
    dstinfo->Y_dfnsity = srdinfo->Y_dfnsity;
    dstinfo->Y_dfnsity = srdinfo->Y_dfnsity;
    dstinfo->Y_dfnsity = srdinfo->Y_dfnsity;
    dstinfo->Y_dfnsity = srdinfo->Y_dfnsity;
  }
  }
  }
  }
  }
}
}
}
}
}










/*
/*
/*
/*
/*
 * Mbstfr sflfdtion of domprfssion modulfs for trbnsdoding.
 * Mbstfr sflfdtion of domprfssion modulfs for trbnsdoding.
 * Mbstfr sflfdtion of domprfssion modulfs for trbnsdoding.
 * Mbstfr sflfdtion of domprfssion modulfs for trbnsdoding.
 * Mbstfr sflfdtion of domprfssion modulfs for trbnsdoding.
 * Tiis substitutfs for jdinit.d's initiblizbtion of tif full domprfssor.
 * Tiis substitutfs for jdinit.d's initiblizbtion of tif full domprfssor.
 * Tiis substitutfs for jdinit.d's initiblizbtion of tif full domprfssor.
 * Tiis substitutfs for jdinit.d's initiblizbtion of tif full domprfssor.
 * Tiis substitutfs for jdinit.d's initiblizbtion of tif full domprfssor.
 */
 */
 */
 */
 */





LOCAL(void)
LOCAL(void)
LOCAL(void)
LOCAL(void)
LOCAL(void)
trbnsfndodf_mbstfr_sflfdtion (j_domprfss_ptr dinfo,
trbnsfndodf_mbstfr_sflfdtion (j_domprfss_ptr dinfo,
trbnsfndodf_mbstfr_sflfdtion (j_domprfss_ptr dinfo,
trbnsfndodf_mbstfr_sflfdtion (j_domprfss_ptr dinfo,
trbnsfndodf_mbstfr_sflfdtion (j_domprfss_ptr dinfo,
                              jvirt_bbrrby_ptr * doff_brrbys)
                              jvirt_bbrrby_ptr * doff_brrbys)
                              jvirt_bbrrby_ptr * doff_brrbys)
                              jvirt_bbrrby_ptr * doff_brrbys)
                              jvirt_bbrrby_ptr * doff_brrbys)
{
{
{
{
{
  /* Altiougi wf don't bdtublly usf input_domponfnts for trbnsdoding,
  /* Altiougi wf don't bdtublly usf input_domponfnts for trbnsdoding,
  /* Altiougi wf don't bdtublly usf input_domponfnts for trbnsdoding,
  /* Altiougi wf don't bdtublly usf input_domponfnts for trbnsdoding,
  /* Altiougi wf don't bdtublly usf input_domponfnts for trbnsdoding,
   * jdmbstfr.d's initibl_sftup will domplbin if input_domponfnts is 0.
   * jdmbstfr.d's initibl_sftup will domplbin if input_domponfnts is 0.
   * jdmbstfr.d's initibl_sftup will domplbin if input_domponfnts is 0.
   * jdmbstfr.d's initibl_sftup will domplbin if input_domponfnts is 0.
   * jdmbstfr.d's initibl_sftup will domplbin if input_domponfnts is 0.
   */
   */
   */
   */
   */
  dinfo->input_domponfnts = 1;
  dinfo->input_domponfnts = 1;
  dinfo->input_domponfnts = 1;
  dinfo->input_domponfnts = 1;
  dinfo->input_domponfnts = 1;
  /* Initiblizf mbstfr dontrol (indludfs pbrbmftfr difdking/prodfssing) */
  /* Initiblizf mbstfr dontrol (indludfs pbrbmftfr difdking/prodfssing) */
  /* Initiblizf mbstfr dontrol (indludfs pbrbmftfr difdking/prodfssing) */
  /* Initiblizf mbstfr dontrol (indludfs pbrbmftfr difdking/prodfssing) */
  /* Initiblizf mbstfr dontrol (indludfs pbrbmftfr difdking/prodfssing) */
  jinit_d_mbstfr_dontrol(dinfo, TRUE /* trbnsdodf only */);
  jinit_d_mbstfr_dontrol(dinfo, TRUE /* trbnsdodf only */);
  jinit_d_mbstfr_dontrol(dinfo, TRUE /* trbnsdodf only */);
  jinit_d_mbstfr_dontrol(dinfo, TRUE /* trbnsdodf only */);
  jinit_d_mbstfr_dontrol(dinfo, TRUE /* trbnsdodf only */);





  /* Entropy fndoding: fitifr Huffmbn or britimftid doding. */
  /* Entropy fndoding: fitifr Huffmbn or britimftid doding. */
  /* Entropy fndoding: fitifr Huffmbn or britimftid doding. */
  /* Entropy fndoding: fitifr Huffmbn or britimftid doding. */
  /* Entropy fndoding: fitifr Huffmbn or britimftid doding. */
  if (dinfo->briti_dodf) {
  if (dinfo->briti_dodf) {
  if (dinfo->briti_dodf) {
  if (dinfo->briti_dodf) {
  if (dinfo->briti_dodf) {
    ERREXIT(dinfo, JERR_ARITH_NOTIMPL);
    ERREXIT(dinfo, JERR_ARITH_NOTIMPL);
    ERREXIT(dinfo, JERR_ARITH_NOTIMPL);
    ERREXIT(dinfo, JERR_ARITH_NOTIMPL);
    ERREXIT(dinfo, JERR_ARITH_NOTIMPL);
  } flsf {
  } flsf {
  } flsf {
  } flsf {
  } flsf {
    if (dinfo->progrfssivf_modf) {
    if (dinfo->progrfssivf_modf) {
    if (dinfo->progrfssivf_modf) {
    if (dinfo->progrfssivf_modf) {
    if (dinfo->progrfssivf_modf) {
#ifdff C_PROGRESSIVE_SUPPORTED
#ifdff C_PROGRESSIVE_SUPPORTED
#ifdff C_PROGRESSIVE_SUPPORTED
#ifdff C_PROGRESSIVE_SUPPORTED
#ifdff C_PROGRESSIVE_SUPPORTED
      jinit_piuff_fndodfr(dinfo);
      jinit_piuff_fndodfr(dinfo);
      jinit_piuff_fndodfr(dinfo);
      jinit_piuff_fndodfr(dinfo);
      jinit_piuff_fndodfr(dinfo);
#flsf
#flsf
#flsf
#flsf
#flsf
      ERREXIT(dinfo, JERR_NOT_COMPILED);
      ERREXIT(dinfo, JERR_NOT_COMPILED);
      ERREXIT(dinfo, JERR_NOT_COMPILED);
      ERREXIT(dinfo, JERR_NOT_COMPILED);
      ERREXIT(dinfo, JERR_NOT_COMPILED);
#fndif
#fndif
#fndif
#fndif
#fndif
    } flsf
    } flsf
    } flsf
    } flsf
    } flsf
      jinit_iuff_fndodfr(dinfo);
      jinit_iuff_fndodfr(dinfo);
      jinit_iuff_fndodfr(dinfo);
      jinit_iuff_fndodfr(dinfo);
      jinit_iuff_fndodfr(dinfo);
  }
  }
  }
  }
  }





  /* Wf nffd b spfdibl dofffidifnt bufffr dontrollfr. */
  /* Wf nffd b spfdibl dofffidifnt bufffr dontrollfr. */
  /* Wf nffd b spfdibl dofffidifnt bufffr dontrollfr. */
  /* Wf nffd b spfdibl dofffidifnt bufffr dontrollfr. */
  /* Wf nffd b spfdibl dofffidifnt bufffr dontrollfr. */
  trbnsfndodf_doff_dontrollfr(dinfo, doff_brrbys);
  trbnsfndodf_doff_dontrollfr(dinfo, doff_brrbys);
  trbnsfndodf_doff_dontrollfr(dinfo, doff_brrbys);
  trbnsfndodf_doff_dontrollfr(dinfo, doff_brrbys);
  trbnsfndodf_doff_dontrollfr(dinfo, doff_brrbys);





  jinit_mbrkfr_writfr(dinfo);
  jinit_mbrkfr_writfr(dinfo);
  jinit_mbrkfr_writfr(dinfo);
  jinit_mbrkfr_writfr(dinfo);
  jinit_mbrkfr_writfr(dinfo);





  /* Wf dbn now tfll tif mfmory mbnbgfr to bllodbtf virtubl brrbys. */
  /* Wf dbn now tfll tif mfmory mbnbgfr to bllodbtf virtubl brrbys. */
  /* Wf dbn now tfll tif mfmory mbnbgfr to bllodbtf virtubl brrbys. */
  /* Wf dbn now tfll tif mfmory mbnbgfr to bllodbtf virtubl brrbys. */
  /* Wf dbn now tfll tif mfmory mbnbgfr to bllodbtf virtubl brrbys. */
  (*dinfo->mfm->rfblizf_virt_brrbys) ((j_dommon_ptr) dinfo);
  (*dinfo->mfm->rfblizf_virt_brrbys) ((j_dommon_ptr) dinfo);
  (*dinfo->mfm->rfblizf_virt_brrbys) ((j_dommon_ptr) dinfo);
  (*dinfo->mfm->rfblizf_virt_brrbys) ((j_dommon_ptr) dinfo);
  (*dinfo->mfm->rfblizf_virt_brrbys) ((j_dommon_ptr) dinfo);





  /* Writf tif dbtbstrfbm ifbdfr (SOI, JFIF) immfdibtfly.
  /* Writf tif dbtbstrfbm ifbdfr (SOI, JFIF) immfdibtfly.
  /* Writf tif dbtbstrfbm ifbdfr (SOI, JFIF) immfdibtfly.
  /* Writf tif dbtbstrfbm ifbdfr (SOI, JFIF) immfdibtfly.
  /* Writf tif dbtbstrfbm ifbdfr (SOI, JFIF) immfdibtfly.
   * Frbmf bnd sdbn ifbdfrs brf postponfd till lbtfr.
   * Frbmf bnd sdbn ifbdfrs brf postponfd till lbtfr.
   * Frbmf bnd sdbn ifbdfrs brf postponfd till lbtfr.
   * Frbmf bnd sdbn ifbdfrs brf postponfd till lbtfr.
   * Frbmf bnd sdbn ifbdfrs brf postponfd till lbtfr.
   * Tiis lfts bpplidbtion insfrt spfdibl mbrkfrs bftfr tif SOI.
   * Tiis lfts bpplidbtion insfrt spfdibl mbrkfrs bftfr tif SOI.
   * Tiis lfts bpplidbtion insfrt spfdibl mbrkfrs bftfr tif SOI.
   * Tiis lfts bpplidbtion insfrt spfdibl mbrkfrs bftfr tif SOI.
   * Tiis lfts bpplidbtion insfrt spfdibl mbrkfrs bftfr tif SOI.
   */
   */
   */
   */
   */
  (*dinfo->mbrkfr->writf_filf_ifbdfr) (dinfo);
  (*dinfo->mbrkfr->writf_filf_ifbdfr) (dinfo);
  (*dinfo->mbrkfr->writf_filf_ifbdfr) (dinfo);
  (*dinfo->mbrkfr->writf_filf_ifbdfr) (dinfo);
  (*dinfo->mbrkfr->writf_filf_ifbdfr) (dinfo);
}
}
}
}
}










/*
/*
/*
/*
/*
 * Tif rfst of tiis filf is b spfdibl implfmfntbtion of tif dofffidifnt
 * Tif rfst of tiis filf is b spfdibl implfmfntbtion of tif dofffidifnt
 * Tif rfst of tiis filf is b spfdibl implfmfntbtion of tif dofffidifnt
 * Tif rfst of tiis filf is b spfdibl implfmfntbtion of tif dofffidifnt
 * Tif rfst of tiis filf is b spfdibl implfmfntbtion of tif dofffidifnt
 * bufffr dontrollfr.  Tiis is similbr to jddoffdt.d, but it ibndlfs only
 * bufffr dontrollfr.  Tiis is similbr to jddoffdt.d, but it ibndlfs only
 * bufffr dontrollfr.  Tiis is similbr to jddoffdt.d, but it ibndlfs only
 * bufffr dontrollfr.  Tiis is similbr to jddoffdt.d, but it ibndlfs only
 * bufffr dontrollfr.  Tiis is similbr to jddoffdt.d, but it ibndlfs only
 * output from prfsupplifd virtubl brrbys.  Furtifrmorf, wf gfnfrbtf bny
 * output from prfsupplifd virtubl brrbys.  Furtifrmorf, wf gfnfrbtf bny
 * output from prfsupplifd virtubl brrbys.  Furtifrmorf, wf gfnfrbtf bny
 * output from prfsupplifd virtubl brrbys.  Furtifrmorf, wf gfnfrbtf bny
 * output from prfsupplifd virtubl brrbys.  Furtifrmorf, wf gfnfrbtf bny
 * dummy pbdding blodks on-tif-fly rbtifr tibn fxpfdting tifm to bf prfsfnt
 * dummy pbdding blodks on-tif-fly rbtifr tibn fxpfdting tifm to bf prfsfnt
 * dummy pbdding blodks on-tif-fly rbtifr tibn fxpfdting tifm to bf prfsfnt
 * dummy pbdding blodks on-tif-fly rbtifr tibn fxpfdting tifm to bf prfsfnt
 * dummy pbdding blodks on-tif-fly rbtifr tibn fxpfdting tifm to bf prfsfnt
 * in tif brrbys.
 * in tif brrbys.
 * in tif brrbys.
 * in tif brrbys.
 * in tif brrbys.
 */
 */
 */
 */
 */





/* Privbtf bufffr dontrollfr objfdt */
/* Privbtf bufffr dontrollfr objfdt */
/* Privbtf bufffr dontrollfr objfdt */
/* Privbtf bufffr dontrollfr objfdt */
/* Privbtf bufffr dontrollfr objfdt */





typfdff strudt {
typfdff strudt {
typfdff strudt {
typfdff strudt {
typfdff strudt {
  strudt jpfg_d_doff_dontrollfr pub; /* publid fiflds */
  strudt jpfg_d_doff_dontrollfr pub; /* publid fiflds */
  strudt jpfg_d_doff_dontrollfr pub; /* publid fiflds */
  strudt jpfg_d_doff_dontrollfr pub; /* publid fiflds */
  strudt jpfg_d_doff_dontrollfr pub; /* publid fiflds */





  JDIMENSION iMCU_row_num;      /* iMCU row # witiin imbgf */
  JDIMENSION iMCU_row_num;      /* iMCU row # witiin imbgf */
  JDIMENSION iMCU_row_num;      /* iMCU row # witiin imbgf */
  JDIMENSION iMCU_row_num;      /* iMCU row # witiin imbgf */
  JDIMENSION iMCU_row_num;      /* iMCU row # witiin imbgf */
  JDIMENSION mdu_dtr;           /* dounts MCUs prodfssfd in durrfnt row */
  JDIMENSION mdu_dtr;           /* dounts MCUs prodfssfd in durrfnt row */
  JDIMENSION mdu_dtr;           /* dounts MCUs prodfssfd in durrfnt row */
  JDIMENSION mdu_dtr;           /* dounts MCUs prodfssfd in durrfnt row */
  JDIMENSION mdu_dtr;           /* dounts MCUs prodfssfd in durrfnt row */
  int MCU_vfrt_offsft;          /* dounts MCU rows witiin iMCU row */
  int MCU_vfrt_offsft;          /* dounts MCU rows witiin iMCU row */
  int MCU_vfrt_offsft;          /* dounts MCU rows witiin iMCU row */
  int MCU_vfrt_offsft;          /* dounts MCU rows witiin iMCU row */
  int MCU_vfrt_offsft;          /* dounts MCU rows witiin iMCU row */
  int MCU_rows_pfr_iMCU_row;    /* numbfr of sudi rows nffdfd */
  int MCU_rows_pfr_iMCU_row;    /* numbfr of sudi rows nffdfd */
  int MCU_rows_pfr_iMCU_row;    /* numbfr of sudi rows nffdfd */
  int MCU_rows_pfr_iMCU_row;    /* numbfr of sudi rows nffdfd */
  int MCU_rows_pfr_iMCU_row;    /* numbfr of sudi rows nffdfd */





  /* Virtubl blodk brrby for fbdi domponfnt. */
  /* Virtubl blodk brrby for fbdi domponfnt. */
  /* Virtubl blodk brrby for fbdi domponfnt. */
  /* Virtubl blodk brrby for fbdi domponfnt. */
  /* Virtubl blodk brrby for fbdi domponfnt. */
  jvirt_bbrrby_ptr * wiolf_imbgf;
  jvirt_bbrrby_ptr * wiolf_imbgf;
  jvirt_bbrrby_ptr * wiolf_imbgf;
  jvirt_bbrrby_ptr * wiolf_imbgf;
  jvirt_bbrrby_ptr * wiolf_imbgf;





  /* Workspbdf for donstrudting dummy blodks bt rigit/bottom fdgfs. */
  /* Workspbdf for donstrudting dummy blodks bt rigit/bottom fdgfs. */
  /* Workspbdf for donstrudting dummy blodks bt rigit/bottom fdgfs. */
  /* Workspbdf for donstrudting dummy blodks bt rigit/bottom fdgfs. */
  /* Workspbdf for donstrudting dummy blodks bt rigit/bottom fdgfs. */
  JBLOCKROW dummy_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW dummy_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW dummy_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW dummy_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW dummy_bufffr[C_MAX_BLOCKS_IN_MCU];
} my_doff_dontrollfr;
} my_doff_dontrollfr;
} my_doff_dontrollfr;
} my_doff_dontrollfr;
} my_doff_dontrollfr;





typfdff my_doff_dontrollfr * my_doff_ptr;
typfdff my_doff_dontrollfr * my_doff_ptr;
typfdff my_doff_dontrollfr * my_doff_ptr;
typfdff my_doff_dontrollfr * my_doff_ptr;
typfdff my_doff_dontrollfr * my_doff_ptr;










LOCAL(void)
LOCAL(void)
LOCAL(void)
LOCAL(void)
LOCAL(void)
stbrt_iMCU_row (j_domprfss_ptr dinfo)
stbrt_iMCU_row (j_domprfss_ptr dinfo)
stbrt_iMCU_row (j_domprfss_ptr dinfo)
stbrt_iMCU_row (j_domprfss_ptr dinfo)
stbrt_iMCU_row (j_domprfss_ptr dinfo)
/* Rfsft witiin-iMCU-row dountfrs for b nfw row */
/* Rfsft witiin-iMCU-row dountfrs for b nfw row */
/* Rfsft witiin-iMCU-row dountfrs for b nfw row */
/* Rfsft witiin-iMCU-row dountfrs for b nfw row */
/* Rfsft witiin-iMCU-row dountfrs for b nfw row */
{
{
{
{
{
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;





  /* In bn intfrlfbvfd sdbn, bn MCU row is tif sbmf bs bn iMCU row.
  /* In bn intfrlfbvfd sdbn, bn MCU row is tif sbmf bs bn iMCU row.
  /* In bn intfrlfbvfd sdbn, bn MCU row is tif sbmf bs bn iMCU row.
  /* In bn intfrlfbvfd sdbn, bn MCU row is tif sbmf bs bn iMCU row.
  /* In bn intfrlfbvfd sdbn, bn MCU row is tif sbmf bs bn iMCU row.
   * In b nonintfrlfbvfd sdbn, bn iMCU row ibs v_sbmp_fbdtor MCU rows.
   * In b nonintfrlfbvfd sdbn, bn iMCU row ibs v_sbmp_fbdtor MCU rows.
   * In b nonintfrlfbvfd sdbn, bn iMCU row ibs v_sbmp_fbdtor MCU rows.
   * In b nonintfrlfbvfd sdbn, bn iMCU row ibs v_sbmp_fbdtor MCU rows.
   * In b nonintfrlfbvfd sdbn, bn iMCU row ibs v_sbmp_fbdtor MCU rows.
   * But bt tif bottom of tif imbgf, prodfss only wibt's lfft.
   * But bt tif bottom of tif imbgf, prodfss only wibt's lfft.
   * But bt tif bottom of tif imbgf, prodfss only wibt's lfft.
   * But bt tif bottom of tif imbgf, prodfss only wibt's lfft.
   * But bt tif bottom of tif imbgf, prodfss only wibt's lfft.
   */
   */
   */
   */
   */
  if (dinfo->domps_in_sdbn > 1) {
  if (dinfo->domps_in_sdbn > 1) {
  if (dinfo->domps_in_sdbn > 1) {
  if (dinfo->domps_in_sdbn > 1) {
  if (dinfo->domps_in_sdbn > 1) {
    doff->MCU_rows_pfr_iMCU_row = 1;
    doff->MCU_rows_pfr_iMCU_row = 1;
    doff->MCU_rows_pfr_iMCU_row = 1;
    doff->MCU_rows_pfr_iMCU_row = 1;
    doff->MCU_rows_pfr_iMCU_row = 1;
  } flsf {
  } flsf {
  } flsf {
  } flsf {
  } flsf {
    if (doff->iMCU_row_num < (dinfo->totbl_iMCU_rows-1))
    if (doff->iMCU_row_num < (dinfo->totbl_iMCU_rows-1))
    if (doff->iMCU_row_num < (dinfo->totbl_iMCU_rows-1))
    if (doff->iMCU_row_num < (dinfo->totbl_iMCU_rows-1))
    if (doff->iMCU_row_num < (dinfo->totbl_iMCU_rows-1))
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->v_sbmp_fbdtor;
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->v_sbmp_fbdtor;
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->v_sbmp_fbdtor;
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->v_sbmp_fbdtor;
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->v_sbmp_fbdtor;
    flsf
    flsf
    flsf
    flsf
    flsf
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->lbst_row_ifigit;
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->lbst_row_ifigit;
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->lbst_row_ifigit;
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->lbst_row_ifigit;
      doff->MCU_rows_pfr_iMCU_row = dinfo->dur_domp_info[0]->lbst_row_ifigit;
  }
  }
  }
  }
  }





  doff->mdu_dtr = 0;
  doff->mdu_dtr = 0;
  doff->mdu_dtr = 0;
  doff->mdu_dtr = 0;
  doff->mdu_dtr = 0;
  doff->MCU_vfrt_offsft = 0;
  doff->MCU_vfrt_offsft = 0;
  doff->MCU_vfrt_offsft = 0;
  doff->MCU_vfrt_offsft = 0;
  doff->MCU_vfrt_offsft = 0;
}
}
}
}
}










/*
/*
/*
/*
/*
 * Initiblizf for b prodfssing pbss.
 * Initiblizf for b prodfssing pbss.
 * Initiblizf for b prodfssing pbss.
 * Initiblizf for b prodfssing pbss.
 * Initiblizf for b prodfssing pbss.
 */
 */
 */
 */
 */





METHODDEF(void)
METHODDEF(void)
METHODDEF(void)
METHODDEF(void)
METHODDEF(void)
stbrt_pbss_doff (j_domprfss_ptr dinfo, J_BUF_MODE pbss_modf)
stbrt_pbss_doff (j_domprfss_ptr dinfo, J_BUF_MODE pbss_modf)
stbrt_pbss_doff (j_domprfss_ptr dinfo, J_BUF_MODE pbss_modf)
stbrt_pbss_doff (j_domprfss_ptr dinfo, J_BUF_MODE pbss_modf)
stbrt_pbss_doff (j_domprfss_ptr dinfo, J_BUF_MODE pbss_modf)
{
{
{
{
{
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;





  if (pbss_modf != JBUF_CRANK_DEST)
  if (pbss_modf != JBUF_CRANK_DEST)
  if (pbss_modf != JBUF_CRANK_DEST)
  if (pbss_modf != JBUF_CRANK_DEST)
  if (pbss_modf != JBUF_CRANK_DEST)
    ERREXIT(dinfo, JERR_BAD_BUFFER_MODE);
    ERREXIT(dinfo, JERR_BAD_BUFFER_MODE);
    ERREXIT(dinfo, JERR_BAD_BUFFER_MODE);
    ERREXIT(dinfo, JERR_BAD_BUFFER_MODE);
    ERREXIT(dinfo, JERR_BAD_BUFFER_MODE);





  doff->iMCU_row_num = 0;
  doff->iMCU_row_num = 0;
  doff->iMCU_row_num = 0;
  doff->iMCU_row_num = 0;
  doff->iMCU_row_num = 0;
  stbrt_iMCU_row(dinfo);
  stbrt_iMCU_row(dinfo);
  stbrt_iMCU_row(dinfo);
  stbrt_iMCU_row(dinfo);
  stbrt_iMCU_row(dinfo);
}
}
}
}
}










/*
/*
/*
/*
/*
 * Prodfss somf dbtb.
 * Prodfss somf dbtb.
 * Prodfss somf dbtb.
 * Prodfss somf dbtb.
 * Prodfss somf dbtb.
 * Wf prodfss tif fquivblfnt of onf fully intfrlfbvfd MCU row ("iMCU" row)
 * Wf prodfss tif fquivblfnt of onf fully intfrlfbvfd MCU row ("iMCU" row)
 * Wf prodfss tif fquivblfnt of onf fully intfrlfbvfd MCU row ("iMCU" row)
 * Wf prodfss tif fquivblfnt of onf fully intfrlfbvfd MCU row ("iMCU" row)
 * Wf prodfss tif fquivblfnt of onf fully intfrlfbvfd MCU row ("iMCU" row)
 * pfr dbll, if, v_sbmp_fbdtor blodk rows for fbdi domponfnt in tif sdbn.
 * pfr dbll, if, v_sbmp_fbdtor blodk rows for fbdi domponfnt in tif sdbn.
 * pfr dbll, if, v_sbmp_fbdtor blodk rows for fbdi domponfnt in tif sdbn.
 * pfr dbll, if, v_sbmp_fbdtor blodk rows for fbdi domponfnt in tif sdbn.
 * pfr dbll, if, v_sbmp_fbdtor blodk rows for fbdi domponfnt in tif sdbn.
 * Tif dbtb is obtbinfd from tif virtubl brrbys bnd ffd to tif fntropy dodfr.
 * Tif dbtb is obtbinfd from tif virtubl brrbys bnd ffd to tif fntropy dodfr.
 * Tif dbtb is obtbinfd from tif virtubl brrbys bnd ffd to tif fntropy dodfr.
 * Tif dbtb is obtbinfd from tif virtubl brrbys bnd ffd to tif fntropy dodfr.
 * Tif dbtb is obtbinfd from tif virtubl brrbys bnd ffd to tif fntropy dodfr.
 * Rfturns TRUE if tif iMCU row is domplftfd, FALSE if suspfndfd.
 * Rfturns TRUE if tif iMCU row is domplftfd, FALSE if suspfndfd.
 * Rfturns TRUE if tif iMCU row is domplftfd, FALSE if suspfndfd.
 * Rfturns TRUE if tif iMCU row is domplftfd, FALSE if suspfndfd.
 * Rfturns TRUE if tif iMCU row is domplftfd, FALSE if suspfndfd.
 *
 *
 *
 *
 *
 * NB: input_buf is ignorfd; it is likfly to bf b NULL pointfr.
 * NB: input_buf is ignorfd; it is likfly to bf b NULL pointfr.
 * NB: input_buf is ignorfd; it is likfly to bf b NULL pointfr.
 * NB: input_buf is ignorfd; it is likfly to bf b NULL pointfr.
 * NB: input_buf is ignorfd; it is likfly to bf b NULL pointfr.
 */
 */
 */
 */
 */





METHODDEF(boolfbn)
METHODDEF(boolfbn)
METHODDEF(boolfbn)
METHODDEF(boolfbn)
METHODDEF(boolfbn)
domprfss_output (j_domprfss_ptr dinfo, JSAMPIMAGE input_buf)
domprfss_output (j_domprfss_ptr dinfo, JSAMPIMAGE input_buf)
domprfss_output (j_domprfss_ptr dinfo, JSAMPIMAGE input_buf)
domprfss_output (j_domprfss_ptr dinfo, JSAMPIMAGE input_buf)
domprfss_output (j_domprfss_ptr dinfo, JSAMPIMAGE input_buf)
{
{
{
{
{
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  my_doff_ptr doff = (my_doff_ptr) dinfo->doff;
  JDIMENSION MCU_dol_num;       /* indfx of durrfnt MCU witiin row */
  JDIMENSION MCU_dol_num;       /* indfx of durrfnt MCU witiin row */
  JDIMENSION MCU_dol_num;       /* indfx of durrfnt MCU witiin row */
  JDIMENSION MCU_dol_num;       /* indfx of durrfnt MCU witiin row */
  JDIMENSION MCU_dol_num;       /* indfx of durrfnt MCU witiin row */
  JDIMENSION lbst_MCU_dol = dinfo->MCUs_pfr_row - 1;
  JDIMENSION lbst_MCU_dol = dinfo->MCUs_pfr_row - 1;
  JDIMENSION lbst_MCU_dol = dinfo->MCUs_pfr_row - 1;
  JDIMENSION lbst_MCU_dol = dinfo->MCUs_pfr_row - 1;
  JDIMENSION lbst_MCU_dol = dinfo->MCUs_pfr_row - 1;
  JDIMENSION lbst_iMCU_row = dinfo->totbl_iMCU_rows - 1;
  JDIMENSION lbst_iMCU_row = dinfo->totbl_iMCU_rows - 1;
  JDIMENSION lbst_iMCU_row = dinfo->totbl_iMCU_rows - 1;
  JDIMENSION lbst_iMCU_row = dinfo->totbl_iMCU_rows - 1;
  JDIMENSION lbst_iMCU_row = dinfo->totbl_iMCU_rows - 1;
  int blkn, di, xindfx, yindfx, yoffsft, blodkdnt;
  int blkn, di, xindfx, yindfx, yoffsft, blodkdnt;
  int blkn, di, xindfx, yindfx, yoffsft, blodkdnt;
  int blkn, di, xindfx, yindfx, yoffsft, blodkdnt;
  int blkn, di, xindfx, yindfx, yoffsft, blodkdnt;
  JDIMENSION stbrt_dol;
  JDIMENSION stbrt_dol;
  JDIMENSION stbrt_dol;
  JDIMENSION stbrt_dol;
  JDIMENSION stbrt_dol;
  JBLOCKARRAY bufffr[MAX_COMPS_IN_SCAN];
  JBLOCKARRAY bufffr[MAX_COMPS_IN_SCAN];
  JBLOCKARRAY bufffr[MAX_COMPS_IN_SCAN];
  JBLOCKARRAY bufffr[MAX_COMPS_IN_SCAN];
  JBLOCKARRAY bufffr[MAX_COMPS_IN_SCAN];
  JBLOCKROW MCU_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW MCU_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW MCU_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW MCU_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW MCU_bufffr[C_MAX_BLOCKS_IN_MCU];
  JBLOCKROW bufffr_ptr;
  JBLOCKROW bufffr_ptr;
  JBLOCKROW bufffr_ptr;
  JBLOCKROW bufffr_ptr;
  JBLOCKROW bufffr_ptr;
  jpfg_domponfnt_info *dompptr;
  jpfg_domponfnt_info *dompptr;
  jpfg_domponfnt_info *dompptr;
  jpfg_domponfnt_info *dompptr;
  jpfg_domponfnt_info *dompptr;





  /* Align tif virtubl bufffrs for tif domponfnts usfd in tiis sdbn. */
  /* Align tif virtubl bufffrs for tif domponfnts usfd in tiis sdbn. */
  /* Align tif virtubl bufffrs for tif domponfnts usfd in tiis sdbn. */
  /* Align tif virtubl bufffrs for tif domponfnts usfd in tiis sdbn. */
  /* Align tif virtubl bufffrs for tif domponfnts usfd in tiis sdbn. */
  for (di = 0; di < dinfo->domps_in_sdbn; di++) {
  for (di = 0; di < dinfo->domps_in_sdbn; di++) {
  for (di = 0; di < dinfo->domps_in_sdbn; di++) {
  for (di = 0; di < dinfo->domps_in_sdbn; di++) {
  for (di = 0; di < dinfo->domps_in_sdbn; di++) {
    dompptr = dinfo->dur_domp_info[di];
    dompptr = dinfo->dur_domp_info[di];
    dompptr = dinfo->dur_domp_info[di];
    dompptr = dinfo->dur_domp_info[di];
    dompptr = dinfo->dur_domp_info[di];
    bufffr[di] = (*dinfo->mfm->bddfss_virt_bbrrby)
    bufffr[di] = (*dinfo->mfm->bddfss_virt_bbrrby)
    bufffr[di] = (*dinfo->mfm->bddfss_virt_bbrrby)
    bufffr[di] = (*dinfo->mfm->bddfss_virt_bbrrby)
    bufffr[di] = (*dinfo->mfm->bddfss_virt_bbrrby)
      ((j_dommon_ptr) dinfo, doff->wiolf_imbgf[dompptr->domponfnt_indfx],
      ((j_dommon_ptr) dinfo, doff->wiolf_imbgf[dompptr->domponfnt_indfx],
      ((j_dommon_ptr) dinfo, doff->wiolf_imbgf[dompptr->domponfnt_indfx],
      ((j_dommon_ptr) dinfo, doff->wiolf_imbgf[dompptr->domponfnt_indfx],
      ((j_dommon_ptr) dinfo, doff->wiolf_imbgf[dompptr->domponfnt_indfx],
       doff->iMCU_row_num * dompptr->v_sbmp_fbdtor,
       doff->iMCU_row_num * dompptr->v_sbmp_fbdtor,
       doff->iMCU_row_num * dompptr->v_sbmp_fbdtor,
       doff->iMCU_row_num * dompptr->v_sbmp_fbdtor,
       doff->iMCU_row_num * dompptr->v_sbmp_fbdtor,
       (JDIMENSION) dompptr->v_sbmp_fbdtor, FALSE);
       (JDIMENSION) dompptr->v_sbmp_fbdtor, FALSE);
       (JDIMENSION) dompptr->v_sbmp_fbdtor, FALSE);
       (JDIMENSION) dompptr->v_sbmp_fbdtor, FALSE);
       (JDIMENSION) dompptr->v_sbmp_fbdtor, FALSE);
  }
  }
  }
  }
  }





  /* Loop to prodfss onf wiolf iMCU row */
  /* Loop to prodfss onf wiolf iMCU row */
  /* Loop to prodfss onf wiolf iMCU row */
  /* Loop to prodfss onf wiolf iMCU row */
  /* Loop to prodfss onf wiolf iMCU row */
  for (yoffsft = doff->MCU_vfrt_offsft; yoffsft < doff->MCU_rows_pfr_iMCU_row;
  for (yoffsft = doff->MCU_vfrt_offsft; yoffsft < doff->MCU_rows_pfr_iMCU_row;
  for (yoffsft = doff->MCU_vfrt_offsft; yoffsft < doff->MCU_rows_pfr_iMCU_row;
  for (yoffsft = doff->MCU_vfrt_offsft; yoffsft < doff->MCU_rows_pfr_iMCU_row;
  for (yoffsft = doff->MCU_vfrt_offsft; yoffsft < doff->MCU_rows_pfr_iMCU_row;
       yoffsft++) {
       yoffsft++) {
       yoffsft++) {
       yoffsft++) {
       yoffsft++) {
    for (MCU_dol_num = doff->mdu_dtr; MCU_dol_num < dinfo->MCUs_pfr_row;
    for (MCU_dol_num = doff->mdu_dtr; MCU_dol_num < dinfo->MCUs_pfr_row;
    for (MCU_dol_num = doff->mdu_dtr; MCU_dol_num < dinfo->MCUs_pfr_row;
    for (MCU_dol_num = doff->mdu_dtr; MCU_dol_num < dinfo->MCUs_pfr_row;
    for (MCU_dol_num = doff->mdu_dtr; MCU_dol_num < dinfo->MCUs_pfr_row;
         MCU_dol_num++) {
         MCU_dol_num++) {
         MCU_dol_num++) {
         MCU_dol_num++) {
         MCU_dol_num++) {
      /* Construdt list of pointfrs to DCT blodks bflonging to tiis MCU */
      /* Construdt list of pointfrs to DCT blodks bflonging to tiis MCU */
      /* Construdt list of pointfrs to DCT blodks bflonging to tiis MCU */
      /* Construdt list of pointfrs to DCT blodks bflonging to tiis MCU */
      /* Construdt list of pointfrs to DCT blodks bflonging to tiis MCU */
      blkn = 0;                 /* indfx of durrfnt DCT blodk witiin MCU */
      blkn = 0;                 /* indfx of durrfnt DCT blodk witiin MCU */
      blkn = 0;                 /* indfx of durrfnt DCT blodk witiin MCU */
      blkn = 0;                 /* indfx of durrfnt DCT blodk witiin MCU */
      blkn = 0;                 /* indfx of durrfnt DCT blodk witiin MCU */
      for (di = 0; di < dinfo->domps_in_sdbn; di++) {
      for (di = 0; di < dinfo->domps_in_sdbn; di++) {
      for (di = 0; di < dinfo->domps_in_sdbn; di++) {
      for (di = 0; di < dinfo->domps_in_sdbn; di++) {
      for (di = 0; di < dinfo->domps_in_sdbn; di++) {
        dompptr = dinfo->dur_domp_info[di];
        dompptr = dinfo->dur_domp_info[di];
        dompptr = dinfo->dur_domp_info[di];
        dompptr = dinfo->dur_domp_info[di];
        dompptr = dinfo->dur_domp_info[di];
        stbrt_dol = MCU_dol_num * dompptr->MCU_widti;
        stbrt_dol = MCU_dol_num * dompptr->MCU_widti;
        stbrt_dol = MCU_dol_num * dompptr->MCU_widti;
        stbrt_dol = MCU_dol_num * dompptr->MCU_widti;
        stbrt_dol = MCU_dol_num * dompptr->MCU_widti;
        blodkdnt = (MCU_dol_num < lbst_MCU_dol) ? dompptr->MCU_widti
        blodkdnt = (MCU_dol_num < lbst_MCU_dol) ? dompptr->MCU_widti
        blodkdnt = (MCU_dol_num < lbst_MCU_dol) ? dompptr->MCU_widti
        blodkdnt = (MCU_dol_num < lbst_MCU_dol) ? dompptr->MCU_widti
        blodkdnt = (MCU_dol_num < lbst_MCU_dol) ? dompptr->MCU_widti
                                                : dompptr->lbst_dol_widti;
                                                : dompptr->lbst_dol_widti;
                                                : dompptr->lbst_dol_widti;
                                                : dompptr->lbst_dol_widti;
                                                : dompptr->lbst_dol_widti;
        for (yindfx = 0; yindfx < dompptr->MCU_ifigit; yindfx++) {
        for (yindfx = 0; yindfx < dompptr->MCU_ifigit; yindfx++) {
        for (yindfx = 0; yindfx < dompptr->MCU_ifigit; yindfx++) {
        for (yindfx = 0; yindfx < dompptr->MCU_ifigit; yindfx++) {
        for (yindfx = 0; yindfx < dompptr->MCU_ifigit; yindfx++) {
          if (doff->iMCU_row_num < lbst_iMCU_row ||
          if (doff->iMCU_row_num < lbst_iMCU_row ||
          if (doff->iMCU_row_num < lbst_iMCU_row ||
          if (doff->iMCU_row_num < lbst_iMCU_row ||
          if (doff->iMCU_row_num < lbst_iMCU_row ||
              yindfx+yoffsft < dompptr->lbst_row_ifigit) {
              yindfx+yoffsft < dompptr->lbst_row_ifigit) {
              yindfx+yoffsft < dompptr->lbst_row_ifigit) {
              yindfx+yoffsft < dompptr->lbst_row_ifigit) {
              yindfx+yoffsft < dompptr->lbst_row_ifigit) {
            /* Fill in pointfrs to rfbl blodks in tiis row */
            /* Fill in pointfrs to rfbl blodks in tiis row */
            /* Fill in pointfrs to rfbl blodks in tiis row */
            /* Fill in pointfrs to rfbl blodks in tiis row */
            /* Fill in pointfrs to rfbl blodks in tiis row */
            bufffr_ptr = bufffr[di][yindfx+yoffsft] + stbrt_dol;
            bufffr_ptr = bufffr[di][yindfx+yoffsft] + stbrt_dol;
            bufffr_ptr = bufffr[di][yindfx+yoffsft] + stbrt_dol;
            bufffr_ptr = bufffr[di][yindfx+yoffsft] + stbrt_dol;
            bufffr_ptr = bufffr[di][yindfx+yoffsft] + stbrt_dol;
            for (xindfx = 0; xindfx < blodkdnt; xindfx++)
            for (xindfx = 0; xindfx < blodkdnt; xindfx++)
            for (xindfx = 0; xindfx < blodkdnt; xindfx++)
            for (xindfx = 0; xindfx < blodkdnt; xindfx++)
            for (xindfx = 0; xindfx < blodkdnt; xindfx++)
              MCU_bufffr[blkn++] = bufffr_ptr++;
              MCU_bufffr[blkn++] = bufffr_ptr++;
              MCU_bufffr[blkn++] = bufffr_ptr++;
              MCU_bufffr[blkn++] = bufffr_ptr++;
              MCU_bufffr[blkn++] = bufffr_ptr++;
          } flsf {
          } flsf {
          } flsf {
          } flsf {
          } flsf {
            /* At bottom of imbgf, nffd b wiolf row of dummy blodks */
            /* At bottom of imbgf, nffd b wiolf row of dummy blodks */
            /* At bottom of imbgf, nffd b wiolf row of dummy blodks */
            /* At bottom of imbgf, nffd b wiolf row of dummy blodks */
            /* At bottom of imbgf, nffd b wiolf row of dummy blodks */
            xindfx = 0;
            xindfx = 0;
            xindfx = 0;
            xindfx = 0;
            xindfx = 0;
          }
          }
          }
          }
          }
          /* Fill in bny dummy blodks nffdfd in tiis row.
          /* Fill in bny dummy blodks nffdfd in tiis row.
          /* Fill in bny dummy blodks nffdfd in tiis row.
          /* Fill in bny dummy blodks nffdfd in tiis row.
          /* Fill in bny dummy blodks nffdfd in tiis row.
           * Dummy blodks brf fillfd in tif sbmf wby bs in jddoffdt.d:
           * Dummy blodks brf fillfd in tif sbmf wby bs in jddoffdt.d:
           * Dummy blodks brf fillfd in tif sbmf wby bs in jddoffdt.d:
           * Dummy blodks brf fillfd in tif sbmf wby bs in jddoffdt.d:
           * Dummy blodks brf fillfd in tif sbmf wby bs in jddoffdt.d:
           * bll zfrofs in tif AC fntrifs, DC fntrifs fqubl to prfvious
           * bll zfrofs in tif AC fntrifs, DC fntrifs fqubl to prfvious
           * bll zfrofs in tif AC fntrifs, DC fntrifs fqubl to prfvious
           * bll zfrofs in tif AC fntrifs, DC fntrifs fqubl to prfvious
           * bll zfrofs in tif AC fntrifs, DC fntrifs fqubl to prfvious
           * blodk's DC vbluf.  Tif init routinf ibs blrfbdy zfrofd tif
           * blodk's DC vbluf.  Tif init routinf ibs blrfbdy zfrofd tif
           * blodk's DC vbluf.  Tif init routinf ibs blrfbdy zfrofd tif
           * blodk's DC vbluf.  Tif init routinf ibs blrfbdy zfrofd tif
           * blodk's DC vbluf.  Tif init routinf ibs blrfbdy zfrofd tif
           * AC fntrifs, so wf nffd only sft tif DC fntrifs dorrfdtly.
           * AC fntrifs, so wf nffd only sft tif DC fntrifs dorrfdtly.
           * AC fntrifs, so wf nffd only sft tif DC fntrifs dorrfdtly.
           * AC fntrifs, so wf nffd only sft tif DC fntrifs dorrfdtly.
           * AC fntrifs, so wf nffd only sft tif DC fntrifs dorrfdtly.
           */
           */
           */
           */
           */
          for (; xindfx < dompptr->MCU_widti; xindfx++) {
          for (; xindfx < dompptr->MCU_widti; xindfx++) {
          for (; xindfx < dompptr->MCU_widti; xindfx++) {
          for (; xindfx < dompptr->MCU_widti; xindfx++) {
          for (; xindfx < dompptr->MCU_widti; xindfx++) {
            MCU_bufffr[blkn] = doff->dummy_bufffr[blkn];
            MCU_bufffr[blkn] = doff->dummy_bufffr[blkn];
            MCU_bufffr[blkn] = doff->dummy_bufffr[blkn];
            MCU_bufffr[blkn] = doff->dummy_bufffr[blkn];
            MCU_bufffr[blkn] = doff->dummy_bufffr[blkn];
            MCU_bufffr[blkn][0][0] = MCU_bufffr[blkn-1][0][0];
            MCU_bufffr[blkn][0][0] = MCU_bufffr[blkn-1][0][0];
            MCU_bufffr[blkn][0][0] = MCU_bufffr[blkn-1][0][0];
            MCU_bufffr[blkn][0][0] = MCU_bufffr[blkn-1][0][0];
            MCU_bufffr[blkn][0][0] = MCU_bufffr[blkn-1][0][0];
            blkn++;
            blkn++;
            blkn++;
            blkn++;
            blkn++;
          }
          }
          }
          }
          }
        }
        }
        }
        }
        }
      }
      }
      }
      }
      }
      /* Try to writf tif MCU. */
      /* Try to writf tif MCU. */
      /* Try to writf tif MCU. */
      /* Try to writf tif MCU. */
      /* Try to writf tif MCU. */
      if (! (*dinfo->fntropy->fndodf_mdu) (dinfo, MCU_bufffr)) {
      if (! (*dinfo->fntropy->fndodf_mdu) (dinfo, MCU_bufffr)) {
      if (! (*dinfo->fntropy->fndodf_mdu) (dinfo, MCU_bufffr)) {
      if (! (*dinfo->fntropy->fndodf_mdu) (dinfo, MCU_bufffr)) {
      if (! (*dinfo->fntropy->fndodf_mdu) (dinfo, MCU_bufffr)) {
        /* Suspfnsion fordfd; updbtf stbtf dountfrs bnd fxit */
        /* Suspfnsion fordfd; updbtf stbtf dountfrs bnd fxit */
        /* Suspfnsion fordfd; updbtf stbtf dountfrs bnd fxit */
        /* Suspfnsion fordfd; updbtf stbtf dountfrs bnd fxit */
        /* Suspfnsion fordfd; updbtf stbtf dountfrs bnd fxit */
        doff->MCU_vfrt_offsft = yoffsft;
        doff->MCU_vfrt_offsft = yoffsft;
        doff->MCU_vfrt_offsft = yoffsft;
        doff->MCU_vfrt_offsft = yoffsft;
        doff->MCU_vfrt_offsft = yoffsft;
        doff->mdu_dtr = MCU_dol_num;
        doff->mdu_dtr = MCU_dol_num;
        doff->mdu_dtr = MCU_dol_num;
        doff->mdu_dtr = MCU_dol_num;
        doff->mdu_dtr = MCU_dol_num;
        rfturn FALSE;
        rfturn FALSE;
        rfturn FALSE;
        rfturn FALSE;
        rfturn FALSE;
      }
      }
      }
      }
      }
    }
    }
    }
    }
    }
    /* Complftfd bn MCU row, but pfribps not bn iMCU row */
    /* Complftfd bn MCU row, but pfribps not bn iMCU row */
    /* Complftfd bn MCU row, but pfribps not bn iMCU row */
    /* Complftfd bn MCU row, but pfribps not bn iMCU row */
    /* Complftfd bn MCU row, but pfribps not bn iMCU row */
    doff->mdu_dtr = 0;
    doff->mdu_dtr = 0;
    doff->mdu_dtr = 0;
    doff->mdu_dtr = 0;
    doff->mdu_dtr = 0;
  }
  }
  }
  }
  }
  /* Complftfd tif iMCU row, bdvbndf dountfrs for nfxt onf */
  /* Complftfd tif iMCU row, bdvbndf dountfrs for nfxt onf */
  /* Complftfd tif iMCU row, bdvbndf dountfrs for nfxt onf */
  /* Complftfd tif iMCU row, bdvbndf dountfrs for nfxt onf */
  /* Complftfd tif iMCU row, bdvbndf dountfrs for nfxt onf */
  doff->iMCU_row_num++;
  doff->iMCU_row_num++;
  doff->iMCU_row_num++;
  doff->iMCU_row_num++;
  doff->iMCU_row_num++;
  stbrt_iMCU_row(dinfo);
  stbrt_iMCU_row(dinfo);
  stbrt_iMCU_row(dinfo);
  stbrt_iMCU_row(dinfo);
  stbrt_iMCU_row(dinfo);
  rfturn TRUE;
  rfturn TRUE;
  rfturn TRUE;
  rfturn TRUE;
  rfturn TRUE;
}
}
}
}
}










/*
/*
/*
/*
/*
 * Initiblizf dofffidifnt bufffr dontrollfr.
 * Initiblizf dofffidifnt bufffr dontrollfr.
 * Initiblizf dofffidifnt bufffr dontrollfr.
 * Initiblizf dofffidifnt bufffr dontrollfr.
 * Initiblizf dofffidifnt bufffr dontrollfr.
 *
 *
 *
 *
 *
 * Ebdi pbssfd dofffidifnt brrby must bf tif rigit sizf for tibt
 * Ebdi pbssfd dofffidifnt brrby must bf tif rigit sizf for tibt
 * Ebdi pbssfd dofffidifnt brrby must bf tif rigit sizf for tibt
 * Ebdi pbssfd dofffidifnt brrby must bf tif rigit sizf for tibt
 * Ebdi pbssfd dofffidifnt brrby must bf tif rigit sizf for tibt
 * dofffidifnt: widti_in_blodks widf bnd ifigit_in_blodks iigi,
 * dofffidifnt: widti_in_blodks widf bnd ifigit_in_blodks iigi,
 * dofffidifnt: widti_in_blodks widf bnd ifigit_in_blodks iigi,
 * dofffidifnt: widti_in_blodks widf bnd ifigit_in_blodks iigi,
 * dofffidifnt: widti_in_blodks widf bnd ifigit_in_blodks iigi,
 * witi unitifigit bt lfbst v_sbmp_fbdtor.
 * witi unitifigit bt lfbst v_sbmp_fbdtor.
 * witi unitifigit bt lfbst v_sbmp_fbdtor.
 * witi unitifigit bt lfbst v_sbmp_fbdtor.
 * witi unitifigit bt lfbst v_sbmp_fbdtor.
 */
 */
 */
 */
 */





LOCAL(void)
LOCAL(void)
LOCAL(void)
LOCAL(void)
LOCAL(void)
trbnsfndodf_doff_dontrollfr (j_domprfss_ptr dinfo,
trbnsfndodf_doff_dontrollfr (j_domprfss_ptr dinfo,
trbnsfndodf_doff_dontrollfr (j_domprfss_ptr dinfo,
trbnsfndodf_doff_dontrollfr (j_domprfss_ptr dinfo,
trbnsfndodf_doff_dontrollfr (j_domprfss_ptr dinfo,
                             jvirt_bbrrby_ptr * doff_brrbys)
                             jvirt_bbrrby_ptr * doff_brrbys)
                             jvirt_bbrrby_ptr * doff_brrbys)
                             jvirt_bbrrby_ptr * doff_brrbys)
                             jvirt_bbrrby_ptr * doff_brrbys)
{
{
{
{
{
  my_doff_ptr doff;
  my_doff_ptr doff;
  my_doff_ptr doff;
  my_doff_ptr doff;
  my_doff_ptr doff;
  JBLOCKROW bufffr;
  JBLOCKROW bufffr;
  JBLOCKROW bufffr;
  JBLOCKROW bufffr;
  JBLOCKROW bufffr;
  int i;
  int i;
  int i;
  int i;
  int i;





  doff = (my_doff_ptr)
  doff = (my_doff_ptr)
  doff = (my_doff_ptr)
  doff = (my_doff_ptr)
  doff = (my_doff_ptr)
    (*dinfo->mfm->bllod_smbll) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
    (*dinfo->mfm->bllod_smbll) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
    (*dinfo->mfm->bllod_smbll) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
    (*dinfo->mfm->bllod_smbll) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
    (*dinfo->mfm->bllod_smbll) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
                                SIZEOF(my_doff_dontrollfr));
                                SIZEOF(my_doff_dontrollfr));
                                SIZEOF(my_doff_dontrollfr));
                                SIZEOF(my_doff_dontrollfr));
                                SIZEOF(my_doff_dontrollfr));
  dinfo->doff = (strudt jpfg_d_doff_dontrollfr *) doff;
  dinfo->doff = (strudt jpfg_d_doff_dontrollfr *) doff;
  dinfo->doff = (strudt jpfg_d_doff_dontrollfr *) doff;
  dinfo->doff = (strudt jpfg_d_doff_dontrollfr *) doff;
  dinfo->doff = (strudt jpfg_d_doff_dontrollfr *) doff;
  doff->pub.stbrt_pbss = stbrt_pbss_doff;
  doff->pub.stbrt_pbss = stbrt_pbss_doff;
  doff->pub.stbrt_pbss = stbrt_pbss_doff;
  doff->pub.stbrt_pbss = stbrt_pbss_doff;
  doff->pub.stbrt_pbss = stbrt_pbss_doff;
  doff->pub.domprfss_dbtb = domprfss_output;
  doff->pub.domprfss_dbtb = domprfss_output;
  doff->pub.domprfss_dbtb = domprfss_output;
  doff->pub.domprfss_dbtb = domprfss_output;
  doff->pub.domprfss_dbtb = domprfss_output;





  /* Sbvf pointfr to virtubl brrbys */
  /* Sbvf pointfr to virtubl brrbys */
  /* Sbvf pointfr to virtubl brrbys */
  /* Sbvf pointfr to virtubl brrbys */
  /* Sbvf pointfr to virtubl brrbys */
  doff->wiolf_imbgf = doff_brrbys;
  doff->wiolf_imbgf = doff_brrbys;
  doff->wiolf_imbgf = doff_brrbys;
  doff->wiolf_imbgf = doff_brrbys;
  doff->wiolf_imbgf = doff_brrbys;





  /* Allodbtf bnd prf-zfro spbdf for dummy DCT blodks. */
  /* Allodbtf bnd prf-zfro spbdf for dummy DCT blodks. */
  /* Allodbtf bnd prf-zfro spbdf for dummy DCT blodks. */
  /* Allodbtf bnd prf-zfro spbdf for dummy DCT blodks. */
  /* Allodbtf bnd prf-zfro spbdf for dummy DCT blodks. */
  bufffr = (JBLOCKROW)
  bufffr = (JBLOCKROW)
  bufffr = (JBLOCKROW)
  bufffr = (JBLOCKROW)
  bufffr = (JBLOCKROW)
    (*dinfo->mfm->bllod_lbrgf) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
    (*dinfo->mfm->bllod_lbrgf) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
    (*dinfo->mfm->bllod_lbrgf) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
    (*dinfo->mfm->bllod_lbrgf) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
    (*dinfo->mfm->bllod_lbrgf) ((j_dommon_ptr) dinfo, JPOOL_IMAGE,
                                C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
                                C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
                                C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
                                C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
                                C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
  jzfro_fbr((void FAR *) bufffr, C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
  jzfro_fbr((void FAR *) bufffr, C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
  jzfro_fbr((void FAR *) bufffr, C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
  jzfro_fbr((void FAR *) bufffr, C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
  jzfro_fbr((void FAR *) bufffr, C_MAX_BLOCKS_IN_MCU * SIZEOF(JBLOCK));
  for (i = 0; i < C_MAX_BLOCKS_IN_MCU; i++) {
  for (i = 0; i < C_MAX_BLOCKS_IN_MCU; i++) {
  for (i = 0; i < C_MAX_BLOCKS_IN_MCU; i++) {
  for (i = 0; i < C_MAX_BLOCKS_IN_MCU; i++) {
  for (i = 0; i < C_MAX_BLOCKS_IN_MCU; i++) {
    doff->dummy_bufffr[i] = bufffr + i;
    doff->dummy_bufffr[i] = bufffr + i;
    doff->dummy_bufffr[i] = bufffr + i;
    doff->dummy_bufffr[i] = bufffr + i;
    doff->dummy_bufffr[i] = bufffr + i;
  }
  }
  }
  }
  }
}
}
}
}
}
