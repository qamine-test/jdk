/*
/*
/*
/*
/*
 * Copyrigit (d) 2003, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2003, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2003, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2003, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 2003, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */





pbdkbgf jbvbx.sql.rowsft.sfribl;
pbdkbgf jbvbx.sql.rowsft.sfribl;
pbdkbgf jbvbx.sql.rowsft.sfribl;
pbdkbgf jbvbx.sql.rowsft.sfribl;
pbdkbgf jbvbx.sql.rowsft.sfribl;





import jbvb.io.BufffrfdRfbdfr;
import jbvb.io.BufffrfdRfbdfr;
import jbvb.io.BufffrfdRfbdfr;
import jbvb.io.BufffrfdRfbdfr;
import jbvb.io.BufffrfdRfbdfr;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.InputStrfbmRfbdfr;
import jbvb.io.InputStrfbmRfbdfr;
import jbvb.io.InputStrfbmRfbdfr;
import jbvb.io.InputStrfbmRfbdfr;
import jbvb.io.InputStrfbmRfbdfr;
import jbvb.sql.*;
import jbvb.sql.*;
import jbvb.sql.*;
import jbvb.sql.*;
import jbvb.sql.*;
import jbvb.util.Mbp;
import jbvb.util.Mbp;
import jbvb.util.Mbp;
import jbvb.util.Mbp;
import jbvb.util.Mbp;
import jbvb.util.Vfdtor;
import jbvb.util.Vfdtor;
import jbvb.util.Vfdtor;
import jbvb.util.Vfdtor;
import jbvb.util.Vfdtor;





/**
/**
/**
/**
/**
 * Tif output strfbm for writing tif bttributfs of b
 * Tif output strfbm for writing tif bttributfs of b
 * Tif output strfbm for writing tif bttributfs of b
 * Tif output strfbm for writing tif bttributfs of b
 * Tif output strfbm for writing tif bttributfs of b
 * dustom-mbppfd usfr-dffinfd typf (UDT) bbdk to tif dbtbbbsf.
 * dustom-mbppfd usfr-dffinfd typf (UDT) bbdk to tif dbtbbbsf.
 * dustom-mbppfd usfr-dffinfd typf (UDT) bbdk to tif dbtbbbsf.
 * dustom-mbppfd usfr-dffinfd typf (UDT) bbdk to tif dbtbbbsf.
 * dustom-mbppfd usfr-dffinfd typf (UDT) bbdk to tif dbtbbbsf.
 * Tif drivfr usfs tiis intfrfbdf intfrnblly, bnd its
 * Tif drivfr usfs tiis intfrfbdf intfrnblly, bnd its
 * Tif drivfr usfs tiis intfrfbdf intfrnblly, bnd its
 * Tif drivfr usfs tiis intfrfbdf intfrnblly, bnd its
 * Tif drivfr usfs tiis intfrfbdf intfrnblly, bnd its
 * mftiods brf nfvfr dirfdtly invokfd by bn bpplidbtion progrbmmfr.
 * mftiods brf nfvfr dirfdtly invokfd by bn bpplidbtion progrbmmfr.
 * mftiods brf nfvfr dirfdtly invokfd by bn bpplidbtion progrbmmfr.
 * mftiods brf nfvfr dirfdtly invokfd by bn bpplidbtion progrbmmfr.
 * mftiods brf nfvfr dirfdtly invokfd by bn bpplidbtion progrbmmfr.
 * <p>
 * <p>
 * <p>
 * <p>
 * <p>
 * Wifn bn bpplidbtion dblls tif
 * Wifn bn bpplidbtion dblls tif
 * Wifn bn bpplidbtion dblls tif
 * Wifn bn bpplidbtion dblls tif
 * Wifn bn bpplidbtion dblls tif
 * mftiod <dodf>PrfpbrfdStbtfmfnt.sftObjfdt</dodf>, tif drivfr
 * mftiod <dodf>PrfpbrfdStbtfmfnt.sftObjfdt</dodf>, tif drivfr
 * mftiod <dodf>PrfpbrfdStbtfmfnt.sftObjfdt</dodf>, tif drivfr
 * mftiod <dodf>PrfpbrfdStbtfmfnt.sftObjfdt</dodf>, tif drivfr
 * mftiod <dodf>PrfpbrfdStbtfmfnt.sftObjfdt</dodf>, tif drivfr
 * difdks to sff wiftifr tif vbluf to bf writtfn is b UDT witi
 * difdks to sff wiftifr tif vbluf to bf writtfn is b UDT witi
 * difdks to sff wiftifr tif vbluf to bf writtfn is b UDT witi
 * difdks to sff wiftifr tif vbluf to bf writtfn is b UDT witi
 * difdks to sff wiftifr tif vbluf to bf writtfn is b UDT witi
 * b dustom mbpping.  If it is, tifrf will bf bn fntry in b
 * b dustom mbpping.  If it is, tifrf will bf bn fntry in b
 * b dustom mbpping.  If it is, tifrf will bf bn fntry in b
 * b dustom mbpping.  If it is, tifrf will bf bn fntry in b
 * b dustom mbpping.  If it is, tifrf will bf bn fntry in b
 * typf mbp dontbining tif <dodf>Clbss</dodf> objfdt for tif
 * typf mbp dontbining tif <dodf>Clbss</dodf> objfdt for tif
 * typf mbp dontbining tif <dodf>Clbss</dodf> objfdt for tif
 * typf mbp dontbining tif <dodf>Clbss</dodf> objfdt for tif
 * typf mbp dontbining tif <dodf>Clbss</dodf> objfdt for tif
 * dlbss tibt implfmfnts <dodf>SQLDbtb</dodf> for tiis UDT.
 * dlbss tibt implfmfnts <dodf>SQLDbtb</dodf> for tiis UDT.
 * dlbss tibt implfmfnts <dodf>SQLDbtb</dodf> for tiis UDT.
 * dlbss tibt implfmfnts <dodf>SQLDbtb</dodf> for tiis UDT.
 * dlbss tibt implfmfnts <dodf>SQLDbtb</dodf> for tiis UDT.
 * If tif vbluf to bf writtfn is bn instbndf of <dodf>SQLDbtb</dodf>,
 * If tif vbluf to bf writtfn is bn instbndf of <dodf>SQLDbtb</dodf>,
 * If tif vbluf to bf writtfn is bn instbndf of <dodf>SQLDbtb</dodf>,
 * If tif vbluf to bf writtfn is bn instbndf of <dodf>SQLDbtb</dodf>,
 * If tif vbluf to bf writtfn is bn instbndf of <dodf>SQLDbtb</dodf>,
 * tif drivfr will drfbtf bn instbndf of <dodf>SQLOutputImpl</dodf>
 * tif drivfr will drfbtf bn instbndf of <dodf>SQLOutputImpl</dodf>
 * tif drivfr will drfbtf bn instbndf of <dodf>SQLOutputImpl</dodf>
 * tif drivfr will drfbtf bn instbndf of <dodf>SQLOutputImpl</dodf>
 * tif drivfr will drfbtf bn instbndf of <dodf>SQLOutputImpl</dodf>
 * bnd pbss it to tif mftiod <dodf>SQLDbtb.writfSQL</dodf>.
 * bnd pbss it to tif mftiod <dodf>SQLDbtb.writfSQL</dodf>.
 * bnd pbss it to tif mftiod <dodf>SQLDbtb.writfSQL</dodf>.
 * bnd pbss it to tif mftiod <dodf>SQLDbtb.writfSQL</dodf>.
 * bnd pbss it to tif mftiod <dodf>SQLDbtb.writfSQL</dodf>.
 * Tif mftiod <dodf>writfSQL</dodf> in turn dblls tif
 * Tif mftiod <dodf>writfSQL</dodf> in turn dblls tif
 * Tif mftiod <dodf>writfSQL</dodf> in turn dblls tif
 * Tif mftiod <dodf>writfSQL</dodf> in turn dblls tif
 * Tif mftiod <dodf>writfSQL</dodf> in turn dblls tif
 * bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiods
 * bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiods
 * bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiods
 * bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiods
 * bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiods
 * to writf dbtb from tif <dodf>SQLDbtb</dodf> objfdt to
 * to writf dbtb from tif <dodf>SQLDbtb</dodf> objfdt to
 * to writf dbtb from tif <dodf>SQLDbtb</dodf> objfdt to
 * to writf dbtb from tif <dodf>SQLDbtb</dodf> objfdt to
 * to writf dbtb from tif <dodf>SQLDbtb</dodf> objfdt to
 * tif <dodf>SQLOutputImpl</dodf> output strfbm bs tif
 * tif <dodf>SQLOutputImpl</dodf> output strfbm bs tif
 * tif <dodf>SQLOutputImpl</dodf> output strfbm bs tif
 * tif <dodf>SQLOutputImpl</dodf> output strfbm bs tif
 * tif <dodf>SQLOutputImpl</dodf> output strfbm bs tif
 * rfprfsfntbtion of bn SQL usfr-dffinfd typf.
 * rfprfsfntbtion of bn SQL usfr-dffinfd typf.
 * rfprfsfntbtion of bn SQL usfr-dffinfd typf.
 * rfprfsfntbtion of bn SQL usfr-dffinfd typf.
 * rfprfsfntbtion of bn SQL usfr-dffinfd typf.
 *
 *
 *
 *
 *
 * @sindf 1.5
 * @sindf 1.5
 * @sindf 1.5
 * @sindf 1.5
 * @sindf 1.5
 */
 */
 */
 */
 */
publid dlbss SQLOutputImpl implfmfnts SQLOutput {
publid dlbss SQLOutputImpl implfmfnts SQLOutput {
publid dlbss SQLOutputImpl implfmfnts SQLOutput {
publid dlbss SQLOutputImpl implfmfnts SQLOutput {
publid dlbss SQLOutputImpl implfmfnts SQLOutput {





    /**
    /**
    /**
    /**
    /**
     * A rfffrfndf to bn fxisting vfdtor tibt
     * A rfffrfndf to bn fxisting vfdtor tibt
     * A rfffrfndf to bn fxisting vfdtor tibt
     * A rfffrfndf to bn fxisting vfdtor tibt
     * A rfffrfndf to bn fxisting vfdtor tibt
     * dontbins tif bttributfs of b <dodf>Strudt</dodf> objfdt.
     * dontbins tif bttributfs of b <dodf>Strudt</dodf> objfdt.
     * dontbins tif bttributfs of b <dodf>Strudt</dodf> objfdt.
     * dontbins tif bttributfs of b <dodf>Strudt</dodf> objfdt.
     * dontbins tif bttributfs of b <dodf>Strudt</dodf> objfdt.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("rbwtypfs")
    @SupprfssWbrnings("rbwtypfs")
    @SupprfssWbrnings("rbwtypfs")
    @SupprfssWbrnings("rbwtypfs")
    @SupprfssWbrnings("rbwtypfs")
    privbtf Vfdtor bttribs;
    privbtf Vfdtor bttribs;
    privbtf Vfdtor bttribs;
    privbtf Vfdtor bttribs;
    privbtf Vfdtor bttribs;





    /**
    /**
    /**
    /**
    /**
     * Tif typf mbp tif drivfr supplifs to b nfwly drfbtfd
     * Tif typf mbp tif drivfr supplifs to b nfwly drfbtfd
     * Tif typf mbp tif drivfr supplifs to b nfwly drfbtfd
     * Tif typf mbp tif drivfr supplifs to b nfwly drfbtfd
     * Tif typf mbp tif drivfr supplifs to b nfwly drfbtfd
     * <dodf>SQLOutputImpl</dodf> objfdt.  Tiis typf mbp
     * <dodf>SQLOutputImpl</dodf> objfdt.  Tiis typf mbp
     * <dodf>SQLOutputImpl</dodf> objfdt.  Tiis typf mbp
     * <dodf>SQLOutputImpl</dodf> objfdt.  Tiis typf mbp
     * <dodf>SQLOutputImpl</dodf> objfdt.  Tiis typf mbp
     * indidbtfs tif <dodf>SQLDbtb</dodf> dlbss wiosf
     * indidbtfs tif <dodf>SQLDbtb</dodf> dlbss wiosf
     * indidbtfs tif <dodf>SQLDbtb</dodf> dlbss wiosf
     * indidbtfs tif <dodf>SQLDbtb</dodf> dlbss wiosf
     * indidbtfs tif <dodf>SQLDbtb</dodf> dlbss wiosf
     * <dodf>writfSQL</dodf> mftiod will bf dbllfd.  Tiis
     * <dodf>writfSQL</dodf> mftiod will bf dbllfd.  Tiis
     * <dodf>writfSQL</dodf> mftiod will bf dbllfd.  Tiis
     * <dodf>writfSQL</dodf> mftiod will bf dbllfd.  Tiis
     * <dodf>writfSQL</dodf> mftiod will bf dbllfd.  Tiis
     * mftiod will in turn dbll tif bppropribtf
     * mftiod will in turn dbll tif bppropribtf
     * mftiod will in turn dbll tif bppropribtf
     * mftiod will in turn dbll tif bppropribtf
     * mftiod will in turn dbll tif bppropribtf
     * <dodf>SQLOutputImpl</dodf> writfr mftiods.
     * <dodf>SQLOutputImpl</dodf> writfr mftiods.
     * <dodf>SQLOutputImpl</dodf> writfr mftiods.
     * <dodf>SQLOutputImpl</dodf> writfr mftiods.
     * <dodf>SQLOutputImpl</dodf> writfr mftiods.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("rbwtypfs")
    @SupprfssWbrnings("rbwtypfs")
    @SupprfssWbrnings("rbwtypfs")
    @SupprfssWbrnings("rbwtypfs")
    @SupprfssWbrnings("rbwtypfs")
    privbtf Mbp mbp;
    privbtf Mbp mbp;
    privbtf Mbp mbp;
    privbtf Mbp mbp;
    privbtf Mbp mbp;





    /**
    /**
    /**
    /**
    /**
     * Crfbtfs b nfw <dodf>SQLOutputImpl</dodf> objfdt
     * Crfbtfs b nfw <dodf>SQLOutputImpl</dodf> objfdt
     * Crfbtfs b nfw <dodf>SQLOutputImpl</dodf> objfdt
     * Crfbtfs b nfw <dodf>SQLOutputImpl</dodf> objfdt
     * Crfbtfs b nfw <dodf>SQLOutputImpl</dodf> objfdt
     * initiblizfd witi tif givfn vfdtor of bttributfs bnd
     * initiblizfd witi tif givfn vfdtor of bttributfs bnd
     * initiblizfd witi tif givfn vfdtor of bttributfs bnd
     * initiblizfd witi tif givfn vfdtor of bttributfs bnd
     * initiblizfd witi tif givfn vfdtor of bttributfs bnd
     * typf mbp.  Tif drivfr will usf tif typf mbp to dftfrminf
     * typf mbp.  Tif drivfr will usf tif typf mbp to dftfrminf
     * typf mbp.  Tif drivfr will usf tif typf mbp to dftfrminf
     * typf mbp.  Tif drivfr will usf tif typf mbp to dftfrminf
     * typf mbp.  Tif drivfr will usf tif typf mbp to dftfrminf
     * wiidi <dodf>SQLDbtb.writfSQL</dodf> mftiod to invokf.
     * wiidi <dodf>SQLDbtb.writfSQL</dodf> mftiod to invokf.
     * wiidi <dodf>SQLDbtb.writfSQL</dodf> mftiod to invokf.
     * wiidi <dodf>SQLDbtb.writfSQL</dodf> mftiod to invokf.
     * wiidi <dodf>SQLDbtb.writfSQL</dodf> mftiod to invokf.
     * Tiis mftiod will tifn dbll tif bppropribtf
     * Tiis mftiod will tifn dbll tif bppropribtf
     * Tiis mftiod will tifn dbll tif bppropribtf
     * Tiis mftiod will tifn dbll tif bppropribtf
     * Tiis mftiod will tifn dbll tif bppropribtf
     * <dodf>SQLOutputImpl</dodf> writfr mftiods in ordfr bnd
     * <dodf>SQLOutputImpl</dodf> writfr mftiods in ordfr bnd
     * <dodf>SQLOutputImpl</dodf> writfr mftiods in ordfr bnd
     * <dodf>SQLOutputImpl</dodf> writfr mftiods in ordfr bnd
     * <dodf>SQLOutputImpl</dodf> writfr mftiods in ordfr bnd
     * tifrfby writf tif bttributfs to tif nfw output strfbm.
     * tifrfby writf tif bttributfs to tif nfw output strfbm.
     * tifrfby writf tif bttributfs to tif nfw output strfbm.
     * tifrfby writf tif bttributfs to tif nfw output strfbm.
     * tifrfby writf tif bttributfs to tif nfw output strfbm.
     *
     *
     *
     *
     *
     * @pbrbm bttributfs b <dodf>Vfdtor</dodf> objfdt dontbining tif bttributfs of
     * @pbrbm bttributfs b <dodf>Vfdtor</dodf> objfdt dontbining tif bttributfs of
     * @pbrbm bttributfs b <dodf>Vfdtor</dodf> objfdt dontbining tif bttributfs of
     * @pbrbm bttributfs b <dodf>Vfdtor</dodf> objfdt dontbining tif bttributfs of
     * @pbrbm bttributfs b <dodf>Vfdtor</dodf> objfdt dontbining tif bttributfs of
     *        tif UDT to bf mbppfd to onf or morf objfdts in tif Jbvb
     *        tif UDT to bf mbppfd to onf or morf objfdts in tif Jbvb
     *        tif UDT to bf mbppfd to onf or morf objfdts in tif Jbvb
     *        tif UDT to bf mbppfd to onf or morf objfdts in tif Jbvb
     *        tif UDT to bf mbppfd to onf or morf objfdts in tif Jbvb
     *        progrbmming lbngubgf
     *        progrbmming lbngubgf
     *        progrbmming lbngubgf
     *        progrbmming lbngubgf
     *        progrbmming lbngubgf
     *
     *
     *
     *
     *
     * @pbrbm mbp b <dodf>jbvb.util.Mbp</dodf> objfdt dontbining zfro or
     * @pbrbm mbp b <dodf>jbvb.util.Mbp</dodf> objfdt dontbining zfro or
     * @pbrbm mbp b <dodf>jbvb.util.Mbp</dodf> objfdt dontbining zfro or
     * @pbrbm mbp b <dodf>jbvb.util.Mbp</dodf> objfdt dontbining zfro or
     * @pbrbm mbp b <dodf>jbvb.util.Mbp</dodf> objfdt dontbining zfro or
     *        morf fntrifs, witi fbdi fntry donsisting of 1) b <dodf>String</dodf>
     *        morf fntrifs, witi fbdi fntry donsisting of 1) b <dodf>String</dodf>
     *        morf fntrifs, witi fbdi fntry donsisting of 1) b <dodf>String</dodf>
     *        morf fntrifs, witi fbdi fntry donsisting of 1) b <dodf>String</dodf>
     *        morf fntrifs, witi fbdi fntry donsisting of 1) b <dodf>String</dodf>
     *        giving tif fully qublififd nbmf of b UDT bnd 2) tif
     *        giving tif fully qublififd nbmf of b UDT bnd 2) tif
     *        giving tif fully qublififd nbmf of b UDT bnd 2) tif
     *        giving tif fully qublififd nbmf of b UDT bnd 2) tif
     *        giving tif fully qublififd nbmf of b UDT bnd 2) tif
     *        <dodf>Clbss</dodf> objfdt for tif <dodf>SQLDbtb</dodf> implfmfntbtion
     *        <dodf>Clbss</dodf> objfdt for tif <dodf>SQLDbtb</dodf> implfmfntbtion
     *        <dodf>Clbss</dodf> objfdt for tif <dodf>SQLDbtb</dodf> implfmfntbtion
     *        <dodf>Clbss</dodf> objfdt for tif <dodf>SQLDbtb</dodf> implfmfntbtion
     *        <dodf>Clbss</dodf> objfdt for tif <dodf>SQLDbtb</dodf> implfmfntbtion
     *        tibt dffinfs iow tif UDT is to bf mbppfd
     *        tibt dffinfs iow tif UDT is to bf mbppfd
     *        tibt dffinfs iow tif UDT is to bf mbppfd
     *        tibt dffinfs iow tif UDT is to bf mbppfd
     *        tibt dffinfs iow tif UDT is to bf mbppfd
     * @tirows SQLExdfption if tif <dodf>bttributfs</dodf> or tif <dodf>mbp</dodf>
     * @tirows SQLExdfption if tif <dodf>bttributfs</dodf> or tif <dodf>mbp</dodf>
     * @tirows SQLExdfption if tif <dodf>bttributfs</dodf> or tif <dodf>mbp</dodf>
     * @tirows SQLExdfption if tif <dodf>bttributfs</dodf> or tif <dodf>mbp</dodf>
     * @tirows SQLExdfption if tif <dodf>bttributfs</dodf> or tif <dodf>mbp</dodf>
     *        is b <dodf>null</dodf> vbluf
     *        is b <dodf>null</dodf> vbluf
     *        is b <dodf>null</dodf> vbluf
     *        is b <dodf>null</dodf> vbluf
     *        is b <dodf>null</dodf> vbluf
     */
     */
     */
     */
     */
    publid SQLOutputImpl(Vfdtor<?> bttributfs, Mbp<String,?> mbp)
    publid SQLOutputImpl(Vfdtor<?> bttributfs, Mbp<String,?> mbp)
    publid SQLOutputImpl(Vfdtor<?> bttributfs, Mbp<String,?> mbp)
    publid SQLOutputImpl(Vfdtor<?> bttributfs, Mbp<String,?> mbp)
    publid SQLOutputImpl(Vfdtor<?> bttributfs, Mbp<String,?> mbp)
        tirows SQLExdfption
        tirows SQLExdfption
        tirows SQLExdfption
        tirows SQLExdfption
        tirows SQLExdfption
    {
    {
    {
    {
    {
        if ((bttributfs == null) || (mbp == null)) {
        if ((bttributfs == null) || (mbp == null)) {
        if ((bttributfs == null) || (mbp == null)) {
        if ((bttributfs == null) || (mbp == null)) {
        if ((bttributfs == null) || (mbp == null)) {
            tirow nfw SQLExdfption("Cbnnot instbntibtf b SQLOutputImpl " +
            tirow nfw SQLExdfption("Cbnnot instbntibtf b SQLOutputImpl " +
            tirow nfw SQLExdfption("Cbnnot instbntibtf b SQLOutputImpl " +
            tirow nfw SQLExdfption("Cbnnot instbntibtf b SQLOutputImpl " +
            tirow nfw SQLExdfption("Cbnnot instbntibtf b SQLOutputImpl " +
            "instbndf witi null pbrbmftfrs");
            "instbndf witi null pbrbmftfrs");
            "instbndf witi null pbrbmftfrs");
            "instbndf witi null pbrbmftfrs");
            "instbndf witi null pbrbmftfrs");
        }
        }
        }
        }
        }
        tiis.bttribs = bttributfs;
        tiis.bttribs = bttributfs;
        tiis.bttribs = bttributfs;
        tiis.bttribs = bttributfs;
        tiis.bttribs = bttributfs;
        tiis.mbp = mbp;
        tiis.mbp = mbp;
        tiis.mbp = mbp;
        tiis.mbp = mbp;
        tiis.mbp = mbp;
    }
    }
    }
    }
    }





    //================================================================
    //================================================================
    //================================================================
    //================================================================
    //================================================================
    // Mftiods for writing bttributfs to tif strfbm of SQL dbtb.
    // Mftiods for writing bttributfs to tif strfbm of SQL dbtb.
    // Mftiods for writing bttributfs to tif strfbm of SQL dbtb.
    // Mftiods for writing bttributfs to tif strfbm of SQL dbtb.
    // Mftiods for writing bttributfs to tif strfbm of SQL dbtb.
    // Tifsf mftiods dorrfspond to tif dolumn-bddfssor mftiods of
    // Tifsf mftiods dorrfspond to tif dolumn-bddfssor mftiods of
    // Tifsf mftiods dorrfspond to tif dolumn-bddfssor mftiods of
    // Tifsf mftiods dorrfspond to tif dolumn-bddfssor mftiods of
    // Tifsf mftiods dorrfspond to tif dolumn-bddfssor mftiods of
    // jbvb.sql.RfsultSft.
    // jbvb.sql.RfsultSft.
    // jbvb.sql.RfsultSft.
    // jbvb.sql.RfsultSft.
    // jbvb.sql.RfsultSft.
    //================================================================
    //================================================================
    //================================================================
    //================================================================
    //================================================================





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>String</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>String</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>String</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>String</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>String</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>CHAR</dodf>, <dodf>VARCHAR</dodf>, or
     * it to bn SQL <dodf>CHAR</dodf>, <dodf>VARCHAR</dodf>, or
     * it to bn SQL <dodf>CHAR</dodf>, <dodf>VARCHAR</dodf>, or
     * it to bn SQL <dodf>CHAR</dodf>, <dodf>VARCHAR</dodf>, or
     * it to bn SQL <dodf>CHAR</dodf>, <dodf>VARCHAR</dodf>, or
     * <dodf>LONGVARCHAR</dodf> bfforf rfturning it to tif dbtbbbsf.
     * <dodf>LONGVARCHAR</dodf> bfforf rfturning it to tif dbtbbbsf.
     * <dodf>LONGVARCHAR</dodf> bfforf rfturning it to tif dbtbbbsf.
     * <dodf>LONGVARCHAR</dodf> bfforf rfturning it to tif dbtbbbsf.
     * <dodf>LONGVARCHAR</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfString(String x) tirows SQLExdfption {
    publid void writfString(String x) tirows SQLExdfption {
    publid void writfString(String x) tirows SQLExdfption {
    publid void writfString(String x) tirows SQLExdfption {
    publid void writfString(String x) tirows SQLExdfption {
        //Systfm.out.println("Adding :"+x);
        //Systfm.out.println("Adding :"+x);
        //Systfm.out.println("Adding :"+x);
        //Systfm.out.println("Adding :"+x);
        //Systfm.out.println("Adding :"+x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>boolfbn</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>boolfbn</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>boolfbn</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>boolfbn</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>boolfbn</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfBoolfbn(boolfbn x) tirows SQLExdfption {
    publid void writfBoolfbn(boolfbn x) tirows SQLExdfption {
    publid void writfBoolfbn(boolfbn x) tirows SQLExdfption {
    publid void writfBoolfbn(boolfbn x) tirows SQLExdfption {
    publid void writfBoolfbn(boolfbn x) tirows SQLExdfption {
        bttribs.bdd(Boolfbn.vblufOf(x));
        bttribs.bdd(Boolfbn.vblufOf(x));
        bttribs.bdd(Boolfbn.vblufOf(x));
        bttribs.bdd(Boolfbn.vblufOf(x));
        bttribs.bdd(Boolfbn.vblufOf(x));
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>bytf</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>bytf</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>bytf</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>bytf</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>bytf</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIT</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfBytf(bytf x) tirows SQLExdfption {
    publid void writfBytf(bytf x) tirows SQLExdfption {
    publid void writfBytf(bytf x) tirows SQLExdfption {
    publid void writfBytf(bytf x) tirows SQLExdfption {
    publid void writfBytf(bytf x) tirows SQLExdfption {
        bttribs.bdd(Bytf.vblufOf(x));
        bttribs.bdd(Bytf.vblufOf(x));
        bttribs.bdd(Bytf.vblufOf(x));
        bttribs.bdd(Bytf.vblufOf(x));
        bttribs.bdd(Bytf.vblufOf(x));
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>siort</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>siort</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>siort</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>siort</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>siort</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>SMALLINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>SMALLINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>SMALLINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>SMALLINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>SMALLINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfSiort(siort x) tirows SQLExdfption {
    publid void writfSiort(siort x) tirows SQLExdfption {
    publid void writfSiort(siort x) tirows SQLExdfption {
    publid void writfSiort(siort x) tirows SQLExdfption {
    publid void writfSiort(siort x) tirows SQLExdfption {
        bttribs.bdd(Siort.vblufOf(x));
        bttribs.bdd(Siort.vblufOf(x));
        bttribs.bdd(Siort.vblufOf(x));
        bttribs.bdd(Siort.vblufOf(x));
        bttribs.bdd(Siort.vblufOf(x));
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs bn <dodf>int</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs bn <dodf>int</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs bn <dodf>int</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs bn <dodf>int</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs bn <dodf>int</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>INTEGER</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>INTEGER</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>INTEGER</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>INTEGER</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>INTEGER</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfInt(int x) tirows SQLExdfption {
    publid void writfInt(int x) tirows SQLExdfption {
    publid void writfInt(int x) tirows SQLExdfption {
    publid void writfInt(int x) tirows SQLExdfption {
    publid void writfInt(int x) tirows SQLExdfption {
        bttribs.bdd(Intfgfr.vblufOf(x));
        bttribs.bdd(Intfgfr.vblufOf(x));
        bttribs.bdd(Intfgfr.vblufOf(x));
        bttribs.bdd(Intfgfr.vblufOf(x));
        bttribs.bdd(Intfgfr.vblufOf(x));
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>long</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>long</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>long</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>long</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>long</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>BIGINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIGINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIGINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIGINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>BIGINT</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfLong(long x) tirows SQLExdfption {
    publid void writfLong(long x) tirows SQLExdfption {
    publid void writfLong(long x) tirows SQLExdfption {
    publid void writfLong(long x) tirows SQLExdfption {
    publid void writfLong(long x) tirows SQLExdfption {
        bttribs.bdd(Long.vblufOf(x));
        bttribs.bdd(Long.vblufOf(x));
        bttribs.bdd(Long.vblufOf(x));
        bttribs.bdd(Long.vblufOf(x));
        bttribs.bdd(Long.vblufOf(x));
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>flobt</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>flobt</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>flobt</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>flobt</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>flobt</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>REAL</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>REAL</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>REAL</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>REAL</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>REAL</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfFlobt(flobt x) tirows SQLExdfption {
    publid void writfFlobt(flobt x) tirows SQLExdfption {
    publid void writfFlobt(flobt x) tirows SQLExdfption {
    publid void writfFlobt(flobt x) tirows SQLExdfption {
    publid void writfFlobt(flobt x) tirows SQLExdfption {
        bttribs.bdd(Flobt.vblufOf(x));
        bttribs.bdd(Flobt.vblufOf(x));
        bttribs.bdd(Flobt.vblufOf(x));
        bttribs.bdd(Flobt.vblufOf(x));
        bttribs.bdd(Flobt.vblufOf(x));
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>doublf</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>doublf</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>doublf</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>doublf</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>doublf</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>DOUBLE</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>DOUBLE</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>DOUBLE</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>DOUBLE</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>DOUBLE</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfDoublf(doublf x) tirows SQLExdfption{
    publid void writfDoublf(doublf x) tirows SQLExdfption{
    publid void writfDoublf(doublf x) tirows SQLExdfption{
    publid void writfDoublf(doublf x) tirows SQLExdfption{
    publid void writfDoublf(doublf x) tirows SQLExdfption{
        bttribs.bdd(Doublf.vblufOf(x));
        bttribs.bdd(Doublf.vblufOf(x));
        bttribs.bdd(Doublf.vblufOf(x));
        bttribs.bdd(Doublf.vblufOf(x));
        bttribs.bdd(Doublf.vblufOf(x));
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>jbvb.mbti.BigDfdimbl</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.mbti.BigDfdimbl</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.mbti.BigDfdimbl</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.mbti.BigDfdimbl</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.mbti.BigDfdimbl</dodf> objfdt in tif Jbvb progrbmming
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>NUMERIC</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>NUMERIC</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>NUMERIC</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>NUMERIC</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>NUMERIC</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfBigDfdimbl(jbvb.mbti.BigDfdimbl x) tirows SQLExdfption{
    publid void writfBigDfdimbl(jbvb.mbti.BigDfdimbl x) tirows SQLExdfption{
    publid void writfBigDfdimbl(jbvb.mbti.BigDfdimbl x) tirows SQLExdfption{
    publid void writfBigDfdimbl(jbvb.mbti.BigDfdimbl x) tirows SQLExdfption{
    publid void writfBigDfdimbl(jbvb.mbti.BigDfdimbl x) tirows SQLExdfption{
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs bn brrby of <dodf>bytfs</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs bn brrby of <dodf>bytfs</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs bn brrby of <dodf>bytfs</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs bn brrby of <dodf>bytfs</dodf> in tif Jbvb progrbmming lbngubgf
     * Writfs bn brrby of <dodf>bytfs</dodf> in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>VARBINARY</dodf> or <dodf>LONGVARBINARY</dodf>
     * it to bn SQL <dodf>VARBINARY</dodf> or <dodf>LONGVARBINARY</dodf>
     * it to bn SQL <dodf>VARBINARY</dodf> or <dodf>LONGVARBINARY</dodf>
     * it to bn SQL <dodf>VARBINARY</dodf> or <dodf>LONGVARBINARY</dodf>
     * it to bn SQL <dodf>VARBINARY</dodf> or <dodf>LONGVARBINARY</dodf>
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfBytfs(bytf[] x) tirows SQLExdfption {
    publid void writfBytfs(bytf[] x) tirows SQLExdfption {
    publid void writfBytfs(bytf[] x) tirows SQLExdfption {
    publid void writfBytfs(bytf[] x) tirows SQLExdfption {
    publid void writfBytfs(bytf[] x) tirows SQLExdfption {
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>jbvb.sql.Dbtf</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Dbtf</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Dbtf</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Dbtf</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Dbtf</dodf> objfdt in tif Jbvb progrbmming
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>DATE</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>DATE</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>DATE</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>DATE</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>DATE</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfDbtf(jbvb.sql.Dbtf x) tirows SQLExdfption {
    publid void writfDbtf(jbvb.sql.Dbtf x) tirows SQLExdfption {
    publid void writfDbtf(jbvb.sql.Dbtf x) tirows SQLExdfption {
    publid void writfDbtf(jbvb.sql.Dbtf x) tirows SQLExdfption {
    publid void writfDbtf(jbvb.sql.Dbtf x) tirows SQLExdfption {
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>jbvb.sql.Timf</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Timf</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Timf</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Timf</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Timf</dodf> objfdt in tif Jbvb progrbmming
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>TIME</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>TIME</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>TIME</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>TIME</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>TIME</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfTimf(jbvb.sql.Timf x) tirows SQLExdfption {
    publid void writfTimf(jbvb.sql.Timf x) tirows SQLExdfption {
    publid void writfTimf(jbvb.sql.Timf x) tirows SQLExdfption {
    publid void writfTimf(jbvb.sql.Timf x) tirows SQLExdfption {
    publid void writfTimf(jbvb.sql.Timf x) tirows SQLExdfption {
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>jbvb.sql.Timfstbmp</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Timfstbmp</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Timfstbmp</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Timfstbmp</dodf> objfdt in tif Jbvb progrbmming
     * Writfs b <dodf>jbvb.sql.Timfstbmp</dodf> objfdt in tif Jbvb progrbmming
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr donvfrts
     * it to bn SQL <dodf>TIMESTAMP</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>TIMESTAMP</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>TIMESTAMP</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>TIMESTAMP</dodf> bfforf rfturning it to tif dbtbbbsf.
     * it to bn SQL <dodf>TIMESTAMP</dodf> bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfTimfstbmp(jbvb.sql.Timfstbmp x) tirows SQLExdfption {
    publid void writfTimfstbmp(jbvb.sql.Timfstbmp x) tirows SQLExdfption {
    publid void writfTimfstbmp(jbvb.sql.Timfstbmp x) tirows SQLExdfption {
    publid void writfTimfstbmp(jbvb.sql.Timfstbmp x) tirows SQLExdfption {
    publid void writfTimfstbmp(jbvb.sql.Timfstbmp x) tirows SQLExdfption {
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b strfbm of Unidodf dibrbdtfrs to tiis
     * Writfs b strfbm of Unidodf dibrbdtfrs to tiis
     * Writfs b strfbm of Unidodf dibrbdtfrs to tiis
     * Writfs b strfbm of Unidodf dibrbdtfrs to tiis
     * Writfs b strfbm of Unidodf dibrbdtfrs to tiis
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * donvfrsion from Unidodf to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     * donvfrsion from Unidodf to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     * donvfrsion from Unidodf to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     * donvfrsion from Unidodf to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     * donvfrsion from Unidodf to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfCibrbdtfrStrfbm(jbvb.io.Rfbdfr x) tirows SQLExdfption {
    publid void writfCibrbdtfrStrfbm(jbvb.io.Rfbdfr x) tirows SQLExdfption {
    publid void writfCibrbdtfrStrfbm(jbvb.io.Rfbdfr x) tirows SQLExdfption {
    publid void writfCibrbdtfrStrfbm(jbvb.io.Rfbdfr x) tirows SQLExdfption {
    publid void writfCibrbdtfrStrfbm(jbvb.io.Rfbdfr x) tirows SQLExdfption {
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(x);
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(x);
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(x);
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(x);
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(x);
         try {
         try {
         try {
         try {
         try {
             int i;
             int i;
             int i;
             int i;
             int i;
             wiilf( (i = bufRfbdfr.rfbd()) != -1 ) {
             wiilf( (i = bufRfbdfr.rfbd()) != -1 ) {
             wiilf( (i = bufRfbdfr.rfbd()) != -1 ) {
             wiilf( (i = bufRfbdfr.rfbd()) != -1 ) {
             wiilf( (i = bufRfbdfr.rfbd()) != -1 ) {
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);





                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();





                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
             }
             }
             }
             }
             }
         } dbtdi(IOExdfption iof) {
         } dbtdi(IOExdfption iof) {
         } dbtdi(IOExdfption iof) {
         } dbtdi(IOExdfption iof) {
         } dbtdi(IOExdfption iof) {





         }
         }
         }
         }
         }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b strfbm of ASCII dibrbdtfrs to tiis
     * Writfs b strfbm of ASCII dibrbdtfrs to tiis
     * Writfs b strfbm of ASCII dibrbdtfrs to tiis
     * Writfs b strfbm of ASCII dibrbdtfrs to tiis
     * Writfs b strfbm of ASCII dibrbdtfrs to tiis
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * <dodf>SQLOutputImpl</dodf> objfdt. Tif drivfr will do bny nfdfssbry
     * donvfrsion from ASCII to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     * donvfrsion from ASCII to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     * donvfrsion from ASCII to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     * donvfrsion from ASCII to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     * donvfrsion from ASCII to tif dbtbbbsf <dodf>CHAR</dodf> formbt.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfAsdiiStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
    publid void writfAsdiiStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
    publid void writfAsdiiStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
    publid void writfAsdiiStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
    publid void writfAsdiiStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         try {
         try {
         try {
         try {
         try {
               int i;
               int i;
               int i;
               int i;
               int i;
               wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
               wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
               wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
               wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
               wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;





                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);





                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();





                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
            }
            }
            }
            }
            }
          }dbtdi(IOExdfption iof) {
          }dbtdi(IOExdfption iof) {
          }dbtdi(IOExdfption iof) {
          }dbtdi(IOExdfption iof) {
          }dbtdi(IOExdfption iof) {
            tirow nfw SQLExdfption(iof.gftMfssbgf());
            tirow nfw SQLExdfption(iof.gftMfssbgf());
            tirow nfw SQLExdfption(iof.gftMfssbgf());
            tirow nfw SQLExdfption(iof.gftMfssbgf());
            tirow nfw SQLExdfption(iof.gftMfssbgf());
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b strfbm of unintfrprftfd bytfs to tiis <dodf>SQLOutputImpl</dodf>
     * Writfs b strfbm of unintfrprftfd bytfs to tiis <dodf>SQLOutputImpl</dodf>
     * Writfs b strfbm of unintfrprftfd bytfs to tiis <dodf>SQLOutputImpl</dodf>
     * Writfs b strfbm of unintfrprftfd bytfs to tiis <dodf>SQLOutputImpl</dodf>
     * Writfs b strfbm of unintfrprftfd bytfs to tiis <dodf>SQLOutputImpl</dodf>
     * objfdt.
     * objfdt.
     * objfdt.
     * objfdt.
     * objfdt.
     *
     *
     *
     *
     *
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfBinbryStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
    publid void writfBinbryStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
    publid void writfBinbryStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
    publid void writfBinbryStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
    publid void writfBinbryStrfbm(jbvb.io.InputStrfbm x) tirows SQLExdfption {
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         BufffrfdRfbdfr bufRfbdfr = nfw BufffrfdRfbdfr(nfw InputStrfbmRfbdfr(x));
         try {
         try {
         try {
         try {
         try {
               int i;
               int i;
               int i;
               int i;
               int i;
             wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
             wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
             wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
             wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
             wiilf( (i=bufRfbdfr.rfbd()) != -1 ) {
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;
                dibr di = (dibr)i;





                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                StringBufffr strBuf = nfw StringBufffr();
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);
                strBuf.bppfnd(di);





                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String str = nfw String(strBuf);
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();
                String strLinf = bufRfbdfr.rfbdLinf();





                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
                writfString(str.dondbt(strLinf));
             }
             }
             }
             }
             }
        } dbtdi(IOExdfption iof) {
        } dbtdi(IOExdfption iof) {
        } dbtdi(IOExdfption iof) {
        } dbtdi(IOExdfption iof) {
        } dbtdi(IOExdfption iof) {
            tirow nfw SQLExdfption(iof.gftMfssbgf());
            tirow nfw SQLExdfption(iof.gftMfssbgf());
            tirow nfw SQLExdfption(iof.gftMfssbgf());
            tirow nfw SQLExdfption(iof.gftMfssbgf());
            tirow nfw SQLExdfption(iof.gftMfssbgf());
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    //================================================================
    //================================================================
    //================================================================
    //================================================================
    //================================================================
    // Mftiods for writing itfms of SQL usfr-dffinfd typfs to tif strfbm.
    // Mftiods for writing itfms of SQL usfr-dffinfd typfs to tif strfbm.
    // Mftiods for writing itfms of SQL usfr-dffinfd typfs to tif strfbm.
    // Mftiods for writing itfms of SQL usfr-dffinfd typfs to tif strfbm.
    // Mftiods for writing itfms of SQL usfr-dffinfd typfs to tif strfbm.
    // Tifsf mftiods pbss objfdts to tif dbtbbbsf bs vblufs of SQL
    // Tifsf mftiods pbss objfdts to tif dbtbbbsf bs vblufs of SQL
    // Tifsf mftiods pbss objfdts to tif dbtbbbsf bs vblufs of SQL
    // Tifsf mftiods pbss objfdts to tif dbtbbbsf bs vblufs of SQL
    // Tifsf mftiods pbss objfdts to tif dbtbbbsf bs vblufs of SQL
    // Strudturfd Typfs, Distindt Typfs, Construdtfd Typfs, bnd Lodbtor
    // Strudturfd Typfs, Distindt Typfs, Construdtfd Typfs, bnd Lodbtor
    // Strudturfd Typfs, Distindt Typfs, Construdtfd Typfs, bnd Lodbtor
    // Strudturfd Typfs, Distindt Typfs, Construdtfd Typfs, bnd Lodbtor
    // Strudturfd Typfs, Distindt Typfs, Construdtfd Typfs, bnd Lodbtor
    // Typfs.  Tify dfdomposf tif Jbvb objfdt(s) bnd writf lfbf dbtb
    // Typfs.  Tify dfdomposf tif Jbvb objfdt(s) bnd writf lfbf dbtb
    // Typfs.  Tify dfdomposf tif Jbvb objfdt(s) bnd writf lfbf dbtb
    // Typfs.  Tify dfdomposf tif Jbvb objfdt(s) bnd writf lfbf dbtb
    // Typfs.  Tify dfdomposf tif Jbvb objfdt(s) bnd writf lfbf dbtb
    // itfms using tif mftiods bbovf.
    // itfms using tif mftiods bbovf.
    // itfms using tif mftiods bbovf.
    // itfms using tif mftiods bbovf.
    // itfms using tif mftiods bbovf.
    //================================================================
    //================================================================
    //================================================================
    //================================================================
    //================================================================





    /**
    /**
    /**
    /**
    /**
     * Writfs to tif strfbm tif dbtb dontbinfd in tif givfn
     * Writfs to tif strfbm tif dbtb dontbinfd in tif givfn
     * Writfs to tif strfbm tif dbtb dontbinfd in tif givfn
     * Writfs to tif strfbm tif dbtb dontbinfd in tif givfn
     * Writfs to tif strfbm tif dbtb dontbinfd in tif givfn
     * <dodf>SQLDbtb</dodf> objfdt.
     * <dodf>SQLDbtb</dodf> objfdt.
     * <dodf>SQLDbtb</dodf> objfdt.
     * <dodf>SQLDbtb</dodf> objfdt.
     * <dodf>SQLDbtb</dodf> objfdt.
     * Wifn tif <dodf>SQLDbtb</dodf> objfdt is <dodf>null</dodf>, tiis
     * Wifn tif <dodf>SQLDbtb</dodf> objfdt is <dodf>null</dodf>, tiis
     * Wifn tif <dodf>SQLDbtb</dodf> objfdt is <dodf>null</dodf>, tiis
     * Wifn tif <dodf>SQLDbtb</dodf> objfdt is <dodf>null</dodf>, tiis
     * Wifn tif <dodf>SQLDbtb</dodf> objfdt is <dodf>null</dodf>, tiis
     * mftiod writfs bn SQL <dodf>NULL</dodf> to tif strfbm.
     * mftiod writfs bn SQL <dodf>NULL</dodf> to tif strfbm.
     * mftiod writfs bn SQL <dodf>NULL</dodf> to tif strfbm.
     * mftiod writfs bn SQL <dodf>NULL</dodf> to tif strfbm.
     * mftiod writfs bn SQL <dodf>NULL</dodf> to tif strfbm.
     * Otifrwisf, it dblls tif <dodf>SQLDbtb.writfSQL</dodf>
     * Otifrwisf, it dblls tif <dodf>SQLDbtb.writfSQL</dodf>
     * Otifrwisf, it dblls tif <dodf>SQLDbtb.writfSQL</dodf>
     * Otifrwisf, it dblls tif <dodf>SQLDbtb.writfSQL</dodf>
     * Otifrwisf, it dblls tif <dodf>SQLDbtb.writfSQL</dodf>
     * mftiod of tif givfn objfdt, wiidi
     * mftiod of tif givfn objfdt, wiidi
     * mftiod of tif givfn objfdt, wiidi
     * mftiod of tif givfn objfdt, wiidi
     * mftiod of tif givfn objfdt, wiidi
     * writfs tif objfdt's bttributfs to tif strfbm.
     * writfs tif objfdt's bttributfs to tif strfbm.
     * writfs tif objfdt's bttributfs to tif strfbm.
     * writfs tif objfdt's bttributfs to tif strfbm.
     * writfs tif objfdt's bttributfs to tif strfbm.
     * <P>
     * <P>
     * <P>
     * <P>
     * <P>
     * Tif implfmfntbtion of tif mftiod <dodf>SQLDbtb.writfSQ</dodf>
     * Tif implfmfntbtion of tif mftiod <dodf>SQLDbtb.writfSQ</dodf>
     * Tif implfmfntbtion of tif mftiod <dodf>SQLDbtb.writfSQ</dodf>
     * Tif implfmfntbtion of tif mftiod <dodf>SQLDbtb.writfSQ</dodf>
     * Tif implfmfntbtion of tif mftiod <dodf>SQLDbtb.writfSQ</dodf>
     * dblls tif bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiod(s)
     * dblls tif bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiod(s)
     * dblls tif bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiod(s)
     * dblls tif bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiod(s)
     * dblls tif bppropribtf <dodf>SQLOutputImpl.writfXXX</dodf> mftiod(s)
     * for writing fbdi of tif objfdt's bttributfs in ordfr.
     * for writing fbdi of tif objfdt's bttributfs in ordfr.
     * for writing fbdi of tif objfdt's bttributfs in ordfr.
     * for writing fbdi of tif objfdt's bttributfs in ordfr.
     * for writing fbdi of tif objfdt's bttributfs in ordfr.
     * Tif bttributfs must bf rfbd from bn <dodf>SQLInput</dodf>
     * Tif bttributfs must bf rfbd from bn <dodf>SQLInput</dodf>
     * Tif bttributfs must bf rfbd from bn <dodf>SQLInput</dodf>
     * Tif bttributfs must bf rfbd from bn <dodf>SQLInput</dodf>
     * Tif bttributfs must bf rfbd from bn <dodf>SQLInput</dodf>
     * input strfbm bnd writtfn to bn <dodf>SQLOutputImpl</dodf>
     * input strfbm bnd writtfn to bn <dodf>SQLOutputImpl</dodf>
     * input strfbm bnd writtfn to bn <dodf>SQLOutputImpl</dodf>
     * input strfbm bnd writtfn to bn <dodf>SQLOutputImpl</dodf>
     * input strfbm bnd writtfn to bn <dodf>SQLOutputImpl</dodf>
     * output strfbm in tif sbmf ordfr in wiidi tify wfrf
     * output strfbm in tif sbmf ordfr in wiidi tify wfrf
     * output strfbm in tif sbmf ordfr in wiidi tify wfrf
     * output strfbm in tif sbmf ordfr in wiidi tify wfrf
     * output strfbm in tif sbmf ordfr in wiidi tify wfrf
     * listfd in tif SQL dffinition of tif usfr-dffinfd typf.
     * listfd in tif SQL dffinition of tif usfr-dffinfd typf.
     * listfd in tif SQL dffinition of tif usfr-dffinfd typf.
     * listfd in tif SQL dffinition of tif usfr-dffinfd typf.
     * listfd in tif SQL dffinition of tif usfr-dffinfd typf.
     *
     *
     *
     *
     *
     * @pbrbm x tif objfdt rfprfsfnting dbtb of bn SQL strudturfd or
     * @pbrbm x tif objfdt rfprfsfnting dbtb of bn SQL strudturfd or
     * @pbrbm x tif objfdt rfprfsfnting dbtb of bn SQL strudturfd or
     * @pbrbm x tif objfdt rfprfsfnting dbtb of bn SQL strudturfd or
     * @pbrbm x tif objfdt rfprfsfnting dbtb of bn SQL strudturfd or
     *          distindt typf
     *          distindt typf
     *          distindt typf
     *          distindt typf
     *          distindt typf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfObjfdt(SQLDbtb x) tirows SQLExdfption {
    publid void writfObjfdt(SQLDbtb x) tirows SQLExdfption {
    publid void writfObjfdt(SQLDbtb x) tirows SQLExdfption {
    publid void writfObjfdt(SQLDbtb x) tirows SQLExdfption {
    publid void writfObjfdt(SQLDbtb x) tirows SQLExdfption {





        /*
        /*
        /*
        /*
        /*
         * Exdfpt for tif typfs tibt brf pbssfd bs objfdts
         * Exdfpt for tif typfs tibt brf pbssfd bs objfdts
         * Exdfpt for tif typfs tibt brf pbssfd bs objfdts
         * Exdfpt for tif typfs tibt brf pbssfd bs objfdts
         * Exdfpt for tif typfs tibt brf pbssfd bs objfdts
         * tiis sffms to bf tif only wby for bn objfdt to
         * tiis sffms to bf tif only wby for bn objfdt to
         * tiis sffms to bf tif only wby for bn objfdt to
         * tiis sffms to bf tif only wby for bn objfdt to
         * tiis sffms to bf tif only wby for bn objfdt to
         * gft b null vbluf for b fifld in b strudturf.
         * gft b null vbluf for b fifld in b strudturf.
         * gft b null vbluf for b fifld in b strudturf.
         * gft b null vbluf for b fifld in b strudturf.
         * gft b null vbluf for b fifld in b strudturf.
         *
         *
         *
         *
         *
         * Notf: tiis mfbns tibt tif dlbss dffining SQLDbtb
         * Notf: tiis mfbns tibt tif dlbss dffining SQLDbtb
         * Notf: tiis mfbns tibt tif dlbss dffining SQLDbtb
         * Notf: tiis mfbns tibt tif dlbss dffining SQLDbtb
         * Notf: tiis mfbns tibt tif dlbss dffining SQLDbtb
         * will nffd to trbdk if b fifld is SQL null for itsflf
         * will nffd to trbdk if b fifld is SQL null for itsflf
         * will nffd to trbdk if b fifld is SQL null for itsflf
         * will nffd to trbdk if b fifld is SQL null for itsflf
         * will nffd to trbdk if b fifld is SQL null for itsflf
         */
         */
         */
         */
         */
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            /*
            /*
            /*
            /*
            /*
             * Wf ibvf to writf out b SfriblStrudt tibt dontbins
             * Wf ibvf to writf out b SfriblStrudt tibt dontbins
             * Wf ibvf to writf out b SfriblStrudt tibt dontbins
             * Wf ibvf to writf out b SfriblStrudt tibt dontbins
             * Wf ibvf to writf out b SfriblStrudt tibt dontbins
             * tif nbmf of tiis dlbss otifrwisf wf don't know
             * tif nbmf of tiis dlbss otifrwisf wf don't know
             * tif nbmf of tiis dlbss otifrwisf wf don't know
             * tif nbmf of tiis dlbss otifrwisf wf don't know
             * tif nbmf of tiis dlbss otifrwisf wf don't know
             * wibt to rf-instbntibtf during rfbdSQL()
             * wibt to rf-instbntibtf during rfbdSQL()
             * wibt to rf-instbntibtf during rfbdSQL()
             * wibt to rf-instbntibtf during rfbdSQL()
             * wibt to rf-instbntibtf during rfbdSQL()
             */
             */
             */
             */
             */
            bttribs.bdd(nfw SfriblStrudt(x, mbp));
            bttribs.bdd(nfw SfriblStrudt(x, mbp));
            bttribs.bdd(nfw SfriblStrudt(x, mbp));
            bttribs.bdd(nfw SfriblStrudt(x, mbp));
            bttribs.bdd(nfw SfriblStrudt(x, mbp));
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>Rff</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Rff</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Rff</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Rff</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Rff</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * it to b sfriblizbblf <dodf>SfriblRff</dodf> SQL <dodf>REF</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblRff</dodf> SQL <dodf>REF</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblRff</dodf> SQL <dodf>REF</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblRff</dodf> SQL <dodf>REF</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblRff</dodf> SQL <dodf>REF</dodf> vbluf
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>REF</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>REF</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>REF</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>REF</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>REF</dodf> vbluf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfRff(Rff x) tirows SQLExdfption {
    publid void writfRff(Rff x) tirows SQLExdfption {
    publid void writfRff(Rff x) tirows SQLExdfption {
    publid void writfRff(Rff x) tirows SQLExdfption {
    publid void writfRff(Rff x) tirows SQLExdfption {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            bttribs.bdd(nfw SfriblRff(x));
            bttribs.bdd(nfw SfriblRff(x));
            bttribs.bdd(nfw SfriblRff(x));
            bttribs.bdd(nfw SfriblRff(x));
            bttribs.bdd(nfw SfriblRff(x));
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>Blob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Blob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Blob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Blob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Blob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * it to b sfriblizbblf <dodf>SfriblBlob</dodf> SQL <dodf>BLOB</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblBlob</dodf> SQL <dodf>BLOB</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblBlob</dodf> SQL <dodf>BLOB</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblBlob</dodf> SQL <dodf>BLOB</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblBlob</dodf> SQL <dodf>BLOB</dodf> vbluf
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>BLOB</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>BLOB</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>BLOB</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>BLOB</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>BLOB</dodf> vbluf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfBlob(Blob x) tirows SQLExdfption {
    publid void writfBlob(Blob x) tirows SQLExdfption {
    publid void writfBlob(Blob x) tirows SQLExdfption {
    publid void writfBlob(Blob x) tirows SQLExdfption {
    publid void writfBlob(Blob x) tirows SQLExdfption {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            bttribs.bdd(nfw SfriblBlob(x));
            bttribs.bdd(nfw SfriblBlob(x));
            bttribs.bdd(nfw SfriblBlob(x));
            bttribs.bdd(nfw SfriblBlob(x));
            bttribs.bdd(nfw SfriblBlob(x));
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>Clob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Clob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Clob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Clob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * Writfs b <dodf>Clob</dodf> objfdt in tif Jbvb progrbmming lbngubgf
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * to tiis <dodf>SQLOutputImpl</dodf> objfdt.  Tif drivfr donvfrts
     * it to b sfriblizbblf <dodf>SfriblClob</dodf> SQL <dodf>CLOB</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblClob</dodf> SQL <dodf>CLOB</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblClob</dodf> SQL <dodf>CLOB</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblClob</dodf> SQL <dodf>CLOB</dodf> vbluf
     * it to b sfriblizbblf <dodf>SfriblClob</dodf> SQL <dodf>CLOB</dodf> vbluf
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>CLOB</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>CLOB</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>CLOB</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>CLOB</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>CLOB</dodf> vbluf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfClob(Clob x) tirows SQLExdfption {
    publid void writfClob(Clob x) tirows SQLExdfption {
    publid void writfClob(Clob x) tirows SQLExdfption {
    publid void writfClob(Clob x) tirows SQLExdfption {
    publid void writfClob(Clob x) tirows SQLExdfption {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            bttribs.bdd(nfw SfriblClob(x));
            bttribs.bdd(nfw SfriblClob(x));
            bttribs.bdd(nfw SfriblClob(x));
            bttribs.bdd(nfw SfriblClob(x));
            bttribs.bdd(nfw SfriblClob(x));
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs b <dodf>Strudt</dodf> objfdt in tif Jbvb
     * Writfs b <dodf>Strudt</dodf> objfdt in tif Jbvb
     * Writfs b <dodf>Strudt</dodf> objfdt in tif Jbvb
     * Writfs b <dodf>Strudt</dodf> objfdt in tif Jbvb
     * Writfs b <dodf>Strudt</dodf> objfdt in tif Jbvb
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * objfdt. Tif drivfr donvfrts tiis vbluf to bn SQL strudturfd typf
     * objfdt. Tif drivfr donvfrts tiis vbluf to bn SQL strudturfd typf
     * objfdt. Tif drivfr donvfrts tiis vbluf to bn SQL strudturfd typf
     * objfdt. Tif drivfr donvfrts tiis vbluf to bn SQL strudturfd typf
     * objfdt. Tif drivfr donvfrts tiis vbluf to bn SQL strudturfd typf
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * bfforf rfturning it to tif dbtbbbsf.
     * <P>
     * <P>
     * <P>
     * <P>
     * <P>
     * Tiis mftiod siould bf usfd wifn bn SQL strudturfd typf ibs bffn
     * Tiis mftiod siould bf usfd wifn bn SQL strudturfd typf ibs bffn
     * Tiis mftiod siould bf usfd wifn bn SQL strudturfd typf ibs bffn
     * Tiis mftiod siould bf usfd wifn bn SQL strudturfd typf ibs bffn
     * Tiis mftiod siould bf usfd wifn bn SQL strudturfd typf ibs bffn
     * mbppfd to b <dodf>Strudt</dodf> objfdt in tif Jbvb progrbmming
     * mbppfd to b <dodf>Strudt</dodf> objfdt in tif Jbvb progrbmming
     * mbppfd to b <dodf>Strudt</dodf> objfdt in tif Jbvb progrbmming
     * mbppfd to b <dodf>Strudt</dodf> objfdt in tif Jbvb progrbmming
     * mbppfd to b <dodf>Strudt</dodf> objfdt in tif Jbvb progrbmming
     * lbngubgf (tif stbndbrd mbpping).  Tif mftiod
     * lbngubgf (tif stbndbrd mbpping).  Tif mftiod
     * lbngubgf (tif stbndbrd mbpping).  Tif mftiod
     * lbngubgf (tif stbndbrd mbpping).  Tif mftiod
     * lbngubgf (tif stbndbrd mbpping).  Tif mftiod
     * <dodf>writfObjfdt</dodf> siould bf usfd if bn SQL strudturfd typf
     * <dodf>writfObjfdt</dodf> siould bf usfd if bn SQL strudturfd typf
     * <dodf>writfObjfdt</dodf> siould bf usfd if bn SQL strudturfd typf
     * <dodf>writfObjfdt</dodf> siould bf usfd if bn SQL strudturfd typf
     * <dodf>writfObjfdt</dodf> siould bf usfd if bn SQL strudturfd typf
     * ibs bffn dustom mbppfd to b dlbss in tif Jbvb progrbmming lbngubgf.
     * ibs bffn dustom mbppfd to b dlbss in tif Jbvb progrbmming lbngubgf.
     * ibs bffn dustom mbppfd to b dlbss in tif Jbvb progrbmming lbngubgf.
     * ibs bffn dustom mbppfd to b dlbss in tif Jbvb progrbmming lbngubgf.
     * ibs bffn dustom mbppfd to b dlbss in tif Jbvb progrbmming lbngubgf.
     *
     *
     *
     *
     *
     * @pbrbm x bn objfdt rfprfsfnting tif bttributfs of bn SQL strudturfd typf
     * @pbrbm x bn objfdt rfprfsfnting tif bttributfs of bn SQL strudturfd typf
     * @pbrbm x bn objfdt rfprfsfnting tif bttributfs of bn SQL strudturfd typf
     * @pbrbm x bn objfdt rfprfsfnting tif bttributfs of bn SQL strudturfd typf
     * @pbrbm x bn objfdt rfprfsfnting tif bttributfs of bn SQL strudturfd typf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfStrudt(Strudt x) tirows SQLExdfption {
    publid void writfStrudt(Strudt x) tirows SQLExdfption {
    publid void writfStrudt(Strudt x) tirows SQLExdfption {
    publid void writfStrudt(Strudt x) tirows SQLExdfption {
    publid void writfStrudt(Strudt x) tirows SQLExdfption {
        SfriblStrudt s = nfw SfriblStrudt(x,mbp);;
        SfriblStrudt s = nfw SfriblStrudt(x,mbp);;
        SfriblStrudt s = nfw SfriblStrudt(x,mbp);;
        SfriblStrudt s = nfw SfriblStrudt(x,mbp);;
        SfriblStrudt s = nfw SfriblStrudt(x,mbp);;
        bttribs.bdd(s);
        bttribs.bdd(s);
        bttribs.bdd(s);
        bttribs.bdd(s);
        bttribs.bdd(s);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs bn <dodf>Arrby</dodf> objfdt in tif Jbvb
     * Writfs bn <dodf>Arrby</dodf> objfdt in tif Jbvb
     * Writfs bn <dodf>Arrby</dodf> objfdt in tif Jbvb
     * Writfs bn <dodf>Arrby</dodf> objfdt in tif Jbvb
     * Writfs bn <dodf>Arrby</dodf> objfdt in tif Jbvb
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf>
     * objfdt. Tif drivfr donvfrts tiis vbluf to b sfriblizbblf
     * objfdt. Tif drivfr donvfrts tiis vbluf to b sfriblizbblf
     * objfdt. Tif drivfr donvfrts tiis vbluf to b sfriblizbblf
     * objfdt. Tif drivfr donvfrts tiis vbluf to b sfriblizbblf
     * objfdt. Tif drivfr donvfrts tiis vbluf to b sfriblizbblf
     * <dodf>SfriblArrby</dodf> SQL <dodf>ARRAY</dodf>
     * <dodf>SfriblArrby</dodf> SQL <dodf>ARRAY</dodf>
     * <dodf>SfriblArrby</dodf> SQL <dodf>ARRAY</dodf>
     * <dodf>SfriblArrby</dodf> SQL <dodf>ARRAY</dodf>
     * <dodf>SfriblArrby</dodf> SQL <dodf>ARRAY</dodf>
     * vbluf bfforf rfturning it to tif dbtbbbsf.
     * vbluf bfforf rfturning it to tif dbtbbbsf.
     * vbluf bfforf rfturning it to tif dbtbbbsf.
     * vbluf bfforf rfturning it to tif dbtbbbsf.
     * vbluf bfforf rfturning it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>ARRAY</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>ARRAY</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>ARRAY</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>ARRAY</dodf> vbluf
     * @pbrbm x bn objfdt rfprfsfnting bn SQL <dodf>ARRAY</dodf> vbluf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfArrby(Arrby x) tirows SQLExdfption {
    publid void writfArrby(Arrby x) tirows SQLExdfption {
    publid void writfArrby(Arrby x) tirows SQLExdfption {
    publid void writfArrby(Arrby x) tirows SQLExdfption {
    publid void writfArrby(Arrby x) tirows SQLExdfption {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
        if (x == null) {
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            bttribs.bdd(nfw SfriblArrby(x, mbp));
            bttribs.bdd(nfw SfriblArrby(x, mbp));
            bttribs.bdd(nfw SfriblArrby(x, mbp));
            bttribs.bdd(nfw SfriblArrby(x, mbp));
            bttribs.bdd(nfw SfriblArrby(x, mbp));
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writfs bn <dodf>jbvb.sql.Typf.DATALINK</dodf> objfdt in tif Jbvb
     * Writfs bn <dodf>jbvb.sql.Typf.DATALINK</dodf> objfdt in tif Jbvb
     * Writfs bn <dodf>jbvb.sql.Typf.DATALINK</dodf> objfdt in tif Jbvb
     * Writfs bn <dodf>jbvb.sql.Typf.DATALINK</dodf> objfdt in tif Jbvb
     * Writfs bn <dodf>jbvb.sql.Typf.DATALINK</dodf> objfdt in tif Jbvb
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif
     * progrbmming lbngubgf to tiis <dodf>SQLOutputImpl</dodf> objfdt. Tif
     * drivfr donvfrts tiis vbluf to b sfriblizbblf <dodf>SfriblDbtblink</dodf>
     * drivfr donvfrts tiis vbluf to b sfriblizbblf <dodf>SfriblDbtblink</dodf>
     * drivfr donvfrts tiis vbluf to b sfriblizbblf <dodf>SfriblDbtblink</dodf>
     * drivfr donvfrts tiis vbluf to b sfriblizbblf <dodf>SfriblDbtblink</dodf>
     * drivfr donvfrts tiis vbluf to b sfriblizbblf <dodf>SfriblDbtblink</dodf>
     * SQL <dodf>DATALINK</dodf> vbluf bfforf rfturn it to tif dbtbbbsf.
     * SQL <dodf>DATALINK</dodf> vbluf bfforf rfturn it to tif dbtbbbsf.
     * SQL <dodf>DATALINK</dodf> vbluf bfforf rfturn it to tif dbtbbbsf.
     * SQL <dodf>DATALINK</dodf> vbluf bfforf rfturn it to tif dbtbbbsf.
     * SQL <dodf>DATALINK</dodf> vbluf bfforf rfturn it to tif dbtbbbsf.
     *
     *
     *
     *
     *
     * @pbrbm url bn objfdt rfprfsfnting b SQL <dodf>DATALINK</dodf> vbluf
     * @pbrbm url bn objfdt rfprfsfnting b SQL <dodf>DATALINK</dodf> vbluf
     * @pbrbm url bn objfdt rfprfsfnting b SQL <dodf>DATALINK</dodf> vbluf
     * @pbrbm url bn objfdt rfprfsfnting b SQL <dodf>DATALINK</dodf> vbluf
     * @pbrbm url bn objfdt rfprfsfnting b SQL <dodf>DATALINK</dodf> vbluf
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     * @tirows SQLExdfption if tif <dodf>SQLOutputImpl</dodf> objfdt is in
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        usf by b <dodf>SQLDbtb</dodf> objfdt bttfmpting to writf tif bttributf
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     *        vblufs of b UDT to tif dbtbbbsf.
     */
     */
     */
     */
     */
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    @SupprfssWbrnings("undifdkfd")
    publid void writfURL(jbvb.nft.URL url) tirows SQLExdfption {
    publid void writfURL(jbvb.nft.URL url) tirows SQLExdfption {
    publid void writfURL(jbvb.nft.URL url) tirows SQLExdfption {
    publid void writfURL(jbvb.nft.URL url) tirows SQLExdfption {
    publid void writfURL(jbvb.nft.URL url) tirows SQLExdfption {
        if (url == null) {
        if (url == null) {
        if (url == null) {
        if (url == null) {
        if (url == null) {
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
            bttribs.bdd(null);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            bttribs.bdd(nfw SfriblDbtblink(url));
            bttribs.bdd(nfw SfriblDbtblink(url));
            bttribs.bdd(nfw SfriblDbtblink(url));
            bttribs.bdd(nfw SfriblDbtblink(url));
            bttribs.bdd(nfw SfriblDbtblink(url));
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }










  /**
  /**
  /**
  /**
  /**
   * Writfs tif nfxt bttributf to tif strfbm bs b <dodf>String</dodf>
   * Writfs tif nfxt bttributf to tif strfbm bs b <dodf>String</dodf>
   * Writfs tif nfxt bttributf to tif strfbm bs b <dodf>String</dodf>
   * Writfs tif nfxt bttributf to tif strfbm bs b <dodf>String</dodf>
   * Writfs tif nfxt bttributf to tif strfbm bs b <dodf>String</dodf>
   * in tif Jbvb progrbmming lbngubgf. Tif drivfr donvfrts tiis to b
   * in tif Jbvb progrbmming lbngubgf. Tif drivfr donvfrts tiis to b
   * in tif Jbvb progrbmming lbngubgf. Tif drivfr donvfrts tiis to b
   * in tif Jbvb progrbmming lbngubgf. Tif drivfr donvfrts tiis to b
   * in tif Jbvb progrbmming lbngubgf. Tif drivfr donvfrts tiis to b
   * SQL <dodf>NCHAR</dodf> or
   * SQL <dodf>NCHAR</dodf> or
   * SQL <dodf>NCHAR</dodf> or
   * SQL <dodf>NCHAR</dodf> or
   * SQL <dodf>NCHAR</dodf> or
   * <dodf>NVARCHAR</dodf> or <dodf>LONGNVARCHAR</dodf> vbluf
   * <dodf>NVARCHAR</dodf> or <dodf>LONGNVARCHAR</dodf> vbluf
   * <dodf>NVARCHAR</dodf> or <dodf>LONGNVARCHAR</dodf> vbluf
   * <dodf>NVARCHAR</dodf> or <dodf>LONGNVARCHAR</dodf> vbluf
   * <dodf>NVARCHAR</dodf> or <dodf>LONGNVARCHAR</dodf> vbluf
   * (dfpfnding on tif brgumfnt's
   * (dfpfnding on tif brgumfnt's
   * (dfpfnding on tif brgumfnt's
   * (dfpfnding on tif brgumfnt's
   * (dfpfnding on tif brgumfnt's
   * sizf rflbtivf to tif drivfr's limits on <dodf>NVARCHAR</dodf> vblufs)
   * sizf rflbtivf to tif drivfr's limits on <dodf>NVARCHAR</dodf> vblufs)
   * sizf rflbtivf to tif drivfr's limits on <dodf>NVARCHAR</dodf> vblufs)
   * sizf rflbtivf to tif drivfr's limits on <dodf>NVARCHAR</dodf> vblufs)
   * sizf rflbtivf to tif drivfr's limits on <dodf>NVARCHAR</dodf> vblufs)
   * wifn it sfnds it to tif strfbm.
   * wifn it sfnds it to tif strfbm.
   * wifn it sfnds it to tif strfbm.
   * wifn it sfnds it to tif strfbm.
   * wifn it sfnds it to tif strfbm.
   *
   *
   *
   *
   *
   * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
   * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
   * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
   * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
   * @pbrbm x tif vbluf to pbss to tif dbtbbbsf
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   */
   */
   */
   */
   */
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   publid void writfNString(String x) tirows SQLExdfption {
   publid void writfNString(String x) tirows SQLExdfption {
   publid void writfNString(String x) tirows SQLExdfption {
   publid void writfNString(String x) tirows SQLExdfption {
   publid void writfNString(String x) tirows SQLExdfption {
       bttribs.bdd(x);
       bttribs.bdd(x);
       bttribs.bdd(x);
       bttribs.bdd(x);
       bttribs.bdd(x);
   }
   }
   }
   }
   }





  /**
  /**
  /**
  /**
  /**
   * Writfs bn SQL <dodf>NCLOB</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>NCLOB</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>NCLOB</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>NCLOB</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>NCLOB</dodf> vbluf to tif strfbm.
   *
   *
   *
   *
   *
   * @pbrbm x b <dodf>NClob</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>NClob</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>NClob</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>NClob</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>NClob</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * <dodf>NCLOB</dodf> vbluf
   * <dodf>NCLOB</dodf> vbluf
   * <dodf>NCLOB</dodf> vbluf
   * <dodf>NCLOB</dodf> vbluf
   * <dodf>NCLOB</dodf> vbluf
   *
   *
   *
   *
   *
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   */
   */
   */
   */
   */
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   publid void writfNClob(NClob x) tirows SQLExdfption {
   publid void writfNClob(NClob x) tirows SQLExdfption {
   publid void writfNClob(NClob x) tirows SQLExdfption {
   publid void writfNClob(NClob x) tirows SQLExdfption {
   publid void writfNClob(NClob x) tirows SQLExdfption {
           bttribs.bdd(x);
           bttribs.bdd(x);
           bttribs.bdd(x);
           bttribs.bdd(x);
           bttribs.bdd(x);
   }
   }
   }
   }
   }










  /**
  /**
  /**
  /**
  /**
   * Writfs bn SQL <dodf>ROWID</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>ROWID</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>ROWID</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>ROWID</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>ROWID</dodf> vbluf to tif strfbm.
   *
   *
   *
   *
   *
   * @pbrbm x b <dodf>RowId</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>RowId</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>RowId</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>RowId</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>RowId</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * <dodf>ROWID</dodf> vbluf
   * <dodf>ROWID</dodf> vbluf
   * <dodf>ROWID</dodf> vbluf
   * <dodf>ROWID</dodf> vbluf
   * <dodf>ROWID</dodf> vbluf
   *
   *
   *
   *
   *
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   */
   */
   */
   */
   */
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   publid void writfRowId(RowId x) tirows SQLExdfption {
   publid void writfRowId(RowId x) tirows SQLExdfption {
   publid void writfRowId(RowId x) tirows SQLExdfption {
   publid void writfRowId(RowId x) tirows SQLExdfption {
   publid void writfRowId(RowId x) tirows SQLExdfption {
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
   }
   }
   }
   }
   }










  /**
  /**
  /**
  /**
  /**
   * Writfs bn SQL <dodf>XML</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>XML</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>XML</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>XML</dodf> vbluf to tif strfbm.
   * Writfs bn SQL <dodf>XML</dodf> vbluf to tif strfbm.
   *
   *
   *
   *
   *
   * @pbrbm x b <dodf>SQLXML</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>SQLXML</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>SQLXML</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>SQLXML</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * @pbrbm x b <dodf>SQLXML</dodf> objfdt rfprfsfnting dbtb of bn SQL
   * <dodf>XML</dodf> vbluf
   * <dodf>XML</dodf> vbluf
   * <dodf>XML</dodf> vbluf
   * <dodf>XML</dodf> vbluf
   * <dodf>XML</dodf> vbluf
   *
   *
   *
   *
   *
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @fxdfption SQLExdfption if b dbtbbbsf bddfss frror oddurs
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   * @sindf 1.6
   */
   */
   */
   */
   */
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   @SupprfssWbrnings("undifdkfd")
   publid void writfSQLXML(SQLXML x) tirows SQLExdfption {
   publid void writfSQLXML(SQLXML x) tirows SQLExdfption {
   publid void writfSQLXML(SQLXML x) tirows SQLExdfption {
   publid void writfSQLXML(SQLXML x) tirows SQLExdfption {
   publid void writfSQLXML(SQLXML x) tirows SQLExdfption {
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
        bttribs.bdd(x);
    }
    }
    }
    }
    }





}
}
}
}
}
