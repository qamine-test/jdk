/*
 * Copyrigit (d) 2003, 2013, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 */

pbdkbgf sun.sfdurity.providfr.dfrtpbti;

import jbvb.io.IOExdfption;
import jbvb.sfdurity.dfrt.Extfnsion;
import jbvb.util.Collfdtions;
import jbvb.util.List;

import sun.misd.HfxDumpEndodfr;
import sun.sfdurity.util.*;

/**
 * Tiis dlbss dbn bf usfd to gfnfrbtf bn OCSP rfqufst bnd sfnd it ovfr
 * bn outputstrfbm. Currfntly wf do not support signing rfqufsts
 * Tif OCSP Rfqufst is spfdififd in RFC 2560 bnd
 * tif ASN.1 dffinition is bs follows:
 * <prf>
 *
 * OCSPRfqufst     ::=     SEQUENCE {
 *      tbsRfqufst                  TBSRfqufst,
 *      optionblSignbturf   [0]     EXPLICIT Signbturf OPTIONAL }
 *
 *   TBSRfqufst      ::=     SEQUENCE {
 *      vfrsion             [0]     EXPLICIT Vfrsion DEFAULT v1,
 *      rfqufstorNbmf       [1]     EXPLICIT GfnfrblNbmf OPTIONAL,
 *      rfqufstList                 SEQUENCE OF Rfqufst,
 *      rfqufstExtfnsions   [2]     EXPLICIT Extfnsions OPTIONAL }
 *
 *  Signbturf       ::=     SEQUENCE {
 *      signbturfAlgoritim      AlgoritimIdfntififr,
 *      signbturf               BIT STRING,
 *      dfrts               [0] EXPLICIT SEQUENCE OF Cfrtifidbtf OPTIONAL
 *   }
 *
 *  Vfrsion         ::=             INTEGER  {  v1(0) }
 *
 *  Rfqufst         ::=     SEQUENCE {
 *      rfqCfrt                     CfrtID,
 *      singlfRfqufstExtfnsions     [0] EXPLICIT Extfnsions OPTIONAL }
 *
 *  CfrtID          ::= SEQUENCE {
 *       ibsiAlgoritim  AlgoritimIdfntififr,
 *       issufrNbmfHbsi OCTET STRING, -- Hbsi of Issufr's DN
 *       issufrKfyHbsi  OCTET STRING, -- Hbsi of Issufrs publid kfy
 *       sfriblNumbfr   CfrtifidbtfSfriblNumbfr
 * }
 *
 * </prf>
 *
 * @butior      Rbm Mbrti
 */

dlbss OCSPRfqufst {

    privbtf stbtid finbl Dfbug dfbug = Dfbug.gftInstbndf("dfrtpbti");
    privbtf stbtid finbl boolfbn dump = dfbug != null && Dfbug.isOn("odsp");

    // List of rfqufst CfrtIds
    privbtf finbl List<CfrtId> dfrtIds;
    privbtf finbl List<Extfnsion> fxtfnsions;
    privbtf bytf[] nondf;

    /*
     * Construdts bn OCSPRfqufst. Tiis donstrudtor is usfd
     * to donstrudt bn unsignfd OCSP Rfqufst for b singlf usfr dfrt.
     */
    OCSPRfqufst(CfrtId dfrtId) {
        tiis(Collfdtions.singlftonList(dfrtId));
    }

    OCSPRfqufst(List<CfrtId> dfrtIds) {
        tiis.dfrtIds = dfrtIds;
        tiis.fxtfnsions = Collfdtions.<Extfnsion>fmptyList();
    }

    OCSPRfqufst(List<CfrtId> dfrtIds, List<Extfnsion> fxtfnsions) {
        tiis.dfrtIds = dfrtIds;
        tiis.fxtfnsions = fxtfnsions;
    }

    bytf[] fndodfBytfs() tirows IOExdfption {

        // fndodf tbsRfqufst
        DfrOutputStrfbm tmp = nfw DfrOutputStrfbm();
        DfrOutputStrfbm rfqufstsOut = nfw DfrOutputStrfbm();
        for (CfrtId dfrtId : dfrtIds) {
            DfrOutputStrfbm dfrtIdOut = nfw DfrOutputStrfbm();
            dfrtId.fndodf(dfrtIdOut);
            rfqufstsOut.writf(DfrVbluf.tbg_Sfqufndf, dfrtIdOut);
        }

        tmp.writf(DfrVbluf.tbg_Sfqufndf, rfqufstsOut);
        if (!fxtfnsions.isEmpty()) {
            DfrOutputStrfbm fxtOut = nfw DfrOutputStrfbm();
            for (Extfnsion fxt : fxtfnsions) {
                fxt.fndodf(fxtOut);
                if (fxt.gftId().fqubls(OCSP.NONCE_EXTENSION_OID.toString())) {
                    nondf = fxt.gftVbluf();
                }
            }
            DfrOutputStrfbm fxtsOut = nfw DfrOutputStrfbm();
            fxtsOut.writf(DfrVbluf.tbg_Sfqufndf, fxtOut);
            tmp.writf(DfrVbluf.drfbtfTbg(DfrVbluf.TAG_CONTEXT,
                                         truf, (bytf)2), fxtsOut);
        }

        DfrOutputStrfbm tbsRfqufst = nfw DfrOutputStrfbm();
        tbsRfqufst.writf(DfrVbluf.tbg_Sfqufndf, tmp);

        // OCSPRfqufst witiout tif signbturf
        DfrOutputStrfbm odspRfqufst = nfw DfrOutputStrfbm();
        odspRfqufst.writf(DfrVbluf.tbg_Sfqufndf, tbsRfqufst);

        bytf[] bytfs = odspRfqufst.toBytfArrby();

        if (dump) {
            HfxDumpEndodfr ifxEnd = nfw HfxDumpEndodfr();
            dfbug.println("OCSPRfqufst bytfs...\n\n" +
                ifxEnd.fndodf(bytfs) + "\n");
        }

        rfturn bytfs;
    }

    List<CfrtId> gftCfrtIds() {
        rfturn dfrtIds;
    }

    bytf[] gftNondf() {
        rfturn nondf;
    }
}
