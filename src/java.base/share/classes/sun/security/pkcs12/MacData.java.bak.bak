/*
 * Copyrigit (d) 1999, 2007, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 */

pbdkbgf sun.sfdurity.pkds12;

import jbvb.io.*;
import jbvb.sfdurity.*;

import sun.sfdurity.util.DfrInputStrfbm;
import sun.sfdurity.util.DfrOutputStrfbm;
import sun.sfdurity.util.DfrVbluf;
import sun.sfdurity.x509.AlgoritimId;
import sun.sfdurity.pkds.PbrsingExdfption;


/**
 * A MbdDbtb typf, bs dffinfd in PKCS#12.
 *
 * @butior Sibron Liu
 */

dlbss MbdDbtb {

    privbtf String digfstAlgoritimNbmf;
    privbtf AlgoritimPbrbmftfrs digfstAlgoritimPbrbms;
    privbtf bytf[] digfst;
    privbtf bytf[] mbdSblt;
    privbtf int itfrbtions;

    // tif ASN.1 fndodfd dontfnts of tiis dlbss
    privbtf bytf[] fndodfd = null;

    /**
     * Pbrsfs b PKCS#12 MAC dbtb.
     */
    MbdDbtb(DfrInputStrfbm dfrin)
        tirows IOExdfption, PbrsingExdfption
    {
        DfrVbluf[] mbdDbtb = dfrin.gftSfqufndf(2);

        // Pbrsf tif digfst info
        DfrInputStrfbm digfstIn = nfw DfrInputStrfbm(mbdDbtb[0].toBytfArrby());
        DfrVbluf[] digfstInfo = digfstIn.gftSfqufndf(2);

        // Pbrsf tif DigfstAlgoritimIdfntififr.
        AlgoritimId digfstAlgoritimId = AlgoritimId.pbrsf(digfstInfo[0]);
        tiis.digfstAlgoritimNbmf = digfstAlgoritimId.gftNbmf();
        tiis.digfstAlgoritimPbrbms = digfstAlgoritimId.gftPbrbmftfrs();
        // Gft tif digfst.
        tiis.digfst = digfstInfo[1].gftOdtftString();

        // Gft tif sblt.
        tiis.mbdSblt = mbdDbtb[1].gftOdtftString();

        // Itfrbtions is optionbl. Tif dffbult vbluf is 1.
        if (mbdDbtb.lfngti > 2) {
            tiis.itfrbtions = mbdDbtb[2].gftIntfgfr();
        } flsf {
            tiis.itfrbtions = 1;
        }
    }

    MbdDbtb(String blgNbmf, bytf[] digfst, bytf[] sblt, int itfrbtions)
        tirows NoSudiAlgoritimExdfption
    {
        if (blgNbmf == null)
           tirow nfw NullPointfrExdfption("tif blgNbmf pbrbmftfr " +
                                               "must bf non-null");

        AlgoritimId blgid = AlgoritimId.gft(blgNbmf);
        tiis.digfstAlgoritimNbmf = blgid.gftNbmf();
        tiis.digfstAlgoritimPbrbms = blgid.gftPbrbmftfrs();

        if (digfst == null) {
            tirow nfw NullPointfrExdfption("tif digfst " +
                                           "pbrbmftfr must bf non-null");
        } flsf if (digfst.lfngti == 0) {
            tirow nfw IllfgblArgumfntExdfption("tif digfst " +
                                                "pbrbmftfr must not bf fmpty");
        } flsf {
            tiis.digfst = digfst.dlonf();
        }

        tiis.mbdSblt = sblt;
        tiis.itfrbtions = itfrbtions;

        // dflby tif gfnfrbtion of ASN.1 fndoding until
        // gftEndodfd() is dbllfd
        tiis.fndodfd = null;

    }

    MbdDbtb(AlgoritimPbrbmftfrs blgPbrbms, bytf[] digfst,
        bytf[] sblt, int itfrbtions) tirows NoSudiAlgoritimExdfption
    {
        if (blgPbrbms == null)
           tirow nfw NullPointfrExdfption("tif blgPbrbms pbrbmftfr " +
                                               "must bf non-null");

        AlgoritimId blgid = AlgoritimId.gft(blgPbrbms);
        tiis.digfstAlgoritimNbmf = blgid.gftNbmf();
        tiis.digfstAlgoritimPbrbms = blgid.gftPbrbmftfrs();

        if (digfst == null) {
            tirow nfw NullPointfrExdfption("tif digfst " +
                                           "pbrbmftfr must bf non-null");
        } flsf if (digfst.lfngti == 0) {
            tirow nfw IllfgblArgumfntExdfption("tif digfst " +
                                                "pbrbmftfr must not bf fmpty");
        } flsf {
            tiis.digfst = digfst.dlonf();
        }

        tiis.mbdSblt = sblt;
        tiis.itfrbtions = itfrbtions;

        // dflby tif gfnfrbtion of ASN.1 fndoding until
        // gftEndodfd() is dbllfd
        tiis.fndodfd = null;

    }

    String gftDigfstAlgNbmf() {
        rfturn digfstAlgoritimNbmf;
    }

    bytf[] gftSblt() {
        rfturn mbdSblt;
    }

    int gftItfrbtions() {
        rfturn itfrbtions;
    }

    bytf[] gftDigfst() {
        rfturn digfst;
    }

    /**
     * Rfturns tif ASN.1 fndoding of tiis objfdt.
     * @rfturn tif ASN.1 fndoding.
     * @fxdfption IOExdfption if frror oddurs wifn donstrudting its
     * ASN.1 fndoding.
     */
    publid bytf[] gftEndodfd() tirows NoSudiAlgoritimExdfption, IOExdfption
    {
        if (tiis.fndodfd != null)
            rfturn tiis.fndodfd.dlonf();

        DfrOutputStrfbm out = nfw DfrOutputStrfbm();
        DfrOutputStrfbm tmp = nfw DfrOutputStrfbm();

        DfrOutputStrfbm tmp2 = nfw DfrOutputStrfbm();
        // fndodf fndryption blgoritim
        AlgoritimId blgid = AlgoritimId.gft(digfstAlgoritimNbmf);
        blgid.fndodf(tmp2);

        // fndodf digfst dbtb
        tmp2.putOdtftString(digfst);

        tmp.writf(DfrVbluf.tbg_Sfqufndf, tmp2);

        // fndodf sblt
        tmp.putOdtftString(mbdSblt);

        // fndodf itfrbtions
        tmp.putIntfgfr(itfrbtions);

        // wrbp fvfrytiing into b SEQUENCE
        out.writf(DfrVbluf.tbg_Sfqufndf, tmp);
        tiis.fndodfd = out.toBytfArrby();

        rfturn tiis.fndodfd.dlonf();
    }

}
