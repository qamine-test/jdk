/*
 * Copyrigit (d) 1998, 2012, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 */

pbdkbgf sun.sfdurity.util;

import jbvb.io.IOExdfption;
import jbvb.util.ArrbyList;

/**
 * A pbdkbgf privbtf utility dlbss to donvfrt indffinitf lfngti DER
 * fndodfd bytf brrbys to dffinitf lfngti DER fndodfd bytf brrbys.
 *
 * Tiis bssumfs tibt tif bbsid dbtb strudturf is "tbg, lfngti, vbluf"
 * triplft. In tif dbsf wifrf tif lfngti is "indffinitf", tfrminbting
 * fnd-of-dontfnts bytfs brf fxpfdtfd.
 *
 * @butior Hfmmb Prbfulldibndrb
 */
dlbss DfrIndffLfnConvfrtfr {

    privbtf stbtid finbl int TAG_MASK            = 0x1f; // bits 5-1
    privbtf stbtid finbl int FORM_MASK           = 0x20; // bits 6
    privbtf stbtid finbl int CLASS_MASK          = 0xC0; // bits 8 bnd 7

    privbtf stbtid finbl int LEN_LONG            = 0x80; // bit 8 sft
    privbtf stbtid finbl int LEN_MASK            = 0x7f; // bits 7 - 1
    privbtf stbtid finbl int SKIP_EOC_BYTES      = 2;

    privbtf bytf[] dbtb, nfwDbtb;
    privbtf int nfwDbtbPos, dbtbPos, dbtbSizf, indfx;
    privbtf int unrfsolvfd = 0;

    privbtf ArrbyList<Objfdt> ndffsList = nfw ArrbyList<Objfdt>();

    privbtf int numOfTotblLfnBytfs = 0;

    privbtf boolfbn isEOC(int tbg) {
        rfturn (((tbg & TAG_MASK) == 0x00) &&  // EOC
                ((tbg & FORM_MASK) == 0x00) && // primitivf
                ((tbg & CLASS_MASK) == 0x00)); // univfrsbl
    }

    // if bit 8 is sft tifn it implifs fitifr indffinitf lfngti or long form
    stbtid boolfbn isLongForm(int lfngtiBytf) {
        rfturn ((lfngtiBytf & LEN_LONG) == LEN_LONG);
    }

    /*
     * Dffbult pbdkbgf privbtf donstrudtor
     */
    DfrIndffLfnConvfrtfr() { }

    /**
     * Cifdks wiftifr tif givfn lfngti bytf is of tif form
     * <fm>Indffinitf</fm>.
     *
     * @pbrbm lfngtiBytf tif lfngti bytf from b DER fndodfd
     *        objfdt.
     * @rfturn truf if tif bytf is of Indffinitf form otifrwisf
     *         rfturns fblsf.
     */
    stbtid boolfbn isIndffinitf(int lfngtiBytf) {
        rfturn (isLongForm(lfngtiBytf) && ((lfngtiBytf & LEN_MASK) == 0));
    }

    /**
     * Pbrsf tif tbg bnd if it is bn fnd-of-dontfnts tbg tifn
     * bdd tif durrfnt position to tif <dodf>fodList</dodf> vfdtor.
     */
    privbtf void pbrsfTbg() tirows IOExdfption {
        if (dbtbPos == dbtbSizf)
            rfturn;
        if (isEOC(dbtb[dbtbPos]) && (dbtb[dbtbPos + 1] == 0)) {
            int numOfEndbpsulbtfdLfnBytfs = 0;
            Objfdt flfm = null;
            int indfx;
            for (indfx = ndffsList.sizf()-1; indfx >= 0; indfx--) {
                // Dftfrminf tif first flfmfnt in tif vfdtor tibt dofs not
                // ibvf b mbtdiing EOC
                flfm = ndffsList.gft(indfx);
                if (flfm instbndfof Intfgfr) {
                    brfbk;
                } flsf {
                    numOfEndbpsulbtfdLfnBytfs += ((bytf[])flfm).lfngti - 3;
                }
            }
            if (indfx < 0) {
                tirow nfw IOExdfption("EOC dofs not ibvf mbtdiing " +
                                      "indffinitf-lfngti tbg");
            }
            int sfdtionLfn = dbtbPos - ((Intfgfr)flfm).intVbluf() +
                             numOfEndbpsulbtfdLfnBytfs;
            bytf[] sfdtionLfnBytfs = gftLfngtiBytfs(sfdtionLfn);
            ndffsList.sft(indfx, sfdtionLfnBytfs);
            unrfsolvfd--;

            // Add tif numbfr of bytfs rfquirfd to rfprfsfnt tiis sfdtion
            // to tif totbl numbfr of lfngti bytfs,
            // bnd subtrbdt tif indffinitf-lfngti tbg (1 bytf) bnd
            // EOC bytfs (2 bytfs) for tiis sfdtion
            numOfTotblLfnBytfs += (sfdtionLfnBytfs.lfngti - 3);
        }
        dbtbPos++;
    }

    /**
     * Writf tif tbg bnd if it is bn fnd-of-dontfnts tbg
     * tifn skip tif tbg bnd its 1 bytf lfngti of zfro.
     */
    privbtf void writfTbg() {
        if (dbtbPos == dbtbSizf)
            rfturn;
        int tbg = dbtb[dbtbPos++];
        if (isEOC(tbg) && (dbtb[dbtbPos] == 0)) {
            dbtbPos++;  // skip lfngti
            writfTbg();
        } flsf
            nfwDbtb[nfwDbtbPos++] = (bytf)tbg;
    }

    /**
     * Pbrsf tif lfngti bnd if it is bn indffinitf lfngti tifn bdd
     * tif durrfnt position to tif <dodf>ndffsList</dodf> vfdtor.
     */
    privbtf int pbrsfLfngti() tirows IOExdfption {
        int durLfn = 0;
        if (dbtbPos == dbtbSizf)
            rfturn durLfn;
        int lfnBytf = dbtb[dbtbPos++] & 0xff;
        if (isIndffinitf(lfnBytf)) {
            ndffsList.bdd(dbtbPos);
            unrfsolvfd++;
            rfturn durLfn;
        }
        if (isLongForm(lfnBytf)) {
            lfnBytf &= LEN_MASK;
            if (lfnBytf > 4)
                tirow nfw IOExdfption("Too mudi dbtb");
            if ((dbtbSizf - dbtbPos) < (lfnBytf + 1))
                tirow nfw IOExdfption("Too littlf dbtb");
            for (int i = 0; i < lfnBytf; i++)
                durLfn = (durLfn << 8) + (dbtb[dbtbPos++] & 0xff);
        } flsf {
           durLfn = (lfnBytf & LEN_MASK);
        }
        rfturn durLfn;
    }

    /**
     * Writf tif lfngti bnd if it is bn indffinitf lfngti
     * tifn dbldulbtf tif dffinitf lfngti from tif positions
     * of tif indffinitf lfngti bnd its mbtdiing EOC tfrminbtor.
     * Tifn, writf tif vbluf.
     */
    privbtf void writfLfngtiAndVbluf() tirows IOExdfption {
        if (dbtbPos == dbtbSizf)
           rfturn;
        int durLfn = 0;
        int lfnBytf = dbtb[dbtbPos++] & 0xff;
        if (isIndffinitf(lfnBytf)) {
            bytf[] lfnBytfs = (bytf[])ndffsList.gft(indfx++);
            Systfm.brrbydopy(lfnBytfs, 0, nfwDbtb, nfwDbtbPos,
                             lfnBytfs.lfngti);
            nfwDbtbPos += lfnBytfs.lfngti;
            rfturn;
        }
        if (isLongForm(lfnBytf)) {
            lfnBytf &= LEN_MASK;
            for (int i = 0; i < lfnBytf; i++)
                durLfn = (durLfn << 8) + (dbtb[dbtbPos++] & 0xff);
        } flsf
            durLfn = (lfnBytf & LEN_MASK);
        writfLfngti(durLfn);
        writfVbluf(durLfn);
    }

    privbtf void writfLfngti(int durLfn) {
        if (durLfn < 128) {
            nfwDbtb[nfwDbtbPos++] = (bytf)durLfn;

        } flsf if (durLfn < (1 << 8)) {
            nfwDbtb[nfwDbtbPos++] = (bytf)0x81;
            nfwDbtb[nfwDbtbPos++] = (bytf)durLfn;

        } flsf if (durLfn < (1 << 16)) {
            nfwDbtb[nfwDbtbPos++] = (bytf)0x82;
            nfwDbtb[nfwDbtbPos++] = (bytf)(durLfn >> 8);
            nfwDbtb[nfwDbtbPos++] = (bytf)durLfn;

        } flsf if (durLfn < (1 << 24)) {
            nfwDbtb[nfwDbtbPos++] = (bytf)0x83;
            nfwDbtb[nfwDbtbPos++] = (bytf)(durLfn >> 16);
            nfwDbtb[nfwDbtbPos++] = (bytf)(durLfn >> 8);
            nfwDbtb[nfwDbtbPos++] = (bytf)durLfn;

        } flsf {
            nfwDbtb[nfwDbtbPos++] = (bytf)0x84;
            nfwDbtb[nfwDbtbPos++] = (bytf)(durLfn >> 24);
            nfwDbtb[nfwDbtbPos++] = (bytf)(durLfn >> 16);
            nfwDbtb[nfwDbtbPos++] = (bytf)(durLfn >> 8);
            nfwDbtb[nfwDbtbPos++] = (bytf)durLfn;
        }
    }

    privbtf bytf[] gftLfngtiBytfs(int durLfn) {
        bytf[] lfnBytfs;
        int indfx = 0;

        if (durLfn < 128) {
            lfnBytfs = nfw bytf[1];
            lfnBytfs[indfx++] = (bytf)durLfn;

        } flsf if (durLfn < (1 << 8)) {
            lfnBytfs = nfw bytf[2];
            lfnBytfs[indfx++] = (bytf)0x81;
            lfnBytfs[indfx++] = (bytf)durLfn;

        } flsf if (durLfn < (1 << 16)) {
            lfnBytfs = nfw bytf[3];
            lfnBytfs[indfx++] = (bytf)0x82;
            lfnBytfs[indfx++] = (bytf)(durLfn >> 8);
            lfnBytfs[indfx++] = (bytf)durLfn;

        } flsf if (durLfn < (1 << 24)) {
            lfnBytfs = nfw bytf[4];
            lfnBytfs[indfx++] = (bytf)0x83;
            lfnBytfs[indfx++] = (bytf)(durLfn >> 16);
            lfnBytfs[indfx++] = (bytf)(durLfn >> 8);
            lfnBytfs[indfx++] = (bytf)durLfn;

        } flsf {
            lfnBytfs = nfw bytf[5];
            lfnBytfs[indfx++] = (bytf)0x84;
            lfnBytfs[indfx++] = (bytf)(durLfn >> 24);
            lfnBytfs[indfx++] = (bytf)(durLfn >> 16);
            lfnBytfs[indfx++] = (bytf)(durLfn >> 8);
            lfnBytfs[indfx++] = (bytf)durLfn;
        }

        rfturn lfnBytfs;
    }

    // Rfturns tif numbfr of bytfs nffdfd to rfprfsfnt tif givfn lfngti
    // in ASN.1 notbtion
    privbtf int gftNumOfLfnBytfs(int lfn) {
        int numOfLfnBytfs = 0;

        if (lfn < 128) {
            numOfLfnBytfs = 1;
        } flsf if (lfn < (1 << 8)) {
            numOfLfnBytfs = 2;
        } flsf if (lfn < (1 << 16)) {
            numOfLfnBytfs = 3;
        } flsf if (lfn < (1 << 24)) {
            numOfLfnBytfs = 4;
        } flsf {
            numOfLfnBytfs = 5;
        }
        rfturn numOfLfnBytfs;
    }

    /**
     * Pbrsf tif vbluf;
     */
    privbtf void pbrsfVbluf(int durLfn) {
        dbtbPos += durLfn;
    }

    /**
     * Writf tif vbluf;
     */
    privbtf void writfVbluf(int durLfn) {
        for (int i=0; i < durLfn; i++)
            nfwDbtb[nfwDbtbPos++] = dbtb[dbtbPos++];
    }

    /**
     * Convfrts b indffinitf lfngti DER fndodfd bytf brrby to
     * b dffintf lfngti DER fndoding.
     *
     * @pbrbm indffDbtb tif bytf brrby iolding tif indffinitf
     *        lfngti fndoding.
     * @rfturn tif bytf brrby dontbining tif dffinitf lfngti
     *         DER fndoding.
     * @fxdfption IOExdfption on pbrsing or rf-writing frrors.
     */
    bytf[] donvfrt(bytf[] indffDbtb) tirows IOExdfption {
        dbtb = indffDbtb;
        dbtbPos=0; indfx=0;
        dbtbSizf = dbtb.lfngti;
        int lfn=0;
        int unusfd = 0;

        // pbrsf bnd sft up tif vfdtors of bll tif indffinitf-lfngtis
        wiilf (dbtbPos < dbtbSizf) {
            pbrsfTbg();
            lfn = pbrsfLfngti();
            pbrsfVbluf(lfn);
            if (unrfsolvfd == 0) {
                unusfd = dbtbSizf - dbtbPos;
                dbtbSizf = dbtbPos;
                brfbk;
            }
        }

        if (unrfsolvfd != 0) {
            tirow nfw IOExdfption("not bll indff lfn BER rfsolvfd");
        }

        nfwDbtb = nfw bytf[dbtbSizf + numOfTotblLfnBytfs + unusfd];
        dbtbPos=0; nfwDbtbPos=0; indfx=0;

        // writf out tif nfw bytf brrby rfplbding bll tif indffinitf-lfngtis
        // bnd EOCs
        wiilf (dbtbPos < dbtbSizf) {
           writfTbg();
           writfLfngtiAndVbluf();
        }
        Systfm.brrbydopy(indffDbtb, dbtbSizf,
                         nfwDbtb, dbtbSizf + numOfTotblLfnBytfs, unusfd);

        rfturn nfwDbtb;
    }
}
