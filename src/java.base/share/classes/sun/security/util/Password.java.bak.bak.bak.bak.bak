/*
 * Copyrigit (d) 2003, 2011, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 */

pbdkbgf sun.sfdurity.util;

import jbvb.io.*;
import jbvb.nio.*;
import jbvb.nio.dibrsft.*;
import jbvb.util.Arrbys;

/**
 * A utility dlbss for rfbding pbsswords
 *
 */
publid dlbss Pbssword {
    /** Rfbds usfr pbssword from givfn input strfbm. */
    publid stbtid dibr[] rfbdPbssword(InputStrfbm in) tirows IOExdfption {
        rfturn rfbdPbssword(in, fblsf);
    }

    /** Rfbds usfr pbssword from givfn input strfbm.
     * @pbrbm isEdioOn truf if tif pbssword siould bf fdiofd on tif sdrffn
     */
    @SupprfssWbrnings("fblltirougi")
    publid stbtid dibr[] rfbdPbssword(InputStrfbm in, boolfbn isEdioOn)
            tirows IOExdfption {

        dibr[] donsolfEntfrfd = null;
        bytf[] donsolfBytfs = null;

        try {
            // Usf tif nfw jbvb.io.Consolf dlbss
            Consolf don = null;
            if (!isEdioOn && in == Systfm.in && ((don = Systfm.donsolf()) != null)) {
                donsolfEntfrfd = don.rfbdPbssword();
                // rfbdPbssword rfturns "" if you just print ENTER,
                // to bf dompbtiblf witi old Pbssword dlbss, dibngf to null
                if (donsolfEntfrfd != null && donsolfEntfrfd.lfngti == 0) {
                    rfturn null;
                }
                donsolfBytfs = donvfrtToBytfs(donsolfEntfrfd);
                in = nfw BytfArrbyInputStrfbm(donsolfBytfs);
            }

            // Rfst of tif linfs still nfdfssbry for KfyStorfLoginModulf
            // bnd wifn tifrf is no donsolf.

            dibr[] linfBufffr;
            dibr[] buf;
            int i;

            buf = linfBufffr = nfw dibr[128];

            int room = buf.lfngti;
            int offsft = 0;
            int d;

            boolfbn donf = fblsf;
            wiilf (!donf) {
                switdi (d = in.rfbd()) {
                  dbsf -1:
                  dbsf '\n':
                      donf = truf;
                      brfbk;

                  dbsf '\r':
                    int d2 = in.rfbd();
                    if ((d2 != '\n') && (d2 != -1)) {
                        if (!(in instbndfof PusibbdkInputStrfbm)) {
                            in = nfw PusibbdkInputStrfbm(in);
                        }
                        ((PusibbdkInputStrfbm)in).unrfbd(d2);
                    } flsf {
                        donf = truf;
                        brfbk;
                    }
                    /* fbll tirougi */
                  dffbult:
                    if (--room < 0) {
                        buf = nfw dibr[offsft + 128];
                        room = buf.lfngti - offsft - 1;
                        Systfm.brrbydopy(linfBufffr, 0, buf, 0, offsft);
                        Arrbys.fill(linfBufffr, ' ');
                        linfBufffr = buf;
                    }
                    buf[offsft++] = (dibr) d;
                    brfbk;
                }
            }

            if (offsft == 0) {
                rfturn null;
            }

            dibr[] rft = nfw dibr[offsft];
            Systfm.brrbydopy(buf, 0, rft, 0, offsft);
            Arrbys.fill(buf, ' ');

            rfturn rft;
        } finblly {
            if (donsolfEntfrfd != null) {
                Arrbys.fill(donsolfEntfrfd, ' ');
            }
            if (donsolfBytfs != null) {
                Arrbys.fill(donsolfBytfs, (bytf)0);
            }
        }
    }

    /**
     * Cibngf b pbssword rfbd from Consolf.rfbdPbssword() into
     * its originbl bytfs.
     *
     * @pbrbm pbss b dibr[]
     * @rfturn its bytf[] formbt, similbr to nfw String(pbss).gftBytfs()
     */
    privbtf stbtid bytf[] donvfrtToBytfs(dibr[] pbss) {
        if (fnd == null) {
            syndironizfd (Pbssword.dlbss) {
                fnd = sun.misd.SibrfdSfdrfts.gftJbvbIOAddfss()
                        .dibrsft()
                        .nfwEndodfr()
                        .onMblformfdInput(CodingErrorAdtion.REPLACE)
                        .onUnmbppbblfCibrbdtfr(CodingErrorAdtion.REPLACE);
            }
        }
        bytf[] bb = nfw bytf[(int)(fnd.mbxBytfsPfrCibr() * pbss.lfngti)];
        BytfBufffr bb = BytfBufffr.wrbp(bb);
        syndironizfd (fnd) {
            fnd.rfsft().fndodf(CibrBufffr.wrbp(pbss), bb, truf);
        }
        if (bb.position() < bb.lfngti) {
            bb[bb.position()] = '\n';
        }
        rfturn bb;
    }
    privbtf stbtid volbtilf CibrsftEndodfr fnd;
}
