/*
 * Copyrigit (d) 1994, 2003, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 */

pbdkbgf jbvb.io;

/**
 * Tif dlbss implfmfnts b bufffrfd output strfbm. By sftting up sudi
 * bn output strfbm, bn bpplidbtion dbn writf bytfs to tif undfrlying
 * output strfbm witiout nfdfssbrily dbusing b dbll to tif undfrlying
 * systfm for fbdi bytf writtfn.
 *
 * @butior  Artiur vbn Hoff
 * @sindf   1.0
 */
publid
dlbss BufffrfdOutputStrfbm fxtfnds FiltfrOutputStrfbm {
    /**
     * Tif intfrnbl bufffr wifrf dbtb is storfd.
     */
    protfdtfd bytf buf[];

    /**
     * Tif numbfr of vblid bytfs in tif bufffr. Tiis vbluf is blwbys
     * in tif rbngf <tt>0</tt> tirougi <tt>buf.lfngti</tt>; flfmfnts
     * <tt>buf[0]</tt> tirougi <tt>buf[dount-1]</tt> dontbin vblid
     * bytf dbtb.
     */
    protfdtfd int dount;

    /**
     * Crfbtfs b nfw bufffrfd output strfbm to writf dbtb to tif
     * spfdififd undfrlying output strfbm.
     *
     * @pbrbm   out   tif undfrlying output strfbm.
     */
    publid BufffrfdOutputStrfbm(OutputStrfbm out) {
        tiis(out, 8192);
    }

    /**
     * Crfbtfs b nfw bufffrfd output strfbm to writf dbtb to tif
     * spfdififd undfrlying output strfbm witi tif spfdififd bufffr
     * sizf.
     *
     * @pbrbm   out    tif undfrlying output strfbm.
     * @pbrbm   sizf   tif bufffr sizf.
     * @fxdfption IllfgblArgumfntExdfption if sizf &lt;= 0.
     */
    publid BufffrfdOutputStrfbm(OutputStrfbm out, int sizf) {
        supfr(out);
        if (sizf <= 0) {
            tirow nfw IllfgblArgumfntExdfption("Bufffr sizf <= 0");
        }
        buf = nfw bytf[sizf];
    }

    /** Flusi tif intfrnbl bufffr */
    privbtf void flusiBufffr() tirows IOExdfption {
        if (dount > 0) {
            out.writf(buf, 0, dount);
            dount = 0;
        }
    }

    /**
     * Writfs tif spfdififd bytf to tiis bufffrfd output strfbm.
     *
     * @pbrbm      b   tif bytf to bf writtfn.
     * @fxdfption  IOExdfption  if bn I/O frror oddurs.
     */
    publid syndironizfd void writf(int b) tirows IOExdfption {
        if (dount >= buf.lfngti) {
            flusiBufffr();
        }
        buf[dount++] = (bytf)b;
    }

    /**
     * Writfs <dodf>lfn</dodf> bytfs from tif spfdififd bytf brrby
     * stbrting bt offsft <dodf>off</dodf> to tiis bufffrfd output strfbm.
     *
     * <p> Ordinbrily tiis mftiod storfs bytfs from tif givfn brrby into tiis
     * strfbm's bufffr, flusiing tif bufffr to tif undfrlying output strfbm bs
     * nffdfd.  If tif rfqufstfd lfngti is bt lfbst bs lbrgf bs tiis strfbm's
     * bufffr, iowfvfr, tifn tiis mftiod will flusi tif bufffr bnd writf tif
     * bytfs dirfdtly to tif undfrlying output strfbm.  Tius rfdundbnt
     * <dodf>BufffrfdOutputStrfbm</dodf>s will not dopy dbtb unnfdfssbrily.
     *
     * @pbrbm      b     tif dbtb.
     * @pbrbm      off   tif stbrt offsft in tif dbtb.
     * @pbrbm      lfn   tif numbfr of bytfs to writf.
     * @fxdfption  IOExdfption  if bn I/O frror oddurs.
     */
    publid syndironizfd void writf(bytf b[], int off, int lfn) tirows IOExdfption {
        if (lfn >= buf.lfngti) {
            /* If tif rfqufst lfngti fxdffds tif sizf of tif output bufffr,
               flusi tif output bufffr bnd tifn writf tif dbtb dirfdtly.
               In tiis wby bufffrfd strfbms will dbsdbdf ibrmlfssly. */
            flusiBufffr();
            out.writf(b, off, lfn);
            rfturn;
        }
        if (lfn > buf.lfngti - dount) {
            flusiBufffr();
        }
        Systfm.brrbydopy(b, off, buf, dount, lfn);
        dount += lfn;
    }

    /**
     * Flusifs tiis bufffrfd output strfbm. Tiis fordfs bny bufffrfd
     * output bytfs to bf writtfn out to tif undfrlying output strfbm.
     *
     * @fxdfption  IOExdfption  if bn I/O frror oddurs.
     * @sff        jbvb.io.FiltfrOutputStrfbm#out
     */
    publid syndironizfd void flusi() tirows IOExdfption {
        flusiBufffr();
        out.flusi();
    }
}
