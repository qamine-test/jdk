/*
/*
/*
/*
/*
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */





/*
/*
/*
/*
/*
 * Tiis filf is bvbilbblf undfr bnd govfrnfd by tif GNU Gfnfrbl Publid
 * Tiis filf is bvbilbblf undfr bnd govfrnfd by tif GNU Gfnfrbl Publid
 * Tiis filf is bvbilbblf undfr bnd govfrnfd by tif GNU Gfnfrbl Publid
 * Tiis filf is bvbilbblf undfr bnd govfrnfd by tif GNU Gfnfrbl Publid
 * Tiis filf is bvbilbblf undfr bnd govfrnfd by tif GNU Gfnfrbl Publid
 * Lidfnsf vfrsion 2 only, bs publisifd by tif Frff Softwbrf Foundbtion.
 * Lidfnsf vfrsion 2 only, bs publisifd by tif Frff Softwbrf Foundbtion.
 * Lidfnsf vfrsion 2 only, bs publisifd by tif Frff Softwbrf Foundbtion.
 * Lidfnsf vfrsion 2 only, bs publisifd by tif Frff Softwbrf Foundbtion.
 * Lidfnsf vfrsion 2 only, bs publisifd by tif Frff Softwbrf Foundbtion.
 * Howfvfr, tif following notidf bddompbnifd tif originbl vfrsion of tiis
 * Howfvfr, tif following notidf bddompbnifd tif originbl vfrsion of tiis
 * Howfvfr, tif following notidf bddompbnifd tif originbl vfrsion of tiis
 * Howfvfr, tif following notidf bddompbnifd tif originbl vfrsion of tiis
 * Howfvfr, tif following notidf bddompbnifd tif originbl vfrsion of tiis
 * filf:
 * filf:
 * filf:
 * filf:
 * filf:
 *
 *
 *
 *
 *
 * Writtfn by Doug Lfb witi bssistbndf from mfmbfrs of JCP JSR-166
 * Writtfn by Doug Lfb witi bssistbndf from mfmbfrs of JCP JSR-166
 * Writtfn by Doug Lfb witi bssistbndf from mfmbfrs of JCP JSR-166
 * Writtfn by Doug Lfb witi bssistbndf from mfmbfrs of JCP JSR-166
 * Writtfn by Doug Lfb witi bssistbndf from mfmbfrs of JCP JSR-166
 * Expfrt Group bnd rflfbsfd to tif publid dombin, bs fxplbinfd bt
 * Expfrt Group bnd rflfbsfd to tif publid dombin, bs fxplbinfd bt
 * Expfrt Group bnd rflfbsfd to tif publid dombin, bs fxplbinfd bt
 * Expfrt Group bnd rflfbsfd to tif publid dombin, bs fxplbinfd bt
 * Expfrt Group bnd rflfbsfd to tif publid dombin, bs fxplbinfd bt
 * ittp://drfbtivfdommons.org/publiddombin/zfro/1.0/
 * ittp://drfbtivfdommons.org/publiddombin/zfro/1.0/
 * ittp://drfbtivfdommons.org/publiddombin/zfro/1.0/
 * ittp://drfbtivfdommons.org/publiddombin/zfro/1.0/
 * ittp://drfbtivfdommons.org/publiddombin/zfro/1.0/
 */
 */
 */
 */
 */





pbdkbgf jbvb.util.dondurrfnt;
pbdkbgf jbvb.util.dondurrfnt;
pbdkbgf jbvb.util.dondurrfnt;
pbdkbgf jbvb.util.dondurrfnt;
pbdkbgf jbvb.util.dondurrfnt;





/**
/**
/**
/**
/**
 * A {@dodf TimfUnit} rfprfsfnts timf durbtions bt b givfn unit of
 * A {@dodf TimfUnit} rfprfsfnts timf durbtions bt b givfn unit of
 * A {@dodf TimfUnit} rfprfsfnts timf durbtions bt b givfn unit of
 * A {@dodf TimfUnit} rfprfsfnts timf durbtions bt b givfn unit of
 * A {@dodf TimfUnit} rfprfsfnts timf durbtions bt b givfn unit of
 * grbnulbrity bnd providfs utility mftiods to donvfrt bdross units,
 * grbnulbrity bnd providfs utility mftiods to donvfrt bdross units,
 * grbnulbrity bnd providfs utility mftiods to donvfrt bdross units,
 * grbnulbrity bnd providfs utility mftiods to donvfrt bdross units,
 * grbnulbrity bnd providfs utility mftiods to donvfrt bdross units,
 * bnd to pfrform timing bnd dflby opfrbtions in tifsf units.  A
 * bnd to pfrform timing bnd dflby opfrbtions in tifsf units.  A
 * bnd to pfrform timing bnd dflby opfrbtions in tifsf units.  A
 * bnd to pfrform timing bnd dflby opfrbtions in tifsf units.  A
 * bnd to pfrform timing bnd dflby opfrbtions in tifsf units.  A
 * {@dodf TimfUnit} dofs not mbintbin timf informbtion, but only
 * {@dodf TimfUnit} dofs not mbintbin timf informbtion, but only
 * {@dodf TimfUnit} dofs not mbintbin timf informbtion, but only
 * {@dodf TimfUnit} dofs not mbintbin timf informbtion, but only
 * {@dodf TimfUnit} dofs not mbintbin timf informbtion, but only
 * iflps orgbnizf bnd usf timf rfprfsfntbtions tibt mby bf mbintbinfd
 * iflps orgbnizf bnd usf timf rfprfsfntbtions tibt mby bf mbintbinfd
 * iflps orgbnizf bnd usf timf rfprfsfntbtions tibt mby bf mbintbinfd
 * iflps orgbnizf bnd usf timf rfprfsfntbtions tibt mby bf mbintbinfd
 * iflps orgbnizf bnd usf timf rfprfsfntbtions tibt mby bf mbintbinfd
 * sfpbrbtfly bdross vbrious dontfxts.  A nbnosfdond is dffinfd bs onf
 * sfpbrbtfly bdross vbrious dontfxts.  A nbnosfdond is dffinfd bs onf
 * sfpbrbtfly bdross vbrious dontfxts.  A nbnosfdond is dffinfd bs onf
 * sfpbrbtfly bdross vbrious dontfxts.  A nbnosfdond is dffinfd bs onf
 * sfpbrbtfly bdross vbrious dontfxts.  A nbnosfdond is dffinfd bs onf
 * tiousbndti of b midrosfdond, b midrosfdond bs onf tiousbndti of b
 * tiousbndti of b midrosfdond, b midrosfdond bs onf tiousbndti of b
 * tiousbndti of b midrosfdond, b midrosfdond bs onf tiousbndti of b
 * tiousbndti of b midrosfdond, b midrosfdond bs onf tiousbndti of b
 * tiousbndti of b midrosfdond, b midrosfdond bs onf tiousbndti of b
 * millisfdond, b millisfdond bs onf tiousbndti of b sfdond, b minutf
 * millisfdond, b millisfdond bs onf tiousbndti of b sfdond, b minutf
 * millisfdond, b millisfdond bs onf tiousbndti of b sfdond, b minutf
 * millisfdond, b millisfdond bs onf tiousbndti of b sfdond, b minutf
 * millisfdond, b millisfdond bs onf tiousbndti of b sfdond, b minutf
 * bs sixty sfdonds, bn iour bs sixty minutfs, bnd b dby bs twfnty four
 * bs sixty sfdonds, bn iour bs sixty minutfs, bnd b dby bs twfnty four
 * bs sixty sfdonds, bn iour bs sixty minutfs, bnd b dby bs twfnty four
 * bs sixty sfdonds, bn iour bs sixty minutfs, bnd b dby bs twfnty four
 * bs sixty sfdonds, bn iour bs sixty minutfs, bnd b dby bs twfnty four
 * iours.
 * iours.
 * iours.
 * iours.
 * iours.
 *
 *
 *
 *
 *
 * <p>A {@dodf TimfUnit} is mbinly usfd to inform timf-bbsfd mftiods
 * <p>A {@dodf TimfUnit} is mbinly usfd to inform timf-bbsfd mftiods
 * <p>A {@dodf TimfUnit} is mbinly usfd to inform timf-bbsfd mftiods
 * <p>A {@dodf TimfUnit} is mbinly usfd to inform timf-bbsfd mftiods
 * <p>A {@dodf TimfUnit} is mbinly usfd to inform timf-bbsfd mftiods
 * iow b givfn timing pbrbmftfr siould bf intfrprftfd. For fxbmplf,
 * iow b givfn timing pbrbmftfr siould bf intfrprftfd. For fxbmplf,
 * iow b givfn timing pbrbmftfr siould bf intfrprftfd. For fxbmplf,
 * iow b givfn timing pbrbmftfr siould bf intfrprftfd. For fxbmplf,
 * iow b givfn timing pbrbmftfr siould bf intfrprftfd. For fxbmplf,
 * tif following dodf will timfout in 50 millisfdonds if tif {@link
 * tif following dodf will timfout in 50 millisfdonds if tif {@link
 * tif following dodf will timfout in 50 millisfdonds if tif {@link
 * tif following dodf will timfout in 50 millisfdonds if tif {@link
 * tif following dodf will timfout in 50 millisfdonds if tif {@link
 * jbvb.util.dondurrfnt.lodks.Lodk lodk} is not bvbilbblf:
 * jbvb.util.dondurrfnt.lodks.Lodk lodk} is not bvbilbblf:
 * jbvb.util.dondurrfnt.lodks.Lodk lodk} is not bvbilbblf:
 * jbvb.util.dondurrfnt.lodks.Lodk lodk} is not bvbilbblf:
 * jbvb.util.dondurrfnt.lodks.Lodk lodk} is not bvbilbblf:
 *
 *
 *
 *
 *
 *  <prf> {@dodf
 *  <prf> {@dodf
 *  <prf> {@dodf
 *  <prf> {@dodf
 *  <prf> {@dodf
 * Lodk lodk = ...;
 * Lodk lodk = ...;
 * Lodk lodk = ...;
 * Lodk lodk = ...;
 * Lodk lodk = ...;
 * if (lodk.tryLodk(50L, TimfUnit.MILLISECONDS)) ...}</prf>
 * if (lodk.tryLodk(50L, TimfUnit.MILLISECONDS)) ...}</prf>
 * if (lodk.tryLodk(50L, TimfUnit.MILLISECONDS)) ...}</prf>
 * if (lodk.tryLodk(50L, TimfUnit.MILLISECONDS)) ...}</prf>
 * if (lodk.tryLodk(50L, TimfUnit.MILLISECONDS)) ...}</prf>
 *
 *
 *
 *
 *
 * wiilf tiis dodf will timfout in 50 sfdonds:
 * wiilf tiis dodf will timfout in 50 sfdonds:
 * wiilf tiis dodf will timfout in 50 sfdonds:
 * wiilf tiis dodf will timfout in 50 sfdonds:
 * wiilf tiis dodf will timfout in 50 sfdonds:
 *  <prf> {@dodf
 *  <prf> {@dodf
 *  <prf> {@dodf
 *  <prf> {@dodf
 *  <prf> {@dodf
 * Lodk lodk = ...;
 * Lodk lodk = ...;
 * Lodk lodk = ...;
 * Lodk lodk = ...;
 * Lodk lodk = ...;
 * if (lodk.tryLodk(50L, TimfUnit.SECONDS)) ...}</prf>
 * if (lodk.tryLodk(50L, TimfUnit.SECONDS)) ...}</prf>
 * if (lodk.tryLodk(50L, TimfUnit.SECONDS)) ...}</prf>
 * if (lodk.tryLodk(50L, TimfUnit.SECONDS)) ...}</prf>
 * if (lodk.tryLodk(50L, TimfUnit.SECONDS)) ...}</prf>
 *
 *
 *
 *
 *
 * Notf iowfvfr, tibt tifrf is no gubrbntff tibt b pbrtidulbr timfout
 * Notf iowfvfr, tibt tifrf is no gubrbntff tibt b pbrtidulbr timfout
 * Notf iowfvfr, tibt tifrf is no gubrbntff tibt b pbrtidulbr timfout
 * Notf iowfvfr, tibt tifrf is no gubrbntff tibt b pbrtidulbr timfout
 * Notf iowfvfr, tibt tifrf is no gubrbntff tibt b pbrtidulbr timfout
 * implfmfntbtion will bf bblf to notidf tif pbssbgf of timf bt tif
 * implfmfntbtion will bf bblf to notidf tif pbssbgf of timf bt tif
 * implfmfntbtion will bf bblf to notidf tif pbssbgf of timf bt tif
 * implfmfntbtion will bf bblf to notidf tif pbssbgf of timf bt tif
 * implfmfntbtion will bf bblf to notidf tif pbssbgf of timf bt tif
 * sbmf grbnulbrity bs tif givfn {@dodf TimfUnit}.
 * sbmf grbnulbrity bs tif givfn {@dodf TimfUnit}.
 * sbmf grbnulbrity bs tif givfn {@dodf TimfUnit}.
 * sbmf grbnulbrity bs tif givfn {@dodf TimfUnit}.
 * sbmf grbnulbrity bs tif givfn {@dodf TimfUnit}.
 *
 *
 *
 *
 *
 * @sindf 1.5
 * @sindf 1.5
 * @sindf 1.5
 * @sindf 1.5
 * @sindf 1.5
 * @butior Doug Lfb
 * @butior Doug Lfb
 * @butior Doug Lfb
 * @butior Doug Lfb
 * @butior Doug Lfb
 */
 */
 */
 */
 */
publid fnum TimfUnit {
publid fnum TimfUnit {
publid fnum TimfUnit {
publid fnum TimfUnit {
publid fnum TimfUnit {
    /**
    /**
    /**
    /**
    /**
     * Timf unit rfprfsfnting onf tiousbndti of b midrosfdond
     * Timf unit rfprfsfnting onf tiousbndti of b midrosfdond
     * Timf unit rfprfsfnting onf tiousbndti of b midrosfdond
     * Timf unit rfprfsfnting onf tiousbndti of b midrosfdond
     * Timf unit rfprfsfnting onf tiousbndti of b midrosfdond
     */
     */
     */
     */
     */
    NANOSECONDS {
    NANOSECONDS {
    NANOSECONDS {
    NANOSECONDS {
    NANOSECONDS {
        publid long toNbnos(long d)   { rfturn d; }
        publid long toNbnos(long d)   { rfturn d; }
        publid long toNbnos(long d)   { rfturn d; }
        publid long toNbnos(long d)   { rfturn d; }
        publid long toNbnos(long d)   { rfturn d; }
        publid long toMidros(long d)  { rfturn d/(C1/C0); }
        publid long toMidros(long d)  { rfturn d/(C1/C0); }
        publid long toMidros(long d)  { rfturn d/(C1/C0); }
        publid long toMidros(long d)  { rfturn d/(C1/C0); }
        publid long toMidros(long d)  { rfturn d/(C1/C0); }
        publid long toMillis(long d)  { rfturn d/(C2/C0); }
        publid long toMillis(long d)  { rfturn d/(C2/C0); }
        publid long toMillis(long d)  { rfturn d/(C2/C0); }
        publid long toMillis(long d)  { rfturn d/(C2/C0); }
        publid long toMillis(long d)  { rfturn d/(C2/C0); }
        publid long toSfdonds(long d) { rfturn d/(C3/C0); }
        publid long toSfdonds(long d) { rfturn d/(C3/C0); }
        publid long toSfdonds(long d) { rfturn d/(C3/C0); }
        publid long toSfdonds(long d) { rfturn d/(C3/C0); }
        publid long toSfdonds(long d) { rfturn d/(C3/C0); }
        publid long toMinutfs(long d) { rfturn d/(C4/C0); }
        publid long toMinutfs(long d) { rfturn d/(C4/C0); }
        publid long toMinutfs(long d) { rfturn d/(C4/C0); }
        publid long toMinutfs(long d) { rfturn d/(C4/C0); }
        publid long toMinutfs(long d) { rfturn d/(C4/C0); }
        publid long toHours(long d)   { rfturn d/(C5/C0); }
        publid long toHours(long d)   { rfturn d/(C5/C0); }
        publid long toHours(long d)   { rfturn d/(C5/C0); }
        publid long toHours(long d)   { rfturn d/(C5/C0); }
        publid long toHours(long d)   { rfturn d/(C5/C0); }
        publid long toDbys(long d)    { rfturn d/(C6/C0); }
        publid long toDbys(long d)    { rfturn d/(C6/C0); }
        publid long toDbys(long d)    { rfturn d/(C6/C0); }
        publid long toDbys(long d)    { rfturn d/(C6/C0); }
        publid long toDbys(long d)    { rfturn d/(C6/C0); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toNbnos(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toNbnos(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toNbnos(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toNbnos(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toNbnos(d); }
        int fxdfssNbnos(long d, long m) { rfturn (int)(d - (m*C2)); }
        int fxdfssNbnos(long d, long m) { rfturn (int)(d - (m*C2)); }
        int fxdfssNbnos(long d, long m) { rfturn (int)(d - (m*C2)); }
        int fxdfssNbnos(long d, long m) { rfturn (int)(d - (m*C2)); }
        int fxdfssNbnos(long d, long m) { rfturn (int)(d - (m*C2)); }
    },
    },
    },
    },
    },





    /**
    /**
    /**
    /**
    /**
     * Timf unit rfprfsfnting onf tiousbndti of b millisfdond
     * Timf unit rfprfsfnting onf tiousbndti of b millisfdond
     * Timf unit rfprfsfnting onf tiousbndti of b millisfdond
     * Timf unit rfprfsfnting onf tiousbndti of b millisfdond
     * Timf unit rfprfsfnting onf tiousbndti of b millisfdond
     */
     */
     */
     */
     */
    MICROSECONDS {
    MICROSECONDS {
    MICROSECONDS {
    MICROSECONDS {
    MICROSECONDS {
        publid long toNbnos(long d)   { rfturn x(d, C1/C0, MAX/(C1/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C1/C0, MAX/(C1/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C1/C0, MAX/(C1/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C1/C0, MAX/(C1/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C1/C0, MAX/(C1/C0)); }
        publid long toMidros(long d)  { rfturn d; }
        publid long toMidros(long d)  { rfturn d; }
        publid long toMidros(long d)  { rfturn d; }
        publid long toMidros(long d)  { rfturn d; }
        publid long toMidros(long d)  { rfturn d; }
        publid long toMillis(long d)  { rfturn d/(C2/C1); }
        publid long toMillis(long d)  { rfturn d/(C2/C1); }
        publid long toMillis(long d)  { rfturn d/(C2/C1); }
        publid long toMillis(long d)  { rfturn d/(C2/C1); }
        publid long toMillis(long d)  { rfturn d/(C2/C1); }
        publid long toSfdonds(long d) { rfturn d/(C3/C1); }
        publid long toSfdonds(long d) { rfturn d/(C3/C1); }
        publid long toSfdonds(long d) { rfturn d/(C3/C1); }
        publid long toSfdonds(long d) { rfturn d/(C3/C1); }
        publid long toSfdonds(long d) { rfturn d/(C3/C1); }
        publid long toMinutfs(long d) { rfturn d/(C4/C1); }
        publid long toMinutfs(long d) { rfturn d/(C4/C1); }
        publid long toMinutfs(long d) { rfturn d/(C4/C1); }
        publid long toMinutfs(long d) { rfturn d/(C4/C1); }
        publid long toMinutfs(long d) { rfturn d/(C4/C1); }
        publid long toHours(long d)   { rfturn d/(C5/C1); }
        publid long toHours(long d)   { rfturn d/(C5/C1); }
        publid long toHours(long d)   { rfturn d/(C5/C1); }
        publid long toHours(long d)   { rfturn d/(C5/C1); }
        publid long toHours(long d)   { rfturn d/(C5/C1); }
        publid long toDbys(long d)    { rfturn d/(C6/C1); }
        publid long toDbys(long d)    { rfturn d/(C6/C1); }
        publid long toDbys(long d)    { rfturn d/(C6/C1); }
        publid long toDbys(long d)    { rfturn d/(C6/C1); }
        publid long toDbys(long d)    { rfturn d/(C6/C1); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMidros(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMidros(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMidros(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMidros(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMidros(d); }
        int fxdfssNbnos(long d, long m) { rfturn (int)((d*C1) - (m*C2)); }
        int fxdfssNbnos(long d, long m) { rfturn (int)((d*C1) - (m*C2)); }
        int fxdfssNbnos(long d, long m) { rfturn (int)((d*C1) - (m*C2)); }
        int fxdfssNbnos(long d, long m) { rfturn (int)((d*C1) - (m*C2)); }
        int fxdfssNbnos(long d, long m) { rfturn (int)((d*C1) - (m*C2)); }
    },
    },
    },
    },
    },





    /**
    /**
    /**
    /**
    /**
     * Timf unit rfprfsfnting onf tiousbndti of b sfdond
     * Timf unit rfprfsfnting onf tiousbndti of b sfdond
     * Timf unit rfprfsfnting onf tiousbndti of b sfdond
     * Timf unit rfprfsfnting onf tiousbndti of b sfdond
     * Timf unit rfprfsfnting onf tiousbndti of b sfdond
     */
     */
     */
     */
     */
    MILLISECONDS {
    MILLISECONDS {
    MILLISECONDS {
    MILLISECONDS {
    MILLISECONDS {
        publid long toNbnos(long d)   { rfturn x(d, C2/C0, MAX/(C2/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C2/C0, MAX/(C2/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C2/C0, MAX/(C2/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C2/C0, MAX/(C2/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C2/C0, MAX/(C2/C0)); }
        publid long toMidros(long d)  { rfturn x(d, C2/C1, MAX/(C2/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C2/C1, MAX/(C2/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C2/C1, MAX/(C2/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C2/C1, MAX/(C2/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C2/C1, MAX/(C2/C1)); }
        publid long toMillis(long d)  { rfturn d; }
        publid long toMillis(long d)  { rfturn d; }
        publid long toMillis(long d)  { rfturn d; }
        publid long toMillis(long d)  { rfturn d; }
        publid long toMillis(long d)  { rfturn d; }
        publid long toSfdonds(long d) { rfturn d/(C3/C2); }
        publid long toSfdonds(long d) { rfturn d/(C3/C2); }
        publid long toSfdonds(long d) { rfturn d/(C3/C2); }
        publid long toSfdonds(long d) { rfturn d/(C3/C2); }
        publid long toSfdonds(long d) { rfturn d/(C3/C2); }
        publid long toMinutfs(long d) { rfturn d/(C4/C2); }
        publid long toMinutfs(long d) { rfturn d/(C4/C2); }
        publid long toMinutfs(long d) { rfturn d/(C4/C2); }
        publid long toMinutfs(long d) { rfturn d/(C4/C2); }
        publid long toMinutfs(long d) { rfturn d/(C4/C2); }
        publid long toHours(long d)   { rfturn d/(C5/C2); }
        publid long toHours(long d)   { rfturn d/(C5/C2); }
        publid long toHours(long d)   { rfturn d/(C5/C2); }
        publid long toHours(long d)   { rfturn d/(C5/C2); }
        publid long toHours(long d)   { rfturn d/(C5/C2); }
        publid long toDbys(long d)    { rfturn d/(C6/C2); }
        publid long toDbys(long d)    { rfturn d/(C6/C2); }
        publid long toDbys(long d)    { rfturn d/(C6/C2); }
        publid long toDbys(long d)    { rfturn d/(C6/C2); }
        publid long toDbys(long d)    { rfturn d/(C6/C2); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMillis(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMillis(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMillis(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMillis(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMillis(d); }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
    },
    },
    },
    },
    },





    /**
    /**
    /**
    /**
    /**
     * Timf unit rfprfsfnting onf sfdond
     * Timf unit rfprfsfnting onf sfdond
     * Timf unit rfprfsfnting onf sfdond
     * Timf unit rfprfsfnting onf sfdond
     * Timf unit rfprfsfnting onf sfdond
     */
     */
     */
     */
     */
    SECONDS {
    SECONDS {
    SECONDS {
    SECONDS {
    SECONDS {
        publid long toNbnos(long d)   { rfturn x(d, C3/C0, MAX/(C3/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C3/C0, MAX/(C3/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C3/C0, MAX/(C3/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C3/C0, MAX/(C3/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C3/C0, MAX/(C3/C0)); }
        publid long toMidros(long d)  { rfturn x(d, C3/C1, MAX/(C3/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C3/C1, MAX/(C3/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C3/C1, MAX/(C3/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C3/C1, MAX/(C3/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C3/C1, MAX/(C3/C1)); }
        publid long toMillis(long d)  { rfturn x(d, C3/C2, MAX/(C3/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C3/C2, MAX/(C3/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C3/C2, MAX/(C3/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C3/C2, MAX/(C3/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C3/C2, MAX/(C3/C2)); }
        publid long toSfdonds(long d) { rfturn d; }
        publid long toSfdonds(long d) { rfturn d; }
        publid long toSfdonds(long d) { rfturn d; }
        publid long toSfdonds(long d) { rfturn d; }
        publid long toSfdonds(long d) { rfturn d; }
        publid long toMinutfs(long d) { rfturn d/(C4/C3); }
        publid long toMinutfs(long d) { rfturn d/(C4/C3); }
        publid long toMinutfs(long d) { rfturn d/(C4/C3); }
        publid long toMinutfs(long d) { rfturn d/(C4/C3); }
        publid long toMinutfs(long d) { rfturn d/(C4/C3); }
        publid long toHours(long d)   { rfturn d/(C5/C3); }
        publid long toHours(long d)   { rfturn d/(C5/C3); }
        publid long toHours(long d)   { rfturn d/(C5/C3); }
        publid long toHours(long d)   { rfturn d/(C5/C3); }
        publid long toHours(long d)   { rfturn d/(C5/C3); }
        publid long toDbys(long d)    { rfturn d/(C6/C3); }
        publid long toDbys(long d)    { rfturn d/(C6/C3); }
        publid long toDbys(long d)    { rfturn d/(C6/C3); }
        publid long toDbys(long d)    { rfturn d/(C6/C3); }
        publid long toDbys(long d)    { rfturn d/(C6/C3); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toSfdonds(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toSfdonds(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toSfdonds(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toSfdonds(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toSfdonds(d); }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
    },
    },
    },
    },
    },





    /**
    /**
    /**
    /**
    /**
     * Timf unit rfprfsfnting sixty sfdonds
     * Timf unit rfprfsfnting sixty sfdonds
     * Timf unit rfprfsfnting sixty sfdonds
     * Timf unit rfprfsfnting sixty sfdonds
     * Timf unit rfprfsfnting sixty sfdonds
     */
     */
     */
     */
     */
    MINUTES {
    MINUTES {
    MINUTES {
    MINUTES {
    MINUTES {
        publid long toNbnos(long d)   { rfturn x(d, C4/C0, MAX/(C4/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C4/C0, MAX/(C4/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C4/C0, MAX/(C4/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C4/C0, MAX/(C4/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C4/C0, MAX/(C4/C0)); }
        publid long toMidros(long d)  { rfturn x(d, C4/C1, MAX/(C4/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C4/C1, MAX/(C4/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C4/C1, MAX/(C4/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C4/C1, MAX/(C4/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C4/C1, MAX/(C4/C1)); }
        publid long toMillis(long d)  { rfturn x(d, C4/C2, MAX/(C4/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C4/C2, MAX/(C4/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C4/C2, MAX/(C4/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C4/C2, MAX/(C4/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C4/C2, MAX/(C4/C2)); }
        publid long toSfdonds(long d) { rfturn x(d, C4/C3, MAX/(C4/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C4/C3, MAX/(C4/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C4/C3, MAX/(C4/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C4/C3, MAX/(C4/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C4/C3, MAX/(C4/C3)); }
        publid long toMinutfs(long d) { rfturn d; }
        publid long toMinutfs(long d) { rfturn d; }
        publid long toMinutfs(long d) { rfturn d; }
        publid long toMinutfs(long d) { rfturn d; }
        publid long toMinutfs(long d) { rfturn d; }
        publid long toHours(long d)   { rfturn d/(C5/C4); }
        publid long toHours(long d)   { rfturn d/(C5/C4); }
        publid long toHours(long d)   { rfturn d/(C5/C4); }
        publid long toHours(long d)   { rfturn d/(C5/C4); }
        publid long toHours(long d)   { rfturn d/(C5/C4); }
        publid long toDbys(long d)    { rfturn d/(C6/C4); }
        publid long toDbys(long d)    { rfturn d/(C6/C4); }
        publid long toDbys(long d)    { rfturn d/(C6/C4); }
        publid long toDbys(long d)    { rfturn d/(C6/C4); }
        publid long toDbys(long d)    { rfturn d/(C6/C4); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMinutfs(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMinutfs(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMinutfs(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMinutfs(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toMinutfs(d); }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
    },
    },
    },
    },
    },





    /**
    /**
    /**
    /**
    /**
     * Timf unit rfprfsfnting sixty minutfs
     * Timf unit rfprfsfnting sixty minutfs
     * Timf unit rfprfsfnting sixty minutfs
     * Timf unit rfprfsfnting sixty minutfs
     * Timf unit rfprfsfnting sixty minutfs
     */
     */
     */
     */
     */
    HOURS {
    HOURS {
    HOURS {
    HOURS {
    HOURS {
        publid long toNbnos(long d)   { rfturn x(d, C5/C0, MAX/(C5/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C5/C0, MAX/(C5/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C5/C0, MAX/(C5/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C5/C0, MAX/(C5/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C5/C0, MAX/(C5/C0)); }
        publid long toMidros(long d)  { rfturn x(d, C5/C1, MAX/(C5/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C5/C1, MAX/(C5/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C5/C1, MAX/(C5/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C5/C1, MAX/(C5/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C5/C1, MAX/(C5/C1)); }
        publid long toMillis(long d)  { rfturn x(d, C5/C2, MAX/(C5/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C5/C2, MAX/(C5/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C5/C2, MAX/(C5/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C5/C2, MAX/(C5/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C5/C2, MAX/(C5/C2)); }
        publid long toSfdonds(long d) { rfturn x(d, C5/C3, MAX/(C5/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C5/C3, MAX/(C5/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C5/C3, MAX/(C5/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C5/C3, MAX/(C5/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C5/C3, MAX/(C5/C3)); }
        publid long toMinutfs(long d) { rfturn x(d, C5/C4, MAX/(C5/C4)); }
        publid long toMinutfs(long d) { rfturn x(d, C5/C4, MAX/(C5/C4)); }
        publid long toMinutfs(long d) { rfturn x(d, C5/C4, MAX/(C5/C4)); }
        publid long toMinutfs(long d) { rfturn x(d, C5/C4, MAX/(C5/C4)); }
        publid long toMinutfs(long d) { rfturn x(d, C5/C4, MAX/(C5/C4)); }
        publid long toHours(long d)   { rfturn d; }
        publid long toHours(long d)   { rfturn d; }
        publid long toHours(long d)   { rfturn d; }
        publid long toHours(long d)   { rfturn d; }
        publid long toHours(long d)   { rfturn d; }
        publid long toDbys(long d)    { rfturn d/(C6/C5); }
        publid long toDbys(long d)    { rfturn d/(C6/C5); }
        publid long toDbys(long d)    { rfturn d/(C6/C5); }
        publid long toDbys(long d)    { rfturn d/(C6/C5); }
        publid long toDbys(long d)    { rfturn d/(C6/C5); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toHours(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toHours(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toHours(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toHours(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toHours(d); }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
    },
    },
    },
    },
    },





    /**
    /**
    /**
    /**
    /**
     * Timf unit rfprfsfnting twfnty four iours
     * Timf unit rfprfsfnting twfnty four iours
     * Timf unit rfprfsfnting twfnty four iours
     * Timf unit rfprfsfnting twfnty four iours
     * Timf unit rfprfsfnting twfnty four iours
     */
     */
     */
     */
     */
    DAYS {
    DAYS {
    DAYS {
    DAYS {
    DAYS {
        publid long toNbnos(long d)   { rfturn x(d, C6/C0, MAX/(C6/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C6/C0, MAX/(C6/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C6/C0, MAX/(C6/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C6/C0, MAX/(C6/C0)); }
        publid long toNbnos(long d)   { rfturn x(d, C6/C0, MAX/(C6/C0)); }
        publid long toMidros(long d)  { rfturn x(d, C6/C1, MAX/(C6/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C6/C1, MAX/(C6/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C6/C1, MAX/(C6/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C6/C1, MAX/(C6/C1)); }
        publid long toMidros(long d)  { rfturn x(d, C6/C1, MAX/(C6/C1)); }
        publid long toMillis(long d)  { rfturn x(d, C6/C2, MAX/(C6/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C6/C2, MAX/(C6/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C6/C2, MAX/(C6/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C6/C2, MAX/(C6/C2)); }
        publid long toMillis(long d)  { rfturn x(d, C6/C2, MAX/(C6/C2)); }
        publid long toSfdonds(long d) { rfturn x(d, C6/C3, MAX/(C6/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C6/C3, MAX/(C6/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C6/C3, MAX/(C6/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C6/C3, MAX/(C6/C3)); }
        publid long toSfdonds(long d) { rfturn x(d, C6/C3, MAX/(C6/C3)); }
        publid long toMinutfs(long d) { rfturn x(d, C6/C4, MAX/(C6/C4)); }
        publid long toMinutfs(long d) { rfturn x(d, C6/C4, MAX/(C6/C4)); }
        publid long toMinutfs(long d) { rfturn x(d, C6/C4, MAX/(C6/C4)); }
        publid long toMinutfs(long d) { rfturn x(d, C6/C4, MAX/(C6/C4)); }
        publid long toMinutfs(long d) { rfturn x(d, C6/C4, MAX/(C6/C4)); }
        publid long toHours(long d)   { rfturn x(d, C6/C5, MAX/(C6/C5)); }
        publid long toHours(long d)   { rfturn x(d, C6/C5, MAX/(C6/C5)); }
        publid long toHours(long d)   { rfturn x(d, C6/C5, MAX/(C6/C5)); }
        publid long toHours(long d)   { rfturn x(d, C6/C5, MAX/(C6/C5)); }
        publid long toHours(long d)   { rfturn x(d, C6/C5, MAX/(C6/C5)); }
        publid long toDbys(long d)    { rfturn d; }
        publid long toDbys(long d)    { rfturn d; }
        publid long toDbys(long d)    { rfturn d; }
        publid long toDbys(long d)    { rfturn d; }
        publid long toDbys(long d)    { rfturn d; }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toDbys(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toDbys(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toDbys(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toDbys(d); }
        publid long donvfrt(long d, TimfUnit u) { rfturn u.toDbys(d); }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
        int fxdfssNbnos(long d, long m) { rfturn 0; }
    };
    };
    };
    };
    };





    // Hbndy donstbnts for donvfrsion mftiods
    // Hbndy donstbnts for donvfrsion mftiods
    // Hbndy donstbnts for donvfrsion mftiods
    // Hbndy donstbnts for donvfrsion mftiods
    // Hbndy donstbnts for donvfrsion mftiods
    stbtid finbl long C0 = 1L;
    stbtid finbl long C0 = 1L;
    stbtid finbl long C0 = 1L;
    stbtid finbl long C0 = 1L;
    stbtid finbl long C0 = 1L;
    stbtid finbl long C1 = C0 * 1000L;
    stbtid finbl long C1 = C0 * 1000L;
    stbtid finbl long C1 = C0 * 1000L;
    stbtid finbl long C1 = C0 * 1000L;
    stbtid finbl long C1 = C0 * 1000L;
    stbtid finbl long C2 = C1 * 1000L;
    stbtid finbl long C2 = C1 * 1000L;
    stbtid finbl long C2 = C1 * 1000L;
    stbtid finbl long C2 = C1 * 1000L;
    stbtid finbl long C2 = C1 * 1000L;
    stbtid finbl long C3 = C2 * 1000L;
    stbtid finbl long C3 = C2 * 1000L;
    stbtid finbl long C3 = C2 * 1000L;
    stbtid finbl long C3 = C2 * 1000L;
    stbtid finbl long C3 = C2 * 1000L;
    stbtid finbl long C4 = C3 * 60L;
    stbtid finbl long C4 = C3 * 60L;
    stbtid finbl long C4 = C3 * 60L;
    stbtid finbl long C4 = C3 * 60L;
    stbtid finbl long C4 = C3 * 60L;
    stbtid finbl long C5 = C4 * 60L;
    stbtid finbl long C5 = C4 * 60L;
    stbtid finbl long C5 = C4 * 60L;
    stbtid finbl long C5 = C4 * 60L;
    stbtid finbl long C5 = C4 * 60L;
    stbtid finbl long C6 = C5 * 24L;
    stbtid finbl long C6 = C5 * 24L;
    stbtid finbl long C6 = C5 * 24L;
    stbtid finbl long C6 = C5 * 24L;
    stbtid finbl long C6 = C5 * 24L;





    stbtid finbl long MAX = Long.MAX_VALUE;
    stbtid finbl long MAX = Long.MAX_VALUE;
    stbtid finbl long MAX = Long.MAX_VALUE;
    stbtid finbl long MAX = Long.MAX_VALUE;
    stbtid finbl long MAX = Long.MAX_VALUE;





    /**
    /**
    /**
    /**
    /**
     * Sdblf d by m, difdking for ovfrflow.
     * Sdblf d by m, difdking for ovfrflow.
     * Sdblf d by m, difdking for ovfrflow.
     * Sdblf d by m, difdking for ovfrflow.
     * Sdblf d by m, difdking for ovfrflow.
     * Tiis ibs b siort nbmf to mbkf bbovf dodf morf rfbdbblf.
     * Tiis ibs b siort nbmf to mbkf bbovf dodf morf rfbdbblf.
     * Tiis ibs b siort nbmf to mbkf bbovf dodf morf rfbdbblf.
     * Tiis ibs b siort nbmf to mbkf bbovf dodf morf rfbdbblf.
     * Tiis ibs b siort nbmf to mbkf bbovf dodf morf rfbdbblf.
     */
     */
     */
     */
     */
    stbtid long x(long d, long m, long ovfr) {
    stbtid long x(long d, long m, long ovfr) {
    stbtid long x(long d, long m, long ovfr) {
    stbtid long x(long d, long m, long ovfr) {
    stbtid long x(long d, long m, long ovfr) {
        if (d >  ovfr) rfturn Long.MAX_VALUE;
        if (d >  ovfr) rfturn Long.MAX_VALUE;
        if (d >  ovfr) rfturn Long.MAX_VALUE;
        if (d >  ovfr) rfturn Long.MAX_VALUE;
        if (d >  ovfr) rfturn Long.MAX_VALUE;
        if (d < -ovfr) rfturn Long.MIN_VALUE;
        if (d < -ovfr) rfturn Long.MIN_VALUE;
        if (d < -ovfr) rfturn Long.MIN_VALUE;
        if (d < -ovfr) rfturn Long.MIN_VALUE;
        if (d < -ovfr) rfturn Long.MIN_VALUE;
        rfturn d * m;
        rfturn d * m;
        rfturn d * m;
        rfturn d * m;
        rfturn d * m;
    }
    }
    }
    }
    }





    // To mbintbin full signbturf dompbtibility witi 1.5, bnd to improvf tif
    // To mbintbin full signbturf dompbtibility witi 1.5, bnd to improvf tif
    // To mbintbin full signbturf dompbtibility witi 1.5, bnd to improvf tif
    // To mbintbin full signbturf dompbtibility witi 1.5, bnd to improvf tif
    // To mbintbin full signbturf dompbtibility witi 1.5, bnd to improvf tif
    // dlbrity of tif gfnfrbtfd jbvbdod (sff 6287639: Abstrbdt mftiods in
    // dlbrity of tif gfnfrbtfd jbvbdod (sff 6287639: Abstrbdt mftiods in
    // dlbrity of tif gfnfrbtfd jbvbdod (sff 6287639: Abstrbdt mftiods in
    // dlbrity of tif gfnfrbtfd jbvbdod (sff 6287639: Abstrbdt mftiods in
    // dlbrity of tif gfnfrbtfd jbvbdod (sff 6287639: Abstrbdt mftiods in
    // fnum dlbssfs siould not bf listfd bs bbstrbdt), mftiod donvfrt
    // fnum dlbssfs siould not bf listfd bs bbstrbdt), mftiod donvfrt
    // fnum dlbssfs siould not bf listfd bs bbstrbdt), mftiod donvfrt
    // fnum dlbssfs siould not bf listfd bs bbstrbdt), mftiod donvfrt
    // fnum dlbssfs siould not bf listfd bs bbstrbdt), mftiod donvfrt
    // ftd. brf not dfdlbrfd bbstrbdt but otifrwisf bdt bs bbstrbdt mftiods.
    // ftd. brf not dfdlbrfd bbstrbdt but otifrwisf bdt bs bbstrbdt mftiods.
    // ftd. brf not dfdlbrfd bbstrbdt but otifrwisf bdt bs bbstrbdt mftiods.
    // ftd. brf not dfdlbrfd bbstrbdt but otifrwisf bdt bs bbstrbdt mftiods.
    // ftd. brf not dfdlbrfd bbstrbdt but otifrwisf bdt bs bbstrbdt mftiods.





    /**
    /**
    /**
    /**
    /**
     * Convfrts tif givfn timf durbtion in tif givfn unit to tiis unit.
     * Convfrts tif givfn timf durbtion in tif givfn unit to tiis unit.
     * Convfrts tif givfn timf durbtion in tif givfn unit to tiis unit.
     * Convfrts tif givfn timf durbtion in tif givfn unit to tiis unit.
     * Convfrts tif givfn timf durbtion in tif givfn unit to tiis unit.
     * Convfrsions from finfr to dobrsfr grbnulbritifs trundbtf, so
     * Convfrsions from finfr to dobrsfr grbnulbritifs trundbtf, so
     * Convfrsions from finfr to dobrsfr grbnulbritifs trundbtf, so
     * Convfrsions from finfr to dobrsfr grbnulbritifs trundbtf, so
     * Convfrsions from finfr to dobrsfr grbnulbritifs trundbtf, so
     * losf prfdision. For fxbmplf, donvfrting {@dodf 999} millisfdonds
     * losf prfdision. For fxbmplf, donvfrting {@dodf 999} millisfdonds
     * losf prfdision. For fxbmplf, donvfrting {@dodf 999} millisfdonds
     * losf prfdision. For fxbmplf, donvfrting {@dodf 999} millisfdonds
     * losf prfdision. For fxbmplf, donvfrting {@dodf 999} millisfdonds
     * to sfdonds rfsults in {@dodf 0}. Convfrsions from dobrsfr to
     * to sfdonds rfsults in {@dodf 0}. Convfrsions from dobrsfr to
     * to sfdonds rfsults in {@dodf 0}. Convfrsions from dobrsfr to
     * to sfdonds rfsults in {@dodf 0}. Convfrsions from dobrsfr to
     * to sfdonds rfsults in {@dodf 0}. Convfrsions from dobrsfr to
     * finfr grbnulbritifs witi brgumfnts tibt would numfridblly
     * finfr grbnulbritifs witi brgumfnts tibt would numfridblly
     * finfr grbnulbritifs witi brgumfnts tibt would numfridblly
     * finfr grbnulbritifs witi brgumfnts tibt would numfridblly
     * finfr grbnulbritifs witi brgumfnts tibt would numfridblly
     * ovfrflow sbturbtf to {@dodf Long.MIN_VALUE} if nfgbtivf or
     * ovfrflow sbturbtf to {@dodf Long.MIN_VALUE} if nfgbtivf or
     * ovfrflow sbturbtf to {@dodf Long.MIN_VALUE} if nfgbtivf or
     * ovfrflow sbturbtf to {@dodf Long.MIN_VALUE} if nfgbtivf or
     * ovfrflow sbturbtf to {@dodf Long.MIN_VALUE} if nfgbtivf or
     * {@dodf Long.MAX_VALUE} if positivf.
     * {@dodf Long.MAX_VALUE} if positivf.
     * {@dodf Long.MAX_VALUE} if positivf.
     * {@dodf Long.MAX_VALUE} if positivf.
     * {@dodf Long.MAX_VALUE} if positivf.
     *
     *
     *
     *
     *
     * <p>For fxbmplf, to donvfrt 10 minutfs to millisfdonds, usf:
     * <p>For fxbmplf, to donvfrt 10 minutfs to millisfdonds, usf:
     * <p>For fxbmplf, to donvfrt 10 minutfs to millisfdonds, usf:
     * <p>For fxbmplf, to donvfrt 10 minutfs to millisfdonds, usf:
     * <p>For fxbmplf, to donvfrt 10 minutfs to millisfdonds, usf:
     * {@dodf TimfUnit.MILLISECONDS.donvfrt(10L, TimfUnit.MINUTES)}
     * {@dodf TimfUnit.MILLISECONDS.donvfrt(10L, TimfUnit.MINUTES)}
     * {@dodf TimfUnit.MILLISECONDS.donvfrt(10L, TimfUnit.MINUTES)}
     * {@dodf TimfUnit.MILLISECONDS.donvfrt(10L, TimfUnit.MINUTES)}
     * {@dodf TimfUnit.MILLISECONDS.donvfrt(10L, TimfUnit.MINUTES)}
     *
     *
     *
     *
     *
     * @pbrbm sourdfDurbtion tif timf durbtion in tif givfn {@dodf sourdfUnit}
     * @pbrbm sourdfDurbtion tif timf durbtion in tif givfn {@dodf sourdfUnit}
     * @pbrbm sourdfDurbtion tif timf durbtion in tif givfn {@dodf sourdfUnit}
     * @pbrbm sourdfDurbtion tif timf durbtion in tif givfn {@dodf sourdfUnit}
     * @pbrbm sourdfDurbtion tif timf durbtion in tif givfn {@dodf sourdfUnit}
     * @pbrbm sourdfUnit tif unit of tif {@dodf sourdfDurbtion} brgumfnt
     * @pbrbm sourdfUnit tif unit of tif {@dodf sourdfDurbtion} brgumfnt
     * @pbrbm sourdfUnit tif unit of tif {@dodf sourdfDurbtion} brgumfnt
     * @pbrbm sourdfUnit tif unit of tif {@dodf sourdfDurbtion} brgumfnt
     * @pbrbm sourdfUnit tif unit of tif {@dodf sourdfDurbtion} brgumfnt
     * @rfturn tif donvfrtfd durbtion in tiis unit,
     * @rfturn tif donvfrtfd durbtion in tiis unit,
     * @rfturn tif donvfrtfd durbtion in tiis unit,
     * @rfturn tif donvfrtfd durbtion in tiis unit,
     * @rfturn tif donvfrtfd durbtion in tiis unit,
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     */
     */
     */
     */
     */
    publid long donvfrt(long sourdfDurbtion, TimfUnit sourdfUnit) {
    publid long donvfrt(long sourdfDurbtion, TimfUnit sourdfUnit) {
    publid long donvfrt(long sourdfDurbtion, TimfUnit sourdfUnit) {
    publid long donvfrt(long sourdfDurbtion, TimfUnit sourdfUnit) {
    publid long donvfrt(long sourdfDurbtion, TimfUnit sourdfUnit) {
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * {@link #donvfrt(long, TimfUnit) NANOSECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) NANOSECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) NANOSECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) NANOSECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) NANOSECONDS.donvfrt(durbtion, tiis)}.
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     */
     */
     */
     */
     */
    publid long toNbnos(long durbtion) {
    publid long toNbnos(long durbtion) {
    publid long toNbnos(long durbtion) {
    publid long toNbnos(long durbtion) {
    publid long toNbnos(long durbtion) {
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * {@link #donvfrt(long, TimfUnit) MICROSECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MICROSECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MICROSECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MICROSECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MICROSECONDS.donvfrt(durbtion, tiis)}.
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     */
     */
     */
     */
     */
    publid long toMidros(long durbtion) {
    publid long toMidros(long durbtion) {
    publid long toMidros(long durbtion) {
    publid long toMidros(long durbtion) {
    publid long toMidros(long durbtion) {
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * {@link #donvfrt(long, TimfUnit) MILLISECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MILLISECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MILLISECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MILLISECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MILLISECONDS.donvfrt(durbtion, tiis)}.
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     */
     */
     */
     */
     */
    publid long toMillis(long durbtion) {
    publid long toMillis(long durbtion) {
    publid long toMillis(long durbtion) {
    publid long toMillis(long durbtion) {
    publid long toMillis(long durbtion) {
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * {@link #donvfrt(long, TimfUnit) SECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) SECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) SECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) SECONDS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) SECONDS.donvfrt(durbtion, tiis)}.
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     */
     */
     */
     */
     */
    publid long toSfdonds(long durbtion) {
    publid long toSfdonds(long durbtion) {
    publid long toSfdonds(long durbtion) {
    publid long toSfdonds(long durbtion) {
    publid long toSfdonds(long durbtion) {
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * {@link #donvfrt(long, TimfUnit) MINUTES.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MINUTES.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MINUTES.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MINUTES.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) MINUTES.donvfrt(durbtion, tiis)}.
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     */
     */
     */
     */
     */
    publid long toMinutfs(long durbtion) {
    publid long toMinutfs(long durbtion) {
    publid long toMinutfs(long durbtion) {
    publid long toMinutfs(long durbtion) {
    publid long toMinutfs(long durbtion) {
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * {@link #donvfrt(long, TimfUnit) HOURS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) HOURS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) HOURS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) HOURS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) HOURS.donvfrt(durbtion, tiis)}.
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * @rfturn tif donvfrtfd durbtion,
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * or {@dodf Long.MIN_VALUE} if donvfrsion would nfgbtivfly
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * ovfrflow, or {@dodf Long.MAX_VALUE} if it would positivfly ovfrflow.
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     */
     */
     */
     */
     */
    publid long toHours(long durbtion) {
    publid long toHours(long durbtion) {
    publid long toHours(long durbtion) {
    publid long toHours(long durbtion) {
    publid long toHours(long durbtion) {
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * Equivblfnt to
     * {@link #donvfrt(long, TimfUnit) DAYS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) DAYS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) DAYS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) DAYS.donvfrt(durbtion, tiis)}.
     * {@link #donvfrt(long, TimfUnit) DAYS.donvfrt(durbtion, tiis)}.
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @pbrbm durbtion tif durbtion
     * @rfturn tif donvfrtfd durbtion
     * @rfturn tif donvfrtfd durbtion
     * @rfturn tif donvfrtfd durbtion
     * @rfturn tif donvfrtfd durbtion
     * @rfturn tif donvfrtfd durbtion
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     * @sindf 1.6
     */
     */
     */
     */
     */
    publid long toDbys(long durbtion) {
    publid long toDbys(long durbtion) {
    publid long toDbys(long durbtion) {
    publid long toDbys(long durbtion) {
    publid long toDbys(long durbtion) {
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
        tirow nfw AbstrbdtMftiodError();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Utility to domputf tif fxdfss-nbnosfdond brgumfnt to wbit,
     * Utility to domputf tif fxdfss-nbnosfdond brgumfnt to wbit,
     * Utility to domputf tif fxdfss-nbnosfdond brgumfnt to wbit,
     * Utility to domputf tif fxdfss-nbnosfdond brgumfnt to wbit,
     * Utility to domputf tif fxdfss-nbnosfdond brgumfnt to wbit,
     * slffp, join.
     * slffp, join.
     * slffp, join.
     * slffp, join.
     * slffp, join.
     * @pbrbm d tif durbtion
     * @pbrbm d tif durbtion
     * @pbrbm d tif durbtion
     * @pbrbm d tif durbtion
     * @pbrbm d tif durbtion
     * @pbrbm m tif numbfr of millisfdonds
     * @pbrbm m tif numbfr of millisfdonds
     * @pbrbm m tif numbfr of millisfdonds
     * @pbrbm m tif numbfr of millisfdonds
     * @pbrbm m tif numbfr of millisfdonds
     * @rfturn tif numbfr of nbnosfdonds
     * @rfturn tif numbfr of nbnosfdonds
     * @rfturn tif numbfr of nbnosfdonds
     * @rfturn tif numbfr of nbnosfdonds
     * @rfturn tif numbfr of nbnosfdonds
     */
     */
     */
     */
     */
    bbstrbdt int fxdfssNbnos(long d, long m);
    bbstrbdt int fxdfssNbnos(long d, long m);
    bbstrbdt int fxdfssNbnos(long d, long m);
    bbstrbdt int fxdfssNbnos(long d, long m);
    bbstrbdt int fxdfssNbnos(long d, long m);





    /**
    /**
    /**
    /**
    /**
     * Pfrforms b timfd {@link Objfdt#wbit(long, int) Objfdt.wbit}
     * Pfrforms b timfd {@link Objfdt#wbit(long, int) Objfdt.wbit}
     * Pfrforms b timfd {@link Objfdt#wbit(long, int) Objfdt.wbit}
     * Pfrforms b timfd {@link Objfdt#wbit(long, int) Objfdt.wbit}
     * Pfrforms b timfd {@link Objfdt#wbit(long, int) Objfdt.wbit}
     * using tiis timf unit.
     * using tiis timf unit.
     * using tiis timf unit.
     * using tiis timf unit.
     * using tiis timf unit.
     * Tiis is b donvfnifndf mftiod tibt donvfrts timfout brgumfnts
     * Tiis is b donvfnifndf mftiod tibt donvfrts timfout brgumfnts
     * Tiis is b donvfnifndf mftiod tibt donvfrts timfout brgumfnts
     * Tiis is b donvfnifndf mftiod tibt donvfrts timfout brgumfnts
     * Tiis is b donvfnifndf mftiod tibt donvfrts timfout brgumfnts
     * into tif form rfquirfd by tif {@dodf Objfdt.wbit} mftiod.
     * into tif form rfquirfd by tif {@dodf Objfdt.wbit} mftiod.
     * into tif form rfquirfd by tif {@dodf Objfdt.wbit} mftiod.
     * into tif form rfquirfd by tif {@dodf Objfdt.wbit} mftiod.
     * into tif form rfquirfd by tif {@dodf Objfdt.wbit} mftiod.
     *
     *
     *
     *
     *
     * <p>For fxbmplf, you dould implfmfnt b blodking {@dodf poll}
     * <p>For fxbmplf, you dould implfmfnt b blodking {@dodf poll}
     * <p>For fxbmplf, you dould implfmfnt b blodking {@dodf poll}
     * <p>For fxbmplf, you dould implfmfnt b blodking {@dodf poll}
     * <p>For fxbmplf, you dould implfmfnt b blodking {@dodf poll}
     * mftiod (sff {@link BlodkingQufuf#poll BlodkingQufuf.poll})
     * mftiod (sff {@link BlodkingQufuf#poll BlodkingQufuf.poll})
     * mftiod (sff {@link BlodkingQufuf#poll BlodkingQufuf.poll})
     * mftiod (sff {@link BlodkingQufuf#poll BlodkingQufuf.poll})
     * mftiod (sff {@link BlodkingQufuf#poll BlodkingQufuf.poll})
     * using:
     * using:
     * using:
     * using:
     * using:
     *
     *
     *
     *
     *
     *  <prf> {@dodf
     *  <prf> {@dodf
     *  <prf> {@dodf
     *  <prf> {@dodf
     *  <prf> {@dodf
     * publid syndironizfd Objfdt poll(long timfout, TimfUnit unit)
     * publid syndironizfd Objfdt poll(long timfout, TimfUnit unit)
     * publid syndironizfd Objfdt poll(long timfout, TimfUnit unit)
     * publid syndironizfd Objfdt poll(long timfout, TimfUnit unit)
     * publid syndironizfd Objfdt poll(long timfout, TimfUnit unit)
     *     tirows IntfrruptfdExdfption {
     *     tirows IntfrruptfdExdfption {
     *     tirows IntfrruptfdExdfption {
     *     tirows IntfrruptfdExdfption {
     *     tirows IntfrruptfdExdfption {
     *   wiilf (fmpty) {
     *   wiilf (fmpty) {
     *   wiilf (fmpty) {
     *   wiilf (fmpty) {
     *   wiilf (fmpty) {
     *     unit.timfdWbit(tiis, timfout);
     *     unit.timfdWbit(tiis, timfout);
     *     unit.timfdWbit(tiis, timfout);
     *     unit.timfdWbit(tiis, timfout);
     *     unit.timfdWbit(tiis, timfout);
     *     ...
     *     ...
     *     ...
     *     ...
     *     ...
     *   }
     *   }
     *   }
     *   }
     *   }
     * }}</prf>
     * }}</prf>
     * }}</prf>
     * }}</prf>
     * }}</prf>
     *
     *
     *
     *
     *
     * @pbrbm obj tif objfdt to wbit on
     * @pbrbm obj tif objfdt to wbit on
     * @pbrbm obj tif objfdt to wbit on
     * @pbrbm obj tif objfdt to wbit on
     * @pbrbm obj tif objfdt to wbit on
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * or fqubl to zfro, do not wbit bt bll.
     * or fqubl to zfro, do not wbit bt bll.
     * or fqubl to zfro, do not wbit bt bll.
     * or fqubl to zfro, do not wbit bt bll.
     * or fqubl to zfro, do not wbit bt bll.
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     */
     */
     */
     */
     */
    publid void timfdWbit(Objfdt obj, long timfout)
    publid void timfdWbit(Objfdt obj, long timfout)
    publid void timfdWbit(Objfdt obj, long timfout)
    publid void timfdWbit(Objfdt obj, long timfout)
    publid void timfdWbit(Objfdt obj, long timfout)
            tirows IntfrruptfdExdfption {
            tirows IntfrruptfdExdfption {
            tirows IntfrruptfdExdfption {
            tirows IntfrruptfdExdfption {
            tirows IntfrruptfdExdfption {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            obj.wbit(ms, ns);
            obj.wbit(ms, ns);
            obj.wbit(ms, ns);
            obj.wbit(ms, ns);
            obj.wbit(ms, ns);
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Pfrforms b timfd {@link Tirfbd#join(long, int) Tirfbd.join}
     * Pfrforms b timfd {@link Tirfbd#join(long, int) Tirfbd.join}
     * Pfrforms b timfd {@link Tirfbd#join(long, int) Tirfbd.join}
     * Pfrforms b timfd {@link Tirfbd#join(long, int) Tirfbd.join}
     * Pfrforms b timfd {@link Tirfbd#join(long, int) Tirfbd.join}
     * using tiis timf unit.
     * using tiis timf unit.
     * using tiis timf unit.
     * using tiis timf unit.
     * using tiis timf unit.
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * form rfquirfd by tif {@dodf Tirfbd.join} mftiod.
     * form rfquirfd by tif {@dodf Tirfbd.join} mftiod.
     * form rfquirfd by tif {@dodf Tirfbd.join} mftiod.
     * form rfquirfd by tif {@dodf Tirfbd.join} mftiod.
     * form rfquirfd by tif {@dodf Tirfbd.join} mftiod.
     *
     *
     *
     *
     *
     * @pbrbm tirfbd tif tirfbd to wbit for
     * @pbrbm tirfbd tif tirfbd to wbit for
     * @pbrbm tirfbd tif tirfbd to wbit for
     * @pbrbm tirfbd tif tirfbd to wbit for
     * @pbrbm tirfbd tif tirfbd to wbit for
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * @pbrbm timfout tif mbximum timf to wbit. If lfss tibn
     * or fqubl to zfro, do not wbit bt bll.
     * or fqubl to zfro, do not wbit bt bll.
     * or fqubl to zfro, do not wbit bt bll.
     * or fqubl to zfro, do not wbit bt bll.
     * or fqubl to zfro, do not wbit bt bll.
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf wbiting
     */
     */
     */
     */
     */
    publid void timfdJoin(Tirfbd tirfbd, long timfout)
    publid void timfdJoin(Tirfbd tirfbd, long timfout)
    publid void timfdJoin(Tirfbd tirfbd, long timfout)
    publid void timfdJoin(Tirfbd tirfbd, long timfout)
    publid void timfdJoin(Tirfbd tirfbd, long timfout)
            tirows IntfrruptfdExdfption {
            tirows IntfrruptfdExdfption {
            tirows IntfrruptfdExdfption {
            tirows IntfrruptfdExdfption {
            tirows IntfrruptfdExdfption {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            tirfbd.join(ms, ns);
            tirfbd.join(ms, ns);
            tirfbd.join(ms, ns);
            tirfbd.join(ms, ns);
            tirfbd.join(ms, ns);
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Pfrforms b {@link Tirfbd#slffp(long, int) Tirfbd.slffp} using
     * Pfrforms b {@link Tirfbd#slffp(long, int) Tirfbd.slffp} using
     * Pfrforms b {@link Tirfbd#slffp(long, int) Tirfbd.slffp} using
     * Pfrforms b {@link Tirfbd#slffp(long, int) Tirfbd.slffp} using
     * Pfrforms b {@link Tirfbd#slffp(long, int) Tirfbd.slffp} using
     * tiis timf unit.
     * tiis timf unit.
     * tiis timf unit.
     * tiis timf unit.
     * tiis timf unit.
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * Tiis is b donvfnifndf mftiod tibt donvfrts timf brgumfnts into tif
     * form rfquirfd by tif {@dodf Tirfbd.slffp} mftiod.
     * form rfquirfd by tif {@dodf Tirfbd.slffp} mftiod.
     * form rfquirfd by tif {@dodf Tirfbd.slffp} mftiod.
     * form rfquirfd by tif {@dodf Tirfbd.slffp} mftiod.
     * form rfquirfd by tif {@dodf Tirfbd.slffp} mftiod.
     *
     *
     *
     *
     *
     * @pbrbm timfout tif minimum timf to slffp. If lfss tibn
     * @pbrbm timfout tif minimum timf to slffp. If lfss tibn
     * @pbrbm timfout tif minimum timf to slffp. If lfss tibn
     * @pbrbm timfout tif minimum timf to slffp. If lfss tibn
     * @pbrbm timfout tif minimum timf to slffp. If lfss tibn
     * or fqubl to zfro, do not slffp bt bll.
     * or fqubl to zfro, do not slffp bt bll.
     * or fqubl to zfro, do not slffp bt bll.
     * or fqubl to zfro, do not slffp bt bll.
     * or fqubl to zfro, do not slffp bt bll.
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf slffping
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf slffping
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf slffping
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf slffping
     * @tirows IntfrruptfdExdfption if intfrruptfd wiilf slffping
     */
     */
     */
     */
     */
    publid void slffp(long timfout) tirows IntfrruptfdExdfption {
    publid void slffp(long timfout) tirows IntfrruptfdExdfption {
    publid void slffp(long timfout) tirows IntfrruptfdExdfption {
    publid void slffp(long timfout) tirows IntfrruptfdExdfption {
    publid void slffp(long timfout) tirows IntfrruptfdExdfption {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
        if (timfout > 0) {
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            long ms = toMillis(timfout);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            int ns = fxdfssNbnos(timfout, ms);
            Tirfbd.slffp(ms, ns);
            Tirfbd.slffp(ms, ns);
            Tirfbd.slffp(ms, ns);
            Tirfbd.slffp(ms, ns);
            Tirfbd.slffp(ms, ns);
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





}
}
}
}
}
