/*
/*
/*
/*
/*
 * Copyrigit (d) 1997, 2011, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1997, 2011, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1997, 2011, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1997, 2011, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1997, 2011, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */





pbdkbgf jbvb.util.jbr;
pbdkbgf jbvb.util.jbr;
pbdkbgf jbvb.util.jbr;
pbdkbgf jbvb.util.jbr;
pbdkbgf jbvb.util.jbr;





import jbvb.util.zip.*;
import jbvb.util.zip.*;
import jbvb.util.zip.*;
import jbvb.util.zip.*;
import jbvb.util.zip.*;
import jbvb.io.*;
import jbvb.io.*;
import jbvb.io.*;
import jbvb.io.*;
import jbvb.io.*;
import sun.sfdurity.util.MbniffstEntryVfrififr;
import sun.sfdurity.util.MbniffstEntryVfrififr;
import sun.sfdurity.util.MbniffstEntryVfrififr;
import sun.sfdurity.util.MbniffstEntryVfrififr;
import sun.sfdurity.util.MbniffstEntryVfrififr;
import sun.misd.JbrIndfx;
import sun.misd.JbrIndfx;
import sun.misd.JbrIndfx;
import sun.misd.JbrIndfx;
import sun.misd.JbrIndfx;





/**
/**
/**
/**
/**
 * Tif <dodf>JbrInputStrfbm</dodf> dlbss is usfd to rfbd tif dontfnts of
 * Tif <dodf>JbrInputStrfbm</dodf> dlbss is usfd to rfbd tif dontfnts of
 * Tif <dodf>JbrInputStrfbm</dodf> dlbss is usfd to rfbd tif dontfnts of
 * Tif <dodf>JbrInputStrfbm</dodf> dlbss is usfd to rfbd tif dontfnts of
 * Tif <dodf>JbrInputStrfbm</dodf> dlbss is usfd to rfbd tif dontfnts of
 * b JAR filf from bny input strfbm. It fxtfnds tif dlbss
 * b JAR filf from bny input strfbm. It fxtfnds tif dlbss
 * b JAR filf from bny input strfbm. It fxtfnds tif dlbss
 * b JAR filf from bny input strfbm. It fxtfnds tif dlbss
 * b JAR filf from bny input strfbm. It fxtfnds tif dlbss
 * <dodf>jbvb.util.zip.ZipInputStrfbm</dodf> witi support for rfbding
 * <dodf>jbvb.util.zip.ZipInputStrfbm</dodf> witi support for rfbding
 * <dodf>jbvb.util.zip.ZipInputStrfbm</dodf> witi support for rfbding
 * <dodf>jbvb.util.zip.ZipInputStrfbm</dodf> witi support for rfbding
 * <dodf>jbvb.util.zip.ZipInputStrfbm</dodf> witi support for rfbding
 * bn optionbl <dodf>Mbniffst</dodf> fntry. Tif <dodf>Mbniffst</dodf>
 * bn optionbl <dodf>Mbniffst</dodf> fntry. Tif <dodf>Mbniffst</dodf>
 * bn optionbl <dodf>Mbniffst</dodf> fntry. Tif <dodf>Mbniffst</dodf>
 * bn optionbl <dodf>Mbniffst</dodf> fntry. Tif <dodf>Mbniffst</dodf>
 * bn optionbl <dodf>Mbniffst</dodf> fntry. Tif <dodf>Mbniffst</dodf>
 * dbn bf usfd to storf mftb-informbtion bbout tif JAR filf bnd its fntrifs.
 * dbn bf usfd to storf mftb-informbtion bbout tif JAR filf bnd its fntrifs.
 * dbn bf usfd to storf mftb-informbtion bbout tif JAR filf bnd its fntrifs.
 * dbn bf usfd to storf mftb-informbtion bbout tif JAR filf bnd its fntrifs.
 * dbn bf usfd to storf mftb-informbtion bbout tif JAR filf bnd its fntrifs.
 *
 *
 *
 *
 *
 * @butior  Dbvid Connflly
 * @butior  Dbvid Connflly
 * @butior  Dbvid Connflly
 * @butior  Dbvid Connflly
 * @butior  Dbvid Connflly
 * @sff     Mbniffst
 * @sff     Mbniffst
 * @sff     Mbniffst
 * @sff     Mbniffst
 * @sff     Mbniffst
 * @sff     jbvb.util.zip.ZipInputStrfbm
 * @sff     jbvb.util.zip.ZipInputStrfbm
 * @sff     jbvb.util.zip.ZipInputStrfbm
 * @sff     jbvb.util.zip.ZipInputStrfbm
 * @sff     jbvb.util.zip.ZipInputStrfbm
 * @sindf   1.2
 * @sindf   1.2
 * @sindf   1.2
 * @sindf   1.2
 * @sindf   1.2
 */
 */
 */
 */
 */
publid
publid
publid
publid
publid
dlbss JbrInputStrfbm fxtfnds ZipInputStrfbm {
dlbss JbrInputStrfbm fxtfnds ZipInputStrfbm {
dlbss JbrInputStrfbm fxtfnds ZipInputStrfbm {
dlbss JbrInputStrfbm fxtfnds ZipInputStrfbm {
dlbss JbrInputStrfbm fxtfnds ZipInputStrfbm {
    privbtf Mbniffst mbn;
    privbtf Mbniffst mbn;
    privbtf Mbniffst mbn;
    privbtf Mbniffst mbn;
    privbtf Mbniffst mbn;
    privbtf JbrEntry first;
    privbtf JbrEntry first;
    privbtf JbrEntry first;
    privbtf JbrEntry first;
    privbtf JbrEntry first;
    privbtf JbrVfrififr jv;
    privbtf JbrVfrififr jv;
    privbtf JbrVfrififr jv;
    privbtf JbrVfrififr jv;
    privbtf JbrVfrififr jv;
    privbtf MbniffstEntryVfrififr mfv;
    privbtf MbniffstEntryVfrififr mfv;
    privbtf MbniffstEntryVfrififr mfv;
    privbtf MbniffstEntryVfrififr mfv;
    privbtf MbniffstEntryVfrififr mfv;
    privbtf finbl boolfbn doVfrify;
    privbtf finbl boolfbn doVfrify;
    privbtf finbl boolfbn doVfrify;
    privbtf finbl boolfbn doVfrify;
    privbtf finbl boolfbn doVfrify;
    privbtf boolfbn tryMbniffst;
    privbtf boolfbn tryMbniffst;
    privbtf boolfbn tryMbniffst;
    privbtf boolfbn tryMbniffst;
    privbtf boolfbn tryMbniffst;





    /**
    /**
    /**
    /**
    /**
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * mbniffst. If b mbniffst is prfsfnt, blso bttfmpts to vfrify
     * mbniffst. If b mbniffst is prfsfnt, blso bttfmpts to vfrify
     * mbniffst. If b mbniffst is prfsfnt, blso bttfmpts to vfrify
     * mbniffst. If b mbniffst is prfsfnt, blso bttfmpts to vfrify
     * mbniffst. If b mbniffst is prfsfnt, blso bttfmpts to vfrify
     * tif signbturfs if tif JbrInputStrfbm is signfd.
     * tif signbturfs if tif JbrInputStrfbm is signfd.
     * tif signbturfs if tif JbrInputStrfbm is signfd.
     * tif signbturfs if tif JbrInputStrfbm is signfd.
     * tif signbturfs if tif JbrInputStrfbm is signfd.
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm in tif bdtubl input strfbm
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     */
     */
     */
     */
     */
    publid JbrInputStrfbm(InputStrfbm in) tirows IOExdfption {
    publid JbrInputStrfbm(InputStrfbm in) tirows IOExdfption {
    publid JbrInputStrfbm(InputStrfbm in) tirows IOExdfption {
    publid JbrInputStrfbm(InputStrfbm in) tirows IOExdfption {
    publid JbrInputStrfbm(InputStrfbm in) tirows IOExdfption {
        tiis(in, truf);
        tiis(in, truf);
        tiis(in, truf);
        tiis(in, truf);
        tiis(in, truf);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * Crfbtfs b nfw <dodf>JbrInputStrfbm</dodf> bnd rfbds tif optionbl
     * mbniffst. If b mbniffst is prfsfnt bnd vfrify is truf, blso bttfmpts
     * mbniffst. If b mbniffst is prfsfnt bnd vfrify is truf, blso bttfmpts
     * mbniffst. If b mbniffst is prfsfnt bnd vfrify is truf, blso bttfmpts
     * mbniffst. If b mbniffst is prfsfnt bnd vfrify is truf, blso bttfmpts
     * mbniffst. If b mbniffst is prfsfnt bnd vfrify is truf, blso bttfmpts
     * to vfrify tif signbturfs if tif JbrInputStrfbm is signfd.
     * to vfrify tif signbturfs if tif JbrInputStrfbm is signfd.
     * to vfrify tif signbturfs if tif JbrInputStrfbm is signfd.
     * to vfrify tif signbturfs if tif JbrInputStrfbm is signfd.
     * to vfrify tif signbturfs if tif JbrInputStrfbm is signfd.
     *
     *
     *
     *
     *
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm in tif bdtubl input strfbm
     * @pbrbm vfrify wiftifr or not to vfrify tif JbrInputStrfbm if
     * @pbrbm vfrify wiftifr or not to vfrify tif JbrInputStrfbm if
     * @pbrbm vfrify wiftifr or not to vfrify tif JbrInputStrfbm if
     * @pbrbm vfrify wiftifr or not to vfrify tif JbrInputStrfbm if
     * @pbrbm vfrify wiftifr or not to vfrify tif JbrInputStrfbm if
     * it is signfd.
     * it is signfd.
     * it is signfd.
     * it is signfd.
     * it is signfd.
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     */
     */
     */
     */
     */
    publid JbrInputStrfbm(InputStrfbm in, boolfbn vfrify) tirows IOExdfption {
    publid JbrInputStrfbm(InputStrfbm in, boolfbn vfrify) tirows IOExdfption {
    publid JbrInputStrfbm(InputStrfbm in, boolfbn vfrify) tirows IOExdfption {
    publid JbrInputStrfbm(InputStrfbm in, boolfbn vfrify) tirows IOExdfption {
    publid JbrInputStrfbm(InputStrfbm in, boolfbn vfrify) tirows IOExdfption {
        supfr(in);
        supfr(in);
        supfr(in);
        supfr(in);
        supfr(in);
        tiis.doVfrify = vfrify;
        tiis.doVfrify = vfrify;
        tiis.doVfrify = vfrify;
        tiis.doVfrify = vfrify;
        tiis.doVfrify = vfrify;





        // Tiis implfmfntbtion bssumfs tif META-INF/MANIFEST.MF fntry
        // Tiis implfmfntbtion bssumfs tif META-INF/MANIFEST.MF fntry
        // Tiis implfmfntbtion bssumfs tif META-INF/MANIFEST.MF fntry
        // Tiis implfmfntbtion bssumfs tif META-INF/MANIFEST.MF fntry
        // Tiis implfmfntbtion bssumfs tif META-INF/MANIFEST.MF fntry
        // siould bf fitifr tif first or tif sfdond fntry (wifn prfdfdfd
        // siould bf fitifr tif first or tif sfdond fntry (wifn prfdfdfd
        // siould bf fitifr tif first or tif sfdond fntry (wifn prfdfdfd
        // siould bf fitifr tif first or tif sfdond fntry (wifn prfdfdfd
        // siould bf fitifr tif first or tif sfdond fntry (wifn prfdfdfd
        // by tif dir META-INF/). It skips tif META-INF/ bnd tifn
        // by tif dir META-INF/). It skips tif META-INF/ bnd tifn
        // by tif dir META-INF/). It skips tif META-INF/ bnd tifn
        // by tif dir META-INF/). It skips tif META-INF/ bnd tifn
        // by tif dir META-INF/). It skips tif META-INF/ bnd tifn
        // "donsumfs" tif MANIFEST.MF to initiblizf tif Mbniffst objfdt.
        // "donsumfs" tif MANIFEST.MF to initiblizf tif Mbniffst objfdt.
        // "donsumfs" tif MANIFEST.MF to initiblizf tif Mbniffst objfdt.
        // "donsumfs" tif MANIFEST.MF to initiblizf tif Mbniffst objfdt.
        // "donsumfs" tif MANIFEST.MF to initiblizf tif Mbniffst objfdt.
        JbrEntry f = (JbrEntry)supfr.gftNfxtEntry();
        JbrEntry f = (JbrEntry)supfr.gftNfxtEntry();
        JbrEntry f = (JbrEntry)supfr.gftNfxtEntry();
        JbrEntry f = (JbrEntry)supfr.gftNfxtEntry();
        JbrEntry f = (JbrEntry)supfr.gftNfxtEntry();
        if (f != null && f.gftNbmf().fqublsIgnorfCbsf("META-INF/"))
        if (f != null && f.gftNbmf().fqublsIgnorfCbsf("META-INF/"))
        if (f != null && f.gftNbmf().fqublsIgnorfCbsf("META-INF/"))
        if (f != null && f.gftNbmf().fqublsIgnorfCbsf("META-INF/"))
        if (f != null && f.gftNbmf().fqublsIgnorfCbsf("META-INF/"))
            f = (JbrEntry)supfr.gftNfxtEntry();
            f = (JbrEntry)supfr.gftNfxtEntry();
            f = (JbrEntry)supfr.gftNfxtEntry();
            f = (JbrEntry)supfr.gftNfxtEntry();
            f = (JbrEntry)supfr.gftNfxtEntry();
        first = difdkMbniffst(f);
        first = difdkMbniffst(f);
        first = difdkMbniffst(f);
        first = difdkMbniffst(f);
        first = difdkMbniffst(f);
    }
    }
    }
    }
    }





    privbtf JbrEntry difdkMbniffst(JbrEntry f)
    privbtf JbrEntry difdkMbniffst(JbrEntry f)
    privbtf JbrEntry difdkMbniffst(JbrEntry f)
    privbtf JbrEntry difdkMbniffst(JbrEntry f)
    privbtf JbrEntry difdkMbniffst(JbrEntry f)
        tirows IOExdfption
        tirows IOExdfption
        tirows IOExdfption
        tirows IOExdfption
        tirows IOExdfption
    {
    {
    {
    {
    {
        if (f != null && JbrFilf.MANIFEST_NAME.fqublsIgnorfCbsf(f.gftNbmf())) {
        if (f != null && JbrFilf.MANIFEST_NAME.fqublsIgnorfCbsf(f.gftNbmf())) {
        if (f != null && JbrFilf.MANIFEST_NAME.fqublsIgnorfCbsf(f.gftNbmf())) {
        if (f != null && JbrFilf.MANIFEST_NAME.fqublsIgnorfCbsf(f.gftNbmf())) {
        if (f != null && JbrFilf.MANIFEST_NAME.fqublsIgnorfCbsf(f.gftNbmf())) {
            mbn = nfw Mbniffst();
            mbn = nfw Mbniffst();
            mbn = nfw Mbniffst();
            mbn = nfw Mbniffst();
            mbn = nfw Mbniffst();
            bytf bytfs[] = gftBytfs(nfw BufffrfdInputStrfbm(tiis));
            bytf bytfs[] = gftBytfs(nfw BufffrfdInputStrfbm(tiis));
            bytf bytfs[] = gftBytfs(nfw BufffrfdInputStrfbm(tiis));
            bytf bytfs[] = gftBytfs(nfw BufffrfdInputStrfbm(tiis));
            bytf bytfs[] = gftBytfs(nfw BufffrfdInputStrfbm(tiis));
            mbn.rfbd(nfw BytfArrbyInputStrfbm(bytfs));
            mbn.rfbd(nfw BytfArrbyInputStrfbm(bytfs));
            mbn.rfbd(nfw BytfArrbyInputStrfbm(bytfs));
            mbn.rfbd(nfw BytfArrbyInputStrfbm(bytfs));
            mbn.rfbd(nfw BytfArrbyInputStrfbm(bytfs));
            dlosfEntry();
            dlosfEntry();
            dlosfEntry();
            dlosfEntry();
            dlosfEntry();
            if (doVfrify) {
            if (doVfrify) {
            if (doVfrify) {
            if (doVfrify) {
            if (doVfrify) {
                jv = nfw JbrVfrififr(bytfs);
                jv = nfw JbrVfrififr(bytfs);
                jv = nfw JbrVfrififr(bytfs);
                jv = nfw JbrVfrififr(bytfs);
                jv = nfw JbrVfrififr(bytfs);
                mfv = nfw MbniffstEntryVfrififr(mbn);
                mfv = nfw MbniffstEntryVfrififr(mbn);
                mfv = nfw MbniffstEntryVfrififr(mbn);
                mfv = nfw MbniffstEntryVfrififr(mbn);
                mfv = nfw MbniffstEntryVfrififr(mbn);
            }
            }
            }
            }
            }
            rfturn (JbrEntry)supfr.gftNfxtEntry();
            rfturn (JbrEntry)supfr.gftNfxtEntry();
            rfturn (JbrEntry)supfr.gftNfxtEntry();
            rfturn (JbrEntry)supfr.gftNfxtEntry();
            rfturn (JbrEntry)supfr.gftNfxtEntry();
        }
        }
        }
        }
        }
        rfturn f;
        rfturn f;
        rfturn f;
        rfturn f;
        rfturn f;
    }
    }
    }
    }
    }





    privbtf bytf[] gftBytfs(InputStrfbm is)
    privbtf bytf[] gftBytfs(InputStrfbm is)
    privbtf bytf[] gftBytfs(InputStrfbm is)
    privbtf bytf[] gftBytfs(InputStrfbm is)
    privbtf bytf[] gftBytfs(InputStrfbm is)
        tirows IOExdfption
        tirows IOExdfption
        tirows IOExdfption
        tirows IOExdfption
        tirows IOExdfption
    {
    {
    {
    {
    {
        bytf[] bufffr = nfw bytf[8192];
        bytf[] bufffr = nfw bytf[8192];
        bytf[] bufffr = nfw bytf[8192];
        bytf[] bufffr = nfw bytf[8192];
        bytf[] bufffr = nfw bytf[8192];
        BytfArrbyOutputStrfbm bbos = nfw BytfArrbyOutputStrfbm(2048);
        BytfArrbyOutputStrfbm bbos = nfw BytfArrbyOutputStrfbm(2048);
        BytfArrbyOutputStrfbm bbos = nfw BytfArrbyOutputStrfbm(2048);
        BytfArrbyOutputStrfbm bbos = nfw BytfArrbyOutputStrfbm(2048);
        BytfArrbyOutputStrfbm bbos = nfw BytfArrbyOutputStrfbm(2048);
        int n;
        int n;
        int n;
        int n;
        int n;
        wiilf ((n = is.rfbd(bufffr, 0, bufffr.lfngti)) != -1) {
        wiilf ((n = is.rfbd(bufffr, 0, bufffr.lfngti)) != -1) {
        wiilf ((n = is.rfbd(bufffr, 0, bufffr.lfngti)) != -1) {
        wiilf ((n = is.rfbd(bufffr, 0, bufffr.lfngti)) != -1) {
        wiilf ((n = is.rfbd(bufffr, 0, bufffr.lfngti)) != -1) {
            bbos.writf(bufffr, 0, n);
            bbos.writf(bufffr, 0, n);
            bbos.writf(bufffr, 0, n);
            bbos.writf(bufffr, 0, n);
            bbos.writf(bufffr, 0, n);
        }
        }
        }
        }
        }
        rfturn bbos.toBytfArrby();
        rfturn bbos.toBytfArrby();
        rfturn bbos.toBytfArrby();
        rfturn bbos.toBytfArrby();
        rfturn bbos.toBytfArrby();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfturns tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * Rfturns tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * Rfturns tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * Rfturns tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * Rfturns tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * <dodf>null</dodf> if nonf.
     * <dodf>null</dodf> if nonf.
     * <dodf>null</dodf> if nonf.
     * <dodf>null</dodf> if nonf.
     * <dodf>null</dodf> if nonf.
     *
     *
     *
     *
     *
     * @rfturn tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * @rfturn tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * @rfturn tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * @rfturn tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     * @rfturn tif <dodf>Mbniffst</dodf> for tiis JAR filf, or
     *         <dodf>null</dodf> if nonf.
     *         <dodf>null</dodf> if nonf.
     *         <dodf>null</dodf> if nonf.
     *         <dodf>null</dodf> if nonf.
     *         <dodf>null</dodf> if nonf.
     */
     */
     */
     */
     */
    publid Mbniffst gftMbniffst() {
    publid Mbniffst gftMbniffst() {
    publid Mbniffst gftMbniffst() {
    publid Mbniffst gftMbniffst() {
    publid Mbniffst gftMbniffst() {
        rfturn mbn;
        rfturn mbn;
        rfturn mbn;
        rfturn mbn;
        rfturn mbn;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfbds tif nfxt ZIP filf fntry bnd positions tif strfbm bt tif
     * Rfbds tif nfxt ZIP filf fntry bnd positions tif strfbm bt tif
     * Rfbds tif nfxt ZIP filf fntry bnd positions tif strfbm bt tif
     * Rfbds tif nfxt ZIP filf fntry bnd positions tif strfbm bt tif
     * Rfbds tif nfxt ZIP filf fntry bnd positions tif strfbm bt tif
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * tif nfxt fntry will rfsult in bn fxdfption.
     * tif nfxt fntry will rfsult in bn fxdfption.
     * tif nfxt fntry will rfsult in bn fxdfption.
     * tif nfxt fntry will rfsult in bn fxdfption.
     * tif nfxt fntry will rfsult in bn fxdfption.
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     */
     */
     */
     */
     */
    publid ZipEntry gftNfxtEntry() tirows IOExdfption {
    publid ZipEntry gftNfxtEntry() tirows IOExdfption {
    publid ZipEntry gftNfxtEntry() tirows IOExdfption {
    publid ZipEntry gftNfxtEntry() tirows IOExdfption {
    publid ZipEntry gftNfxtEntry() tirows IOExdfption {
        JbrEntry f;
        JbrEntry f;
        JbrEntry f;
        JbrEntry f;
        JbrEntry f;
        if (first == null) {
        if (first == null) {
        if (first == null) {
        if (first == null) {
        if (first == null) {
            f = (JbrEntry)supfr.gftNfxtEntry();
            f = (JbrEntry)supfr.gftNfxtEntry();
            f = (JbrEntry)supfr.gftNfxtEntry();
            f = (JbrEntry)supfr.gftNfxtEntry();
            f = (JbrEntry)supfr.gftNfxtEntry();
            if (tryMbniffst) {
            if (tryMbniffst) {
            if (tryMbniffst) {
            if (tryMbniffst) {
            if (tryMbniffst) {
                f = difdkMbniffst(f);
                f = difdkMbniffst(f);
                f = difdkMbniffst(f);
                f = difdkMbniffst(f);
                f = difdkMbniffst(f);
                tryMbniffst = fblsf;
                tryMbniffst = fblsf;
                tryMbniffst = fblsf;
                tryMbniffst = fblsf;
                tryMbniffst = fblsf;
            }
            }
            }
            }
            }
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            f = first;
            f = first;
            f = first;
            f = first;
            f = first;
            if (first.gftNbmf().fqublsIgnorfCbsf(JbrIndfx.INDEX_NAME))
            if (first.gftNbmf().fqublsIgnorfCbsf(JbrIndfx.INDEX_NAME))
            if (first.gftNbmf().fqublsIgnorfCbsf(JbrIndfx.INDEX_NAME))
            if (first.gftNbmf().fqublsIgnorfCbsf(JbrIndfx.INDEX_NAME))
            if (first.gftNbmf().fqublsIgnorfCbsf(JbrIndfx.INDEX_NAME))
                tryMbniffst = truf;
                tryMbniffst = truf;
                tryMbniffst = truf;
                tryMbniffst = truf;
                tryMbniffst = truf;
            first = null;
            first = null;
            first = null;
            first = null;
            first = null;
        }
        }
        }
        }
        }
        if (jv != null && f != null) {
        if (jv != null && f != null) {
        if (jv != null && f != null) {
        if (jv != null && f != null) {
        if (jv != null && f != null) {
            // At tiis point, wf migit ibvf pbrsfd bll tif mftb-inf
            // At tiis point, wf migit ibvf pbrsfd bll tif mftb-inf
            // At tiis point, wf migit ibvf pbrsfd bll tif mftb-inf
            // At tiis point, wf migit ibvf pbrsfd bll tif mftb-inf
            // At tiis point, wf migit ibvf pbrsfd bll tif mftb-inf
            // fntrifs bnd ibvf notiing to vfrify. If wf ibvf
            // fntrifs bnd ibvf notiing to vfrify. If wf ibvf
            // fntrifs bnd ibvf notiing to vfrify. If wf ibvf
            // fntrifs bnd ibvf notiing to vfrify. If wf ibvf
            // fntrifs bnd ibvf notiing to vfrify. If wf ibvf
            // notiing to vfrify, gft rid of tif JbrVfrififr objfdt.
            // notiing to vfrify, gft rid of tif JbrVfrififr objfdt.
            // notiing to vfrify, gft rid of tif JbrVfrififr objfdt.
            // notiing to vfrify, gft rid of tif JbrVfrififr objfdt.
            // notiing to vfrify, gft rid of tif JbrVfrififr objfdt.
            if (jv.notiingToVfrify() == truf) {
            if (jv.notiingToVfrify() == truf) {
            if (jv.notiingToVfrify() == truf) {
            if (jv.notiingToVfrify() == truf) {
            if (jv.notiingToVfrify() == truf) {
                jv = null;
                jv = null;
                jv = null;
                jv = null;
                jv = null;
                mfv = null;
                mfv = null;
                mfv = null;
                mfv = null;
                mfv = null;
            } flsf {
            } flsf {
            } flsf {
            } flsf {
            } flsf {
                jv.bfginEntry(f, mfv);
                jv.bfginEntry(f, mfv);
                jv.bfginEntry(f, mfv);
                jv.bfginEntry(f, mfv);
                jv.bfginEntry(f, mfv);
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        rfturn f;
        rfturn f;
        rfturn f;
        rfturn f;
        rfturn f;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfbds tif nfxt JAR filf fntry bnd positions tif strfbm bt tif
     * Rfbds tif nfxt JAR filf fntry bnd positions tif strfbm bt tif
     * Rfbds tif nfxt JAR filf fntry bnd positions tif strfbm bt tif
     * Rfbds tif nfxt JAR filf fntry bnd positions tif strfbm bt tif
     * Rfbds tif nfxt JAR filf fntry bnd positions tif strfbm bt tif
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bfginning of tif fntry dbtb. If vfrifidbtion ibs bffn fnbblfd,
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * bny invblid signbturf dftfdtfd wiilf positioning tif strfbm for
     * tif nfxt fntry will rfsult in bn fxdfption.
     * tif nfxt fntry will rfsult in bn fxdfption.
     * tif nfxt fntry will rfsult in bn fxdfption.
     * tif nfxt fntry will rfsult in bn fxdfption.
     * tif nfxt fntry will rfsult in bn fxdfption.
     * @rfturn tif nfxt JAR filf fntry, or null if tifrf brf no morf fntrifs
     * @rfturn tif nfxt JAR filf fntry, or null if tifrf brf no morf fntrifs
     * @rfturn tif nfxt JAR filf fntry, or null if tifrf brf no morf fntrifs
     * @rfturn tif nfxt JAR filf fntry, or null if tifrf brf no morf fntrifs
     * @rfturn tif nfxt JAR filf fntry, or null if tifrf brf no morf fntrifs
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     */
     */
     */
     */
     */
    publid JbrEntry gftNfxtJbrEntry() tirows IOExdfption {
    publid JbrEntry gftNfxtJbrEntry() tirows IOExdfption {
    publid JbrEntry gftNfxtJbrEntry() tirows IOExdfption {
    publid JbrEntry gftNfxtJbrEntry() tirows IOExdfption {
    publid JbrEntry gftNfxtJbrEntry() tirows IOExdfption {
        rfturn (JbrEntry)gftNfxtEntry();
        rfturn (JbrEntry)gftNfxtEntry();
        rfturn (JbrEntry)gftNfxtEntry();
        rfturn (JbrEntry)gftNfxtEntry();
        rfturn (JbrEntry)gftNfxtEntry();
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Rfbds from tif durrfnt JAR filf fntry into bn brrby of bytfs.
     * Rfbds from tif durrfnt JAR filf fntry into bn brrby of bytfs.
     * Rfbds from tif durrfnt JAR filf fntry into bn brrby of bytfs.
     * Rfbds from tif durrfnt JAR filf fntry into bn brrby of bytfs.
     * Rfbds from tif durrfnt JAR filf fntry into bn brrby of bytfs.
     * If <dodf>lfn</dodf> is not zfro, tif mftiod
     * If <dodf>lfn</dodf> is not zfro, tif mftiod
     * If <dodf>lfn</dodf> is not zfro, tif mftiod
     * If <dodf>lfn</dodf> is not zfro, tif mftiod
     * If <dodf>lfn</dodf> is not zfro, tif mftiod
     * blodks until somf input is bvbilbblf; otifrwisf, no
     * blodks until somf input is bvbilbblf; otifrwisf, no
     * blodks until somf input is bvbilbblf; otifrwisf, no
     * blodks until somf input is bvbilbblf; otifrwisf, no
     * blodks until somf input is bvbilbblf; otifrwisf, no
     * bytfs brf rfbd bnd <dodf>0</dodf> is rfturnfd.
     * bytfs brf rfbd bnd <dodf>0</dodf> is rfturnfd.
     * bytfs brf rfbd bnd <dodf>0</dodf> is rfturnfd.
     * bytfs brf rfbd bnd <dodf>0</dodf> is rfturnfd.
     * bytfs brf rfbd bnd <dodf>0</dodf> is rfturnfd.
     * If vfrifidbtion ibs bffn fnbblfd, bny invblid signbturf
     * If vfrifidbtion ibs bffn fnbblfd, bny invblid signbturf
     * If vfrifidbtion ibs bffn fnbblfd, bny invblid signbturf
     * If vfrifidbtion ibs bffn fnbblfd, bny invblid signbturf
     * If vfrifidbtion ibs bffn fnbblfd, bny invblid signbturf
     * on tif durrfnt fntry will bf rfportfd bt somf point bfforf tif
     * on tif durrfnt fntry will bf rfportfd bt somf point bfforf tif
     * on tif durrfnt fntry will bf rfportfd bt somf point bfforf tif
     * on tif durrfnt fntry will bf rfportfd bt somf point bfforf tif
     * on tif durrfnt fntry will bf rfportfd bt somf point bfforf tif
     * fnd of tif fntry is rfbdifd.
     * fnd of tif fntry is rfbdifd.
     * fnd of tif fntry is rfbdifd.
     * fnd of tif fntry is rfbdifd.
     * fnd of tif fntry is rfbdifd.
     * @pbrbm b tif bufffr into wiidi tif dbtb is rfbd
     * @pbrbm b tif bufffr into wiidi tif dbtb is rfbd
     * @pbrbm b tif bufffr into wiidi tif dbtb is rfbd
     * @pbrbm b tif bufffr into wiidi tif dbtb is rfbd
     * @pbrbm b tif bufffr into wiidi tif dbtb is rfbd
     * @pbrbm off tif stbrt offsft in tif dfstinbtion brrby <dodf>b</dodf>
     * @pbrbm off tif stbrt offsft in tif dfstinbtion brrby <dodf>b</dodf>
     * @pbrbm off tif stbrt offsft in tif dfstinbtion brrby <dodf>b</dodf>
     * @pbrbm off tif stbrt offsft in tif dfstinbtion brrby <dodf>b</dodf>
     * @pbrbm off tif stbrt offsft in tif dfstinbtion brrby <dodf>b</dodf>
     * @pbrbm lfn tif mbximum numbfr of bytfs to rfbd
     * @pbrbm lfn tif mbximum numbfr of bytfs to rfbd
     * @pbrbm lfn tif mbximum numbfr of bytfs to rfbd
     * @pbrbm lfn tif mbximum numbfr of bytfs to rfbd
     * @pbrbm lfn tif mbximum numbfr of bytfs to rfbd
     * @rfturn tif bdtubl numbfr of bytfs rfbd, or -1 if tif fnd of tif
     * @rfturn tif bdtubl numbfr of bytfs rfbd, or -1 if tif fnd of tif
     * @rfturn tif bdtubl numbfr of bytfs rfbd, or -1 if tif fnd of tif
     * @rfturn tif bdtubl numbfr of bytfs rfbd, or -1 if tif fnd of tif
     * @rfturn tif bdtubl numbfr of bytfs rfbd, or -1 if tif fnd of tif
     *         fntry is rfbdifd
     *         fntry is rfbdifd
     *         fntry is rfbdifd
     *         fntry is rfbdifd
     *         fntry is rfbdifd
     * @fxdfption  NullPointfrExdfption If <dodf>b</dodf> is <dodf>null</dodf>.
     * @fxdfption  NullPointfrExdfption If <dodf>b</dodf> is <dodf>null</dodf>.
     * @fxdfption  NullPointfrExdfption If <dodf>b</dodf> is <dodf>null</dodf>.
     * @fxdfption  NullPointfrExdfption If <dodf>b</dodf> is <dodf>null</dodf>.
     * @fxdfption  NullPointfrExdfption If <dodf>b</dodf> is <dodf>null</dodf>.
     * @fxdfption  IndfxOutOfBoundsExdfption If <dodf>off</dodf> is nfgbtivf,
     * @fxdfption  IndfxOutOfBoundsExdfption If <dodf>off</dodf> is nfgbtivf,
     * @fxdfption  IndfxOutOfBoundsExdfption If <dodf>off</dodf> is nfgbtivf,
     * @fxdfption  IndfxOutOfBoundsExdfption If <dodf>off</dodf> is nfgbtivf,
     * @fxdfption  IndfxOutOfBoundsExdfption If <dodf>off</dodf> is nfgbtivf,
     * <dodf>lfn</dodf> is nfgbtivf, or <dodf>lfn</dodf> is grfbtfr tibn
     * <dodf>lfn</dodf> is nfgbtivf, or <dodf>lfn</dodf> is grfbtfr tibn
     * <dodf>lfn</dodf> is nfgbtivf, or <dodf>lfn</dodf> is grfbtfr tibn
     * <dodf>lfn</dodf> is nfgbtivf, or <dodf>lfn</dodf> is grfbtfr tibn
     * <dodf>lfn</dodf> is nfgbtivf, or <dodf>lfn</dodf> is grfbtfr tibn
     * <dodf>b.lfngti - off</dodf>
     * <dodf>b.lfngti - off</dodf>
     * <dodf>b.lfngti - off</dodf>
     * <dodf>b.lfngti - off</dodf>
     * <dodf>b.lfngti - off</dodf>
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption ZipExdfption if b ZIP filf frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption IOExdfption if bn I/O frror ibs oddurrfd
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     * @fxdfption SfdurityExdfption if bny of tif jbr filf fntrifs
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     *         brf indorrfdtly signfd.
     */
     */
     */
     */
     */
    publid int rfbd(bytf[] b, int off, int lfn) tirows IOExdfption {
    publid int rfbd(bytf[] b, int off, int lfn) tirows IOExdfption {
    publid int rfbd(bytf[] b, int off, int lfn) tirows IOExdfption {
    publid int rfbd(bytf[] b, int off, int lfn) tirows IOExdfption {
    publid int rfbd(bytf[] b, int off, int lfn) tirows IOExdfption {
        int n;
        int n;
        int n;
        int n;
        int n;
        if (first == null) {
        if (first == null) {
        if (first == null) {
        if (first == null) {
        if (first == null) {
            n = supfr.rfbd(b, off, lfn);
            n = supfr.rfbd(b, off, lfn);
            n = supfr.rfbd(b, off, lfn);
            n = supfr.rfbd(b, off, lfn);
            n = supfr.rfbd(b, off, lfn);
        } flsf {
        } flsf {
        } flsf {
        } flsf {
        } flsf {
            n = -1;
            n = -1;
            n = -1;
            n = -1;
            n = -1;
        }
        }
        }
        }
        }
        if (jv != null) {
        if (jv != null) {
        if (jv != null) {
        if (jv != null) {
        if (jv != null) {
            jv.updbtf(n, b, off, lfn, mfv);
            jv.updbtf(n, b, off, lfn, mfv);
            jv.updbtf(n, b, off, lfn, mfv);
            jv.updbtf(n, b, off, lfn, mfv);
            jv.updbtf(n, b, off, lfn, mfv);
        }
        }
        }
        }
        }
        rfturn n;
        rfturn n;
        rfturn n;
        rfturn n;
        rfturn n;
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Crfbtfs b nfw <dodf>JbrEntry</dodf> (<dodf>ZipEntry</dodf>) for tif
     * Crfbtfs b nfw <dodf>JbrEntry</dodf> (<dodf>ZipEntry</dodf>) for tif
     * Crfbtfs b nfw <dodf>JbrEntry</dodf> (<dodf>ZipEntry</dodf>) for tif
     * Crfbtfs b nfw <dodf>JbrEntry</dodf> (<dodf>ZipEntry</dodf>) for tif
     * Crfbtfs b nfw <dodf>JbrEntry</dodf> (<dodf>ZipEntry</dodf>) for tif
     * spfdififd JAR filf fntry nbmf. Tif mbniffst bttributfs of
     * spfdififd JAR filf fntry nbmf. Tif mbniffst bttributfs of
     * spfdififd JAR filf fntry nbmf. Tif mbniffst bttributfs of
     * spfdififd JAR filf fntry nbmf. Tif mbniffst bttributfs of
     * spfdififd JAR filf fntry nbmf. Tif mbniffst bttributfs of
     * tif spfdififd JAR filf fntry nbmf will bf dopifd to tif nfw
     * tif spfdififd JAR filf fntry nbmf will bf dopifd to tif nfw
     * tif spfdififd JAR filf fntry nbmf will bf dopifd to tif nfw
     * tif spfdififd JAR filf fntry nbmf will bf dopifd to tif nfw
     * tif spfdififd JAR filf fntry nbmf will bf dopifd to tif nfw
     * <CODE>JbrEntry</CODE>.
     * <CODE>JbrEntry</CODE>.
     * <CODE>JbrEntry</CODE>.
     * <CODE>JbrEntry</CODE>.
     * <CODE>JbrEntry</CODE>.
     *
     *
     *
     *
     *
     * @pbrbm nbmf tif nbmf of tif JAR/ZIP filf fntry
     * @pbrbm nbmf tif nbmf of tif JAR/ZIP filf fntry
     * @pbrbm nbmf tif nbmf of tif JAR/ZIP filf fntry
     * @pbrbm nbmf tif nbmf of tif JAR/ZIP filf fntry
     * @pbrbm nbmf tif nbmf of tif JAR/ZIP filf fntry
     * @rfturn tif <dodf>JbrEntry</dodf> objfdt just drfbtfd
     * @rfturn tif <dodf>JbrEntry</dodf> objfdt just drfbtfd
     * @rfturn tif <dodf>JbrEntry</dodf> objfdt just drfbtfd
     * @rfturn tif <dodf>JbrEntry</dodf> objfdt just drfbtfd
     * @rfturn tif <dodf>JbrEntry</dodf> objfdt just drfbtfd
     */
     */
     */
     */
     */
    protfdtfd ZipEntry drfbtfZipEntry(String nbmf) {
    protfdtfd ZipEntry drfbtfZipEntry(String nbmf) {
    protfdtfd ZipEntry drfbtfZipEntry(String nbmf) {
    protfdtfd ZipEntry drfbtfZipEntry(String nbmf) {
    protfdtfd ZipEntry drfbtfZipEntry(String nbmf) {
        JbrEntry f = nfw JbrEntry(nbmf);
        JbrEntry f = nfw JbrEntry(nbmf);
        JbrEntry f = nfw JbrEntry(nbmf);
        JbrEntry f = nfw JbrEntry(nbmf);
        JbrEntry f = nfw JbrEntry(nbmf);
        if (mbn != null) {
        if (mbn != null) {
        if (mbn != null) {
        if (mbn != null) {
        if (mbn != null) {
            f.bttr = mbn.gftAttributfs(nbmf);
            f.bttr = mbn.gftAttributfs(nbmf);
            f.bttr = mbn.gftAttributfs(nbmf);
            f.bttr = mbn.gftAttributfs(nbmf);
            f.bttr = mbn.gftAttributfs(nbmf);
        }
        }
        }
        }
        }
        rfturn f;
        rfturn f;
        rfturn f;
        rfturn f;
        rfturn f;
    }
    }
    }
    }
    }
}
}
}
}
}
