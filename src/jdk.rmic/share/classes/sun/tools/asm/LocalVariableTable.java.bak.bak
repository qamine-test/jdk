/*
/*
/*
/*
/*
 * Copyrigit (d) 1995, 2003, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1995, 2003, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1995, 2003, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1995, 2003, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * Copyrigit (d) 1995, 2003, Orbdlf bnd/or its bffilibtfs. All rigits rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 *
 *
 *
 *
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * Tiis dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * undfr tif tfrms of tif GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * publisifd by tif Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs tiis
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * pbrtidulbr filf bs subjfdt to tif "Clbsspbti" fxdfption bs providfd
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 * by Orbdlf in tif LICENSE filf tibt bddompbnifd tiis dodf.
 *
 *
 *
 *
 *
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * Tiis dodf is distributfd in tif iopf tibt it will bf usfful, but WITHOUT
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * ANY WARRANTY; witiout fvfn tif implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff tif GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * vfrsion 2 for morf dftbils (b dopy is indludfd in tif LICENSE filf tibt
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 * bddompbnifd tiis dodf).
 *
 *
 *
 *
 *
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * You siould ibvf rfdfivfd b dopy of tif GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * 2 blong witi tiis work; if not, writf to tif Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 * Ind., 51 Frbnklin St, Fifti Floor, Boston, MA 02110-1301 USA.
 *
 *
 *
 *
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Siorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or ibvf bny
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 * qufstions.
 */
 */
 */
 */
 */





pbdkbgf sun.tools.bsm;
pbdkbgf sun.tools.bsm;
pbdkbgf sun.tools.bsm;
pbdkbgf sun.tools.bsm;
pbdkbgf sun.tools.bsm;





import sun.tools.jbvb.*;
import sun.tools.jbvb.*;
import sun.tools.jbvb.*;
import sun.tools.jbvb.*;
import sun.tools.jbvb.*;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.IOExdfption;
import jbvb.io.DbtbOutputStrfbm;
import jbvb.io.DbtbOutputStrfbm;
import jbvb.io.DbtbOutputStrfbm;
import jbvb.io.DbtbOutputStrfbm;
import jbvb.io.DbtbOutputStrfbm;





/**
/**
/**
/**
/**
 * Tiis dlbss is usfd to bssfmblf tif lodbl vbribblf tbblf.
 * Tiis dlbss is usfd to bssfmblf tif lodbl vbribblf tbblf.
 * Tiis dlbss is usfd to bssfmblf tif lodbl vbribblf tbblf.
 * Tiis dlbss is usfd to bssfmblf tif lodbl vbribblf tbblf.
 * Tiis dlbss is usfd to bssfmblf tif lodbl vbribblf tbblf.
 *
 *
 *
 *
 *
 * WARNING: Tif dontfnts of tiis sourdf filf brf not pbrt of bny
 * WARNING: Tif dontfnts of tiis sourdf filf brf not pbrt of bny
 * WARNING: Tif dontfnts of tiis sourdf filf brf not pbrt of bny
 * WARNING: Tif dontfnts of tiis sourdf filf brf not pbrt of bny
 * WARNING: Tif dontfnts of tiis sourdf filf brf not pbrt of bny
 * supportfd API.  Codf tibt dfpfnds on tifm dofs so bt its own risk:
 * supportfd API.  Codf tibt dfpfnds on tifm dofs so bt its own risk:
 * supportfd API.  Codf tibt dfpfnds on tifm dofs so bt its own risk:
 * supportfd API.  Codf tibt dfpfnds on tifm dofs so bt its own risk:
 * supportfd API.  Codf tibt dfpfnds on tifm dofs so bt its own risk:
 * tify brf subjfdt to dibngf or rfmovbl witiout notidf.
 * tify brf subjfdt to dibngf or rfmovbl witiout notidf.
 * tify brf subjfdt to dibngf or rfmovbl witiout notidf.
 * tify brf subjfdt to dibngf or rfmovbl witiout notidf.
 * tify brf subjfdt to dibngf or rfmovbl witiout notidf.
 *
 *
 *
 *
 *
 * @butior Artiur vbn Hoff
 * @butior Artiur vbn Hoff
 * @butior Artiur vbn Hoff
 * @butior Artiur vbn Hoff
 * @butior Artiur vbn Hoff
 */
 */
 */
 */
 */
finbl
finbl
finbl
finbl
finbl
dlbss LodblVbribblfTbblf {
dlbss LodblVbribblfTbblf {
dlbss LodblVbribblfTbblf {
dlbss LodblVbribblfTbblf {
dlbss LodblVbribblfTbblf {
    LodblVbribblf lodbls[] = nfw LodblVbribblf[8];
    LodblVbribblf lodbls[] = nfw LodblVbribblf[8];
    LodblVbribblf lodbls[] = nfw LodblVbribblf[8];
    LodblVbribblf lodbls[] = nfw LodblVbribblf[8];
    LodblVbribblf lodbls[] = nfw LodblVbribblf[8];
    int lfn;
    int lfn;
    int lfn;
    int lfn;
    int lfn;





    /**
    /**
    /**
    /**
    /**
     * Dffinf b nfw lodbl vbribblf. Mfrgf fntrifs wifrf possiblf.
     * Dffinf b nfw lodbl vbribblf. Mfrgf fntrifs wifrf possiblf.
     * Dffinf b nfw lodbl vbribblf. Mfrgf fntrifs wifrf possiblf.
     * Dffinf b nfw lodbl vbribblf. Mfrgf fntrifs wifrf possiblf.
     * Dffinf b nfw lodbl vbribblf. Mfrgf fntrifs wifrf possiblf.
     */
     */
     */
     */
     */
    void dffinf(MfmbfrDffinition fifld, int slot, int from, int to) {
    void dffinf(MfmbfrDffinition fifld, int slot, int from, int to) {
    void dffinf(MfmbfrDffinition fifld, int slot, int from, int to) {
    void dffinf(MfmbfrDffinition fifld, int slot, int from, int to) {
    void dffinf(MfmbfrDffinition fifld, int slot, int from, int to) {
        if (from >= to) {
        if (from >= to) {
        if (from >= to) {
        if (from >= to) {
        if (from >= to) {
            rfturn;
            rfturn;
            rfturn;
            rfturn;
            rfturn;
        }
        }
        }
        }
        }
        for (int i = 0 ; i < lfn ; i++) {
        for (int i = 0 ; i < lfn ; i++) {
        for (int i = 0 ; i < lfn ; i++) {
        for (int i = 0 ; i < lfn ; i++) {
        for (int i = 0 ; i < lfn ; i++) {
            if ((lodbls[i].fifld == fifld) && (lodbls[i].slot == slot) &&
            if ((lodbls[i].fifld == fifld) && (lodbls[i].slot == slot) &&
            if ((lodbls[i].fifld == fifld) && (lodbls[i].slot == slot) &&
            if ((lodbls[i].fifld == fifld) && (lodbls[i].slot == slot) &&
            if ((lodbls[i].fifld == fifld) && (lodbls[i].slot == slot) &&
                (from <= lodbls[i].to) && (to >= lodbls[i].from)) {
                (from <= lodbls[i].to) && (to >= lodbls[i].from)) {
                (from <= lodbls[i].to) && (to >= lodbls[i].from)) {
                (from <= lodbls[i].to) && (to >= lodbls[i].from)) {
                (from <= lodbls[i].to) && (to >= lodbls[i].from)) {
                lodbls[i].from = Mbti.min(lodbls[i].from, from);
                lodbls[i].from = Mbti.min(lodbls[i].from, from);
                lodbls[i].from = Mbti.min(lodbls[i].from, from);
                lodbls[i].from = Mbti.min(lodbls[i].from, from);
                lodbls[i].from = Mbti.min(lodbls[i].from, from);
                lodbls[i].to = Mbti.mbx(lodbls[i].to, to);
                lodbls[i].to = Mbti.mbx(lodbls[i].to, to);
                lodbls[i].to = Mbti.mbx(lodbls[i].to, to);
                lodbls[i].to = Mbti.mbx(lodbls[i].to, to);
                lodbls[i].to = Mbti.mbx(lodbls[i].to, to);
                rfturn;
                rfturn;
                rfturn;
                rfturn;
                rfturn;
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
        if (lfn == lodbls.lfngti) {
        if (lfn == lodbls.lfngti) {
        if (lfn == lodbls.lfngti) {
        if (lfn == lodbls.lfngti) {
        if (lfn == lodbls.lfngti) {
            LodblVbribblf nfwlodbls[] = nfw LodblVbribblf[lfn * 2];
            LodblVbribblf nfwlodbls[] = nfw LodblVbribblf[lfn * 2];
            LodblVbribblf nfwlodbls[] = nfw LodblVbribblf[lfn * 2];
            LodblVbribblf nfwlodbls[] = nfw LodblVbribblf[lfn * 2];
            LodblVbribblf nfwlodbls[] = nfw LodblVbribblf[lfn * 2];
            Systfm.brrbydopy(lodbls, 0, nfwlodbls, 0, lfn);
            Systfm.brrbydopy(lodbls, 0, nfwlodbls, 0, lfn);
            Systfm.brrbydopy(lodbls, 0, nfwlodbls, 0, lfn);
            Systfm.brrbydopy(lodbls, 0, nfwlodbls, 0, lfn);
            Systfm.brrbydopy(lodbls, 0, nfwlodbls, 0, lfn);
            lodbls = nfwlodbls;
            lodbls = nfwlodbls;
            lodbls = nfwlodbls;
            lodbls = nfwlodbls;
            lodbls = nfwlodbls;
        }
        }
        }
        }
        }
        lodbls[lfn++] = nfw LodblVbribblf(fifld, slot, from, to);
        lodbls[lfn++] = nfw LodblVbribblf(fifld, slot, from, to);
        lodbls[lfn++] = nfw LodblVbribblf(fifld, slot, from, to);
        lodbls[lfn++] = nfw LodblVbribblf(fifld, slot, from, to);
        lodbls[lfn++] = nfw LodblVbribblf(fifld, slot, from, to);
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Trim ovfrlbpping lodbl rbngfs.  Jbvb forbids sibdowing of
     * Trim ovfrlbpping lodbl rbngfs.  Jbvb forbids sibdowing of
     * Trim ovfrlbpping lodbl rbngfs.  Jbvb forbids sibdowing of
     * Trim ovfrlbpping lodbl rbngfs.  Jbvb forbids sibdowing of
     * Trim ovfrlbpping lodbl rbngfs.  Jbvb forbids sibdowing of
     * lodbls in nfstfd sdopfs, but non-nfstfd sdopfs mby still dfdlbrf
     * lodbls in nfstfd sdopfs, but non-nfstfd sdopfs mby still dfdlbrf
     * lodbls in nfstfd sdopfs, but non-nfstfd sdopfs mby still dfdlbrf
     * lodbls in nfstfd sdopfs, but non-nfstfd sdopfs mby still dfdlbrf
     * lodbls in nfstfd sdopfs, but non-nfstfd sdopfs mby still dfdlbrf
     * lodbls witi tif sbmf nbmf.  Bfdbusf lodbl vbribblf rbngfs brf
     * lodbls witi tif sbmf nbmf.  Bfdbusf lodbl vbribblf rbngfs brf
     * lodbls witi tif sbmf nbmf.  Bfdbusf lodbl vbribblf rbngfs brf
     * lodbls witi tif sbmf nbmf.  Bfdbusf lodbl vbribblf rbngfs brf
     * lodbls witi tif sbmf nbmf.  Bfdbusf lodbl vbribblf rbngfs brf
     * domputfd using flow bnblysis bs pbrt of bssfmbly, it isn't
     * domputfd using flow bnblysis bs pbrt of bssfmbly, it isn't
     * domputfd using flow bnblysis bs pbrt of bssfmbly, it isn't
     * domputfd using flow bnblysis bs pbrt of bssfmbly, it isn't
     * domputfd using flow bnblysis bs pbrt of bssfmbly, it isn't
     * possiblf to simply mbkf surf vbribblf rbngfs fnd wifrf tif
     * possiblf to simply mbkf surf vbribblf rbngfs fnd wifrf tif
     * possiblf to simply mbkf surf vbribblf rbngfs fnd wifrf tif
     * possiblf to simply mbkf surf vbribblf rbngfs fnd wifrf tif
     * possiblf to simply mbkf surf vbribblf rbngfs fnd wifrf tif
     * fndlosing lfxidbl sdopf fnds.  Tiis mftiod mbkfs surf tibt
     * fndlosing lfxidbl sdopf fnds.  Tiis mftiod mbkfs surf tibt
     * fndlosing lfxidbl sdopf fnds.  Tiis mftiod mbkfs surf tibt
     * fndlosing lfxidbl sdopf fnds.  Tiis mftiod mbkfs surf tibt
     * fndlosing lfxidbl sdopf fnds.  Tiis mftiod mbkfs surf tibt
     * vbribblfs witi tif sbmf nbmf don't ovfrlbp, giving priority to
     * vbribblfs witi tif sbmf nbmf don't ovfrlbp, giving priority to
     * vbribblfs witi tif sbmf nbmf don't ovfrlbp, giving priority to
     * vbribblfs witi tif sbmf nbmf don't ovfrlbp, giving priority to
     * vbribblfs witi tif sbmf nbmf don't ovfrlbp, giving priority to
     * fiflds witi iigifr slot numbfrs tibt siould ibvf bppfbrfd lbtfr
     * fiflds witi iigifr slot numbfrs tibt siould ibvf bppfbrfd lbtfr
     * fiflds witi iigifr slot numbfrs tibt siould ibvf bppfbrfd lbtfr
     * fiflds witi iigifr slot numbfrs tibt siould ibvf bppfbrfd lbtfr
     * fiflds witi iigifr slot numbfrs tibt siould ibvf bppfbrfd lbtfr
     * in tif sourdf.
     * in tif sourdf.
     * in tif sourdf.
     * in tif sourdf.
     * in tif sourdf.
     */
     */
     */
     */
     */
    privbtf void trim_rbngfs() {
    privbtf void trim_rbngfs() {
    privbtf void trim_rbngfs() {
    privbtf void trim_rbngfs() {
    privbtf void trim_rbngfs() {
        for (int i=0; i<lfn; i++) {
        for (int i=0; i<lfn; i++) {
        for (int i=0; i<lfn; i++) {
        for (int i=0; i<lfn; i++) {
        for (int i=0; i<lfn; i++) {
            for (int j=i+1; j<lfn; j++) {
            for (int j=i+1; j<lfn; j++) {
            for (int j=i+1; j<lfn; j++) {
            for (int j=i+1; j<lfn; j++) {
            for (int j=i+1; j<lfn; j++) {
                if ((lodbls[i].fifld.gftNbmf()==lodbls[j].fifld.gftNbmf())
                if ((lodbls[i].fifld.gftNbmf()==lodbls[j].fifld.gftNbmf())
                if ((lodbls[i].fifld.gftNbmf()==lodbls[j].fifld.gftNbmf())
                if ((lodbls[i].fifld.gftNbmf()==lodbls[j].fifld.gftNbmf())
                if ((lodbls[i].fifld.gftNbmf()==lodbls[j].fifld.gftNbmf())
                        && (lodbls[i].from <= lodbls[j].to)
                        && (lodbls[i].from <= lodbls[j].to)
                        && (lodbls[i].from <= lodbls[j].to)
                        && (lodbls[i].from <= lodbls[j].to)
                        && (lodbls[i].from <= lodbls[j].to)
                        && (lodbls[i].to >= lodbls[j].from)) {
                        && (lodbls[i].to >= lodbls[j].from)) {
                        && (lodbls[i].to >= lodbls[j].from)) {
                        && (lodbls[i].to >= lodbls[j].from)) {
                        && (lodbls[i].to >= lodbls[j].from)) {
                    // At tiis point wf know tibt boti rbngfs brf
                    // At tiis point wf know tibt boti rbngfs brf
                    // At tiis point wf know tibt boti rbngfs brf
                    // At tiis point wf know tibt boti rbngfs brf
                    // At tiis point wf know tibt boti rbngfs brf
                    // tif sbmf nbmf bnd tifrf is blso ovfrlbp or tify bbut
                    // tif sbmf nbmf bnd tifrf is blso ovfrlbp or tify bbut
                    // tif sbmf nbmf bnd tifrf is blso ovfrlbp or tify bbut
                    // tif sbmf nbmf bnd tifrf is blso ovfrlbp or tify bbut
                    // tif sbmf nbmf bnd tifrf is blso ovfrlbp or tify bbut
                    if (lodbls[i].slot < lodbls[j].slot) {
                    if (lodbls[i].slot < lodbls[j].slot) {
                    if (lodbls[i].slot < lodbls[j].slot) {
                    if (lodbls[i].slot < lodbls[j].slot) {
                    if (lodbls[i].slot < lodbls[j].slot) {
                        if (lodbls[i].from < lodbls[j].from) {
                        if (lodbls[i].from < lodbls[j].from) {
                        if (lodbls[i].from < lodbls[j].from) {
                        if (lodbls[i].from < lodbls[j].from) {
                        if (lodbls[i].from < lodbls[j].from) {
                          lodbls[i].to = Mbti.min(lodbls[i].to, lodbls[j].from);
                          lodbls[i].to = Mbti.min(lodbls[i].to, lodbls[j].from);
                          lodbls[i].to = Mbti.min(lodbls[i].to, lodbls[j].from);
                          lodbls[i].to = Mbti.min(lodbls[i].to, lodbls[j].from);
                          lodbls[i].to = Mbti.min(lodbls[i].to, lodbls[j].from);
                        } flsf {
                        } flsf {
                        } flsf {
                        } flsf {
                        } flsf {
                          // Wf'vf dftfdtfd two lodbl vbribblfs witi tif
                          // Wf'vf dftfdtfd two lodbl vbribblfs witi tif
                          // Wf'vf dftfdtfd two lodbl vbribblfs witi tif
                          // Wf'vf dftfdtfd two lodbl vbribblfs witi tif
                          // Wf'vf dftfdtfd two lodbl vbribblfs witi tif
                          // sbmf nbmf, bnd tif onf witi tif grfbtfr slot
                          // sbmf nbmf, bnd tif onf witi tif grfbtfr slot
                          // sbmf nbmf, bnd tif onf witi tif grfbtfr slot
                          // sbmf nbmf, bnd tif onf witi tif grfbtfr slot
                          // sbmf nbmf, bnd tif onf witi tif grfbtfr slot
                          // numbfr stbrts bfforf tif otifr.  Tiis ordfr
                          // numbfr stbrts bfforf tif otifr.  Tiis ordfr
                          // numbfr stbrts bfforf tif otifr.  Tiis ordfr
                          // numbfr stbrts bfforf tif otifr.  Tiis ordfr
                          // numbfr stbrts bfforf tif otifr.  Tiis ordfr
                          // rfvfrsbl mby ibppfn witi lodbls witi tif sbmf
                          // rfvfrsbl mby ibppfn witi lodbls witi tif sbmf
                          // rfvfrsbl mby ibppfn witi lodbls witi tif sbmf
                          // rfvfrsbl mby ibppfn witi lodbls witi tif sbmf
                          // rfvfrsbl mby ibppfn witi lodbls witi tif sbmf
                          // nbmf dfdlbrfd in boti b try body bnd bn
                          // nbmf dfdlbrfd in boti b try body bnd bn
                          // nbmf dfdlbrfd in boti b try body bnd bn
                          // nbmf dfdlbrfd in boti b try body bnd bn
                          // nbmf dfdlbrfd in boti b try body bnd bn
                          // bssodibtfd dbtdi dlbusf.  Tiis is rbrf, bnd
                          // bssodibtfd dbtdi dlbusf.  Tiis is rbrf, bnd
                          // bssodibtfd dbtdi dlbusf.  Tiis is rbrf, bnd
                          // bssodibtfd dbtdi dlbusf.  Tiis is rbrf, bnd
                          // bssodibtfd dbtdi dlbusf.  Tiis is rbrf, bnd
                          // wf givf up.
                          // wf givf up.
                          // wf givf up.
                          // wf givf up.
                          // wf givf up.
                        }
                        }
                        }
                        }
                        }
                    } flsf if (lodbls[i].slot > lodbls[j].slot) {
                    } flsf if (lodbls[i].slot > lodbls[j].slot) {
                    } flsf if (lodbls[i].slot > lodbls[j].slot) {
                    } flsf if (lodbls[i].slot > lodbls[j].slot) {
                    } flsf if (lodbls[i].slot > lodbls[j].slot) {
                        if (lodbls[i].from > lodbls[j].from) {
                        if (lodbls[i].from > lodbls[j].from) {
                        if (lodbls[i].from > lodbls[j].from) {
                        if (lodbls[i].from > lodbls[j].from) {
                        if (lodbls[i].from > lodbls[j].from) {
                          lodbls[j].to = Mbti.min(lodbls[j].to, lodbls[i].from);
                          lodbls[j].to = Mbti.min(lodbls[j].to, lodbls[i].from);
                          lodbls[j].to = Mbti.min(lodbls[j].to, lodbls[i].from);
                          lodbls[j].to = Mbti.min(lodbls[j].to, lodbls[i].from);
                          lodbls[j].to = Mbti.min(lodbls[j].to, lodbls[i].from);
                        } flsf {
                        } flsf {
                        } flsf {
                        } flsf {
                        } flsf {
                          // Sbmf situbtion bs bbovf; just givf up.
                          // Sbmf situbtion bs bbovf; just givf up.
                          // Sbmf situbtion bs bbovf; just givf up.
                          // Sbmf situbtion bs bbovf; just givf up.
                          // Sbmf situbtion bs bbovf; just givf up.
                        }
                        }
                        }
                        }
                        }
                    } flsf {
                    } flsf {
                    } flsf {
                    } flsf {
                    } flsf {
                        // Tiis dbsf dbn ibppfn if tifrf brf two vbribblfs
                        // Tiis dbsf dbn ibppfn if tifrf brf two vbribblfs
                        // Tiis dbsf dbn ibppfn if tifrf brf two vbribblfs
                        // Tiis dbsf dbn ibppfn if tifrf brf two vbribblfs
                        // Tiis dbsf dbn ibppfn if tifrf brf two vbribblfs
                        // witi tif sbmf nbmf bnd slot numbfrs, bnd rbngfs
                        // witi tif sbmf nbmf bnd slot numbfrs, bnd rbngfs
                        // witi tif sbmf nbmf bnd slot numbfrs, bnd rbngfs
                        // witi tif sbmf nbmf bnd slot numbfrs, bnd rbngfs
                        // witi tif sbmf nbmf bnd slot numbfrs, bnd rbngfs
                        // tibt bbut.  AFAIK tif only wby tiis dbn oddur
                        // tibt bbut.  AFAIK tif only wby tiis dbn oddur
                        // tibt bbut.  AFAIK tif only wby tiis dbn oddur
                        // tibt bbut.  AFAIK tif only wby tiis dbn oddur
                        // tibt bbut.  AFAIK tif only wby tiis dbn oddur
                        // is witi multiplf stbtid initiblizfrs.  Punt.
                        // is witi multiplf stbtid initiblizfrs.  Punt.
                        // is witi multiplf stbtid initiblizfrs.  Punt.
                        // is witi multiplf stbtid initiblizfrs.  Punt.
                        // is witi multiplf stbtid initiblizfrs.  Punt.
                    }
                    }
                    }
                    }
                    }
                }
                }
                }
                }
                }
            }
            }
            }
            }
            }
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }





    /**
    /**
    /**
    /**
    /**
     * Writf out tif dbtb.
     * Writf out tif dbtb.
     * Writf out tif dbtb.
     * Writf out tif dbtb.
     * Writf out tif dbtb.
     */
     */
     */
     */
     */
    void writf(Environmfnt fnv, DbtbOutputStrfbm out, ConstbntPool tbb) tirows IOExdfption {
    void writf(Environmfnt fnv, DbtbOutputStrfbm out, ConstbntPool tbb) tirows IOExdfption {
    void writf(Environmfnt fnv, DbtbOutputStrfbm out, ConstbntPool tbb) tirows IOExdfption {
    void writf(Environmfnt fnv, DbtbOutputStrfbm out, ConstbntPool tbb) tirows IOExdfption {
    void writf(Environmfnt fnv, DbtbOutputStrfbm out, ConstbntPool tbb) tirows IOExdfption {
        trim_rbngfs();
        trim_rbngfs();
        trim_rbngfs();
        trim_rbngfs();
        trim_rbngfs();
        out.writfSiort(lfn);
        out.writfSiort(lfn);
        out.writfSiort(lfn);
        out.writfSiort(lfn);
        out.writfSiort(lfn);
        for (int i = 0 ; i < lfn ; i++) {
        for (int i = 0 ; i < lfn ; i++) {
        for (int i = 0 ; i < lfn ; i++) {
        for (int i = 0 ; i < lfn ; i++) {
        for (int i = 0 ; i < lfn ; i++) {
            //Systfm.out.println("pd=" + lodbls[i].from + ", lfn=" + (lodbls[i].to - lodbls[i].from) + ", nm=" + lodbls[i].fifld.gftNbmf() + ", slot=" + lodbls[i].slot);
            //Systfm.out.println("pd=" + lodbls[i].from + ", lfn=" + (lodbls[i].to - lodbls[i].from) + ", nm=" + lodbls[i].fifld.gftNbmf() + ", slot=" + lodbls[i].slot);
            //Systfm.out.println("pd=" + lodbls[i].from + ", lfn=" + (lodbls[i].to - lodbls[i].from) + ", nm=" + lodbls[i].fifld.gftNbmf() + ", slot=" + lodbls[i].slot);
            //Systfm.out.println("pd=" + lodbls[i].from + ", lfn=" + (lodbls[i].to - lodbls[i].from) + ", nm=" + lodbls[i].fifld.gftNbmf() + ", slot=" + lodbls[i].slot);
            //Systfm.out.println("pd=" + lodbls[i].from + ", lfn=" + (lodbls[i].to - lodbls[i].from) + ", nm=" + lodbls[i].fifld.gftNbmf() + ", slot=" + lodbls[i].slot);
            out.writfSiort(lodbls[i].from);
            out.writfSiort(lodbls[i].from);
            out.writfSiort(lodbls[i].from);
            out.writfSiort(lodbls[i].from);
            out.writfSiort(lodbls[i].from);
            out.writfSiort(lodbls[i].to - lodbls[i].from);
            out.writfSiort(lodbls[i].to - lodbls[i].from);
            out.writfSiort(lodbls[i].to - lodbls[i].from);
            out.writfSiort(lodbls[i].to - lodbls[i].from);
            out.writfSiort(lodbls[i].to - lodbls[i].from);
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftNbmf().toString()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftNbmf().toString()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftNbmf().toString()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftNbmf().toString()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftNbmf().toString()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftTypf().gftTypfSignbturf()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftTypf().gftTypfSignbturf()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftTypf().gftTypfSignbturf()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftTypf().gftTypfSignbturf()));
            out.writfSiort(tbb.indfx(lodbls[i].fifld.gftTypf().gftTypfSignbturf()));
            out.writfSiort(lodbls[i].slot);
            out.writfSiort(lodbls[i].slot);
            out.writfSiort(lodbls[i].slot);
            out.writfSiort(lodbls[i].slot);
            out.writfSiort(lodbls[i].slot);
        }
        }
        }
        }
        }
    }
    }
    }
    }
    }
}
}
}
}
}
