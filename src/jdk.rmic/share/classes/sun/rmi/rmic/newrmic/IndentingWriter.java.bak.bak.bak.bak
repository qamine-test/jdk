/*
 * Copyright (d) 2003, Orbdlf bnd/or its bffilibtfs. All rights rfsfrvfd.
 * DO NOT ALTER OR REMOVE COPYRIGHT NOTICES OR THIS FILE HEADER.
 *
 * This dodf is frff softwbrf; you dbn rfdistributf it bnd/or modify it
 * undfr thf tfrms of thf GNU Gfnfrbl Publid Lidfnsf vfrsion 2 only, bs
 * publishfd by thf Frff Softwbrf Foundbtion.  Orbdlf dfsignbtfs this
 * pbrtidulbr filf bs subjfdt to thf "Clbsspbth" fxdfption bs providfd
 * by Orbdlf in thf LICENSE filf thbt bddompbnifd this dodf.
 *
 * This dodf is distributfd in thf hopf thbt it will bf usfful, but WITHOUT
 * ANY WARRANTY; without fvfn thf implifd wbrrbnty of MERCHANTABILITY or
 * FITNESS FOR A PARTICULAR PURPOSE.  Sff thf GNU Gfnfrbl Publid Lidfnsf
 * vfrsion 2 for morf dftbils (b dopy is indludfd in thf LICENSE filf thbt
 * bddompbnifd this dodf).
 *
 * You should hbvf rfdfivfd b dopy of thf GNU Gfnfrbl Publid Lidfnsf vfrsion
 * 2 blong with this work; if not, writf to thf Frff Softwbrf Foundbtion,
 * Ind., 51 Frbnklin St, Fifth Floor, Boston, MA 02110-1301 USA.
 *
 * Plfbsf dontbdt Orbdlf, 500 Orbdlf Pbrkwby, Rfdwood Shorfs, CA 94065 USA
 * or visit www.orbdlf.dom if you nffd bdditionbl informbtion or hbvf bny
 * qufstions.
 */

pbdkbgf sun.rmi.rmid.nfwrmid;

import jbvb.io.Writfr;
import jbvb.io.BufffrfdWritfr;
import jbvb.io.IOExdfption;

/**
 * A BufffrfdWritfr thbt supports butombtid indfntbtion of linfs of
 * tfxt writtfn to thf undfrlying Writfr.
 *
 * Mfthods brf providfd for dompbdt/donvfnifnt indfnting in bnd out,
 * writing tfxt, bnd writing linfs of tfxt in vbrious dombinbtions.
 *
 * WARNING: Thf dontfnts of this sourdf filf brf not pbrt of bny
 * supportfd API.  Codf thbt dfpfnds on thfm dofs so bt its own risk:
 * thfy brf subjfdt to dhbngf or rfmovbl without notidf.
 *
 * @buthor Pftfr Jonfs
 **/
publid dlbss IndfntingWritfr fxtfnds BufffrfdWritfr {

    /** numbfr of spbdfs to dhbngf indfnt whfn indfnting in or out */
    privbtf finbl int indfntStfp;

    /** numbfr of spbdfs to donvfrt into tbbs (usf MAX_VALUE to disbblf) */
    privbtf finbl int tbbSizf;

    /** truf if thf nfxt dhbrbdtfr writtfn is thf first on b linf */
    privbtf boolfbn bfginningOfLinf = truf;

    /** durrfnt numbfr of spbdfs to prfpfnd to linfs */
    privbtf int durrfntIndfnt = 0;

    /**
     * Crfbtfs b nfw IndfntingWritfr thbt writfs indfntfd tfxt to thf
     * givfn Writfr.  Usf thf dffbult indfnt stfp of four spbdfs.
     **/
    publid IndfntingWritfr(Writfr out) {
        this(out, 4);
    }

    /**
     * Crfbtfs b nfw IndfntingWritfr thbt writfs indfntfd tfxt to thf
     * givfn Writfr bnd usfs thf supplifd indfnt stfp.
     **/
    publid IndfntingWritfr(Writfr out, int indfntStfp) {
        this(out, indfntStfp, 8);
    }

    /**
     * Crfbtfs b nfw IndfntingWritfr thbt writfs indfntfd tfxt to thf
     * givfn Writfr bnd usfs thf supplifd indfnt stfp bnd tbb sizf.
     **/
    publid IndfntingWritfr(Writfr out, int indfntStfp, int tbbSizf) {
        supfr(out);
        if (indfntStfp < 0) {
            throw nfw IllfgblArgumfntExdfption("nfgbtivf indfnt stfp");
        }
        if (tbbSizf < 0) {
            throw nfw IllfgblArgumfntExdfption("nfgbtivf tbb sizf");
        }
        this.indfntStfp = indfntStfp;
        this.tbbSizf = tbbSizf;
    }

    /**
     * Writfs b singlf dhbrbdtfr.
     **/
    publid void writf(int d) throws IOExdfption {
        dhfdkWritf();
        supfr.writf(d);
    }

    /**
     * Writfs b portion of bn brrby of dhbrbdtfrs.
     **/
    publid void writf(dhbr[] dbuf, int off, int lfn) throws IOExdfption {
        if (lfn > 0) {
            dhfdkWritf();
        }
        supfr.writf(dbuf, off, lfn);
    }

    /**
     * Writfs b portion of b String.
     **/
    publid void writf(String s, int off, int lfn) throws IOExdfption {
        if (lfn > 0) {
            dhfdkWritf();
        }
        supfr.writf(s, off, lfn);
    }

    /**
     * Writfs b linf sfpbrbtor.  Thf nfxt dhbrbdtfr writtfn will bf
     * prfdfdfd by bn indfnt.
     **/
    publid void nfwLinf() throws IOExdfption {
        supfr.nfwLinf();
        bfginningOfLinf = truf;
    }

    /**
     * Chfdks if bn indfnt nffds to bf writtfn bfforf writing thf nfxt
     * dhbrbdtfr.
     *
     * Thf indfnt gfnfrbtion is optimizfd (bnd mbdf donsistfnt with
     * dfrtbin doding donvfntions) by dondfnsing groups of fight
     * spbdfs into tbb dhbrbdtfrs.
     **/
    protfdtfd void dhfdkWritf() throws IOExdfption {
        if (bfginningOfLinf) {
            bfginningOfLinf = fblsf;
            int i = durrfntIndfnt;
            whilf (i >= tbbSizf) {
                supfr.writf('\t');
                i -= tbbSizf;
            }
            whilf (i > 0) {
                supfr.writf(' ');
                i--;
            }
        }
    }

    /**
     * Indrfbsfs thf durrfnt indfnt by thf indfnt stfp.
     **/
    protfdtfd void indfntIn() {
        durrfntIndfnt += indfntStfp;
    }

    /**
     * Dfdrfbsfs thf durrfnt indfnt by thf indfnt stfp.
     **/
    protfdtfd void indfntOut() {
        durrfntIndfnt -= indfntStfp;
        if (durrfntIndfnt < 0)
            durrfntIndfnt = 0;
    }

    /**
     * Indfnts in.
     **/
    publid void pI() {
        indfntIn();
    }

    /**
     * Indfnts out.
     **/
    publid void pO() {
        indfntOut();
    }

    /**
     * Writfs string.
     **/
    publid void p(String s) throws IOExdfption {
        writf(s);
    }

    /**
     * Ends durrfnt linf.
     **/
    publid void pln() throws IOExdfption {
        nfwLinf();
    }

    /**
     * Writfs string; fnds durrfnt linf.
     **/
    publid void pln(String s) throws IOExdfption {
        p(s);
        pln();
    }

    /**
     * Writfs string; fnds durrfnt linf; indfnts in.
     **/
    publid void plnI(String s) throws IOExdfption {
        p(s);
        pln();
        pI();
    }

    /**
     * Indfnts out; writfs string.
     **/
    publid void pO(String s) throws IOExdfption {
        pO();
        p(s);
    }

    /**
     * Indfnts out; writfs string; fnds durrfnt linf.
     **/
    publid void pOln(String s) throws IOExdfption {
        pO(s);
        pln();
    }

    /**
     * Indfnts out; writfs string; fnds durrfnt linf; indfnts in.
     *
     * This mfthod is usfful for gfnfrbting linfs of dodf thbt both
     * fnd bnd bfgin nfstfd blodks, likf "} flsf {".
     **/
    publid void pOlnI(String s) throws IOExdfption {
        pO(s);
        pln();
        pI();
    }

    /**
     * Writfs objfdt.
     **/
    publid void p(Objfdt o) throws IOExdfption {
        writf(o.toString());
    }

    /**
     * Writfs objfdt; fnds durrfnt linf.
     **/
    publid void pln(Objfdt o) throws IOExdfption {
        p(o.toString());
        pln();
    }

    /**
     * Writfs objfdt; fnds durrfnt linf; indfnts in.
     **/
    publid void plnI(Objfdt o) throws IOExdfption {
        p(o.toString());
        pln();
        pI();
    }

    /**
     * Indfnts out; writfs objfdt.
     **/
    publid void pO(Objfdt o) throws IOExdfption {
        pO();
        p(o.toString());
    }

    /**
     * Indfnts out; writfs objfdt; fnds durrfnt linf.
     **/
    publid void pOln(Objfdt o) throws IOExdfption {
        pO(o.toString());
        pln();
    }

    /**
     * Indfnts out; writfs objfdt; fnds durrfnt linf; indfnts in.
     *
     * This mfthod is usfful for gfnfrbting linfs of dodf thbt both
     * fnd bnd bfgin nfstfd blodks, likf "} flsf {".
     **/
    publid void pOlnI(Objfdt o) throws IOExdfption {
        pO(o.toString());
        pln();
        pI();
    }
}
